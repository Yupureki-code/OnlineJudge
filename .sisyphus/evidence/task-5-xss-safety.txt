XSS Safety: textContent used for all user-supplied content, NOT innerHTML:

Line 743: avatar.textContent = (comment.author_name || '?').charAt(0).toUpperCase();
Line 747: author.textContent = comment.author_name || '匿名';
Line 751: time.textContent = formatRelativeTime(comment.created_at);
Line 755: content.textContent = comment.content;  // SAFE: textContent, not innerHTML
Line 766: edited.textContent = '(已编辑)';
Line 775: editBtn.textContent = '编辑';
Line 783: deleteBtn.textContent = '删除';

No innerHTML usage for user-supplied comment content. The only innerHTML usage is:
Line 709: document.getElementById('comment-list').innerHTML = ''; 
(This is a safe reset to empty string, not injecting user content)

All user-provided data (author_name, content, timestamps) rendered via textContent.