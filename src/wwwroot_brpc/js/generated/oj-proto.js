var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// src/wwwroot_brpc/node_modules/protobufjs/src/util/aspromise.js
var require_aspromise = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/util/aspromise.js"(exports, module) {
    "use strict";
    module.exports = asPromise;
    function asPromise(fn, ctx) {
      var params = new Array(arguments.length - 1), offset = 0, index = 2, pending = true;
      while (index < arguments.length)
        params[offset++] = arguments[index++];
      return new Promise(function executor(resolve, reject) {
        params[offset] = function callback(err) {
          if (pending) {
            pending = false;
            if (err)
              reject(err);
            else {
              var params2 = new Array(arguments.length - 1), offset2 = 0;
              while (offset2 < params2.length)
                params2[offset2++] = arguments[offset2];
              resolve.apply(null, params2);
            }
          }
        };
        try {
          fn.apply(ctx || null, params);
        } catch (err) {
          if (pending) {
            pending = false;
            reject(err);
          }
        }
      });
    }
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/util/base64.js
var require_base64 = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/util/base64.js"(exports) {
    "use strict";
    var base64 = exports;
    base64.length = function length(string) {
      var p = string.length;
      if (!p)
        return 0;
      while (p > 0 && string.charAt(p - 1) === "=")
        --p;
      return Math.floor(p * 3 / 4);
    };
    var b64 = new Array(64);
    var s64 = new Array(123);
    for (i = 0; i < 64; )
      s64[b64[i] = i < 26 ? i + 65 : i < 52 ? i + 71 : i < 62 ? i - 4 : i - 59 | 43] = i++;
    var i;
    s64[45] = 62;
    s64[95] = 63;
    base64.encode = function encode(buffer, start, end) {
      var parts = null, chunk = [];
      var i2 = 0, j = 0, t;
      while (start < end) {
        var b = buffer[start++];
        switch (j) {
          case 0:
            chunk[i2++] = b64[b >> 2];
            t = (b & 3) << 4;
            j = 1;
            break;
          case 1:
            chunk[i2++] = b64[t | b >> 4];
            t = (b & 15) << 2;
            j = 2;
            break;
          case 2:
            chunk[i2++] = b64[t | b >> 6];
            chunk[i2++] = b64[b & 63];
            j = 0;
            break;
        }
        if (i2 > 8191) {
          (parts || (parts = [])).push(String.fromCharCode.apply(String, chunk));
          i2 = 0;
        }
      }
      if (j) {
        chunk[i2++] = b64[t];
        chunk[i2++] = 61;
        if (j === 1)
          chunk[i2++] = 61;
      }
      if (parts) {
        if (i2)
          parts.push(String.fromCharCode.apply(String, chunk.slice(0, i2)));
        return parts.join("");
      }
      return String.fromCharCode.apply(String, chunk.slice(0, i2));
    };
    var invalidEncoding = "invalid encoding";
    base64.decode = function decode(string, buffer, offset) {
      var start = offset;
      var j = 0, t;
      for (var i2 = 0; i2 < string.length; ) {
        var c = string.charCodeAt(i2++);
        if (c === 61 && j > 1)
          break;
        if ((c = s64[c]) === void 0)
          throw Error(invalidEncoding);
        switch (j) {
          case 0:
            t = c;
            j = 1;
            break;
          case 1:
            buffer[offset++] = t << 2 | (c & 48) >> 4;
            t = c;
            j = 2;
            break;
          case 2:
            buffer[offset++] = (t & 15) << 4 | (c & 60) >> 2;
            t = c;
            j = 3;
            break;
          case 3:
            buffer[offset++] = (t & 3) << 6 | c;
            j = 0;
            break;
        }
      }
      if (j === 1)
        throw Error(invalidEncoding);
      return offset - start;
    };
    var base64Re = /^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/;
    var base64UrlRe = /[-_]/;
    var base64UrlNoPaddingRe = /^(?:[A-Za-z0-9_-]{4})*(?:[A-Za-z0-9_-]{2}(?:==)?|[A-Za-z0-9_-]{3}=?)?$/;
    base64.test = function test(string) {
      return base64Re.test(string) || base64UrlRe.test(string) && base64UrlNoPaddingRe.test(string);
    };
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/util/eventemitter.js
var require_eventemitter = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/util/eventemitter.js"(exports, module) {
    "use strict";
    module.exports = EventEmitter;
    function EventEmitter() {
      this._listeners = /* @__PURE__ */ Object.create(null);
    }
    EventEmitter.prototype.on = function on(evt, fn, ctx) {
      (this._listeners[evt] || (this._listeners[evt] = [])).push({
        fn,
        ctx: ctx || this
      });
      return this;
    };
    EventEmitter.prototype.off = function off(evt, fn) {
      if (evt === void 0)
        this._listeners = /* @__PURE__ */ Object.create(null);
      else {
        if (fn === void 0)
          this._listeners[evt] = [];
        else {
          var listeners = this._listeners[evt];
          if (!listeners)
            return this;
          for (var i = 0; i < listeners.length; )
            if (listeners[i].fn === fn)
              listeners.splice(i, 1);
            else
              ++i;
        }
      }
      return this;
    };
    EventEmitter.prototype.emit = function emit(evt) {
      var listeners = this._listeners[evt];
      if (listeners) {
        var args = [], i = 1;
        for (; i < arguments.length; )
          args.push(arguments[i++]);
        for (i = 0; i < listeners.length; )
          listeners[i].fn.apply(listeners[i++].ctx, args);
      }
      return this;
    };
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/util/float.js
var require_float = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/util/float.js"(exports, module) {
    "use strict";
    module.exports = factory(factory);
    function factory(exports2) {
      if (typeof Float32Array !== "undefined") (function() {
        var f32 = new Float32Array([-0]), f8b = new Uint8Array(f32.buffer), le = f8b[3] === 128;
        function writeFloat_f32_cpy(val, buf, pos) {
          f32[0] = val;
          buf[pos] = f8b[0];
          buf[pos + 1] = f8b[1];
          buf[pos + 2] = f8b[2];
          buf[pos + 3] = f8b[3];
        }
        function writeFloat_f32_rev(val, buf, pos) {
          f32[0] = val;
          buf[pos] = f8b[3];
          buf[pos + 1] = f8b[2];
          buf[pos + 2] = f8b[1];
          buf[pos + 3] = f8b[0];
        }
        exports2.writeFloatLE = le ? writeFloat_f32_cpy : writeFloat_f32_rev;
        exports2.writeFloatBE = le ? writeFloat_f32_rev : writeFloat_f32_cpy;
        function readFloat_f32_cpy(buf, pos) {
          f8b[0] = buf[pos];
          f8b[1] = buf[pos + 1];
          f8b[2] = buf[pos + 2];
          f8b[3] = buf[pos + 3];
          return f32[0];
        }
        function readFloat_f32_rev(buf, pos) {
          f8b[3] = buf[pos];
          f8b[2] = buf[pos + 1];
          f8b[1] = buf[pos + 2];
          f8b[0] = buf[pos + 3];
          return f32[0];
        }
        exports2.readFloatLE = le ? readFloat_f32_cpy : readFloat_f32_rev;
        exports2.readFloatBE = le ? readFloat_f32_rev : readFloat_f32_cpy;
      })();
      else (function() {
        function writeFloat_ieee754(writeUint, val, buf, pos) {
          var sign = val < 0 ? 1 : 0;
          if (sign)
            val = -val;
          if (val === 0)
            writeUint(1 / val > 0 ? (
              /* positive */
              0
            ) : (
              /* negative 0 */
              2147483648
            ), buf, pos);
          else if (isNaN(val))
            writeUint(2143289344, buf, pos);
          else if (val > 34028234663852886e22)
            writeUint((sign << 31 | 2139095040) >>> 0, buf, pos);
          else if (val < 11754943508222875e-54)
            writeUint((sign << 31 | Math.round(val / 1401298464324817e-60)) >>> 0, buf, pos);
          else {
            var exponent = Math.floor(Math.log(val) / Math.LN2), mantissa = Math.round(val * Math.pow(2, -exponent) * 8388608) & 8388607;
            writeUint((sign << 31 | exponent + 127 << 23 | mantissa) >>> 0, buf, pos);
          }
        }
        exports2.writeFloatLE = writeFloat_ieee754.bind(null, writeUintLE);
        exports2.writeFloatBE = writeFloat_ieee754.bind(null, writeUintBE);
        function readFloat_ieee754(readUint, buf, pos) {
          var uint = readUint(buf, pos), sign = (uint >> 31) * 2 + 1, exponent = uint >>> 23 & 255, mantissa = uint & 8388607;
          return exponent === 255 ? mantissa ? NaN : sign * Infinity : exponent === 0 ? sign * 1401298464324817e-60 * mantissa : sign * Math.pow(2, exponent - 150) * (mantissa + 8388608);
        }
        exports2.readFloatLE = readFloat_ieee754.bind(null, readUintLE);
        exports2.readFloatBE = readFloat_ieee754.bind(null, readUintBE);
      })();
      if (typeof Float64Array !== "undefined") (function() {
        var f64 = new Float64Array([-0]), f8b = new Uint8Array(f64.buffer), le = f8b[7] === 128;
        function writeDouble_f64_cpy(val, buf, pos) {
          f64[0] = val;
          buf[pos] = f8b[0];
          buf[pos + 1] = f8b[1];
          buf[pos + 2] = f8b[2];
          buf[pos + 3] = f8b[3];
          buf[pos + 4] = f8b[4];
          buf[pos + 5] = f8b[5];
          buf[pos + 6] = f8b[6];
          buf[pos + 7] = f8b[7];
        }
        function writeDouble_f64_rev(val, buf, pos) {
          f64[0] = val;
          buf[pos] = f8b[7];
          buf[pos + 1] = f8b[6];
          buf[pos + 2] = f8b[5];
          buf[pos + 3] = f8b[4];
          buf[pos + 4] = f8b[3];
          buf[pos + 5] = f8b[2];
          buf[pos + 6] = f8b[1];
          buf[pos + 7] = f8b[0];
        }
        exports2.writeDoubleLE = le ? writeDouble_f64_cpy : writeDouble_f64_rev;
        exports2.writeDoubleBE = le ? writeDouble_f64_rev : writeDouble_f64_cpy;
        function readDouble_f64_cpy(buf, pos) {
          f8b[0] = buf[pos];
          f8b[1] = buf[pos + 1];
          f8b[2] = buf[pos + 2];
          f8b[3] = buf[pos + 3];
          f8b[4] = buf[pos + 4];
          f8b[5] = buf[pos + 5];
          f8b[6] = buf[pos + 6];
          f8b[7] = buf[pos + 7];
          return f64[0];
        }
        function readDouble_f64_rev(buf, pos) {
          f8b[7] = buf[pos];
          f8b[6] = buf[pos + 1];
          f8b[5] = buf[pos + 2];
          f8b[4] = buf[pos + 3];
          f8b[3] = buf[pos + 4];
          f8b[2] = buf[pos + 5];
          f8b[1] = buf[pos + 6];
          f8b[0] = buf[pos + 7];
          return f64[0];
        }
        exports2.readDoubleLE = le ? readDouble_f64_cpy : readDouble_f64_rev;
        exports2.readDoubleBE = le ? readDouble_f64_rev : readDouble_f64_cpy;
      })();
      else (function() {
        function writeDouble_ieee754(writeUint, off0, off1, val, buf, pos) {
          var sign = val < 0 ? 1 : 0;
          if (sign)
            val = -val;
          if (val === 0) {
            writeUint(0, buf, pos + off0);
            writeUint(1 / val > 0 ? (
              /* positive */
              0
            ) : (
              /* negative 0 */
              2147483648
            ), buf, pos + off1);
          } else if (isNaN(val)) {
            writeUint(0, buf, pos + off0);
            writeUint(2146959360, buf, pos + off1);
          } else if (val > 17976931348623157e292) {
            writeUint(0, buf, pos + off0);
            writeUint((sign << 31 | 2146435072) >>> 0, buf, pos + off1);
          } else {
            var mantissa;
            if (val < 22250738585072014e-324) {
              mantissa = val / 5e-324;
              writeUint(mantissa >>> 0, buf, pos + off0);
              writeUint((sign << 31 | mantissa / 4294967296) >>> 0, buf, pos + off1);
            } else {
              var exponent = Math.floor(Math.log(val) / Math.LN2);
              if (exponent === 1024)
                exponent = 1023;
              mantissa = val * Math.pow(2, -exponent);
              writeUint(mantissa * 4503599627370496 >>> 0, buf, pos + off0);
              writeUint((sign << 31 | exponent + 1023 << 20 | mantissa * 1048576 & 1048575) >>> 0, buf, pos + off1);
            }
          }
        }
        exports2.writeDoubleLE = writeDouble_ieee754.bind(null, writeUintLE, 0, 4);
        exports2.writeDoubleBE = writeDouble_ieee754.bind(null, writeUintBE, 4, 0);
        function readDouble_ieee754(readUint, off0, off1, buf, pos) {
          var lo = readUint(buf, pos + off0), hi = readUint(buf, pos + off1);
          var sign = (hi >> 31) * 2 + 1, exponent = hi >>> 20 & 2047, mantissa = 4294967296 * (hi & 1048575) + lo;
          return exponent === 2047 ? mantissa ? NaN : sign * Infinity : exponent === 0 ? sign * 5e-324 * mantissa : sign * Math.pow(2, exponent - 1075) * (mantissa + 4503599627370496);
        }
        exports2.readDoubleLE = readDouble_ieee754.bind(null, readUintLE, 0, 4);
        exports2.readDoubleBE = readDouble_ieee754.bind(null, readUintBE, 4, 0);
      })();
      return exports2;
    }
    function writeUintLE(val, buf, pos) {
      buf[pos] = val & 255;
      buf[pos + 1] = val >>> 8 & 255;
      buf[pos + 2] = val >>> 16 & 255;
      buf[pos + 3] = val >>> 24;
    }
    function writeUintBE(val, buf, pos) {
      buf[pos] = val >>> 24;
      buf[pos + 1] = val >>> 16 & 255;
      buf[pos + 2] = val >>> 8 & 255;
      buf[pos + 3] = val & 255;
    }
    function readUintLE(buf, pos) {
      return (buf[pos] | buf[pos + 1] << 8 | buf[pos + 2] << 16 | buf[pos + 3] << 24) >>> 0;
    }
    function readUintBE(buf, pos) {
      return (buf[pos] << 24 | buf[pos + 1] << 16 | buf[pos + 2] << 8 | buf[pos + 3]) >>> 0;
    }
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/util/utf8.js
var require_utf8 = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/util/utf8.js"(exports) {
    "use strict";
    var utf8 = exports;
    var replacementChar = "\uFFFD";
    var looseDecoder = new TextDecoder("utf-8", { ignoreBOM: true });
    var strictDecoder;
    var TEXT_DECODER_MIN_LENGTH = 64;
    try {
      strictDecoder = new TextDecoder("utf-8", { fatal: true, ignoreBOM: true });
    } catch (err) {
      strictDecoder = looseDecoder;
    }
    utf8.length = function utf8_length(string) {
      var len = 0, c = 0;
      for (var i = 0; i < string.length; ++i) {
        c = string.charCodeAt(i);
        if (c < 128)
          len += 1;
        else if (c < 2048)
          len += 2;
        else if ((c & 64512) === 55296 && (string.charCodeAt(i + 1) & 64512) === 56320) {
          ++i;
          len += 4;
        } else
          len += 3;
      }
      return len;
    };
    function utf8_read_js(buffer, start, end, str) {
      for (var i = start; i < end; ) {
        var t = buffer[i++];
        if (t <= 127) {
          str += String.fromCharCode(t);
        } else if (t >= 192 && t < 224) {
          var c2 = (t & 31) << 6 | buffer[i++] & 63;
          str += c2 >= 128 ? String.fromCharCode(c2) : replacementChar;
        } else if (t >= 224 && t < 240) {
          var c3 = (t & 15) << 12 | (buffer[i++] & 63) << 6 | buffer[i++] & 63;
          str += c3 >= 2048 ? String.fromCharCode(c3) : replacementChar;
        } else if (t >= 240) {
          var t2 = (t & 7) << 18 | (buffer[i++] & 63) << 12 | (buffer[i++] & 63) << 6 | buffer[i++] & 63;
          if (t2 < 65536 || t2 > 1114111)
            str += replacementChar;
          else {
            t2 -= 65536;
            str += String.fromCharCode(55296 + (t2 >> 10));
            str += String.fromCharCode(56320 + (t2 & 1023));
          }
        }
      }
      return str;
    }
    function utf8_read_decoder(decoder, buffer, start, end) {
      var source = start === 0 && end === buffer.length ? buffer : buffer.subarray(start, end);
      return decoder.decode(source);
    }
    utf8.read = function utf8_read_loose(buffer, start, end) {
      if (end - start < 1)
        return "";
      if (end - start >= TEXT_DECODER_MIN_LENGTH)
        return utf8_read_decoder(looseDecoder, buffer, start, end);
      var str = "", i = start, c1, c2, c3, c4, c5, c6, c7, c8;
      for (; i + 7 < end; i += 8) {
        c1 = buffer[i];
        c2 = buffer[i + 1];
        c3 = buffer[i + 2];
        c4 = buffer[i + 3];
        c5 = buffer[i + 4];
        c6 = buffer[i + 5];
        c7 = buffer[i + 6];
        c8 = buffer[i + 7];
        if ((c1 | c2 | c3 | c4 | c5 | c6 | c7 | c8) & 128)
          return utf8_read_js(buffer, i, end, str);
        str += String.fromCharCode(c1, c2, c3, c4, c5, c6, c7, c8);
      }
      for (; i < end; ++i) {
        c1 = buffer[i];
        if (c1 & 128)
          return utf8_read_js(buffer, i, end, str);
        str += String.fromCharCode(c1);
      }
      return str;
    };
    utf8.readStrict = function utf8_read_strict(buffer, start, end) {
      if (end - start < 1)
        return "";
      if (end - start >= TEXT_DECODER_MIN_LENGTH)
        return utf8_read_decoder(strictDecoder, buffer, start, end);
      var str = "", i = start, c1, c2, c3, c4, c5, c6, c7, c8;
      for (; i + 7 < end; i += 8) {
        c1 = buffer[i];
        c2 = buffer[i + 1];
        c3 = buffer[i + 2];
        c4 = buffer[i + 3];
        c5 = buffer[i + 4];
        c6 = buffer[i + 5];
        c7 = buffer[i + 6];
        c8 = buffer[i + 7];
        if ((c1 | c2 | c3 | c4 | c5 | c6 | c7 | c8) & 128)
          return str + utf8_read_decoder(strictDecoder, buffer, i, end);
        str += String.fromCharCode(c1, c2, c3, c4, c5, c6, c7, c8);
      }
      for (; i < end; ++i) {
        c1 = buffer[i];
        if (c1 & 128)
          return str + utf8_read_decoder(strictDecoder, buffer, i, end);
        str += String.fromCharCode(c1);
      }
      return str;
    };
    utf8.write = function utf8_write(string, buffer, offset) {
      var start = offset, c1, c2;
      for (var i = 0; i < string.length; ++i) {
        c1 = string.charCodeAt(i);
        if (c1 < 128) {
          buffer[offset++] = c1;
        } else if (c1 < 2048) {
          buffer[offset++] = c1 >> 6 | 192;
          buffer[offset++] = c1 & 63 | 128;
        } else if ((c1 & 64512) === 55296 && ((c2 = string.charCodeAt(i + 1)) & 64512) === 56320) {
          c1 = 65536 + ((c1 & 1023) << 10) + (c2 & 1023);
          ++i;
          buffer[offset++] = c1 >> 18 | 240;
          buffer[offset++] = c1 >> 12 & 63 | 128;
          buffer[offset++] = c1 >> 6 & 63 | 128;
          buffer[offset++] = c1 & 63 | 128;
        } else {
          buffer[offset++] = c1 >> 12 | 224;
          buffer[offset++] = c1 >> 6 & 63 | 128;
          buffer[offset++] = c1 & 63 | 128;
        }
      }
      return offset - start;
    };
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/util/pool.js
var require_pool = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/util/pool.js"(exports, module) {
    "use strict";
    module.exports = pool;
    function pool(alloc, slice, size) {
      var SIZE = size || 8192;
      var MAX = SIZE >>> 1;
      var slab = null;
      var offset = SIZE;
      return function pool_alloc(size2) {
        if (size2 < 1 || size2 > MAX)
          return alloc(size2);
        if (offset + size2 > SIZE) {
          slab = alloc(SIZE);
          offset = 0;
        }
        var buf = slice.call(slab, offset, offset += size2);
        if (offset & 7)
          offset = (offset | 7) + 1;
        return buf;
      };
    }
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/util/longbits.js
var require_longbits = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/util/longbits.js"(exports, module) {
    "use strict";
    module.exports = LongBits;
    var Long;
    function LongBits(lo, hi) {
      this.lo = lo >>> 0;
      this.hi = hi >>> 0;
    }
    var zero = LongBits.zero = new LongBits(0, 0);
    zero.toNumber = function() {
      return 0;
    };
    zero.zzEncode = zero.zzDecode = function() {
      return this;
    };
    zero.length = function() {
      return 1;
    };
    var zeroHash = LongBits.zeroHash = "\0\0\0\0\0\0\0\0";
    LongBits.fromNumber = function fromNumber(value) {
      if (value === 0)
        return zero;
      var sign = value < 0;
      if (sign)
        value = -value;
      var lo = value >>> 0, hi = (value - lo) / 4294967296 >>> 0;
      if (sign) {
        hi = ~hi >>> 0;
        lo = ~lo >>> 0;
        if (++lo > 4294967295) {
          lo = 0;
          if (++hi > 4294967295)
            hi = 0;
        }
      }
      return new LongBits(lo, hi);
    };
    LongBits.from = function from(value) {
      if (typeof value === "number")
        return LongBits.fromNumber(value);
      if (typeof value === "string" || value instanceof String) {
        if (Long)
          value = Long.fromString(value);
        else
          return LongBits.fromNumber(parseInt(value, 10));
      }
      return value.low || value.high ? new LongBits(value.low >>> 0, value.high >>> 0) : zero;
    };
    LongBits.prototype.toNumber = function toNumber(unsigned) {
      if (!unsigned && this.hi >>> 31) {
        var lo = ~this.lo + 1 >>> 0, hi = ~this.hi >>> 0;
        if (!lo)
          hi = hi + 1 >>> 0;
        return -(lo + hi * 4294967296);
      }
      return this.lo + this.hi * 4294967296;
    };
    LongBits.prototype.toLong = function toLong(unsigned) {
      return Long ? new Long(this.lo | 0, this.hi | 0, Boolean(unsigned)) : { low: this.lo | 0, high: this.hi | 0, unsigned: Boolean(unsigned) };
    };
    var charCodeAt = String.prototype.charCodeAt;
    LongBits.fromHash = function fromHash(hash) {
      if (hash === zeroHash)
        return zero;
      return new LongBits(
        (charCodeAt.call(hash, 0) | charCodeAt.call(hash, 1) << 8 | charCodeAt.call(hash, 2) << 16 | charCodeAt.call(hash, 3) << 24) >>> 0,
        (charCodeAt.call(hash, 4) | charCodeAt.call(hash, 5) << 8 | charCodeAt.call(hash, 6) << 16 | charCodeAt.call(hash, 7) << 24) >>> 0
      );
    };
    LongBits.prototype.toHash = function toHash() {
      return String.fromCharCode(
        this.lo & 255,
        this.lo >>> 8 & 255,
        this.lo >>> 16 & 255,
        this.lo >>> 24,
        this.hi & 255,
        this.hi >>> 8 & 255,
        this.hi >>> 16 & 255,
        this.hi >>> 24
      );
    };
    LongBits.prototype.zzEncode = function zzEncode() {
      var mask = this.hi >> 31;
      this.hi = ((this.hi << 1 | this.lo >>> 31) ^ mask) >>> 0;
      this.lo = (this.lo << 1 ^ mask) >>> 0;
      return this;
    };
    LongBits.prototype.zzDecode = function zzDecode() {
      var mask = -(this.lo & 1);
      this.lo = ((this.lo >>> 1 | this.hi << 31) ^ mask) >>> 0;
      this.hi = (this.hi >>> 1 ^ mask) >>> 0;
      return this;
    };
    LongBits.prototype.length = function length() {
      var part0 = this.lo, part1 = (this.lo >>> 28 | this.hi << 4) >>> 0, part2 = this.hi >>> 24;
      return part2 === 0 ? part1 === 0 ? part0 < 16384 ? part0 < 128 ? 1 : 2 : part0 < 2097152 ? 3 : 4 : part1 < 16384 ? part1 < 128 ? 5 : 6 : part1 < 2097152 ? 7 : 8 : part2 < 128 ? 9 : 10;
    };
    LongBits._configure = function(Long_) {
      Long = Long_;
    };
  }
});

// src/wwwroot_brpc/node_modules/long/umd/index.js
var require_umd = __commonJS({
  "src/wwwroot_brpc/node_modules/long/umd/index.js"(exports, module) {
    (function(global2, factory) {
      function preferDefault(exports2) {
        return exports2.default || exports2;
      }
      if (typeof define === "function" && define.amd) {
        define([], function() {
          var exports2 = {};
          factory(exports2);
          return preferDefault(exports2);
        });
      } else if (typeof exports === "object") {
        factory(exports);
        if (typeof module === "object") module.exports = preferDefault(exports);
      } else {
        (function() {
          var exports2 = {};
          factory(exports2);
          global2.Long = preferDefault(exports2);
        })();
      }
    })(
      typeof globalThis !== "undefined" ? globalThis : typeof self !== "undefined" ? self : exports,
      function(_exports) {
        "use strict";
        Object.defineProperty(_exports, "__esModule", {
          value: true
        });
        _exports.default = void 0;
        var wasm = null;
        try {
          wasm = new WebAssembly.Instance(
            new WebAssembly.Module(
              new Uint8Array([
                // \0asm
                0,
                97,
                115,
                109,
                // version 1
                1,
                0,
                0,
                0,
                // section "type"
                1,
                13,
                2,
                // 0, () => i32
                96,
                0,
                1,
                127,
                // 1, (i32, i32, i32, i32) => i32
                96,
                4,
                127,
                127,
                127,
                127,
                1,
                127,
                // section "function"
                3,
                7,
                6,
                // 0, type 0
                0,
                // 1, type 1
                1,
                // 2, type 1
                1,
                // 3, type 1
                1,
                // 4, type 1
                1,
                // 5, type 1
                1,
                // section "global"
                6,
                6,
                1,
                // 0, "high", mutable i32
                127,
                1,
                65,
                0,
                11,
                // section "export"
                7,
                50,
                6,
                // 0, "mul"
                3,
                109,
                117,
                108,
                0,
                1,
                // 1, "div_s"
                5,
                100,
                105,
                118,
                95,
                115,
                0,
                2,
                // 2, "div_u"
                5,
                100,
                105,
                118,
                95,
                117,
                0,
                3,
                // 3, "rem_s"
                5,
                114,
                101,
                109,
                95,
                115,
                0,
                4,
                // 4, "rem_u"
                5,
                114,
                101,
                109,
                95,
                117,
                0,
                5,
                // 5, "get_high"
                8,
                103,
                101,
                116,
                95,
                104,
                105,
                103,
                104,
                0,
                0,
                // section "code"
                10,
                191,
                1,
                6,
                // 0, "get_high"
                4,
                0,
                35,
                0,
                11,
                // 1, "mul"
                36,
                1,
                1,
                126,
                32,
                0,
                173,
                32,
                1,
                173,
                66,
                32,
                134,
                132,
                32,
                2,
                173,
                32,
                3,
                173,
                66,
                32,
                134,
                132,
                126,
                34,
                4,
                66,
                32,
                135,
                167,
                36,
                0,
                32,
                4,
                167,
                11,
                // 2, "div_s"
                36,
                1,
                1,
                126,
                32,
                0,
                173,
                32,
                1,
                173,
                66,
                32,
                134,
                132,
                32,
                2,
                173,
                32,
                3,
                173,
                66,
                32,
                134,
                132,
                127,
                34,
                4,
                66,
                32,
                135,
                167,
                36,
                0,
                32,
                4,
                167,
                11,
                // 3, "div_u"
                36,
                1,
                1,
                126,
                32,
                0,
                173,
                32,
                1,
                173,
                66,
                32,
                134,
                132,
                32,
                2,
                173,
                32,
                3,
                173,
                66,
                32,
                134,
                132,
                128,
                34,
                4,
                66,
                32,
                135,
                167,
                36,
                0,
                32,
                4,
                167,
                11,
                // 4, "rem_s"
                36,
                1,
                1,
                126,
                32,
                0,
                173,
                32,
                1,
                173,
                66,
                32,
                134,
                132,
                32,
                2,
                173,
                32,
                3,
                173,
                66,
                32,
                134,
                132,
                129,
                34,
                4,
                66,
                32,
                135,
                167,
                36,
                0,
                32,
                4,
                167,
                11,
                // 5, "rem_u"
                36,
                1,
                1,
                126,
                32,
                0,
                173,
                32,
                1,
                173,
                66,
                32,
                134,
                132,
                32,
                2,
                173,
                32,
                3,
                173,
                66,
                32,
                134,
                132,
                130,
                34,
                4,
                66,
                32,
                135,
                167,
                36,
                0,
                32,
                4,
                167,
                11
              ])
            ),
            {}
          ).exports;
        } catch {
        }
        function Long(low, high, unsigned) {
          this.low = low | 0;
          this.high = high | 0;
          this.unsigned = !!unsigned;
        }
        Long.prototype.__isLong__;
        Object.defineProperty(Long.prototype, "__isLong__", {
          value: true
        });
        function isLong(obj) {
          return (obj && obj["__isLong__"]) === true;
        }
        function ctz32(value) {
          var c = Math.clz32(value & -value);
          return value ? 31 - c : c;
        }
        Long.isLong = isLong;
        var INT_CACHE = {};
        var UINT_CACHE = {};
        function fromInt(value, unsigned) {
          var obj, cachedObj, cache;
          if (unsigned) {
            value >>>= 0;
            if (cache = 0 <= value && value < 256) {
              cachedObj = UINT_CACHE[value];
              if (cachedObj) return cachedObj;
            }
            obj = fromBits(value, 0, true);
            if (cache) UINT_CACHE[value] = obj;
            return obj;
          } else {
            value |= 0;
            if (cache = -128 <= value && value < 128) {
              cachedObj = INT_CACHE[value];
              if (cachedObj) return cachedObj;
            }
            obj = fromBits(value, value < 0 ? -1 : 0, false);
            if (cache) INT_CACHE[value] = obj;
            return obj;
          }
        }
        Long.fromInt = fromInt;
        function fromNumber(value, unsigned) {
          if (isNaN(value)) return unsigned ? UZERO : ZERO;
          if (unsigned) {
            if (value < 0) return UZERO;
            if (value >= TWO_PWR_64_DBL) return MAX_UNSIGNED_VALUE;
          } else {
            if (value <= -TWO_PWR_63_DBL) return MIN_VALUE;
            if (value + 1 >= TWO_PWR_63_DBL) return MAX_VALUE;
          }
          if (value < 0) return fromNumber(-value, unsigned).neg();
          return fromBits(
            value % TWO_PWR_32_DBL | 0,
            value / TWO_PWR_32_DBL | 0,
            unsigned
          );
        }
        Long.fromNumber = fromNumber;
        function fromBits(lowBits, highBits, unsigned) {
          return new Long(lowBits, highBits, unsigned);
        }
        Long.fromBits = fromBits;
        var pow_dbl = Math.pow;
        function fromString(str, unsigned, radix) {
          if (str.length === 0) throw Error("empty string");
          if (typeof unsigned === "number") {
            radix = unsigned;
            unsigned = false;
          } else {
            unsigned = !!unsigned;
          }
          if (str === "NaN" || str === "Infinity" || str === "+Infinity" || str === "-Infinity")
            return unsigned ? UZERO : ZERO;
          radix = radix || 10;
          if (radix < 2 || 36 < radix) throw RangeError("radix");
          var p;
          if ((p = str.indexOf("-")) > 0) throw Error("interior hyphen");
          else if (p === 0) {
            return fromString(str.substring(1), unsigned, radix).neg();
          }
          var radixToPower = fromNumber(pow_dbl(radix, 8));
          var result = ZERO;
          for (var i = 0; i < str.length; i += 8) {
            var size = Math.min(8, str.length - i), value = parseInt(str.substring(i, i + size), radix);
            if (size < 8) {
              var power = fromNumber(pow_dbl(radix, size));
              result = result.mul(power).add(fromNumber(value));
            } else {
              result = result.mul(radixToPower);
              result = result.add(fromNumber(value));
            }
          }
          result.unsigned = unsigned;
          return result;
        }
        Long.fromString = fromString;
        function fromValue(val, unsigned) {
          if (typeof val === "number") return fromNumber(val, unsigned);
          if (typeof val === "string") return fromString(val, unsigned);
          return fromBits(
            val.low,
            val.high,
            typeof unsigned === "boolean" ? unsigned : val.unsigned
          );
        }
        Long.fromValue = fromValue;
        var TWO_PWR_16_DBL = 1 << 16;
        var TWO_PWR_24_DBL = 1 << 24;
        var TWO_PWR_32_DBL = TWO_PWR_16_DBL * TWO_PWR_16_DBL;
        var TWO_PWR_64_DBL = TWO_PWR_32_DBL * TWO_PWR_32_DBL;
        var TWO_PWR_63_DBL = TWO_PWR_64_DBL / 2;
        var TWO_PWR_24 = fromInt(TWO_PWR_24_DBL);
        var ZERO = fromInt(0);
        Long.ZERO = ZERO;
        var UZERO = fromInt(0, true);
        Long.UZERO = UZERO;
        var ONE = fromInt(1);
        Long.ONE = ONE;
        var UONE = fromInt(1, true);
        Long.UONE = UONE;
        var NEG_ONE = fromInt(-1);
        Long.NEG_ONE = NEG_ONE;
        var MAX_VALUE = fromBits(4294967295 | 0, 2147483647 | 0, false);
        Long.MAX_VALUE = MAX_VALUE;
        var MAX_UNSIGNED_VALUE = fromBits(4294967295 | 0, 4294967295 | 0, true);
        Long.MAX_UNSIGNED_VALUE = MAX_UNSIGNED_VALUE;
        var MIN_VALUE = fromBits(0, 2147483648 | 0, false);
        Long.MIN_VALUE = MIN_VALUE;
        var LongPrototype = Long.prototype;
        LongPrototype.toInt = function toInt() {
          return this.unsigned ? this.low >>> 0 : this.low;
        };
        LongPrototype.toNumber = function toNumber() {
          if (this.unsigned)
            return (this.high >>> 0) * TWO_PWR_32_DBL + (this.low >>> 0);
          return this.high * TWO_PWR_32_DBL + (this.low >>> 0);
        };
        LongPrototype.toString = function toString(radix) {
          radix = radix || 10;
          if (radix < 2 || 36 < radix) throw RangeError("radix");
          if (this.isZero()) return "0";
          if (this.isNegative()) {
            if (this.eq(MIN_VALUE)) {
              var radixLong = fromNumber(radix), div = this.div(radixLong), rem1 = div.mul(radixLong).sub(this);
              return div.toString(radix) + rem1.toInt().toString(radix);
            } else return "-" + this.neg().toString(radix);
          }
          var radixToPower = fromNumber(pow_dbl(radix, 6), this.unsigned), rem = this;
          var result = "";
          while (true) {
            var remDiv = rem.div(radixToPower), intval = rem.sub(remDiv.mul(radixToPower)).toInt() >>> 0, digits = intval.toString(radix);
            rem = remDiv;
            if (rem.isZero()) return digits + result;
            else {
              while (digits.length < 6) digits = "0" + digits;
              result = "" + digits + result;
            }
          }
        };
        LongPrototype.getHighBits = function getHighBits() {
          return this.high;
        };
        LongPrototype.getHighBitsUnsigned = function getHighBitsUnsigned() {
          return this.high >>> 0;
        };
        LongPrototype.getLowBits = function getLowBits() {
          return this.low;
        };
        LongPrototype.getLowBitsUnsigned = function getLowBitsUnsigned() {
          return this.low >>> 0;
        };
        LongPrototype.getNumBitsAbs = function getNumBitsAbs() {
          if (this.isNegative())
            return this.eq(MIN_VALUE) ? 64 : this.neg().getNumBitsAbs();
          var val = this.high != 0 ? this.high : this.low;
          for (var bit = 31; bit > 0; bit--) if ((val & 1 << bit) != 0) break;
          return this.high != 0 ? bit + 33 : bit + 1;
        };
        LongPrototype.isSafeInteger = function isSafeInteger() {
          var top11Bits = this.high >> 21;
          if (!top11Bits) return true;
          if (this.unsigned) return false;
          return top11Bits === -1 && !(this.low === 0 && this.high === -2097152);
        };
        LongPrototype.isZero = function isZero() {
          return this.high === 0 && this.low === 0;
        };
        LongPrototype.eqz = LongPrototype.isZero;
        LongPrototype.isNegative = function isNegative() {
          return !this.unsigned && this.high < 0;
        };
        LongPrototype.isPositive = function isPositive() {
          return this.unsigned || this.high >= 0;
        };
        LongPrototype.isOdd = function isOdd() {
          return (this.low & 1) === 1;
        };
        LongPrototype.isEven = function isEven() {
          return (this.low & 1) === 0;
        };
        LongPrototype.equals = function equals(other) {
          if (!isLong(other)) other = fromValue(other);
          if (this.unsigned !== other.unsigned && this.high >>> 31 === 1 && other.high >>> 31 === 1)
            return false;
          return this.high === other.high && this.low === other.low;
        };
        LongPrototype.eq = LongPrototype.equals;
        LongPrototype.notEquals = function notEquals(other) {
          return !this.eq(
            /* validates */
            other
          );
        };
        LongPrototype.neq = LongPrototype.notEquals;
        LongPrototype.ne = LongPrototype.notEquals;
        LongPrototype.lessThan = function lessThan(other) {
          return this.comp(
            /* validates */
            other
          ) < 0;
        };
        LongPrototype.lt = LongPrototype.lessThan;
        LongPrototype.lessThanOrEqual = function lessThanOrEqual(other) {
          return this.comp(
            /* validates */
            other
          ) <= 0;
        };
        LongPrototype.lte = LongPrototype.lessThanOrEqual;
        LongPrototype.le = LongPrototype.lessThanOrEqual;
        LongPrototype.greaterThan = function greaterThan(other) {
          return this.comp(
            /* validates */
            other
          ) > 0;
        };
        LongPrototype.gt = LongPrototype.greaterThan;
        LongPrototype.greaterThanOrEqual = function greaterThanOrEqual(other) {
          return this.comp(
            /* validates */
            other
          ) >= 0;
        };
        LongPrototype.gte = LongPrototype.greaterThanOrEqual;
        LongPrototype.ge = LongPrototype.greaterThanOrEqual;
        LongPrototype.compare = function compare(other) {
          if (!isLong(other)) other = fromValue(other);
          if (this.eq(other)) return 0;
          var thisNeg = this.isNegative(), otherNeg = other.isNegative();
          if (thisNeg && !otherNeg) return -1;
          if (!thisNeg && otherNeg) return 1;
          if (!this.unsigned) return this.sub(other).isNegative() ? -1 : 1;
          return other.high >>> 0 > this.high >>> 0 || other.high === this.high && other.low >>> 0 > this.low >>> 0 ? -1 : 1;
        };
        LongPrototype.comp = LongPrototype.compare;
        LongPrototype.negate = function negate() {
          if (!this.unsigned && this.eq(MIN_VALUE)) return MIN_VALUE;
          return this.not().add(ONE);
        };
        LongPrototype.neg = LongPrototype.negate;
        LongPrototype.add = function add(addend) {
          if (!isLong(addend)) addend = fromValue(addend);
          var a48 = this.high >>> 16;
          var a32 = this.high & 65535;
          var a16 = this.low >>> 16;
          var a00 = this.low & 65535;
          var b48 = addend.high >>> 16;
          var b32 = addend.high & 65535;
          var b16 = addend.low >>> 16;
          var b00 = addend.low & 65535;
          var c48 = 0, c32 = 0, c16 = 0, c00 = 0;
          c00 += a00 + b00;
          c16 += c00 >>> 16;
          c00 &= 65535;
          c16 += a16 + b16;
          c32 += c16 >>> 16;
          c16 &= 65535;
          c32 += a32 + b32;
          c48 += c32 >>> 16;
          c32 &= 65535;
          c48 += a48 + b48;
          c48 &= 65535;
          return fromBits(c16 << 16 | c00, c48 << 16 | c32, this.unsigned);
        };
        LongPrototype.subtract = function subtract(subtrahend) {
          if (!isLong(subtrahend)) subtrahend = fromValue(subtrahend);
          return this.add(subtrahend.neg());
        };
        LongPrototype.sub = LongPrototype.subtract;
        LongPrototype.multiply = function multiply(multiplier) {
          if (this.isZero()) return this;
          if (!isLong(multiplier)) multiplier = fromValue(multiplier);
          if (wasm) {
            var low = wasm["mul"](
              this.low,
              this.high,
              multiplier.low,
              multiplier.high
            );
            return fromBits(low, wasm["get_high"](), this.unsigned);
          }
          if (multiplier.isZero()) return this.unsigned ? UZERO : ZERO;
          if (this.eq(MIN_VALUE)) return multiplier.isOdd() ? MIN_VALUE : ZERO;
          if (multiplier.eq(MIN_VALUE)) return this.isOdd() ? MIN_VALUE : ZERO;
          if (this.isNegative()) {
            if (multiplier.isNegative()) return this.neg().mul(multiplier.neg());
            else return this.neg().mul(multiplier).neg();
          } else if (multiplier.isNegative())
            return this.mul(multiplier.neg()).neg();
          if (this.lt(TWO_PWR_24) && multiplier.lt(TWO_PWR_24))
            return fromNumber(
              this.toNumber() * multiplier.toNumber(),
              this.unsigned
            );
          var a48 = this.high >>> 16;
          var a32 = this.high & 65535;
          var a16 = this.low >>> 16;
          var a00 = this.low & 65535;
          var b48 = multiplier.high >>> 16;
          var b32 = multiplier.high & 65535;
          var b16 = multiplier.low >>> 16;
          var b00 = multiplier.low & 65535;
          var c48 = 0, c32 = 0, c16 = 0, c00 = 0;
          c00 += a00 * b00;
          c16 += c00 >>> 16;
          c00 &= 65535;
          c16 += a16 * b00;
          c32 += c16 >>> 16;
          c16 &= 65535;
          c16 += a00 * b16;
          c32 += c16 >>> 16;
          c16 &= 65535;
          c32 += a32 * b00;
          c48 += c32 >>> 16;
          c32 &= 65535;
          c32 += a16 * b16;
          c48 += c32 >>> 16;
          c32 &= 65535;
          c32 += a00 * b32;
          c48 += c32 >>> 16;
          c32 &= 65535;
          c48 += a48 * b00 + a32 * b16 + a16 * b32 + a00 * b48;
          c48 &= 65535;
          return fromBits(c16 << 16 | c00, c48 << 16 | c32, this.unsigned);
        };
        LongPrototype.mul = LongPrototype.multiply;
        LongPrototype.divide = function divide(divisor) {
          if (!isLong(divisor)) divisor = fromValue(divisor);
          if (divisor.isZero()) throw Error("division by zero");
          if (wasm) {
            if (!this.unsigned && this.high === -2147483648 && divisor.low === -1 && divisor.high === -1) {
              return this;
            }
            var low = (this.unsigned ? wasm["div_u"] : wasm["div_s"])(
              this.low,
              this.high,
              divisor.low,
              divisor.high
            );
            return fromBits(low, wasm["get_high"](), this.unsigned);
          }
          if (this.isZero()) return this.unsigned ? UZERO : ZERO;
          var approx, rem, res;
          if (!this.unsigned) {
            if (this.eq(MIN_VALUE)) {
              if (divisor.eq(ONE) || divisor.eq(NEG_ONE))
                return MIN_VALUE;
              else if (divisor.eq(MIN_VALUE)) return ONE;
              else {
                var halfThis = this.shr(1);
                approx = halfThis.div(divisor).shl(1);
                if (approx.eq(ZERO)) {
                  return divisor.isNegative() ? ONE : NEG_ONE;
                } else {
                  rem = this.sub(divisor.mul(approx));
                  res = approx.add(rem.div(divisor));
                  return res;
                }
              }
            } else if (divisor.eq(MIN_VALUE)) return this.unsigned ? UZERO : ZERO;
            if (this.isNegative()) {
              if (divisor.isNegative()) return this.neg().div(divisor.neg());
              return this.neg().div(divisor).neg();
            } else if (divisor.isNegative()) return this.div(divisor.neg()).neg();
            res = ZERO;
          } else {
            if (!divisor.unsigned) divisor = divisor.toUnsigned();
            if (divisor.gt(this)) return UZERO;
            if (divisor.gt(this.shru(1)))
              return UONE;
            res = UZERO;
          }
          rem = this;
          while (rem.gte(divisor)) {
            approx = Math.max(1, Math.floor(rem.toNumber() / divisor.toNumber()));
            var log2 = Math.ceil(Math.log(approx) / Math.LN2), delta = log2 <= 48 ? 1 : pow_dbl(2, log2 - 48), approxRes = fromNumber(approx), approxRem = approxRes.mul(divisor);
            while (approxRem.isNegative() || approxRem.gt(rem)) {
              approx -= delta;
              approxRes = fromNumber(approx, this.unsigned);
              approxRem = approxRes.mul(divisor);
            }
            if (approxRes.isZero()) approxRes = ONE;
            res = res.add(approxRes);
            rem = rem.sub(approxRem);
          }
          return res;
        };
        LongPrototype.div = LongPrototype.divide;
        LongPrototype.modulo = function modulo(divisor) {
          if (!isLong(divisor)) divisor = fromValue(divisor);
          if (wasm) {
            var low = (this.unsigned ? wasm["rem_u"] : wasm["rem_s"])(
              this.low,
              this.high,
              divisor.low,
              divisor.high
            );
            return fromBits(low, wasm["get_high"](), this.unsigned);
          }
          return this.sub(this.div(divisor).mul(divisor));
        };
        LongPrototype.mod = LongPrototype.modulo;
        LongPrototype.rem = LongPrototype.modulo;
        LongPrototype.not = function not() {
          return fromBits(~this.low, ~this.high, this.unsigned);
        };
        LongPrototype.countLeadingZeros = function countLeadingZeros() {
          return this.high ? Math.clz32(this.high) : Math.clz32(this.low) + 32;
        };
        LongPrototype.clz = LongPrototype.countLeadingZeros;
        LongPrototype.countTrailingZeros = function countTrailingZeros() {
          return this.low ? ctz32(this.low) : ctz32(this.high) + 32;
        };
        LongPrototype.ctz = LongPrototype.countTrailingZeros;
        LongPrototype.and = function and(other) {
          if (!isLong(other)) other = fromValue(other);
          return fromBits(
            this.low & other.low,
            this.high & other.high,
            this.unsigned
          );
        };
        LongPrototype.or = function or(other) {
          if (!isLong(other)) other = fromValue(other);
          return fromBits(
            this.low | other.low,
            this.high | other.high,
            this.unsigned
          );
        };
        LongPrototype.xor = function xor(other) {
          if (!isLong(other)) other = fromValue(other);
          return fromBits(
            this.low ^ other.low,
            this.high ^ other.high,
            this.unsigned
          );
        };
        LongPrototype.shiftLeft = function shiftLeft(numBits) {
          if (isLong(numBits)) numBits = numBits.toInt();
          if ((numBits &= 63) === 0) return this;
          else if (numBits < 32)
            return fromBits(
              this.low << numBits,
              this.high << numBits | this.low >>> 32 - numBits,
              this.unsigned
            );
          else return fromBits(0, this.low << numBits - 32, this.unsigned);
        };
        LongPrototype.shl = LongPrototype.shiftLeft;
        LongPrototype.shiftRight = function shiftRight(numBits) {
          if (isLong(numBits)) numBits = numBits.toInt();
          if ((numBits &= 63) === 0) return this;
          else if (numBits < 32)
            return fromBits(
              this.low >>> numBits | this.high << 32 - numBits,
              this.high >> numBits,
              this.unsigned
            );
          else
            return fromBits(
              this.high >> numBits - 32,
              this.high >= 0 ? 0 : -1,
              this.unsigned
            );
        };
        LongPrototype.shr = LongPrototype.shiftRight;
        LongPrototype.shiftRightUnsigned = function shiftRightUnsigned(numBits) {
          if (isLong(numBits)) numBits = numBits.toInt();
          if ((numBits &= 63) === 0) return this;
          if (numBits < 32)
            return fromBits(
              this.low >>> numBits | this.high << 32 - numBits,
              this.high >>> numBits,
              this.unsigned
            );
          if (numBits === 32) return fromBits(this.high, 0, this.unsigned);
          return fromBits(this.high >>> numBits - 32, 0, this.unsigned);
        };
        LongPrototype.shru = LongPrototype.shiftRightUnsigned;
        LongPrototype.shr_u = LongPrototype.shiftRightUnsigned;
        LongPrototype.rotateLeft = function rotateLeft(numBits) {
          var b;
          if (isLong(numBits)) numBits = numBits.toInt();
          if ((numBits &= 63) === 0) return this;
          if (numBits === 32) return fromBits(this.high, this.low, this.unsigned);
          if (numBits < 32) {
            b = 32 - numBits;
            return fromBits(
              this.low << numBits | this.high >>> b,
              this.high << numBits | this.low >>> b,
              this.unsigned
            );
          }
          numBits -= 32;
          b = 32 - numBits;
          return fromBits(
            this.high << numBits | this.low >>> b,
            this.low << numBits | this.high >>> b,
            this.unsigned
          );
        };
        LongPrototype.rotl = LongPrototype.rotateLeft;
        LongPrototype.rotateRight = function rotateRight(numBits) {
          var b;
          if (isLong(numBits)) numBits = numBits.toInt();
          if ((numBits &= 63) === 0) return this;
          if (numBits === 32) return fromBits(this.high, this.low, this.unsigned);
          if (numBits < 32) {
            b = 32 - numBits;
            return fromBits(
              this.high << b | this.low >>> numBits,
              this.low << b | this.high >>> numBits,
              this.unsigned
            );
          }
          numBits -= 32;
          b = 32 - numBits;
          return fromBits(
            this.low << b | this.high >>> numBits,
            this.high << b | this.low >>> numBits,
            this.unsigned
          );
        };
        LongPrototype.rotr = LongPrototype.rotateRight;
        LongPrototype.toSigned = function toSigned() {
          if (!this.unsigned) return this;
          return fromBits(this.low, this.high, false);
        };
        LongPrototype.toUnsigned = function toUnsigned() {
          if (this.unsigned) return this;
          return fromBits(this.low, this.high, true);
        };
        LongPrototype.toBytes = function toBytes(le) {
          return le ? this.toBytesLE() : this.toBytesBE();
        };
        LongPrototype.toBytesLE = function toBytesLE() {
          var hi = this.high, lo = this.low;
          return [
            lo & 255,
            lo >>> 8 & 255,
            lo >>> 16 & 255,
            lo >>> 24,
            hi & 255,
            hi >>> 8 & 255,
            hi >>> 16 & 255,
            hi >>> 24
          ];
        };
        LongPrototype.toBytesBE = function toBytesBE() {
          var hi = this.high, lo = this.low;
          return [
            hi >>> 24,
            hi >>> 16 & 255,
            hi >>> 8 & 255,
            hi & 255,
            lo >>> 24,
            lo >>> 16 & 255,
            lo >>> 8 & 255,
            lo & 255
          ];
        };
        Long.fromBytes = function fromBytes(bytes, unsigned, le) {
          return le ? Long.fromBytesLE(bytes, unsigned) : Long.fromBytesBE(bytes, unsigned);
        };
        Long.fromBytesLE = function fromBytesLE(bytes, unsigned) {
          return new Long(
            bytes[0] | bytes[1] << 8 | bytes[2] << 16 | bytes[3] << 24,
            bytes[4] | bytes[5] << 8 | bytes[6] << 16 | bytes[7] << 24,
            unsigned
          );
        };
        Long.fromBytesBE = function fromBytesBE(bytes, unsigned) {
          return new Long(
            bytes[4] << 24 | bytes[5] << 16 | bytes[6] << 8 | bytes[7],
            bytes[0] << 24 | bytes[1] << 16 | bytes[2] << 8 | bytes[3],
            unsigned
          );
        };
        if (typeof BigInt === "function") {
          Long.fromBigInt = function fromBigInt(value, unsigned) {
            var lowBits = Number(BigInt.asIntN(32, value));
            var highBits = Number(BigInt.asIntN(32, value >> BigInt(32)));
            return fromBits(lowBits, highBits, unsigned);
          };
          Long.fromValue = function fromValueWithBigInt(value, unsigned) {
            if (typeof value === "bigint") return Long.fromBigInt(value, unsigned);
            return fromValue(value, unsigned);
          };
          LongPrototype.toBigInt = function toBigInt() {
            var lowBigInt = BigInt(this.low >>> 0);
            var highBigInt = BigInt(this.unsigned ? this.high >>> 0 : this.high);
            return highBigInt << BigInt(32) | lowBigInt;
          };
        }
        var _default = _exports.default = Long;
      }
    );
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/util/minimal.js
var require_minimal = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/util/minimal.js"(exports) {
    "use strict";
    var util = exports;
    util.asPromise = require_aspromise();
    util.base64 = require_base64();
    util.EventEmitter = require_eventemitter();
    util.float = require_float();
    util.utf8 = require_utf8();
    util.pool = require_pool();
    util.LongBits = require_longbits();
    function isUnsafeProperty(key) {
      return key === "__proto__" || key === "prototype" || key === "constructor";
    }
    util.isUnsafeProperty = isUnsafeProperty;
    util.isNode = Boolean(typeof global !== "undefined" && global && global.process && global.process.versions && global.process.versions.node);
    util.global = util.isNode && global || typeof window !== "undefined" && window || typeof self !== "undefined" && self || typeof globalThis !== "undefined" && globalThis || exports;
    util.emptyArray = Object.freeze ? Object.freeze([]) : (
      /* istanbul ignore next */
      []
    );
    util.emptyObject = Object.freeze ? Object.freeze({}) : (
      /* istanbul ignore next */
      {}
    );
    util.isInteger = Number.isInteger || /* istanbul ignore next */
    function isInteger(value) {
      return typeof value === "number" && isFinite(value) && Math.floor(value) === value;
    };
    util.isString = function isString(value) {
      return typeof value === "string" || value instanceof String;
    };
    util.isObject = function isObject(value) {
      return value && typeof value === "object";
    };
    util.isset = /**
     * Checks if a property on a message is considered to be present.
     * @param {Object} obj Plain object or message instance
     * @param {string} prop Property name
     * @returns {boolean} `true` if considered to be present, otherwise `false`
     */
    util.isSet = function isSet(obj, prop) {
      var value = obj[prop];
      if (value != null && Object.hasOwnProperty.call(obj, prop))
        return typeof value !== "object" || (Array.isArray(value) ? value.length : Object.keys(value).length) > 0;
      return false;
    };
    util.Buffer = (function() {
      try {
        var Buffer2 = util.global.Buffer;
        return Buffer2.prototype.utf8Write || util.isNode ? Buffer2 : (
          /* istanbul ignore next */
          null
        );
      } catch (e) {
        return null;
      }
    })();
    util.newBuffer = function newBuffer(sizeOrArray) {
      var Buffer2 = util.Buffer;
      return typeof sizeOrArray === "number" ? Buffer2 ? Buffer2.allocUnsafe(sizeOrArray) : new Uint8Array(sizeOrArray) : Buffer2 ? Buffer2.from(sizeOrArray) : new Uint8Array(sizeOrArray);
    };
    util.rawField = function rawField(id, wireType, data) {
      var out = [], tag = id << 3 | wireType;
      tag >>>= 0;
      while (tag > 127) {
        out.push(tag & 127 | 128);
        tag >>>= 7;
      }
      out.push(tag);
      for (var i = 0; i < data.length; ++i)
        out.push(data[i]);
      return util.newBuffer(out);
    };
    util.Array = Uint8Array;
    util.Long = /* istanbul ignore next */
    util.global.dcodeIO && /* istanbul ignore next */
    util.global.dcodeIO.Long || /* istanbul ignore next */
    util.global.Long || (function() {
      try {
        var Long = require_umd();
        return Long && Long.isLong ? Long : null;
      } catch (e) {
        return null;
      }
    })();
    util.key2Re = /^(?:true|false|0|1)$/;
    util.key32Re = /^-?(?:0|[1-9][0-9]*)$/;
    util.key64Re = /^(?:[\x00-\xff]{8}|-?(?:0|[1-9][0-9]*))$/;
    util.longToHash = function longToHash(value) {
      return value ? util.LongBits.from(value).toHash() : util.LongBits.zeroHash;
    };
    util.longFromHash = function longFromHash(hash, unsigned) {
      var bits = util.LongBits.fromHash(hash);
      if (util.Long)
        return util.Long.fromBits(bits.lo, bits.hi, unsigned);
      return bits.toNumber(Boolean(unsigned));
    };
    util.longFromKey = function longFromKey(key, unsigned) {
      return util.key64Re.test(key) && !util.key32Re.test(key) ? util.longFromHash(key, unsigned) : key;
    };
    util.boolFromKey = function boolFromKey(key) {
      return key === "true" || key === "1";
    };
    function merge(dst) {
      var ifNotSet = typeof arguments[arguments.length - 1] === "boolean", limit = ifNotSet ? arguments.length - 1 : arguments.length;
      ifNotSet = ifNotSet && arguments[arguments.length - 1];
      for (var a = 1; a < limit; ++a) {
        var src = arguments[a];
        if (!src)
          continue;
        for (var keys = Object.keys(src), i = 0; i < keys.length; ++i)
          if (!isUnsafeProperty(keys[i]) && (!ifNotSet || !Object.prototype.hasOwnProperty.call(dst, keys[i]) || dst[keys[i]] === void 0))
            dst[keys[i]] = src[keys[i]];
      }
      return dst;
    }
    util.merge = merge;
    util.nestingLimit = 32;
    util.recursionLimit = 100;
    util.makeProp = function makeProp(obj, key, enumerable) {
      if (Object.prototype.hasOwnProperty.call(obj, key))
        return;
      Object.defineProperty(obj, key, {
        enumerable: enumerable === void 0 ? true : enumerable,
        configurable: true,
        writable: true
      });
    };
    util.lcFirst = function lcFirst(str) {
      return str.charAt(0).toLowerCase() + str.substring(1);
    };
    function newError(name) {
      function CustomError(message, properties) {
        if (!(this instanceof CustomError))
          return new CustomError(message, properties);
        Object.defineProperty(this, "message", { get: function() {
          return message;
        } });
        if (Error.captureStackTrace)
          Error.captureStackTrace(this, CustomError);
        else
          Object.defineProperty(this, "stack", { value: new Error().stack || "" });
        if (properties)
          merge(this, properties);
      }
      CustomError.prototype = Object.create(Error.prototype, {
        constructor: {
          value: CustomError,
          writable: true,
          enumerable: false,
          configurable: true
        },
        name: {
          get: function get() {
            return name;
          },
          set: void 0,
          enumerable: false,
          // configurable: false would accurately preserve the behavior of
          // the original, but I'm guessing that was not intentional.
          // For an actual error subclass, this property would
          // be configurable.
          configurable: true
        },
        toString: {
          value: function value() {
            return this.name + ": " + this.message;
          },
          writable: true,
          enumerable: false,
          configurable: true
        }
      });
      return CustomError;
    }
    util.newError = newError;
    util.ProtocolError = newError("ProtocolError");
    util.oneOfGetter = function getOneOf(fieldNames) {
      var fieldMap = {};
      for (var i = 0; i < fieldNames.length; ++i)
        fieldMap[fieldNames[i]] = 1;
      return function() {
        for (var keys = Object.keys(this), i2 = keys.length - 1; i2 > -1; --i2)
          if (fieldMap[keys[i2]] === 1 && this[keys[i2]] !== void 0 && this[keys[i2]] !== null)
            return keys[i2];
      };
    };
    util.oneOfSetter = function setOneOf(fieldNames) {
      return function(name) {
        for (var i = 0; i < fieldNames.length; ++i)
          if (fieldNames[i] !== name)
            delete this[fieldNames[i]];
      };
    };
    util.toJSONOptions = {
      longs: String,
      enums: String,
      bytes: String,
      json: true
    };
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/writer.js
var require_writer = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/writer.js"(exports, module) {
    "use strict";
    module.exports = Writer;
    var util = require_minimal();
    var BufferWriter;
    var LongBits = util.LongBits;
    var base64 = util.base64;
    var utf8 = util.utf8;
    function Writer() {
      this.pos = 0;
      this.buf = this.constructor.alloc(64);
      this.view = null;
      this.states = null;
    }
    Object.defineProperty(Writer.prototype, "len", {
      configurable: true,
      enumerable: true,
      get: function get_len() {
        return this.pos;
      }
    });
    var create = function create2() {
      return util.Buffer ? function create_buffer_setup() {
        return (Writer.create = function create_buffer() {
          return new BufferWriter();
        })();
      } : function create_array() {
        return new Writer();
      };
    };
    Writer.create = create();
    Writer.alloc = function alloc(size) {
      return new Uint8Array(size);
    };
    Writer.alloc = util.pool(Writer.alloc, Uint8Array.prototype.subarray);
    function sizeVarint32(value) {
      return value < 128 ? 1 : value < 16384 ? 2 : value < 2097152 ? 3 : value < 268435456 ? 4 : 5;
    }
    Writer.prototype._reserve = function _reserve(n) {
      var need = this.pos + n;
      if (need > this.buf.length) {
        var size = this.buf.length << 1;
        if (size < need)
          size = need;
        var buf = this.constructor.alloc(size);
        buf.set(this.buf.subarray(0, this.pos), 0);
        this.buf = buf;
        this.view = null;
      }
    };
    function writeStringAscii(val, buf, pos) {
      for (var i = 0; i < val.length; )
        buf[pos++] = val.charCodeAt(i++);
    }
    function writeVarint32(val, buf, pos) {
      while (val > 127) {
        buf[pos++] = val & 127 | 128;
        val >>>= 7;
      }
      buf[pos] = val;
      return pos + 1;
    }
    Writer.prototype.uint32 = function write_uint32(value) {
      value = value >>> 0;
      this._reserve(5);
      var pos = this.pos;
      this.pos = writeVarint32(value, this.buf, pos);
      return this;
    };
    Writer.prototype.int32 = function write_int32(value) {
      if ((value |= 0) < 0) {
        this._reserve(10);
        writeVarint64(LongBits.fromNumber(value), this.buf, this.pos);
        this.pos += 10;
        return this;
      }
      return this.uint32(value);
    };
    Writer.prototype.sint32 = function write_sint32(value) {
      return this.uint32((value << 1 ^ value >> 31) >>> 0);
    };
    function writeVarint64(val, buf, pos) {
      var lo = val.lo, hi = val.hi;
      while (hi) {
        buf[pos++] = lo & 127 | 128;
        lo = (lo >>> 7 | hi << 25) >>> 0;
        hi >>>= 7;
      }
      while (lo > 127) {
        buf[pos++] = lo & 127 | 128;
        lo = lo >>> 7;
      }
      buf[pos] = lo;
      return pos + 1;
    }
    Writer.prototype.uint64 = function write_uint64(value) {
      var bits = LongBits.from(value);
      this._reserve(10);
      var pos = this.pos;
      this.pos = writeVarint64(bits, this.buf, pos);
      return this;
    };
    Writer.prototype.int64 = Writer.prototype.uint64;
    Writer.prototype.sint64 = function write_sint64(value) {
      var bits = LongBits.from(value).zzEncode();
      this._reserve(10);
      var pos = this.pos;
      this.pos = writeVarint64(bits, this.buf, pos);
      return this;
    };
    Writer.prototype.bool = function write_bool(value) {
      this._reserve(1);
      this.buf[this.pos++] = value ? 1 : 0;
      return this;
    };
    function writeFixed32(val, buf, pos) {
      buf[pos] = val & 255;
      buf[pos + 1] = val >>> 8 & 255;
      buf[pos + 2] = val >>> 16 & 255;
      buf[pos + 3] = val >>> 24;
    }
    Writer.prototype.fixed32 = function write_fixed32(value) {
      this._reserve(4);
      writeFixed32(value >>> 0, this.buf, this.pos);
      this.pos += 4;
      return this;
    };
    Writer.prototype.sfixed32 = Writer.prototype.fixed32;
    Writer.prototype.fixed64 = function write_fixed64(value) {
      var bits = LongBits.from(value);
      this._reserve(8);
      writeFixed32(bits.lo, this.buf, this.pos);
      writeFixed32(bits.hi, this.buf, this.pos + 4);
      this.pos += 8;
      return this;
    };
    Writer.prototype.sfixed64 = Writer.prototype.fixed64;
    Writer.prototype.float = function write_float(value) {
      this._reserve(4);
      util.float.writeFloatLE(value, this.buf, this.pos);
      this.pos += 4;
      return this;
    };
    Writer.prototype.double = function write_double(value) {
      this._reserve(8);
      util.float.writeDoubleLE(value, this.buf, this.pos);
      this.pos += 8;
      return this;
    };
    Writer.prototype.bytes = function write_bytes(value) {
      var len = value.length >>> 0;
      if (!len) {
        this._reserve(1);
        this.buf[this.pos++] = 0;
        return this;
      }
      if (util.isString(value)) {
        var buf = Writer.alloc(len = base64.length(value));
        base64.decode(value, buf, 0);
        value = buf;
      }
      this.uint32(len);
      this._reserve(len);
      this.buf.set(value, this.pos);
      this.pos += len;
      return this;
    };
    Writer.prototype.raw = function write_raw(value) {
      var len = value.length >>> 0;
      if (!len)
        return this;
      this._reserve(len);
      this.buf.set(value, this.pos);
      this.pos += len;
      return this;
    };
    Writer.prototype._delim = function _delim(pos, len) {
      var n = sizeVarint32(len);
      if (n > 1)
        this.buf.copyWithin(pos + n, pos + 1, pos + 1 + len);
      writeVarint32(len, this.buf, pos);
      this.pos = pos + n + len;
      return this;
    };
    Writer.prototype.string = function write_string(value) {
      var n = value.length;
      if (!n) {
        this._reserve(1);
        this.buf[this.pos++] = 0;
        return this;
      }
      if (n < 128) {
        this._reserve(n * 3 + 5);
        var lenPos = this.pos;
        return this._delim(lenPos, utf8.write(value, this.buf, lenPos + 1));
      }
      var len = utf8.length(value);
      this.uint32(len);
      this._reserve(len);
      if (len === value.length)
        writeStringAscii(value, this.buf, this.pos);
      else
        utf8.write(value, this.buf, this.pos);
      this.pos += len;
      return this;
    };
    Writer.prototype.uint32s = function write_uint32s(value) {
      var n = value.length;
      this._reserve(n * 5 + 5);
      var buf = this.buf, lenPos = this.pos, p = lenPos + 1;
      for (var i = 0; i < n; ++i)
        p = writeVarint32(value[i] >>> 0, buf, p);
      return this._delim(lenPos, p - lenPos - 1);
    };
    Writer.prototype.int32s = function write_int32s(value) {
      var n = value.length;
      this._reserve(n * 10 + 5);
      var buf = this.buf, lenPos = this.pos, pos = lenPos + 1, val;
      for (var i = 0; i < n; ++i) {
        if ((val = value[i] | 0) < 0) {
          pos = writeVarint64(LongBits.fromNumber(val), buf, pos);
        } else {
          pos = writeVarint32(val, buf, pos);
        }
      }
      return this._delim(lenPos, pos - lenPos - 1);
    };
    Writer.prototype.sint32s = function write_sint32s(value) {
      var n = value.length;
      this._reserve(n * 5 + 5);
      var buf = this.buf, lenPos = this.pos, pos = lenPos + 1;
      for (var i = 0; i < n; ++i)
        pos = writeVarint32((value[i] << 1 ^ value[i] >> 31) >>> 0, buf, pos);
      return this._delim(lenPos, pos - lenPos - 1);
    };
    Writer.prototype.uint64s = function write_uint64s(value) {
      var n = value.length;
      this._reserve(n * 10 + 5);
      var buf = this.buf, lenPos = this.pos, pos = lenPos + 1;
      for (var i = 0; i < n; ++i) {
        pos = writeVarint64(LongBits.from(value[i]), buf, pos);
      }
      return this._delim(lenPos, pos - lenPos - 1);
    };
    Writer.prototype.int64s = Writer.prototype.uint64s;
    Writer.prototype.sint64s = function write_sint64s(value) {
      var n = value.length;
      this._reserve(n * 10 + 5);
      var buf = this.buf, lenPos = this.pos, pos = lenPos + 1;
      for (var i = 0; i < n; ++i) {
        pos = writeVarint64(LongBits.from(value[i]).zzEncode(), buf, pos);
      }
      return this._delim(lenPos, pos - lenPos - 1);
    };
    Writer.prototype.bools = function write_bools(value) {
      var n = value.length;
      this.uint32(n);
      this._reserve(n);
      var buf = this.buf, p = this.pos;
      for (var i = 0; i < n; ++i)
        buf[p++] = value[i] ? 1 : 0;
      this.pos += n;
      return this;
    };
    var VIEW_THRESHOLD_FLOAT = 16;
    var VIEW_THRESHOLD_INT = 128;
    function getLazyView(writer, count, threshold) {
      var view = writer.view;
      if (view || count < threshold)
        return view;
      var buf = writer.buf;
      return writer.view = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    }
    Writer.prototype.fixed32s = function write_fixed32s(value) {
      var n = value.length, bytes = n * 4;
      this.uint32(bytes);
      this._reserve(bytes);
      var p = this.pos, i, dv = getLazyView(this, n, VIEW_THRESHOLD_INT);
      if (dv)
        for (i = 0; i < n; ++i) {
          dv.setUint32(p, value[i] >>> 0, true);
          p += 4;
        }
      else {
        var buf = this.buf;
        for (i = 0; i < n; ++i) {
          writeFixed32(value[i] >>> 0, buf, p);
          p += 4;
        }
      }
      this.pos += bytes;
      return this;
    };
    Writer.prototype.sfixed32s = Writer.prototype.fixed32s;
    Writer.prototype.fixed64s = function write_fixed64s(value) {
      var n = value.length, bytes = n * 8;
      this.uint32(bytes);
      this._reserve(bytes);
      var p = this.pos, i, bits, dv = getLazyView(this, n, VIEW_THRESHOLD_INT);
      if (dv)
        for (i = 0; i < n; ++i) {
          bits = LongBits.from(value[i]);
          dv.setUint32(p, bits.lo, true);
          dv.setUint32(p + 4, bits.hi, true);
          p += 8;
        }
      else {
        var buf = this.buf;
        for (i = 0; i < n; ++i) {
          bits = LongBits.from(value[i]);
          writeFixed32(bits.lo, buf, p);
          writeFixed32(bits.hi, buf, p + 4);
          p += 8;
        }
      }
      this.pos += bytes;
      return this;
    };
    Writer.prototype.sfixed64s = Writer.prototype.fixed64s;
    Writer.prototype.floats = function write_floats(value) {
      var n = value.length, bytes = n * 4;
      this.uint32(bytes);
      this._reserve(bytes);
      var p = this.pos, i, dv = getLazyView(this, n, VIEW_THRESHOLD_FLOAT);
      if (dv)
        for (i = 0; i < n; ++i) {
          dv.setFloat32(p, value[i], true);
          p += 4;
        }
      else {
        var buf = this.buf;
        for (i = 0; i < n; ++i) {
          util.float.writeFloatLE(value[i], buf, p);
          p += 4;
        }
      }
      this.pos += bytes;
      return this;
    };
    Writer.prototype.doubles = function write_doubles(value) {
      var n = value.length, bytes = n * 8;
      this.uint32(bytes);
      this._reserve(bytes);
      var p = this.pos, i, dv = getLazyView(this, n, VIEW_THRESHOLD_FLOAT);
      if (dv)
        for (i = 0; i < n; ++i) {
          dv.setFloat64(p, value[i], true);
          p += 8;
        }
      else {
        var buf = this.buf;
        for (i = 0; i < n; ++i) {
          util.float.writeDoubleLE(value[i], buf, p);
          p += 8;
        }
      }
      this.pos += bytes;
      return this;
    };
    Writer.prototype.fork = function fork() {
      this._reserve(1);
      (this.states || (this.states = [])).push(this.pos);
      this.pos += 1;
      return this;
    };
    Writer.prototype.reset = function reset() {
      var states = this.states;
      if (states && states.length) {
        this.pos = states.pop();
      } else {
        this.pos = 0;
      }
      return this;
    };
    Writer.prototype.ldelim = function ldelim() {
      var states = this.states, len, vlen;
      if (states && states.length) {
        var lenPos = states.pop();
        len = this.pos - lenPos - 1;
        vlen = sizeVarint32(len);
        if (vlen > 1) {
          this._reserve(vlen - 1);
          this.buf.copyWithin(lenPos + vlen, lenPos + 1, lenPos + 1 + len);
          this.pos += vlen - 1;
          writeVarint32(len, this.buf, lenPos);
        } else {
          this.buf[lenPos] = len;
        }
      } else {
        len = this.pos;
        vlen = sizeVarint32(len);
        this._reserve(vlen);
        this.buf.copyWithin(vlen, 0, len);
        writeVarint32(len, this.buf, 0);
        this.pos += vlen;
      }
      return this;
    };
    Writer.prototype.finish = function finish(shared) {
      if (shared)
        return this.buf.subarray(0, this.pos);
      var buf = this.constructor.alloc(this.pos);
      buf.set(this.buf.subarray(0, this.pos), 0);
      return buf;
    };
    Writer.prototype.finishInto = function finishInto(buf, offset) {
      if (offset === void 0)
        offset = 0;
      buf.set(this.buf.subarray(0, this.pos), offset);
      return buf;
    };
    Writer._configure = function(BufferWriter_) {
      BufferWriter = BufferWriter_;
      Writer.create = create();
      BufferWriter._configure();
    };
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/writer_buffer.js
var require_writer_buffer = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/writer_buffer.js"(exports, module) {
    "use strict";
    module.exports = BufferWriter;
    var Writer = require_writer();
    BufferWriter.prototype = Object.create(Writer.prototype, {
      constructor: {
        value: BufferWriter,
        writable: true,
        enumerable: false,
        configurable: true
      }
    });
    var util = require_minimal();
    function BufferWriter() {
      Writer.call(this);
    }
    var writeStringBuffer;
    BufferWriter._configure = function() {
      BufferWriter.alloc = util.Buffer && util.Buffer.allocUnsafe;
      writeStringBuffer = util.Buffer && util.Buffer.prototype.utf8Write ? function writeStringBuffer_utf8Write(val, buf, pos) {
        return buf.utf8Write(val, pos);
      } : function writeStringBuffer_write(val, buf, pos) {
        return buf.write(val, pos);
      };
    };
    BufferWriter.prototype.bytes = function write_bytes_buffer(value) {
      if (util.isString(value))
        value = util.Buffer.from(value, "base64");
      var len = value.length >>> 0;
      this.uint32(len);
      if (len) {
        this._reserve(len);
        this.buf.set(value, this.pos);
        this.pos += len;
      }
      return this;
    };
    BufferWriter.prototype.string = function write_string_buffer(value) {
      var n = value.length;
      if (!n) {
        this._reserve(1);
        this.buf[this.pos++] = 0;
        return this;
      }
      if (n < 128) {
        this._reserve(n * 3 + 5);
        var pos = this.pos, buf = this.buf;
        return this._delim(
          pos,
          n < 40 ? util.utf8.write(value, buf, pos + 1) : writeStringBuffer(value, buf, pos + 1)
        );
      }
      var len = util.Buffer.byteLength(value);
      this.uint32(len);
      this._reserve(len);
      writeStringBuffer(value, this.buf, this.pos);
      this.pos += len;
      return this;
    };
    BufferWriter._configure();
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/reader.js
var require_reader = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/reader.js"(exports, module) {
    "use strict";
    module.exports = Reader;
    var util = require_minimal();
    var BufferReader;
    var LongBits = util.LongBits;
    var utf8 = util.utf8;
    function indexOutOfRange(reader, writeLength) {
      return RangeError("index out of range: " + reader.pos + " + " + (writeLength || 1) + " > " + reader.len);
    }
    function Reader(buffer) {
      this.buf = buffer;
      this.pos = 0;
      this.len = buffer.length;
      this.view = null;
      this.discardUnknown = Reader.discardUnknown;
    }
    function create_array(buffer) {
      if (Array.isArray(buffer))
        buffer = new Uint8Array(buffer);
      if (buffer instanceof Uint8Array)
        return new Reader(buffer);
      throw Error("illegal buffer");
    }
    var create = function create2() {
      return util.Buffer ? function create_buffer_setup(buffer) {
        return (Reader.create = function create_buffer(buffer2) {
          return util.Buffer.isBuffer(buffer2) ? new BufferReader(buffer2) : create_array(buffer2);
        })(buffer);
      } : create_array;
    };
    Reader.create = create();
    Reader.prototype.raw = function read_raw(start, end) {
      return this.buf.subarray(start, end);
    };
    Reader.prototype.uint32 = function read_uint32() {
      var buf = this.buf, pos = this.pos, value = (buf[pos] & 127) >>> 0;
      if (buf[pos++] < 128) {
        this.pos = pos;
        return value;
      }
      value = (value | (buf[pos] & 127) << 7) >>> 0;
      if (buf[pos++] < 128) {
        this.pos = pos;
        return value;
      }
      value = (value | (buf[pos] & 127) << 14) >>> 0;
      if (buf[pos++] < 128) {
        this.pos = pos;
        return value;
      }
      value = (value | (buf[pos] & 127) << 21) >>> 0;
      if (buf[pos++] < 128) {
        this.pos = pos;
        return value;
      }
      value = (value | (buf[pos] & 15) << 28) >>> 0;
      if (buf[pos++] < 128) {
        this.pos = pos;
        return value;
      }
      for (var i = 0; i < 5; ++i) {
        if (pos >= this.len) {
          this.pos = pos;
          throw indexOutOfRange(this);
        }
        if (buf[pos++] < 128) {
          this.pos = pos;
          return value;
        }
      }
      this.pos = pos;
      throw Error("invalid varint encoding");
    };
    Reader.prototype.tag = function read_tag() {
      var buf = this.buf, pos = this.pos, value = (buf[pos] & 127) >>> 0;
      if (buf[pos++] < 128) {
        this.pos = pos;
        return value;
      }
      value = (value | (buf[pos] & 127) << 7) >>> 0;
      if (buf[pos++] < 128) {
        this.pos = pos;
        return value;
      }
      value = (value | (buf[pos] & 127) << 14) >>> 0;
      if (buf[pos++] < 128) {
        this.pos = pos;
        return value;
      }
      value = (value | (buf[pos] & 127) << 21) >>> 0;
      if (buf[pos++] < 128) {
        this.pos = pos;
        return value;
      }
      value = (value | (buf[pos] & 15) << 28) >>> 0;
      if (buf[pos] < 128 && (buf[pos] & 112) === 0) {
        this.pos = pos + 1;
        return value;
      }
      this.pos = pos + 1;
      throw Error("invalid tag encoding");
    };
    Reader.prototype.int32 = function read_int32() {
      return this.uint32() | 0;
    };
    Reader.prototype.sint32 = function read_sint32() {
      var value = this.uint32();
      return value >>> 1 ^ -(value & 1) | 0;
    };
    function readLongVarint() {
      var bits = new LongBits(0, 0);
      var i = 0;
      if (this.len - this.pos > 4) {
        for (; i < 4; ++i) {
          bits.lo = (bits.lo | (this.buf[this.pos] & 127) << i * 7) >>> 0;
          if (this.buf[this.pos++] < 128)
            return bits;
        }
        bits.lo = (bits.lo | (this.buf[this.pos] & 127) << 28) >>> 0;
        bits.hi = (bits.hi | (this.buf[this.pos] & 127) >> 4) >>> 0;
        if (this.buf[this.pos++] < 128)
          return bits;
        i = 0;
      } else {
        for (; i < 4; ++i) {
          if (this.pos >= this.len)
            throw indexOutOfRange(this);
          bits.lo = (bits.lo | (this.buf[this.pos] & 127) << i * 7) >>> 0;
          if (this.buf[this.pos++] < 128)
            return bits;
        }
        throw indexOutOfRange(this);
      }
      if (this.len - this.pos > 4) {
        for (; i < 5; ++i) {
          bits.hi = (bits.hi | (this.buf[this.pos] & 127) << i * 7 + 3) >>> 0;
          if (this.buf[this.pos++] < 128)
            return bits;
        }
      } else {
        for (; i < 5; ++i) {
          if (this.pos >= this.len)
            throw indexOutOfRange(this);
          bits.hi = (bits.hi | (this.buf[this.pos] & 127) << i * 7 + 3) >>> 0;
          if (this.buf[this.pos++] < 128)
            return bits;
        }
      }
      throw Error("invalid varint encoding");
    }
    Reader.prototype.bool = function read_bool() {
      var value = false, b;
      for (var i = 0; i < 10; ++i) {
        if (this.pos >= this.len)
          throw indexOutOfRange(this);
        b = this.buf[this.pos++];
        if (b & 127)
          value = true;
        if (b < 128)
          return value;
      }
      throw Error("invalid varint encoding");
    };
    function readFixed32_end(buf, end) {
      return (buf[end - 4] | buf[end - 3] << 8 | buf[end - 2] << 16 | buf[end - 1] << 24) >>> 0;
    }
    Reader.prototype.fixed32 = function read_fixed32() {
      if (this.pos + 4 > this.len)
        throw indexOutOfRange(this, 4);
      return readFixed32_end(this.buf, this.pos += 4);
    };
    Reader.prototype.sfixed32 = function read_sfixed32() {
      if (this.pos + 4 > this.len)
        throw indexOutOfRange(this, 4);
      return readFixed32_end(this.buf, this.pos += 4) | 0;
    };
    function readFixed64() {
      if (this.pos + 8 > this.len)
        throw indexOutOfRange(this, 8);
      return new LongBits(readFixed32_end(this.buf, this.pos += 4), readFixed32_end(this.buf, this.pos += 4));
    }
    Reader.prototype.float = function read_float() {
      if (this.pos + 4 > this.len)
        throw indexOutOfRange(this, 4);
      var value = util.float.readFloatLE(this.buf, this.pos);
      this.pos += 4;
      return value;
    };
    Reader.prototype.double = function read_double() {
      if (this.pos + 8 > this.len)
        throw indexOutOfRange(this, 4);
      var value = util.float.readDoubleLE(this.buf, this.pos);
      this.pos += 8;
      return value;
    };
    Reader.prototype.uint32s = function read_uint32s(array) {
      if (array === void 0) array = [];
      var end = this.uint32() + this.pos;
      while (this.pos < end)
        array.push(this.uint32());
      return array;
    };
    Reader.prototype.int32s = function read_int32s(array) {
      if (array === void 0) array = [];
      var end = this.uint32() + this.pos;
      while (this.pos < end)
        array.push(this.int32());
      return array;
    };
    Reader.prototype.sint32s = function read_sint32s(array) {
      if (array === void 0) array = [];
      var end = this.uint32() + this.pos;
      while (this.pos < end)
        array.push(this.sint32());
      return array;
    };
    Reader.prototype.bools = function read_bools(array) {
      if (array === void 0) array = [];
      var end = this.uint32() + this.pos;
      while (this.pos < end)
        array.push(this.bool());
      return array;
    };
    var VIEW_THRESHOLD_FLOAT = 8;
    var VIEW_THRESHOLD_INT = 128;
    function getLazyView(reader, count, threshold) {
      var view = reader.view;
      if (view || count < threshold)
        return view;
      var buf = reader.buf;
      return reader.view = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    }
    Reader.prototype.fixed32s = function read_fixed32s(array) {
      if (array === void 0) array = [];
      var len = this.uint32(), end = this.pos + len;
      if (end > this.len) throw indexOutOfRange(this, len);
      var count = len >>> 2, i = array.length, pos = this.pos;
      array.length = i + count;
      var dv = getLazyView(this, count, VIEW_THRESHOLD_INT);
      if (dv)
        for (var k = 0; k < count; ++k, pos += 4) array[i++] = dv.getUint32(pos, true);
      else {
        var buf = this.buf;
        for (var j = 0; j < count; ++j, pos += 4) array[i++] = readFixed32_end(buf, pos + 4);
      }
      this.pos = pos;
      if (pos !== end) throw indexOutOfRange(this, 4);
      return array;
    };
    Reader.prototype.sfixed32s = function read_sfixed32s(array) {
      if (array === void 0) array = [];
      var len = this.uint32(), end = this.pos + len;
      if (end > this.len) throw indexOutOfRange(this, len);
      var count = len >>> 2, i = array.length, pos = this.pos;
      array.length = i + count;
      var dv = getLazyView(this, count, VIEW_THRESHOLD_INT);
      if (dv)
        for (var k = 0; k < count; ++k, pos += 4) array[i++] = dv.getInt32(pos, true);
      else {
        var buf = this.buf;
        for (var j = 0; j < count; ++j, pos += 4) array[i++] = readFixed32_end(buf, pos + 4) | 0;
      }
      this.pos = pos;
      if (pos !== end) throw indexOutOfRange(this, 4);
      return array;
    };
    Reader.prototype.floats = function read_floats(array) {
      if (array === void 0) array = [];
      var len = this.uint32(), end = this.pos + len;
      if (end > this.len) throw indexOutOfRange(this, len);
      var count = len >>> 2, i = array.length, pos = this.pos;
      array.length = i + count;
      var dv = getLazyView(this, count, VIEW_THRESHOLD_FLOAT);
      if (dv)
        for (var k = 0; k < count; ++k, pos += 4) array[i++] = dv.getFloat32(pos, true);
      else {
        var buf = this.buf;
        for (var j = 0; j < count; ++j, pos += 4) array[i++] = util.float.readFloatLE(buf, pos);
      }
      this.pos = pos;
      if (pos !== end) throw indexOutOfRange(this, 4);
      return array;
    };
    Reader.prototype.doubles = function read_doubles(array) {
      if (array === void 0) array = [];
      var len = this.uint32(), end = this.pos + len;
      if (end > this.len) throw indexOutOfRange(this, len);
      var count = len >>> 3, i = array.length, pos = this.pos;
      array.length = i + count;
      var dv = getLazyView(this, count, VIEW_THRESHOLD_FLOAT);
      if (dv)
        for (var k = 0; k < count; ++k, pos += 8) array[i++] = dv.getFloat64(pos, true);
      else {
        var buf = this.buf;
        for (var j = 0; j < count; ++j, pos += 8) array[i++] = util.float.readDoubleLE(buf, pos);
      }
      this.pos = pos;
      if (pos !== end) throw indexOutOfRange(this, 8);
      return array;
    };
    Reader.prototype.uint64s = function read_uint64s(array) {
      if (array === void 0) array = [];
      var end = this.uint32() + this.pos;
      while (this.pos < end)
        array.push(this.uint64());
      return array;
    };
    Reader.prototype.int64s = function read_int64s(array) {
      if (array === void 0) array = [];
      var end = this.uint32() + this.pos;
      while (this.pos < end)
        array.push(this.int64());
      return array;
    };
    Reader.prototype.sint64s = function read_sint64s(array) {
      if (array === void 0) array = [];
      var end = this.uint32() + this.pos;
      while (this.pos < end)
        array.push(this.sint64());
      return array;
    };
    Reader.prototype.fixed64s = function read_fixed64s(array) {
      if (array === void 0) array = [];
      var len = this.uint32(), end = this.pos + len, i = array.length;
      if (end > this.len) throw indexOutOfRange(this, len);
      var count = len >>> 3;
      array.length = i + count;
      for (var j = 0; j < count; ++j)
        array[i++] = this.fixed64();
      if (this.pos !== end) throw indexOutOfRange(this, 8);
      return array;
    };
    Reader.prototype.sfixed64s = function read_sfixed64s(array) {
      if (array === void 0) array = [];
      var len = this.uint32(), end = this.pos + len, i = array.length;
      if (end > this.len) throw indexOutOfRange(this, len);
      var count = len >>> 3;
      array.length = i + count;
      for (var j = 0; j < count; ++j)
        array[i++] = this.sfixed64();
      if (this.pos !== end) throw indexOutOfRange(this, 8);
      return array;
    };
    Reader.prototype.bytes = function read_bytes() {
      var length = this.uint32(), start = this.pos, end = this.pos + length;
      if (end > this.len)
        throw indexOutOfRange(this, length);
      this.pos = end;
      return this.raw(start, end);
    };
    Reader.prototype.string = function read_string() {
      var length = this.uint32(), start = this.pos, end = this.pos + length;
      if (end > this.len)
        throw indexOutOfRange(this, length);
      this.pos = end;
      return utf8.read(this.buf, start, end);
    };
    Reader.prototype.stringVerify = function read_string_verify() {
      var length = this.uint32(), start = this.pos, end = this.pos + length;
      if (end > this.len)
        throw indexOutOfRange(this, length);
      this.pos = end;
      return utf8.readStrict(this.buf, start, end);
    };
    Reader.prototype.skip = function skip(length) {
      if (typeof length === "number") {
        if (this.pos + length > this.len)
          throw indexOutOfRange(this, length);
        this.pos += length;
      } else {
        do {
          if (this.pos >= this.len)
            throw indexOutOfRange(this);
        } while (this.buf[this.pos++] & 128);
      }
      return this;
    };
    Reader.recursionLimit = util.recursionLimit;
    Reader.discardUnknown = true;
    Reader.prototype.skipType = function(wireType, depth, fieldNumber) {
      if (depth === void 0) depth = 0;
      if (depth > Reader.recursionLimit)
        throw Error("max depth exceeded");
      if (fieldNumber === 0)
        throw Error("illegal tag: field number 0");
      switch (wireType) {
        case 0:
          this.skip();
          break;
        case 1:
          this.skip(8);
          break;
        case 2:
          this.skip(this.uint32());
          break;
        case 3:
          while (true) {
            var tag = this.tag();
            var nestedField = tag >>> 3;
            wireType = tag & 7;
            if (!nestedField)
              throw Error("illegal tag: field number 0");
            if (wireType === 4) {
              if (fieldNumber !== void 0 && nestedField !== fieldNumber)
                throw Error("invalid end group tag");
              break;
            }
            this.skipType(wireType, depth + 1, nestedField);
          }
          break;
        case 5:
          this.skip(4);
          break;
        /* istanbul ignore next */
        default:
          throw Error("invalid wire type " + wireType + " at offset " + this.pos);
      }
      return this;
    };
    Reader._configure = function(BufferReader_) {
      BufferReader = BufferReader_;
      Reader.create = create();
      BufferReader._configure();
      var fn = util.Long ? "toLong" : (
        /* istanbul ignore next */
        "toNumber"
      );
      util.merge(Reader.prototype, {
        int64: function read_int64() {
          return readLongVarint.call(this)[fn](false);
        },
        uint64: function read_uint64() {
          return readLongVarint.call(this)[fn](true);
        },
        sint64: function read_sint64() {
          return readLongVarint.call(this).zzDecode()[fn](false);
        },
        fixed64: function read_fixed64() {
          return readFixed64.call(this)[fn](true);
        },
        sfixed64: function read_sfixed64() {
          return readFixed64.call(this)[fn](false);
        }
      });
    };
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/reader_buffer.js
var require_reader_buffer = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/reader_buffer.js"(exports, module) {
    "use strict";
    module.exports = BufferReader;
    var Reader = require_reader();
    BufferReader.prototype = Object.create(Reader.prototype, {
      constructor: {
        value: BufferReader,
        writable: true,
        enumerable: false,
        configurable: true
      }
    });
    var util = require_minimal();
    function BufferReader(buffer) {
      Reader.call(this, buffer);
    }
    BufferReader._configure = function() {
      if (util.Buffer)
        BufferReader.prototype._slice = util.Buffer.prototype.slice;
    };
    BufferReader.prototype.raw = function read_raw_buffer(start, end) {
      return this._slice.call(this.buf, start, end);
    };
    BufferReader.prototype.string = function read_string_buffer() {
      var len = this.uint32(), start = this.pos, end = this.pos + len;
      if (end > this.len)
        throw RangeError("index out of range: " + this.pos + " + " + len + " > " + this.len);
      this.pos = end;
      return this.buf.utf8Slice ? this.buf.utf8Slice(start, end) : this.buf.toString("utf-8", start, end);
    };
    BufferReader._configure();
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/rpc/service.js
var require_service = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/rpc/service.js"(exports, module) {
    "use strict";
    module.exports = Service;
    var util = require_minimal();
    Service.prototype = Object.create(util.EventEmitter.prototype, {
      constructor: {
        value: Service,
        writable: true,
        enumerable: false,
        configurable: true
      }
    });
    function Service(rpcImpl, requestDelimited, responseDelimited) {
      if (typeof rpcImpl !== "function")
        throw TypeError("rpcImpl must be a function");
      util.EventEmitter.call(this);
      this.rpcImpl = rpcImpl;
      this.requestDelimited = Boolean(requestDelimited);
      this.responseDelimited = Boolean(responseDelimited);
    }
    Service.prototype.rpcCall = function rpcCall(method, requestCtor, responseCtor, request, callback) {
      if (!request)
        throw TypeError("request must be specified");
      var self2 = this;
      if (!callback)
        return util.asPromise(rpcCall, self2, method, requestCtor, responseCtor, request);
      if (!self2.rpcImpl) {
        setTimeout(function() {
          callback(Error("already ended"));
        }, 0);
        return void 0;
      }
      try {
        return self2.rpcImpl(
          method,
          requestCtor[self2.requestDelimited ? "encodeDelimited" : "encode"](request).finish(),
          function rpcCallback(err, response) {
            if (err) {
              self2.emit("error", err, method);
              return callback(err);
            }
            if (response === null) {
              self2.end(
                /* endedByRPC */
                true
              );
              return void 0;
            }
            if (!(response instanceof responseCtor)) {
              try {
                response = responseCtor[self2.responseDelimited ? "decodeDelimited" : "decode"](response);
              } catch (err2) {
                self2.emit("error", err2, method);
                return callback(err2);
              }
            }
            self2.emit("data", response, method);
            return callback(null, response);
          }
        );
      } catch (err) {
        self2.emit("error", err, method);
        setTimeout(function() {
          callback(err);
        }, 0);
        return void 0;
      }
    };
    Service.prototype.end = function end(endedByRPC) {
      if (this.rpcImpl) {
        if (!endedByRPC)
          this.rpcImpl(null, null, null);
        this.rpcImpl = null;
        this.emit("end").off();
      }
      return this;
    };
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/rpc.js
var require_rpc = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/rpc.js"(exports) {
    "use strict";
    var rpc = exports;
    rpc.Service = require_service();
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/roots.js
var require_roots = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/roots.js"(exports, module) {
    "use strict";
    module.exports = /* @__PURE__ */ Object.create(null);
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/src/index-minimal.js
var require_index_minimal = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/src/index-minimal.js"(exports) {
    "use strict";
    exports.build = "minimal";
    exports.Writer = require_writer();
    exports.BufferWriter = require_writer_buffer();
    exports.Reader = require_reader();
    exports.BufferReader = require_reader_buffer();
    exports.util = require_minimal();
    exports.rpc = require_rpc();
    exports.roots = require_roots();
    exports.configure = configure;
    function configure() {
      exports.util.LongBits._configure(exports.util.Long);
      exports.Writer._configure(exports.BufferWriter);
      exports.Reader._configure(exports.BufferReader);
    }
    configure();
  }
});

// src/wwwroot_brpc/node_modules/protobufjs/minimal.js
var require_minimal2 = __commonJS({
  "src/wwwroot_brpc/node_modules/protobufjs/minimal.js"(exports, module) {
    "use strict";
    module.exports = require_index_minimal();
  }
});

// src/wwwroot_brpc/.proto-build/oj-proto.mjs
var import_minimal = __toESM(require_minimal2(), 1);
var $Reader = import_minimal.default.Reader;
var $Writer = import_minimal.default.Writer;
var $util = import_minimal.default.util;
var $Object = $util.global.Object;
var $undefined = $util.global.undefined;
var $Error = $util.global.Error;
var $TypeError = $util.global.TypeError;
var $String = $util.global.String;
var $Number = $util.global.Number;
var $Array = $util.global.Array;
var $Boolean = $util.global.Boolean;
var $parseInt = $util.global.parseInt;
var $BigInt = $util.global.BigInt;
var $root = import_minimal.default.roots["default"] || (import_minimal.default.roots["default"] = {});
var oj = $root.oj = (() => {
  const oj2 = {};
  oj2.biz = (function() {
    const biz = {};
    biz.SendVerificationCodeRequest = (function() {
      const SendVerificationCodeRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      SendVerificationCodeRequest.prototype.email = "";
      SendVerificationCodeRequest.create = function(properties) {
        return new SendVerificationCodeRequest(properties);
      };
      SendVerificationCodeRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.email != null && $Object.hasOwnProperty.call(message, "email") && message.email !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.email);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      SendVerificationCodeRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      SendVerificationCodeRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.SendVerificationCodeRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.email = value;
              else
                delete message.email;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      SendVerificationCodeRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      SendVerificationCodeRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.email != null && $Object.hasOwnProperty.call(message, "email")) {
          if (!$util.isString(message.email))
            return "email: string expected";
        }
        return null;
      };
      SendVerificationCodeRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.SendVerificationCodeRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.SendVerificationCodeRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.SendVerificationCodeRequest();
        if (object.email != null) {
          if (typeof object.email !== "string" || object.email.length)
            message.email = $String(object.email);
        }
        return message;
      };
      SendVerificationCodeRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.email = "";
        if (message.email != null && $Object.hasOwnProperty.call(message, "email"))
          object.email = message.email;
        return object;
      };
      SendVerificationCodeRequest.prototype.toJSON = function() {
        return SendVerificationCodeRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      SendVerificationCodeRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.SendVerificationCodeRequest";
      };
      return SendVerificationCodeRequest;
    })();
    biz.SendVerificationCodeResponse = (function() {
      const SendVerificationCodeResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      SendVerificationCodeResponse.prototype.status = null;
      SendVerificationCodeResponse.prototype.retryAfterSeconds = 0;
      SendVerificationCodeResponse.create = function(properties) {
        return new SendVerificationCodeResponse(properties);
      };
      SendVerificationCodeResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.retryAfterSeconds != null && $Object.hasOwnProperty.call(message, "retryAfterSeconds") && message.retryAfterSeconds !== 0)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint32(message.retryAfterSeconds);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      SendVerificationCodeResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      SendVerificationCodeResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.SendVerificationCodeResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.retryAfterSeconds = value;
              else
                delete message.retryAfterSeconds;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      SendVerificationCodeResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      SendVerificationCodeResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.retryAfterSeconds != null && $Object.hasOwnProperty.call(message, "retryAfterSeconds")) {
          if (!$util.isInteger(message.retryAfterSeconds))
            return "retryAfterSeconds: integer expected";
        }
        return null;
      };
      SendVerificationCodeResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.SendVerificationCodeResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.SendVerificationCodeResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.SendVerificationCodeResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.SendVerificationCodeResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.retryAfterSeconds != null) {
          if ($Number(object.retryAfterSeconds) !== 0)
            message.retryAfterSeconds = object.retryAfterSeconds >>> 0;
        }
        return message;
      };
      SendVerificationCodeResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.retryAfterSeconds = 0;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.retryAfterSeconds != null && $Object.hasOwnProperty.call(message, "retryAfterSeconds"))
          object.retryAfterSeconds = message.retryAfterSeconds;
        return object;
      };
      SendVerificationCodeResponse.prototype.toJSON = function() {
        return SendVerificationCodeResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      SendVerificationCodeResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.SendVerificationCodeResponse";
      };
      return SendVerificationCodeResponse;
    })();
    biz.RegisterRequest = (function() {
      const RegisterRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      RegisterRequest.prototype.email = "";
      RegisterRequest.prototype.verificationCode = "";
      RegisterRequest.prototype.name = "";
      RegisterRequest.prototype.password = "";
      RegisterRequest.create = function(properties) {
        return new RegisterRequest(properties);
      };
      RegisterRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.email != null && $Object.hasOwnProperty.call(message, "email") && message.email !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.email);
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode") && message.verificationCode !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.verificationCode);
        if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.name);
        if (message.password != null && $Object.hasOwnProperty.call(message, "password") && message.password !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.password);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      RegisterRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      RegisterRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.RegisterRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.email = value;
              else
                delete message.email;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.verificationCode = value;
              else
                delete message.verificationCode;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.name = value;
              else
                delete message.name;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.password = value;
              else
                delete message.password;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      RegisterRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      RegisterRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.email != null && $Object.hasOwnProperty.call(message, "email")) {
          if (!$util.isString(message.email))
            return "email: string expected";
        }
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode")) {
          if (!$util.isString(message.verificationCode))
            return "verificationCode: string expected";
        }
        if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
          if (!$util.isString(message.name))
            return "name: string expected";
        }
        if (message.password != null && $Object.hasOwnProperty.call(message, "password")) {
          if (!$util.isString(message.password))
            return "password: string expected";
        }
        return null;
      };
      RegisterRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.RegisterRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.RegisterRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.RegisterRequest();
        if (object.email != null) {
          if (typeof object.email !== "string" || object.email.length)
            message.email = $String(object.email);
        }
        if (object.verificationCode != null) {
          if (typeof object.verificationCode !== "string" || object.verificationCode.length)
            message.verificationCode = $String(object.verificationCode);
        }
        if (object.name != null) {
          if (typeof object.name !== "string" || object.name.length)
            message.name = $String(object.name);
        }
        if (object.password != null) {
          if (typeof object.password !== "string" || object.password.length)
            message.password = $String(object.password);
        }
        return message;
      };
      RegisterRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.email = "";
          object.verificationCode = "";
          object.name = "";
          object.password = "";
        }
        if (message.email != null && $Object.hasOwnProperty.call(message, "email"))
          object.email = message.email;
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode"))
          object.verificationCode = message.verificationCode;
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
          object.name = message.name;
        if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
          object.password = message.password;
        return object;
      };
      RegisterRequest.prototype.toJSON = function() {
        return RegisterRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      RegisterRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.RegisterRequest";
      };
      return RegisterRequest;
    })();
    biz.AuthResponse = (function() {
      const AuthResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AuthResponse.prototype.status = null;
      AuthResponse.prototype.user = null;
      AuthResponse.create = function(properties) {
        return new AuthResponse(properties);
      };
      AuthResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          $root.oj.common.User.encode(message.user, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AuthResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AuthResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AuthResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.user = $root.oj.common.User.decode(reader, reader.uint32(), $undefined, _depth + 1, message.user);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AuthResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AuthResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.user != null && $Object.hasOwnProperty.call(message, "user")) {
          let error = $root.oj.common.User.verify(message.user, _depth + 1);
          if (error)
            return "user." + error;
        }
        return null;
      };
      AuthResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AuthResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AuthResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AuthResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.AuthResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.user != null) {
          if (!$util.isObject(object.user))
            throw $TypeError(".oj.biz.AuthResponse.user: object expected");
          message.user = $root.oj.common.User.fromObject(object.user, _depth + 1);
        }
        return message;
      };
      AuthResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.user = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          object.user = $root.oj.common.User.toObject(message.user, options, _depth + 1);
        return object;
      };
      AuthResponse.prototype.toJSON = function() {
        return AuthResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AuthResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AuthResponse";
      };
      return AuthResponse;
    })();
    biz.VerificationCodeLoginRequest = (function() {
      const VerificationCodeLoginRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      VerificationCodeLoginRequest.prototype.email = "";
      VerificationCodeLoginRequest.prototype.verificationCode = "";
      VerificationCodeLoginRequest.create = function(properties) {
        return new VerificationCodeLoginRequest(properties);
      };
      VerificationCodeLoginRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.email != null && $Object.hasOwnProperty.call(message, "email") && message.email !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.email);
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode") && message.verificationCode !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.verificationCode);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      VerificationCodeLoginRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      VerificationCodeLoginRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.VerificationCodeLoginRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.email = value;
              else
                delete message.email;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.verificationCode = value;
              else
                delete message.verificationCode;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      VerificationCodeLoginRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      VerificationCodeLoginRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.email != null && $Object.hasOwnProperty.call(message, "email")) {
          if (!$util.isString(message.email))
            return "email: string expected";
        }
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode")) {
          if (!$util.isString(message.verificationCode))
            return "verificationCode: string expected";
        }
        return null;
      };
      VerificationCodeLoginRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.VerificationCodeLoginRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.VerificationCodeLoginRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.VerificationCodeLoginRequest();
        if (object.email != null) {
          if (typeof object.email !== "string" || object.email.length)
            message.email = $String(object.email);
        }
        if (object.verificationCode != null) {
          if (typeof object.verificationCode !== "string" || object.verificationCode.length)
            message.verificationCode = $String(object.verificationCode);
        }
        return message;
      };
      VerificationCodeLoginRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.email = "";
          object.verificationCode = "";
        }
        if (message.email != null && $Object.hasOwnProperty.call(message, "email"))
          object.email = message.email;
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode"))
          object.verificationCode = message.verificationCode;
        return object;
      };
      VerificationCodeLoginRequest.prototype.toJSON = function() {
        return VerificationCodeLoginRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      VerificationCodeLoginRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.VerificationCodeLoginRequest";
      };
      return VerificationCodeLoginRequest;
    })();
    biz.PasswordLoginRequest = (function() {
      const PasswordLoginRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      PasswordLoginRequest.prototype.emailOrName = "";
      PasswordLoginRequest.prototype.password = "";
      PasswordLoginRequest.create = function(properties) {
        return new PasswordLoginRequest(properties);
      };
      PasswordLoginRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.emailOrName != null && $Object.hasOwnProperty.call(message, "emailOrName") && message.emailOrName !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.emailOrName);
        if (message.password != null && $Object.hasOwnProperty.call(message, "password") && message.password !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.password);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      PasswordLoginRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      PasswordLoginRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.PasswordLoginRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.emailOrName = value;
              else
                delete message.emailOrName;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.password = value;
              else
                delete message.password;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      PasswordLoginRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      PasswordLoginRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.emailOrName != null && $Object.hasOwnProperty.call(message, "emailOrName")) {
          if (!$util.isString(message.emailOrName))
            return "emailOrName: string expected";
        }
        if (message.password != null && $Object.hasOwnProperty.call(message, "password")) {
          if (!$util.isString(message.password))
            return "password: string expected";
        }
        return null;
      };
      PasswordLoginRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.PasswordLoginRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.PasswordLoginRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.PasswordLoginRequest();
        if (object.emailOrName != null) {
          if (typeof object.emailOrName !== "string" || object.emailOrName.length)
            message.emailOrName = $String(object.emailOrName);
        }
        if (object.password != null) {
          if (typeof object.password !== "string" || object.password.length)
            message.password = $String(object.password);
        }
        return message;
      };
      PasswordLoginRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.emailOrName = "";
          object.password = "";
        }
        if (message.emailOrName != null && $Object.hasOwnProperty.call(message, "emailOrName"))
          object.emailOrName = message.emailOrName;
        if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
          object.password = message.password;
        return object;
      };
      PasswordLoginRequest.prototype.toJSON = function() {
        return PasswordLoginRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      PasswordLoginRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.PasswordLoginRequest";
      };
      return PasswordLoginRequest;
    })();
    biz.SetPasswordRequest = (function() {
      const SetPasswordRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      SetPasswordRequest.prototype.password = "";
      SetPasswordRequest.create = function(properties) {
        return new SetPasswordRequest(properties);
      };
      SetPasswordRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.password != null && $Object.hasOwnProperty.call(message, "password") && message.password !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.password);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      SetPasswordRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      SetPasswordRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.SetPasswordRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.password = value;
              else
                delete message.password;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      SetPasswordRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      SetPasswordRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.password != null && $Object.hasOwnProperty.call(message, "password")) {
          if (!$util.isString(message.password))
            return "password: string expected";
        }
        return null;
      };
      SetPasswordRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.SetPasswordRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.SetPasswordRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.SetPasswordRequest();
        if (object.password != null) {
          if (typeof object.password !== "string" || object.password.length)
            message.password = $String(object.password);
        }
        return message;
      };
      SetPasswordRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.password = "";
        if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
          object.password = message.password;
        return object;
      };
      SetPasswordRequest.prototype.toJSON = function() {
        return SetPasswordRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      SetPasswordRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.SetPasswordRequest";
      };
      return SetPasswordRequest;
    })();
    biz.ChangePasswordRequest = (function() {
      const ChangePasswordRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ChangePasswordRequest.prototype.verificationCode = "";
      ChangePasswordRequest.prototype.newPassword = "";
      ChangePasswordRequest.create = function(properties) {
        return new ChangePasswordRequest(properties);
      };
      ChangePasswordRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode") && message.verificationCode !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.verificationCode);
        if (message.newPassword != null && $Object.hasOwnProperty.call(message, "newPassword") && message.newPassword !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.newPassword);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ChangePasswordRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ChangePasswordRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ChangePasswordRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.verificationCode = value;
              else
                delete message.verificationCode;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.newPassword = value;
              else
                delete message.newPassword;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ChangePasswordRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ChangePasswordRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode")) {
          if (!$util.isString(message.verificationCode))
            return "verificationCode: string expected";
        }
        if (message.newPassword != null && $Object.hasOwnProperty.call(message, "newPassword")) {
          if (!$util.isString(message.newPassword))
            return "newPassword: string expected";
        }
        return null;
      };
      ChangePasswordRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ChangePasswordRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ChangePasswordRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ChangePasswordRequest();
        if (object.verificationCode != null) {
          if (typeof object.verificationCode !== "string" || object.verificationCode.length)
            message.verificationCode = $String(object.verificationCode);
        }
        if (object.newPassword != null) {
          if (typeof object.newPassword !== "string" || object.newPassword.length)
            message.newPassword = $String(object.newPassword);
        }
        return message;
      };
      ChangePasswordRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.verificationCode = "";
          object.newPassword = "";
        }
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode"))
          object.verificationCode = message.verificationCode;
        if (message.newPassword != null && $Object.hasOwnProperty.call(message, "newPassword"))
          object.newPassword = message.newPassword;
        return object;
      };
      ChangePasswordRequest.prototype.toJSON = function() {
        return ChangePasswordRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ChangePasswordRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ChangePasswordRequest";
      };
      return ChangePasswordRequest;
    })();
    biz.ChangeEmailRequest = (function() {
      const ChangeEmailRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ChangeEmailRequest.prototype.newEmail = "";
      ChangeEmailRequest.prototype.verificationCode = "";
      ChangeEmailRequest.create = function(properties) {
        return new ChangeEmailRequest(properties);
      };
      ChangeEmailRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.newEmail != null && $Object.hasOwnProperty.call(message, "newEmail") && message.newEmail !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.newEmail);
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode") && message.verificationCode !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.verificationCode);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ChangeEmailRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ChangeEmailRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ChangeEmailRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.newEmail = value;
              else
                delete message.newEmail;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.verificationCode = value;
              else
                delete message.verificationCode;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ChangeEmailRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ChangeEmailRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.newEmail != null && $Object.hasOwnProperty.call(message, "newEmail")) {
          if (!$util.isString(message.newEmail))
            return "newEmail: string expected";
        }
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode")) {
          if (!$util.isString(message.verificationCode))
            return "verificationCode: string expected";
        }
        return null;
      };
      ChangeEmailRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ChangeEmailRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ChangeEmailRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ChangeEmailRequest();
        if (object.newEmail != null) {
          if (typeof object.newEmail !== "string" || object.newEmail.length)
            message.newEmail = $String(object.newEmail);
        }
        if (object.verificationCode != null) {
          if (typeof object.verificationCode !== "string" || object.verificationCode.length)
            message.verificationCode = $String(object.verificationCode);
        }
        return message;
      };
      ChangeEmailRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.newEmail = "";
          object.verificationCode = "";
        }
        if (message.newEmail != null && $Object.hasOwnProperty.call(message, "newEmail"))
          object.newEmail = message.newEmail;
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode"))
          object.verificationCode = message.verificationCode;
        return object;
      };
      ChangeEmailRequest.prototype.toJSON = function() {
        return ChangeEmailRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ChangeEmailRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ChangeEmailRequest";
      };
      return ChangeEmailRequest;
    })();
    biz.DeleteAccountRequest = (function() {
      const DeleteAccountRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      DeleteAccountRequest.prototype.verificationCode = "";
      DeleteAccountRequest.create = function(properties) {
        return new DeleteAccountRequest(properties);
      };
      DeleteAccountRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode") && message.verificationCode !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.verificationCode);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      DeleteAccountRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      DeleteAccountRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.DeleteAccountRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.verificationCode = value;
              else
                delete message.verificationCode;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      DeleteAccountRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      DeleteAccountRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode")) {
          if (!$util.isString(message.verificationCode))
            return "verificationCode: string expected";
        }
        return null;
      };
      DeleteAccountRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.DeleteAccountRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.DeleteAccountRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.DeleteAccountRequest();
        if (object.verificationCode != null) {
          if (typeof object.verificationCode !== "string" || object.verificationCode.length)
            message.verificationCode = $String(object.verificationCode);
        }
        return message;
      };
      DeleteAccountRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.verificationCode = "";
        if (message.verificationCode != null && $Object.hasOwnProperty.call(message, "verificationCode"))
          object.verificationCode = message.verificationCode;
        return object;
      };
      DeleteAccountRequest.prototype.toJSON = function() {
        return DeleteAccountRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      DeleteAccountRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.DeleteAccountRequest";
      };
      return DeleteAccountRequest;
    })();
    biz.GetUserProfileRequest = (function() {
      const GetUserProfileRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetUserProfileRequest.prototype.uid = 0;
      GetUserProfileRequest.create = function(properties) {
        return new GetUserProfileRequest(properties);
      };
      GetUserProfileRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid") && message.uid !== 0)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint32(message.uid);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetUserProfileRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetUserProfileRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetUserProfileRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.uid = value;
              else
                delete message.uid;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetUserProfileRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetUserProfileRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
          if (!$util.isInteger(message.uid))
            return "uid: integer expected";
        }
        return null;
      };
      GetUserProfileRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetUserProfileRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetUserProfileRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetUserProfileRequest();
        if (object.uid != null) {
          if ($Number(object.uid) !== 0)
            message.uid = object.uid >>> 0;
        }
        return message;
      };
      GetUserProfileRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.uid = 0;
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
          object.uid = message.uid;
        return object;
      };
      GetUserProfileRequest.prototype.toJSON = function() {
        return GetUserProfileRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetUserProfileRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetUserProfileRequest";
      };
      return GetUserProfileRequest;
    })();
    biz.GetUserProfileResponse = (function() {
      const GetUserProfileResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetUserProfileResponse.prototype.status = null;
      GetUserProfileResponse.prototype.user = null;
      GetUserProfileResponse.prototype.statistics = null;
      GetUserProfileResponse.create = function(properties) {
        return new GetUserProfileResponse(properties);
      };
      GetUserProfileResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          $root.oj.common.User.encode(message.user, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.statistics != null && $Object.hasOwnProperty.call(message, "statistics"))
          $root.oj.common.UserStatistics.encode(message.statistics, writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetUserProfileResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetUserProfileResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetUserProfileResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.user = $root.oj.common.User.decode(reader, reader.uint32(), $undefined, _depth + 1, message.user);
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              message.statistics = $root.oj.common.UserStatistics.decode(reader, reader.uint32(), $undefined, _depth + 1, message.statistics);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetUserProfileResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetUserProfileResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.user != null && $Object.hasOwnProperty.call(message, "user")) {
          let error = $root.oj.common.User.verify(message.user, _depth + 1);
          if (error)
            return "user." + error;
        }
        if (message.statistics != null && $Object.hasOwnProperty.call(message, "statistics")) {
          let error = $root.oj.common.UserStatistics.verify(message.statistics, _depth + 1);
          if (error)
            return "statistics." + error;
        }
        return null;
      };
      GetUserProfileResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetUserProfileResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetUserProfileResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetUserProfileResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetUserProfileResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.user != null) {
          if (!$util.isObject(object.user))
            throw $TypeError(".oj.biz.GetUserProfileResponse.user: object expected");
          message.user = $root.oj.common.User.fromObject(object.user, _depth + 1);
        }
        if (object.statistics != null) {
          if (!$util.isObject(object.statistics))
            throw $TypeError(".oj.biz.GetUserProfileResponse.statistics: object expected");
          message.statistics = $root.oj.common.UserStatistics.fromObject(object.statistics, _depth + 1);
        }
        return message;
      };
      GetUserProfileResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.user = null;
          object.statistics = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          object.user = $root.oj.common.User.toObject(message.user, options, _depth + 1);
        if (message.statistics != null && $Object.hasOwnProperty.call(message, "statistics"))
          object.statistics = $root.oj.common.UserStatistics.toObject(message.statistics, options, _depth + 1);
        return object;
      };
      GetUserProfileResponse.prototype.toJSON = function() {
        return GetUserProfileResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetUserProfileResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetUserProfileResponse";
      };
      return GetUserProfileResponse;
    })();
    biz.UpdateProfileRequest = (function() {
      const UpdateProfileRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      UpdateProfileRequest.prototype.name = "";
      UpdateProfileRequest.create = function(properties) {
        return new UpdateProfileRequest(properties);
      };
      UpdateProfileRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.name);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      UpdateProfileRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      UpdateProfileRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.UpdateProfileRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.name = value;
              else
                delete message.name;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      UpdateProfileRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      UpdateProfileRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
          if (!$util.isString(message.name))
            return "name: string expected";
        }
        return null;
      };
      UpdateProfileRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.UpdateProfileRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.UpdateProfileRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.UpdateProfileRequest();
        if (object.name != null) {
          if (typeof object.name !== "string" || object.name.length)
            message.name = $String(object.name);
        }
        return message;
      };
      UpdateProfileRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.name = "";
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
          object.name = message.name;
        return object;
      };
      UpdateProfileRequest.prototype.toJSON = function() {
        return UpdateProfileRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      UpdateProfileRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.UpdateProfileRequest";
      };
      return UpdateProfileRequest;
    })();
    biz.UpdateProfileResponse = (function() {
      const UpdateProfileResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      UpdateProfileResponse.prototype.status = null;
      UpdateProfileResponse.prototype.user = null;
      UpdateProfileResponse.create = function(properties) {
        return new UpdateProfileResponse(properties);
      };
      UpdateProfileResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          $root.oj.common.User.encode(message.user, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      UpdateProfileResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      UpdateProfileResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.UpdateProfileResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.user = $root.oj.common.User.decode(reader, reader.uint32(), $undefined, _depth + 1, message.user);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      UpdateProfileResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      UpdateProfileResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.user != null && $Object.hasOwnProperty.call(message, "user")) {
          let error = $root.oj.common.User.verify(message.user, _depth + 1);
          if (error)
            return "user." + error;
        }
        return null;
      };
      UpdateProfileResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.UpdateProfileResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.UpdateProfileResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.UpdateProfileResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.UpdateProfileResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.user != null) {
          if (!$util.isObject(object.user))
            throw $TypeError(".oj.biz.UpdateProfileResponse.user: object expected");
          message.user = $root.oj.common.User.fromObject(object.user, _depth + 1);
        }
        return message;
      };
      UpdateProfileResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.user = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          object.user = $root.oj.common.User.toObject(message.user, options, _depth + 1);
        return object;
      };
      UpdateProfileResponse.prototype.toJSON = function() {
        return UpdateProfileResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      UpdateProfileResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.UpdateProfileResponse";
      };
      return UpdateProfileResponse;
    })();
    biz.UpdateAvatarRequest = (function() {
      const UpdateAvatarRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      UpdateAvatarRequest.prototype.content = $util.newBuffer([]);
      UpdateAvatarRequest.prototype.contentType = "";
      UpdateAvatarRequest.create = function(properties) {
        return new UpdateAvatarRequest(properties);
      };
      UpdateAvatarRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.content != null && $Object.hasOwnProperty.call(message, "content") && message.content.length)
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).bytes(message.content);
        if (message.contentType != null && $Object.hasOwnProperty.call(message, "contentType") && message.contentType !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.contentType);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      UpdateAvatarRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      UpdateAvatarRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.UpdateAvatarRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.bytes()).length)
                message.content = value;
              else
                delete message.content;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.contentType = value;
              else
                delete message.contentType;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      UpdateAvatarRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      UpdateAvatarRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.content != null && $Object.hasOwnProperty.call(message, "content")) {
          if (!(message.content && typeof message.content.length === "number" || $util.isString(message.content)))
            return "content: buffer expected";
        }
        if (message.contentType != null && $Object.hasOwnProperty.call(message, "contentType")) {
          if (!$util.isString(message.contentType))
            return "contentType: string expected";
        }
        return null;
      };
      UpdateAvatarRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.UpdateAvatarRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.UpdateAvatarRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.UpdateAvatarRequest();
        if (object.content != null) {
          if (object.content.length) {
            if (typeof object.content === "string")
              $util.base64.decode(object.content, message.content = $util.newBuffer($util.base64.length(object.content)), 0);
            else if (object.content.length >= 0)
              message.content = object.content;
          }
        }
        if (object.contentType != null) {
          if (typeof object.contentType !== "string" || object.contentType.length)
            message.contentType = $String(object.contentType);
        }
        return message;
      };
      UpdateAvatarRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if (options.bytes === $String)
            object.content = "";
          else {
            object.content = [];
            if (options.bytes !== $Array)
              object.content = $util.newBuffer(object.content);
          }
          object.contentType = "";
        }
        if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
          object.content = options.bytes === $String ? $util.base64.encode(message.content, 0, message.content.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.content) : message.content;
        if (message.contentType != null && $Object.hasOwnProperty.call(message, "contentType"))
          object.contentType = message.contentType;
        return object;
      };
      UpdateAvatarRequest.prototype.toJSON = function() {
        return UpdateAvatarRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      UpdateAvatarRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.UpdateAvatarRequest";
      };
      return UpdateAvatarRequest;
    })();
    biz.UpdateAvatarResponse = (function() {
      const UpdateAvatarResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      UpdateAvatarResponse.prototype.status = null;
      UpdateAvatarResponse.prototype.avatar = null;
      UpdateAvatarResponse.create = function(properties) {
        return new UpdateAvatarResponse(properties);
      };
      UpdateAvatarResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar"))
          $root.oj.common.AvatarMetadata.encode(message.avatar, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      UpdateAvatarResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      UpdateAvatarResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.UpdateAvatarResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.avatar = $root.oj.common.AvatarMetadata.decode(reader, reader.uint32(), $undefined, _depth + 1, message.avatar);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      UpdateAvatarResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      UpdateAvatarResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar")) {
          let error = $root.oj.common.AvatarMetadata.verify(message.avatar, _depth + 1);
          if (error)
            return "avatar." + error;
        }
        return null;
      };
      UpdateAvatarResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.UpdateAvatarResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.UpdateAvatarResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.UpdateAvatarResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.UpdateAvatarResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.avatar != null) {
          if (!$util.isObject(object.avatar))
            throw $TypeError(".oj.biz.UpdateAvatarResponse.avatar: object expected");
          message.avatar = $root.oj.common.AvatarMetadata.fromObject(object.avatar, _depth + 1);
        }
        return message;
      };
      UpdateAvatarResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.avatar = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar"))
          object.avatar = $root.oj.common.AvatarMetadata.toObject(message.avatar, options, _depth + 1);
        return object;
      };
      UpdateAvatarResponse.prototype.toJSON = function() {
        return UpdateAvatarResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      UpdateAvatarResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.UpdateAvatarResponse";
      };
      return UpdateAvatarResponse;
    })();
    biz.GetUserStatisticsResponse = (function() {
      const GetUserStatisticsResponse = function(properties) {
        this.recentSubmissions = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetUserStatisticsResponse.prototype.status = null;
      GetUserStatisticsResponse.prototype.statistics = null;
      GetUserStatisticsResponse.prototype.recentSubmissions = $util.emptyArray;
      GetUserStatisticsResponse.create = function(properties) {
        return new GetUserStatisticsResponse(properties);
      };
      GetUserStatisticsResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.statistics != null && $Object.hasOwnProperty.call(message, "statistics"))
          $root.oj.common.UserStatistics.encode(message.statistics, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.recentSubmissions != null && message.recentSubmissions.length)
          for (let i = 0; i < message.recentSubmissions.length; ++i)
            $root.oj.common.Submission.encode(message.recentSubmissions[i], writer.uint32(
              /* id 3, wireType 2 =*/
              26
            ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetUserStatisticsResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetUserStatisticsResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetUserStatisticsResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.statistics = $root.oj.common.UserStatistics.decode(reader, reader.uint32(), $undefined, _depth + 1, message.statistics);
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if (!(message.recentSubmissions && message.recentSubmissions.length))
                message.recentSubmissions = [];
              message.recentSubmissions.push($root.oj.common.Submission.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetUserStatisticsResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetUserStatisticsResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.statistics != null && $Object.hasOwnProperty.call(message, "statistics")) {
          let error = $root.oj.common.UserStatistics.verify(message.statistics, _depth + 1);
          if (error)
            return "statistics." + error;
        }
        if (message.recentSubmissions != null && $Object.hasOwnProperty.call(message, "recentSubmissions")) {
          if (!$Array.isArray(message.recentSubmissions))
            return "recentSubmissions: array expected";
          for (let i = 0; i < message.recentSubmissions.length; ++i) {
            let error = $root.oj.common.Submission.verify(message.recentSubmissions[i], _depth + 1);
            if (error)
              return "recentSubmissions." + error;
          }
        }
        return null;
      };
      GetUserStatisticsResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetUserStatisticsResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetUserStatisticsResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetUserStatisticsResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetUserStatisticsResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.statistics != null) {
          if (!$util.isObject(object.statistics))
            throw $TypeError(".oj.biz.GetUserStatisticsResponse.statistics: object expected");
          message.statistics = $root.oj.common.UserStatistics.fromObject(object.statistics, _depth + 1);
        }
        if (object.recentSubmissions) {
          if (!$Array.isArray(object.recentSubmissions))
            throw $TypeError(".oj.biz.GetUserStatisticsResponse.recentSubmissions: array expected");
          message.recentSubmissions = $Array(object.recentSubmissions.length);
          for (let i = 0; i < object.recentSubmissions.length; ++i) {
            if (!$util.isObject(object.recentSubmissions[i]))
              throw $TypeError(".oj.biz.GetUserStatisticsResponse.recentSubmissions: object expected");
            message.recentSubmissions[i] = $root.oj.common.Submission.fromObject(object.recentSubmissions[i], _depth + 1);
          }
        }
        return message;
      };
      GetUserStatisticsResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.recentSubmissions = [];
        if (options.defaults) {
          object.status = null;
          object.statistics = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.statistics != null && $Object.hasOwnProperty.call(message, "statistics"))
          object.statistics = $root.oj.common.UserStatistics.toObject(message.statistics, options, _depth + 1);
        if (message.recentSubmissions && message.recentSubmissions.length) {
          object.recentSubmissions = $Array(message.recentSubmissions.length);
          for (let j = 0; j < message.recentSubmissions.length; ++j)
            object.recentSubmissions[j] = $root.oj.common.Submission.toObject(message.recentSubmissions[j], options, _depth + 1);
        }
        return object;
      };
      GetUserStatisticsResponse.prototype.toJSON = function() {
        return GetUserStatisticsResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetUserStatisticsResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetUserStatisticsResponse";
      };
      return GetUserStatisticsResponse;
    })();
    biz.UserService = (function() {
      const UserService = function(rpcImpl, requestDelimited, responseDelimited) {
        import_minimal.default.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperty(UserService.prototype = $Object.create(import_minimal.default.rpc.Service.prototype), "constructor", { value: UserService, writable: true, enumerable: false, configurable: true });
      UserService.create = function(rpcImpl, requestDelimited, responseDelimited) {
        return new this(rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperties(UserService.prototype.sendRegistrationCode = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.sendRegistrationCode, $root.oj.biz.SendVerificationCodeRequest, $root.oj.biz.SendVerificationCodeResponse, request, callback);
      }, {
        name: { value: "SendRegistrationCode" },
        path: { value: "/oj.biz.UserService/SendRegistrationCode" },
        requestType: { value: "SendVerificationCodeRequest" },
        responseType: { value: "SendVerificationCodeResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.register = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.register, $root.oj.biz.RegisterRequest, $root.oj.biz.AuthResponse, request, callback);
      }, {
        name: { value: "Register" },
        path: { value: "/oj.biz.UserService/Register" },
        requestType: { value: "RegisterRequest" },
        responseType: { value: "AuthResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.loginWithVerificationCode = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.loginWithVerificationCode, $root.oj.biz.VerificationCodeLoginRequest, $root.oj.biz.AuthResponse, request, callback);
      }, {
        name: { value: "LoginWithVerificationCode" },
        path: { value: "/oj.biz.UserService/LoginWithVerificationCode" },
        requestType: { value: "VerificationCodeLoginRequest" },
        responseType: { value: "AuthResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.loginWithPassword = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.loginWithPassword, $root.oj.biz.PasswordLoginRequest, $root.oj.biz.AuthResponse, request, callback);
      }, {
        name: { value: "LoginWithPassword" },
        path: { value: "/oj.biz.UserService/LoginWithPassword" },
        requestType: { value: "PasswordLoginRequest" },
        responseType: { value: "AuthResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.logout = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.logout, $root.oj.common.EmptyRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "Logout" },
        path: { value: "/oj.biz.UserService/Logout" },
        requestType: { value: "oj.common.EmptyRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.setPassword = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.setPassword, $root.oj.biz.SetPasswordRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "SetPassword" },
        path: { value: "/oj.biz.UserService/SetPassword" },
        requestType: { value: "SetPasswordRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.sendSecurityCode = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.sendSecurityCode, $root.oj.common.EmptyRequest, $root.oj.biz.SendVerificationCodeResponse, request, callback);
      }, {
        name: { value: "SendSecurityCode" },
        path: { value: "/oj.biz.UserService/SendSecurityCode" },
        requestType: { value: "oj.common.EmptyRequest" },
        responseType: { value: "SendVerificationCodeResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.changePassword = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.changePassword, $root.oj.biz.ChangePasswordRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "ChangePassword" },
        path: { value: "/oj.biz.UserService/ChangePassword" },
        requestType: { value: "ChangePasswordRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.changeEmail = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.changeEmail, $root.oj.biz.ChangeEmailRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "ChangeEmail" },
        path: { value: "/oj.biz.UserService/ChangeEmail" },
        requestType: { value: "ChangeEmailRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.deleteAccount = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.deleteAccount, $root.oj.biz.DeleteAccountRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "DeleteAccount" },
        path: { value: "/oj.biz.UserService/DeleteAccount" },
        requestType: { value: "DeleteAccountRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.getCurrentUser = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.getCurrentUser, $root.oj.common.EmptyRequest, $root.oj.biz.GetUserProfileResponse, request, callback);
      }, {
        name: { value: "GetCurrentUser" },
        path: { value: "/oj.biz.UserService/GetCurrentUser" },
        requestType: { value: "oj.common.EmptyRequest" },
        responseType: { value: "GetUserProfileResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.getUserProfile = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.getUserProfile, $root.oj.biz.GetUserProfileRequest, $root.oj.biz.GetUserProfileResponse, request, callback);
      }, {
        name: { value: "GetUserProfile" },
        path: { value: "/oj.biz.UserService/GetUserProfile" },
        requestType: { value: "GetUserProfileRequest" },
        responseType: { value: "GetUserProfileResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.updateProfile = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.updateProfile, $root.oj.biz.UpdateProfileRequest, $root.oj.biz.UpdateProfileResponse, request, callback);
      }, {
        name: { value: "UpdateProfile" },
        path: { value: "/oj.biz.UserService/UpdateProfile" },
        requestType: { value: "UpdateProfileRequest" },
        responseType: { value: "UpdateProfileResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.updateAvatar = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.updateAvatar, $root.oj.biz.UpdateAvatarRequest, $root.oj.biz.UpdateAvatarResponse, request, callback);
      }, {
        name: { value: "UpdateAvatar" },
        path: { value: "/oj.biz.UserService/UpdateAvatar" },
        requestType: { value: "UpdateAvatarRequest" },
        responseType: { value: "UpdateAvatarResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.deleteAvatar = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.deleteAvatar, $root.oj.common.EmptyRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "DeleteAvatar" },
        path: { value: "/oj.biz.UserService/DeleteAvatar" },
        requestType: { value: "oj.common.EmptyRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(UserService.prototype.getStatistics = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, UserService.prototype.getStatistics, $root.oj.common.EmptyRequest, $root.oj.biz.GetUserStatisticsResponse, request, callback);
      }, {
        name: { value: "GetStatistics" },
        path: { value: "/oj.biz.UserService/GetStatistics" },
        requestType: { value: "oj.common.EmptyRequest" },
        responseType: { value: "GetUserStatisticsResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      return UserService;
    })();
    biz.GetQuestionListRequest = (function() {
      const GetQuestionListRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetQuestionListRequest.prototype.page = null;
      GetQuestionListRequest.prototype.search = "";
      GetQuestionListRequest.prototype.difficulty = "";
      GetQuestionListRequest.prototype.includeHidden = false;
      GetQuestionListRequest.create = function(properties) {
        return new GetQuestionListRequest(properties);
      };
      GetQuestionListRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageRequest.encode(message.page, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.search != null && $Object.hasOwnProperty.call(message, "search") && message.search !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.search);
        if (message.difficulty != null && $Object.hasOwnProperty.call(message, "difficulty") && message.difficulty !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.difficulty);
        if (message.includeHidden != null && $Object.hasOwnProperty.call(message, "includeHidden") && message.includeHidden !== false)
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).bool(message.includeHidden);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetQuestionListRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetQuestionListRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetQuestionListRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.search = value;
              else
                delete message.search;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.difficulty = value;
              else
                delete message.difficulty;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.includeHidden = value;
              else
                delete message.includeHidden;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetQuestionListRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetQuestionListRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageRequest.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.search != null && $Object.hasOwnProperty.call(message, "search")) {
          if (!$util.isString(message.search))
            return "search: string expected";
        }
        if (message.difficulty != null && $Object.hasOwnProperty.call(message, "difficulty")) {
          if (!$util.isString(message.difficulty))
            return "difficulty: string expected";
        }
        if (message.includeHidden != null && $Object.hasOwnProperty.call(message, "includeHidden")) {
          if (typeof message.includeHidden !== "boolean")
            return "includeHidden: boolean expected";
        }
        return null;
      };
      GetQuestionListRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetQuestionListRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetQuestionListRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetQuestionListRequest();
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.GetQuestionListRequest.page: object expected");
          message.page = $root.oj.common.PageRequest.fromObject(object.page, _depth + 1);
        }
        if (object.search != null) {
          if (typeof object.search !== "string" || object.search.length)
            message.search = $String(object.search);
        }
        if (object.difficulty != null) {
          if (typeof object.difficulty !== "string" || object.difficulty.length)
            message.difficulty = $String(object.difficulty);
        }
        if (object.includeHidden != null) {
          if (object.includeHidden)
            message.includeHidden = $Boolean(object.includeHidden);
        }
        return message;
      };
      GetQuestionListRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.page = null;
          object.search = "";
          object.difficulty = "";
          object.includeHidden = false;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageRequest.toObject(message.page, options, _depth + 1);
        if (message.search != null && $Object.hasOwnProperty.call(message, "search"))
          object.search = message.search;
        if (message.difficulty != null && $Object.hasOwnProperty.call(message, "difficulty"))
          object.difficulty = message.difficulty;
        if (message.includeHidden != null && $Object.hasOwnProperty.call(message, "includeHidden"))
          object.includeHidden = message.includeHidden;
        return object;
      };
      GetQuestionListRequest.prototype.toJSON = function() {
        return GetQuestionListRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetQuestionListRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetQuestionListRequest";
      };
      return GetQuestionListRequest;
    })();
    biz.GetQuestionListResponse = (function() {
      const GetQuestionListResponse = function(properties) {
        this.questions = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetQuestionListResponse.prototype.status = null;
      GetQuestionListResponse.prototype.page = null;
      GetQuestionListResponse.prototype.questions = $util.emptyArray;
      GetQuestionListResponse.create = function(properties) {
        return new GetQuestionListResponse(properties);
      };
      GetQuestionListResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageResponse.encode(message.page, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.questions != null && message.questions.length)
          for (let i = 0; i < message.questions.length; ++i)
            $root.oj.common.Question.encode(message.questions[i], writer.uint32(
              /* id 3, wireType 2 =*/
              26
            ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetQuestionListResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetQuestionListResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetQuestionListResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if (!(message.questions && message.questions.length))
                message.questions = [];
              message.questions.push($root.oj.common.Question.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetQuestionListResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetQuestionListResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageResponse.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.questions != null && $Object.hasOwnProperty.call(message, "questions")) {
          if (!$Array.isArray(message.questions))
            return "questions: array expected";
          for (let i = 0; i < message.questions.length; ++i) {
            let error = $root.oj.common.Question.verify(message.questions[i], _depth + 1);
            if (error)
              return "questions." + error;
          }
        }
        return null;
      };
      GetQuestionListResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetQuestionListResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetQuestionListResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetQuestionListResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetQuestionListResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.GetQuestionListResponse.page: object expected");
          message.page = $root.oj.common.PageResponse.fromObject(object.page, _depth + 1);
        }
        if (object.questions) {
          if (!$Array.isArray(object.questions))
            throw $TypeError(".oj.biz.GetQuestionListResponse.questions: array expected");
          message.questions = $Array(object.questions.length);
          for (let i = 0; i < object.questions.length; ++i) {
            if (!$util.isObject(object.questions[i]))
              throw $TypeError(".oj.biz.GetQuestionListResponse.questions: object expected");
            message.questions[i] = $root.oj.common.Question.fromObject(object.questions[i], _depth + 1);
          }
        }
        return message;
      };
      GetQuestionListResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.questions = [];
        if (options.defaults) {
          object.status = null;
          object.page = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageResponse.toObject(message.page, options, _depth + 1);
        if (message.questions && message.questions.length) {
          object.questions = $Array(message.questions.length);
          for (let j = 0; j < message.questions.length; ++j)
            object.questions[j] = $root.oj.common.Question.toObject(message.questions[j], options, _depth + 1);
        }
        return object;
      };
      GetQuestionListResponse.prototype.toJSON = function() {
        return GetQuestionListResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetQuestionListResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetQuestionListResponse";
      };
      return GetQuestionListResponse;
    })();
    biz.QuestionIdRequest = (function() {
      const QuestionIdRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      QuestionIdRequest.prototype.questionId = "";
      QuestionIdRequest.create = function(properties) {
        return new QuestionIdRequest(properties);
      };
      QuestionIdRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.questionId);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      QuestionIdRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      QuestionIdRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.QuestionIdRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      QuestionIdRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      QuestionIdRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        return null;
      };
      QuestionIdRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.QuestionIdRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.QuestionIdRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.QuestionIdRequest();
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        return message;
      };
      QuestionIdRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.questionId = "";
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        return object;
      };
      QuestionIdRequest.prototype.toJSON = function() {
        return QuestionIdRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      QuestionIdRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.QuestionIdRequest";
      };
      return QuestionIdRequest;
    })();
    biz.GetQuestionDetailResponse = (function() {
      const GetQuestionDetailResponse = function(properties) {
        this.sampleTests = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetQuestionDetailResponse.prototype.status = null;
      GetQuestionDetailResponse.prototype.question = null;
      GetQuestionDetailResponse.prototype.sampleTests = $util.emptyArray;
      GetQuestionDetailResponse.prototype.passedByCurrentUser = false;
      GetQuestionDetailResponse.create = function(properties) {
        return new GetQuestionDetailResponse(properties);
      };
      GetQuestionDetailResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.question != null && $Object.hasOwnProperty.call(message, "question"))
          $root.oj.common.Question.encode(message.question, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.sampleTests != null && message.sampleTests.length)
          for (let i = 0; i < message.sampleTests.length; ++i)
            $root.oj.common.TestCase.encode(message.sampleTests[i], writer.uint32(
              /* id 3, wireType 2 =*/
              26
            ).fork(), _depth + 1).ldelim();
        if (message.passedByCurrentUser != null && $Object.hasOwnProperty.call(message, "passedByCurrentUser") && message.passedByCurrentUser !== false)
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).bool(message.passedByCurrentUser);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetQuestionDetailResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetQuestionDetailResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetQuestionDetailResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.question = $root.oj.common.Question.decode(reader, reader.uint32(), $undefined, _depth + 1, message.question);
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if (!(message.sampleTests && message.sampleTests.length))
                message.sampleTests = [];
              message.sampleTests.push($root.oj.common.TestCase.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.passedByCurrentUser = value;
              else
                delete message.passedByCurrentUser;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetQuestionDetailResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetQuestionDetailResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.question != null && $Object.hasOwnProperty.call(message, "question")) {
          let error = $root.oj.common.Question.verify(message.question, _depth + 1);
          if (error)
            return "question." + error;
        }
        if (message.sampleTests != null && $Object.hasOwnProperty.call(message, "sampleTests")) {
          if (!$Array.isArray(message.sampleTests))
            return "sampleTests: array expected";
          for (let i = 0; i < message.sampleTests.length; ++i) {
            let error = $root.oj.common.TestCase.verify(message.sampleTests[i], _depth + 1);
            if (error)
              return "sampleTests." + error;
          }
        }
        if (message.passedByCurrentUser != null && $Object.hasOwnProperty.call(message, "passedByCurrentUser")) {
          if (typeof message.passedByCurrentUser !== "boolean")
            return "passedByCurrentUser: boolean expected";
        }
        return null;
      };
      GetQuestionDetailResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetQuestionDetailResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetQuestionDetailResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetQuestionDetailResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetQuestionDetailResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.question != null) {
          if (!$util.isObject(object.question))
            throw $TypeError(".oj.biz.GetQuestionDetailResponse.question: object expected");
          message.question = $root.oj.common.Question.fromObject(object.question, _depth + 1);
        }
        if (object.sampleTests) {
          if (!$Array.isArray(object.sampleTests))
            throw $TypeError(".oj.biz.GetQuestionDetailResponse.sampleTests: array expected");
          message.sampleTests = $Array(object.sampleTests.length);
          for (let i = 0; i < object.sampleTests.length; ++i) {
            if (!$util.isObject(object.sampleTests[i]))
              throw $TypeError(".oj.biz.GetQuestionDetailResponse.sampleTests: object expected");
            message.sampleTests[i] = $root.oj.common.TestCase.fromObject(object.sampleTests[i], _depth + 1);
          }
        }
        if (object.passedByCurrentUser != null) {
          if (object.passedByCurrentUser)
            message.passedByCurrentUser = $Boolean(object.passedByCurrentUser);
        }
        return message;
      };
      GetQuestionDetailResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.sampleTests = [];
        if (options.defaults) {
          object.status = null;
          object.question = null;
          object.passedByCurrentUser = false;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.question != null && $Object.hasOwnProperty.call(message, "question"))
          object.question = $root.oj.common.Question.toObject(message.question, options, _depth + 1);
        if (message.sampleTests && message.sampleTests.length) {
          object.sampleTests = $Array(message.sampleTests.length);
          for (let j = 0; j < message.sampleTests.length; ++j)
            object.sampleTests[j] = $root.oj.common.TestCase.toObject(message.sampleTests[j], options, _depth + 1);
        }
        if (message.passedByCurrentUser != null && $Object.hasOwnProperty.call(message, "passedByCurrentUser"))
          object.passedByCurrentUser = message.passedByCurrentUser;
        return object;
      };
      GetQuestionDetailResponse.prototype.toJSON = function() {
        return GetQuestionDetailResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetQuestionDetailResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetQuestionDetailResponse";
      };
      return GetQuestionDetailResponse;
    })();
    biz.GetQuestionPassStatusResponse = (function() {
      const GetQuestionPassStatusResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetQuestionPassStatusResponse.prototype.status = null;
      GetQuestionPassStatusResponse.prototype.passed = false;
      GetQuestionPassStatusResponse.create = function(properties) {
        return new GetQuestionPassStatusResponse(properties);
      };
      GetQuestionPassStatusResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.passed != null && $Object.hasOwnProperty.call(message, "passed") && message.passed !== false)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).bool(message.passed);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetQuestionPassStatusResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetQuestionPassStatusResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetQuestionPassStatusResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.passed = value;
              else
                delete message.passed;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetQuestionPassStatusResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetQuestionPassStatusResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.passed != null && $Object.hasOwnProperty.call(message, "passed")) {
          if (typeof message.passed !== "boolean")
            return "passed: boolean expected";
        }
        return null;
      };
      GetQuestionPassStatusResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetQuestionPassStatusResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetQuestionPassStatusResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetQuestionPassStatusResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetQuestionPassStatusResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.passed != null) {
          if (object.passed)
            message.passed = $Boolean(object.passed);
        }
        return message;
      };
      GetQuestionPassStatusResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.passed = false;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.passed != null && $Object.hasOwnProperty.call(message, "passed"))
          object.passed = message.passed;
        return object;
      };
      GetQuestionPassStatusResponse.prototype.toJSON = function() {
        return GetQuestionPassStatusResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetQuestionPassStatusResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetQuestionPassStatusResponse";
      };
      return GetQuestionPassStatusResponse;
    })();
    biz.SaveQuestionRequest = (function() {
      const SaveQuestionRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      SaveQuestionRequest.prototype.question = null;
      SaveQuestionRequest.create = function(properties) {
        return new SaveQuestionRequest(properties);
      };
      SaveQuestionRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.question != null && $Object.hasOwnProperty.call(message, "question"))
          $root.oj.common.Question.encode(message.question, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      SaveQuestionRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      SaveQuestionRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.SaveQuestionRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.question = $root.oj.common.Question.decode(reader, reader.uint32(), $undefined, _depth + 1, message.question);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      SaveQuestionRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      SaveQuestionRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.question != null && $Object.hasOwnProperty.call(message, "question")) {
          let error = $root.oj.common.Question.verify(message.question, _depth + 1);
          if (error)
            return "question." + error;
        }
        return null;
      };
      SaveQuestionRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.SaveQuestionRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.SaveQuestionRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.SaveQuestionRequest();
        if (object.question != null) {
          if (!$util.isObject(object.question))
            throw $TypeError(".oj.biz.SaveQuestionRequest.question: object expected");
          message.question = $root.oj.common.Question.fromObject(object.question, _depth + 1);
        }
        return message;
      };
      SaveQuestionRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.question = null;
        if (message.question != null && $Object.hasOwnProperty.call(message, "question"))
          object.question = $root.oj.common.Question.toObject(message.question, options, _depth + 1);
        return object;
      };
      SaveQuestionRequest.prototype.toJSON = function() {
        return SaveQuestionRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      SaveQuestionRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.SaveQuestionRequest";
      };
      return SaveQuestionRequest;
    })();
    biz.QuestionResponse = (function() {
      const QuestionResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      QuestionResponse.prototype.status = null;
      QuestionResponse.prototype.question = null;
      QuestionResponse.create = function(properties) {
        return new QuestionResponse(properties);
      };
      QuestionResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.question != null && $Object.hasOwnProperty.call(message, "question"))
          $root.oj.common.Question.encode(message.question, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      QuestionResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      QuestionResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.QuestionResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.question = $root.oj.common.Question.decode(reader, reader.uint32(), $undefined, _depth + 1, message.question);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      QuestionResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      QuestionResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.question != null && $Object.hasOwnProperty.call(message, "question")) {
          let error = $root.oj.common.Question.verify(message.question, _depth + 1);
          if (error)
            return "question." + error;
        }
        return null;
      };
      QuestionResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.QuestionResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.QuestionResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.QuestionResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.QuestionResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.question != null) {
          if (!$util.isObject(object.question))
            throw $TypeError(".oj.biz.QuestionResponse.question: object expected");
          message.question = $root.oj.common.Question.fromObject(object.question, _depth + 1);
        }
        return message;
      };
      QuestionResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.question = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.question != null && $Object.hasOwnProperty.call(message, "question"))
          object.question = $root.oj.common.Question.toObject(message.question, options, _depth + 1);
        return object;
      };
      QuestionResponse.prototype.toJSON = function() {
        return QuestionResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      QuestionResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.QuestionResponse";
      };
      return QuestionResponse;
    })();
    biz.ListTestCasesRequest = (function() {
      const ListTestCasesRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ListTestCasesRequest.prototype.questionId = "";
      ListTestCasesRequest.prototype.sampleOnly = false;
      ListTestCasesRequest.create = function(properties) {
        return new ListTestCasesRequest(properties);
      };
      ListTestCasesRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.questionId);
        if (message.sampleOnly != null && $Object.hasOwnProperty.call(message, "sampleOnly") && message.sampleOnly !== false)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).bool(message.sampleOnly);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ListTestCasesRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ListTestCasesRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ListTestCasesRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.sampleOnly = value;
              else
                delete message.sampleOnly;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ListTestCasesRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ListTestCasesRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        if (message.sampleOnly != null && $Object.hasOwnProperty.call(message, "sampleOnly")) {
          if (typeof message.sampleOnly !== "boolean")
            return "sampleOnly: boolean expected";
        }
        return null;
      };
      ListTestCasesRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ListTestCasesRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ListTestCasesRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ListTestCasesRequest();
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        if (object.sampleOnly != null) {
          if (object.sampleOnly)
            message.sampleOnly = $Boolean(object.sampleOnly);
        }
        return message;
      };
      ListTestCasesRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.questionId = "";
          object.sampleOnly = false;
        }
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        if (message.sampleOnly != null && $Object.hasOwnProperty.call(message, "sampleOnly"))
          object.sampleOnly = message.sampleOnly;
        return object;
      };
      ListTestCasesRequest.prototype.toJSON = function() {
        return ListTestCasesRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ListTestCasesRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ListTestCasesRequest";
      };
      return ListTestCasesRequest;
    })();
    biz.ListTestCasesResponse = (function() {
      const ListTestCasesResponse = function(properties) {
        this.testCases = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ListTestCasesResponse.prototype.status = null;
      ListTestCasesResponse.prototype.testCases = $util.emptyArray;
      ListTestCasesResponse.create = function(properties) {
        return new ListTestCasesResponse(properties);
      };
      ListTestCasesResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.testCases != null && message.testCases.length)
          for (let i = 0; i < message.testCases.length; ++i)
            $root.oj.common.TestCase.encode(message.testCases[i], writer.uint32(
              /* id 2, wireType 2 =*/
              18
            ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ListTestCasesResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ListTestCasesResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ListTestCasesResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if (!(message.testCases && message.testCases.length))
                message.testCases = [];
              message.testCases.push($root.oj.common.TestCase.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ListTestCasesResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ListTestCasesResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.testCases != null && $Object.hasOwnProperty.call(message, "testCases")) {
          if (!$Array.isArray(message.testCases))
            return "testCases: array expected";
          for (let i = 0; i < message.testCases.length; ++i) {
            let error = $root.oj.common.TestCase.verify(message.testCases[i], _depth + 1);
            if (error)
              return "testCases." + error;
          }
        }
        return null;
      };
      ListTestCasesResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ListTestCasesResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ListTestCasesResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ListTestCasesResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.ListTestCasesResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.testCases) {
          if (!$Array.isArray(object.testCases))
            throw $TypeError(".oj.biz.ListTestCasesResponse.testCases: array expected");
          message.testCases = $Array(object.testCases.length);
          for (let i = 0; i < object.testCases.length; ++i) {
            if (!$util.isObject(object.testCases[i]))
              throw $TypeError(".oj.biz.ListTestCasesResponse.testCases: object expected");
            message.testCases[i] = $root.oj.common.TestCase.fromObject(object.testCases[i], _depth + 1);
          }
        }
        return message;
      };
      ListTestCasesResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.testCases = [];
        if (options.defaults)
          object.status = null;
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.testCases && message.testCases.length) {
          object.testCases = $Array(message.testCases.length);
          for (let j = 0; j < message.testCases.length; ++j)
            object.testCases[j] = $root.oj.common.TestCase.toObject(message.testCases[j], options, _depth + 1);
        }
        return object;
      };
      ListTestCasesResponse.prototype.toJSON = function() {
        return ListTestCasesResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ListTestCasesResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ListTestCasesResponse";
      };
      return ListTestCasesResponse;
    })();
    biz.CreateTestCaseRequest = (function() {
      const CreateTestCaseRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CreateTestCaseRequest.prototype.questionId = "";
      CreateTestCaseRequest.prototype.ordinal = 0;
      CreateTestCaseRequest.prototype.input = "";
      CreateTestCaseRequest.prototype.expectedOutput = "";
      CreateTestCaseRequest.prototype.isSample = false;
      CreateTestCaseRequest.create = function(properties) {
        return new CreateTestCaseRequest(properties);
      };
      CreateTestCaseRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.questionId);
        if (message.ordinal != null && $Object.hasOwnProperty.call(message, "ordinal") && message.ordinal !== 0)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint32(message.ordinal);
        if (message.input != null && $Object.hasOwnProperty.call(message, "input") && message.input !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.input);
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput") && message.expectedOutput !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.expectedOutput);
        if (message.isSample != null && $Object.hasOwnProperty.call(message, "isSample") && message.isSample !== false)
          writer.uint32(
            /* id 5, wireType 0 =*/
            40
          ).bool(message.isSample);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CreateTestCaseRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CreateTestCaseRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.CreateTestCaseRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.ordinal = value;
              else
                delete message.ordinal;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.input = value;
              else
                delete message.input;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.expectedOutput = value;
              else
                delete message.expectedOutput;
              continue;
            }
            case 5: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.isSample = value;
              else
                delete message.isSample;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CreateTestCaseRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CreateTestCaseRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        if (message.ordinal != null && $Object.hasOwnProperty.call(message, "ordinal")) {
          if (!$util.isInteger(message.ordinal))
            return "ordinal: integer expected";
        }
        if (message.input != null && $Object.hasOwnProperty.call(message, "input")) {
          if (!$util.isString(message.input))
            return "input: string expected";
        }
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput")) {
          if (!$util.isString(message.expectedOutput))
            return "expectedOutput: string expected";
        }
        if (message.isSample != null && $Object.hasOwnProperty.call(message, "isSample")) {
          if (typeof message.isSample !== "boolean")
            return "isSample: boolean expected";
        }
        return null;
      };
      CreateTestCaseRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.CreateTestCaseRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.CreateTestCaseRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.CreateTestCaseRequest();
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        if (object.ordinal != null) {
          if ($Number(object.ordinal) !== 0)
            message.ordinal = object.ordinal >>> 0;
        }
        if (object.input != null) {
          if (typeof object.input !== "string" || object.input.length)
            message.input = $String(object.input);
        }
        if (object.expectedOutput != null) {
          if (typeof object.expectedOutput !== "string" || object.expectedOutput.length)
            message.expectedOutput = $String(object.expectedOutput);
        }
        if (object.isSample != null) {
          if (object.isSample)
            message.isSample = $Boolean(object.isSample);
        }
        return message;
      };
      CreateTestCaseRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.questionId = "";
          object.ordinal = 0;
          object.input = "";
          object.expectedOutput = "";
          object.isSample = false;
        }
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        if (message.ordinal != null && $Object.hasOwnProperty.call(message, "ordinal"))
          object.ordinal = message.ordinal;
        if (message.input != null && $Object.hasOwnProperty.call(message, "input"))
          object.input = message.input;
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput"))
          object.expectedOutput = message.expectedOutput;
        if (message.isSample != null && $Object.hasOwnProperty.call(message, "isSample"))
          object.isSample = message.isSample;
        return object;
      };
      CreateTestCaseRequest.prototype.toJSON = function() {
        return CreateTestCaseRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CreateTestCaseRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.CreateTestCaseRequest";
      };
      return CreateTestCaseRequest;
    })();
    biz.UpdateTestCaseRequest = (function() {
      const UpdateTestCaseRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      UpdateTestCaseRequest.prototype.testCase = null;
      UpdateTestCaseRequest.create = function(properties) {
        return new UpdateTestCaseRequest(properties);
      };
      UpdateTestCaseRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.testCase != null && $Object.hasOwnProperty.call(message, "testCase"))
          $root.oj.common.TestCase.encode(message.testCase, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      UpdateTestCaseRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      UpdateTestCaseRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.UpdateTestCaseRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.testCase = $root.oj.common.TestCase.decode(reader, reader.uint32(), $undefined, _depth + 1, message.testCase);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      UpdateTestCaseRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      UpdateTestCaseRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.testCase != null && $Object.hasOwnProperty.call(message, "testCase")) {
          let error = $root.oj.common.TestCase.verify(message.testCase, _depth + 1);
          if (error)
            return "testCase." + error;
        }
        return null;
      };
      UpdateTestCaseRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.UpdateTestCaseRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.UpdateTestCaseRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.UpdateTestCaseRequest();
        if (object.testCase != null) {
          if (!$util.isObject(object.testCase))
            throw $TypeError(".oj.biz.UpdateTestCaseRequest.testCase: object expected");
          message.testCase = $root.oj.common.TestCase.fromObject(object.testCase, _depth + 1);
        }
        return message;
      };
      UpdateTestCaseRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.testCase = null;
        if (message.testCase != null && $Object.hasOwnProperty.call(message, "testCase"))
          object.testCase = $root.oj.common.TestCase.toObject(message.testCase, options, _depth + 1);
        return object;
      };
      UpdateTestCaseRequest.prototype.toJSON = function() {
        return UpdateTestCaseRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      UpdateTestCaseRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.UpdateTestCaseRequest";
      };
      return UpdateTestCaseRequest;
    })();
    biz.TestCaseIdRequest = (function() {
      const TestCaseIdRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      TestCaseIdRequest.prototype.testCaseId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      TestCaseIdRequest.create = function(properties) {
        return new TestCaseIdRequest(properties);
      };
      TestCaseIdRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.testCaseId != null && $Object.hasOwnProperty.call(message, "testCaseId") && (typeof message.testCaseId === "object" ? message.testCaseId.low || message.testCaseId.high : message.testCaseId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.testCaseId);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      TestCaseIdRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      TestCaseIdRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.TestCaseIdRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.testCaseId = value;
              else
                delete message.testCaseId;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      TestCaseIdRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      TestCaseIdRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.testCaseId != null && $Object.hasOwnProperty.call(message, "testCaseId")) {
          if (!$util.isInteger(message.testCaseId) && !(message.testCaseId && $util.isInteger(message.testCaseId.low) && $util.isInteger(message.testCaseId.high)))
            return "testCaseId: integer|Long expected";
        }
        return null;
      };
      TestCaseIdRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.TestCaseIdRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.TestCaseIdRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.TestCaseIdRequest();
        if (object.testCaseId != null) {
          if (typeof object.testCaseId === "object" ? object.testCaseId.low || object.testCaseId.high : $Number(object.testCaseId) !== 0) {
            if ($util.Long)
              message.testCaseId = $util.Long.fromValue(object.testCaseId, true);
            else if (typeof object.testCaseId === "string")
              message.testCaseId = $parseInt(object.testCaseId, 10);
            else if (typeof object.testCaseId === "number")
              message.testCaseId = object.testCaseId;
            else if (typeof object.testCaseId === "object")
              message.testCaseId = new $util.LongBits(object.testCaseId.low >>> 0, object.testCaseId.high >>> 0).toNumber(true);
          }
        }
        return message;
      };
      TestCaseIdRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.testCaseId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.testCaseId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        if (message.testCaseId != null && $Object.hasOwnProperty.call(message, "testCaseId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.testCaseId = typeof message.testCaseId === "number" ? $BigInt(message.testCaseId) : $util.Long.fromBits(message.testCaseId.low >>> 0, message.testCaseId.high >>> 0, true).toBigInt();
          else if (typeof message.testCaseId === "number")
            object.testCaseId = options.longs === $String ? $String(message.testCaseId) : message.testCaseId;
          else
            object.testCaseId = options.longs === $String ? $util.Long.prototype.toString.call(message.testCaseId) : options.longs === $Number ? new $util.LongBits(message.testCaseId.low >>> 0, message.testCaseId.high >>> 0).toNumber(true) : message.testCaseId;
        return object;
      };
      TestCaseIdRequest.prototype.toJSON = function() {
        return TestCaseIdRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      TestCaseIdRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.TestCaseIdRequest";
      };
      return TestCaseIdRequest;
    })();
    biz.TestCaseResponse = (function() {
      const TestCaseResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      TestCaseResponse.prototype.status = null;
      TestCaseResponse.prototype.testCase = null;
      TestCaseResponse.create = function(properties) {
        return new TestCaseResponse(properties);
      };
      TestCaseResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.testCase != null && $Object.hasOwnProperty.call(message, "testCase"))
          $root.oj.common.TestCase.encode(message.testCase, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      TestCaseResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      TestCaseResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.TestCaseResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.testCase = $root.oj.common.TestCase.decode(reader, reader.uint32(), $undefined, _depth + 1, message.testCase);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      TestCaseResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      TestCaseResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.testCase != null && $Object.hasOwnProperty.call(message, "testCase")) {
          let error = $root.oj.common.TestCase.verify(message.testCase, _depth + 1);
          if (error)
            return "testCase." + error;
        }
        return null;
      };
      TestCaseResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.TestCaseResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.TestCaseResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.TestCaseResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.TestCaseResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.testCase != null) {
          if (!$util.isObject(object.testCase))
            throw $TypeError(".oj.biz.TestCaseResponse.testCase: object expected");
          message.testCase = $root.oj.common.TestCase.fromObject(object.testCase, _depth + 1);
        }
        return message;
      };
      TestCaseResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.testCase = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.testCase != null && $Object.hasOwnProperty.call(message, "testCase"))
          object.testCase = $root.oj.common.TestCase.toObject(message.testCase, options, _depth + 1);
        return object;
      };
      TestCaseResponse.prototype.toJSON = function() {
        return TestCaseResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      TestCaseResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.TestCaseResponse";
      };
      return TestCaseResponse;
    })();
    biz.InvalidateQuestionCacheRequest = (function() {
      const InvalidateQuestionCacheRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      InvalidateQuestionCacheRequest.prototype.questionId = "";
      InvalidateQuestionCacheRequest.create = function(properties) {
        return new InvalidateQuestionCacheRequest(properties);
      };
      InvalidateQuestionCacheRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.questionId);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      InvalidateQuestionCacheRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      InvalidateQuestionCacheRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.InvalidateQuestionCacheRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      InvalidateQuestionCacheRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      InvalidateQuestionCacheRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        return null;
      };
      InvalidateQuestionCacheRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.InvalidateQuestionCacheRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.InvalidateQuestionCacheRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.InvalidateQuestionCacheRequest();
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        return message;
      };
      InvalidateQuestionCacheRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.questionId = "";
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        return object;
      };
      InvalidateQuestionCacheRequest.prototype.toJSON = function() {
        return InvalidateQuestionCacheRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      InvalidateQuestionCacheRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.InvalidateQuestionCacheRequest";
      };
      return InvalidateQuestionCacheRequest;
    })();
    biz.QuestionService = (function() {
      const QuestionService = function(rpcImpl, requestDelimited, responseDelimited) {
        import_minimal.default.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperty(QuestionService.prototype = $Object.create(import_minimal.default.rpc.Service.prototype), "constructor", { value: QuestionService, writable: true, enumerable: false, configurable: true });
      QuestionService.create = function(rpcImpl, requestDelimited, responseDelimited) {
        return new this(rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperties(QuestionService.prototype.listQuestions = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, QuestionService.prototype.listQuestions, $root.oj.biz.GetQuestionListRequest, $root.oj.biz.GetQuestionListResponse, request, callback);
      }, {
        name: { value: "ListQuestions" },
        path: { value: "/oj.biz.QuestionService/ListQuestions" },
        requestType: { value: "GetQuestionListRequest" },
        responseType: { value: "GetQuestionListResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(QuestionService.prototype.getQuestion = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, QuestionService.prototype.getQuestion, $root.oj.biz.QuestionIdRequest, $root.oj.biz.GetQuestionDetailResponse, request, callback);
      }, {
        name: { value: "GetQuestion" },
        path: { value: "/oj.biz.QuestionService/GetQuestion" },
        requestType: { value: "QuestionIdRequest" },
        responseType: { value: "GetQuestionDetailResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(QuestionService.prototype.getPassStatus = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, QuestionService.prototype.getPassStatus, $root.oj.biz.QuestionIdRequest, $root.oj.biz.GetQuestionPassStatusResponse, request, callback);
      }, {
        name: { value: "GetPassStatus" },
        path: { value: "/oj.biz.QuestionService/GetPassStatus" },
        requestType: { value: "QuestionIdRequest" },
        responseType: { value: "GetQuestionPassStatusResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(QuestionService.prototype.createQuestion = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, QuestionService.prototype.createQuestion, $root.oj.biz.SaveQuestionRequest, $root.oj.biz.QuestionResponse, request, callback);
      }, {
        name: { value: "CreateQuestion" },
        path: { value: "/oj.biz.QuestionService/CreateQuestion" },
        requestType: { value: "SaveQuestionRequest" },
        responseType: { value: "QuestionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(QuestionService.prototype.updateQuestion = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, QuestionService.prototype.updateQuestion, $root.oj.biz.SaveQuestionRequest, $root.oj.biz.QuestionResponse, request, callback);
      }, {
        name: { value: "UpdateQuestion" },
        path: { value: "/oj.biz.QuestionService/UpdateQuestion" },
        requestType: { value: "SaveQuestionRequest" },
        responseType: { value: "QuestionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(QuestionService.prototype.deleteQuestion = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, QuestionService.prototype.deleteQuestion, $root.oj.biz.QuestionIdRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "DeleteQuestion" },
        path: { value: "/oj.biz.QuestionService/DeleteQuestion" },
        requestType: { value: "QuestionIdRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(QuestionService.prototype.listTestCases = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, QuestionService.prototype.listTestCases, $root.oj.biz.ListTestCasesRequest, $root.oj.biz.ListTestCasesResponse, request, callback);
      }, {
        name: { value: "ListTestCases" },
        path: { value: "/oj.biz.QuestionService/ListTestCases" },
        requestType: { value: "ListTestCasesRequest" },
        responseType: { value: "ListTestCasesResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(QuestionService.prototype.createTestCase = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, QuestionService.prototype.createTestCase, $root.oj.biz.CreateTestCaseRequest, $root.oj.biz.TestCaseResponse, request, callback);
      }, {
        name: { value: "CreateTestCase" },
        path: { value: "/oj.biz.QuestionService/CreateTestCase" },
        requestType: { value: "CreateTestCaseRequest" },
        responseType: { value: "TestCaseResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(QuestionService.prototype.updateTestCase = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, QuestionService.prototype.updateTestCase, $root.oj.biz.UpdateTestCaseRequest, $root.oj.biz.TestCaseResponse, request, callback);
      }, {
        name: { value: "UpdateTestCase" },
        path: { value: "/oj.biz.QuestionService/UpdateTestCase" },
        requestType: { value: "UpdateTestCaseRequest" },
        responseType: { value: "TestCaseResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(QuestionService.prototype.deleteTestCase = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, QuestionService.prototype.deleteTestCase, $root.oj.biz.TestCaseIdRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "DeleteTestCase" },
        path: { value: "/oj.biz.QuestionService/DeleteTestCase" },
        requestType: { value: "TestCaseIdRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(QuestionService.prototype.invalidateCache = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, QuestionService.prototype.invalidateCache, $root.oj.biz.InvalidateQuestionCacheRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "InvalidateCache" },
        path: { value: "/oj.biz.QuestionService/InvalidateCache" },
        requestType: { value: "InvalidateQuestionCacheRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      return QuestionService;
    })();
    biz.ListSolutionsRequest = (function() {
      const ListSolutionsRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ListSolutionsRequest.prototype.page = null;
      ListSolutionsRequest.prototype.questionId = "";
      ListSolutionsRequest.prototype.sortBy = "";
      ListSolutionsRequest.prototype.authorUid = 0;
      ListSolutionsRequest.create = function(properties) {
        return new ListSolutionsRequest(properties);
      };
      ListSolutionsRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageRequest.encode(message.page, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.questionId);
        if (message.sortBy != null && $Object.hasOwnProperty.call(message, "sortBy") && message.sortBy !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.sortBy);
        if (message.authorUid != null && $Object.hasOwnProperty.call(message, "authorUid") && message.authorUid !== 0)
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).uint32(message.authorUid);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ListSolutionsRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ListSolutionsRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ListSolutionsRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.sortBy = value;
              else
                delete message.sortBy;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.authorUid = value;
              else
                delete message.authorUid;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ListSolutionsRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ListSolutionsRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageRequest.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        if (message.sortBy != null && $Object.hasOwnProperty.call(message, "sortBy")) {
          if (!$util.isString(message.sortBy))
            return "sortBy: string expected";
        }
        if (message.authorUid != null && $Object.hasOwnProperty.call(message, "authorUid")) {
          if (!$util.isInteger(message.authorUid))
            return "authorUid: integer expected";
        }
        return null;
      };
      ListSolutionsRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ListSolutionsRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ListSolutionsRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ListSolutionsRequest();
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.ListSolutionsRequest.page: object expected");
          message.page = $root.oj.common.PageRequest.fromObject(object.page, _depth + 1);
        }
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        if (object.sortBy != null) {
          if (typeof object.sortBy !== "string" || object.sortBy.length)
            message.sortBy = $String(object.sortBy);
        }
        if (object.authorUid != null) {
          if ($Number(object.authorUid) !== 0)
            message.authorUid = object.authorUid >>> 0;
        }
        return message;
      };
      ListSolutionsRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.page = null;
          object.questionId = "";
          object.sortBy = "";
          object.authorUid = 0;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageRequest.toObject(message.page, options, _depth + 1);
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        if (message.sortBy != null && $Object.hasOwnProperty.call(message, "sortBy"))
          object.sortBy = message.sortBy;
        if (message.authorUid != null && $Object.hasOwnProperty.call(message, "authorUid"))
          object.authorUid = message.authorUid;
        return object;
      };
      ListSolutionsRequest.prototype.toJSON = function() {
        return ListSolutionsRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ListSolutionsRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ListSolutionsRequest";
      };
      return ListSolutionsRequest;
    })();
    biz.ListSolutionsResponse = (function() {
      const ListSolutionsResponse = function(properties) {
        this.solutions = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ListSolutionsResponse.prototype.status = null;
      ListSolutionsResponse.prototype.page = null;
      ListSolutionsResponse.prototype.solutions = $util.emptyArray;
      ListSolutionsResponse.create = function(properties) {
        return new ListSolutionsResponse(properties);
      };
      ListSolutionsResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageResponse.encode(message.page, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.solutions != null && message.solutions.length)
          for (let i = 0; i < message.solutions.length; ++i)
            $root.oj.common.Solution.encode(message.solutions[i], writer.uint32(
              /* id 3, wireType 2 =*/
              26
            ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ListSolutionsResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ListSolutionsResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ListSolutionsResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if (!(message.solutions && message.solutions.length))
                message.solutions = [];
              message.solutions.push($root.oj.common.Solution.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ListSolutionsResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ListSolutionsResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageResponse.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.solutions != null && $Object.hasOwnProperty.call(message, "solutions")) {
          if (!$Array.isArray(message.solutions))
            return "solutions: array expected";
          for (let i = 0; i < message.solutions.length; ++i) {
            let error = $root.oj.common.Solution.verify(message.solutions[i], _depth + 1);
            if (error)
              return "solutions." + error;
          }
        }
        return null;
      };
      ListSolutionsResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ListSolutionsResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ListSolutionsResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ListSolutionsResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.ListSolutionsResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.ListSolutionsResponse.page: object expected");
          message.page = $root.oj.common.PageResponse.fromObject(object.page, _depth + 1);
        }
        if (object.solutions) {
          if (!$Array.isArray(object.solutions))
            throw $TypeError(".oj.biz.ListSolutionsResponse.solutions: array expected");
          message.solutions = $Array(object.solutions.length);
          for (let i = 0; i < object.solutions.length; ++i) {
            if (!$util.isObject(object.solutions[i]))
              throw $TypeError(".oj.biz.ListSolutionsResponse.solutions: object expected");
            message.solutions[i] = $root.oj.common.Solution.fromObject(object.solutions[i], _depth + 1);
          }
        }
        return message;
      };
      ListSolutionsResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.solutions = [];
        if (options.defaults) {
          object.status = null;
          object.page = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageResponse.toObject(message.page, options, _depth + 1);
        if (message.solutions && message.solutions.length) {
          object.solutions = $Array(message.solutions.length);
          for (let j = 0; j < message.solutions.length; ++j)
            object.solutions[j] = $root.oj.common.Solution.toObject(message.solutions[j], options, _depth + 1);
        }
        return object;
      };
      ListSolutionsResponse.prototype.toJSON = function() {
        return ListSolutionsResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ListSolutionsResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ListSolutionsResponse";
      };
      return ListSolutionsResponse;
    })();
    biz.SolutionIdRequest = (function() {
      const SolutionIdRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      SolutionIdRequest.prototype.solutionId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      SolutionIdRequest.create = function(properties) {
        return new SolutionIdRequest(properties);
      };
      SolutionIdRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId") && (typeof message.solutionId === "object" ? message.solutionId.low || message.solutionId.high : message.solutionId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.solutionId);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      SolutionIdRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      SolutionIdRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.SolutionIdRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.solutionId = value;
              else
                delete message.solutionId;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      SolutionIdRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      SolutionIdRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId")) {
          if (!$util.isInteger(message.solutionId) && !(message.solutionId && $util.isInteger(message.solutionId.low) && $util.isInteger(message.solutionId.high)))
            return "solutionId: integer|Long expected";
        }
        return null;
      };
      SolutionIdRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.SolutionIdRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.SolutionIdRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.SolutionIdRequest();
        if (object.solutionId != null) {
          if (typeof object.solutionId === "object" ? object.solutionId.low || object.solutionId.high : $Number(object.solutionId) !== 0) {
            if ($util.Long)
              message.solutionId = $util.Long.fromValue(object.solutionId, true);
            else if (typeof object.solutionId === "string")
              message.solutionId = $parseInt(object.solutionId, 10);
            else if (typeof object.solutionId === "number")
              message.solutionId = object.solutionId;
            else if (typeof object.solutionId === "object")
              message.solutionId = new $util.LongBits(object.solutionId.low >>> 0, object.solutionId.high >>> 0).toNumber(true);
          }
        }
        return message;
      };
      SolutionIdRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.solutionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.solutionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.solutionId = typeof message.solutionId === "number" ? $BigInt(message.solutionId) : $util.Long.fromBits(message.solutionId.low >>> 0, message.solutionId.high >>> 0, true).toBigInt();
          else if (typeof message.solutionId === "number")
            object.solutionId = options.longs === $String ? $String(message.solutionId) : message.solutionId;
          else
            object.solutionId = options.longs === $String ? $util.Long.prototype.toString.call(message.solutionId) : options.longs === $Number ? new $util.LongBits(message.solutionId.low >>> 0, message.solutionId.high >>> 0).toNumber(true) : message.solutionId;
        return object;
      };
      SolutionIdRequest.prototype.toJSON = function() {
        return SolutionIdRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      SolutionIdRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.SolutionIdRequest";
      };
      return SolutionIdRequest;
    })();
    biz.GetSolutionResponse = (function() {
      const GetSolutionResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetSolutionResponse.prototype.status = null;
      GetSolutionResponse.prototype.solution = null;
      GetSolutionResponse.prototype.author = null;
      GetSolutionResponse.prototype.currentUserActions = null;
      GetSolutionResponse.create = function(properties) {
        return new GetSolutionResponse(properties);
      };
      GetSolutionResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.solution != null && $Object.hasOwnProperty.call(message, "solution"))
          $root.oj.common.Solution.encode(message.solution, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.author != null && $Object.hasOwnProperty.call(message, "author"))
          $root.oj.common.User.encode(message.author, writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).fork(), _depth + 1).ldelim();
        if (message.currentUserActions != null && $Object.hasOwnProperty.call(message, "currentUserActions"))
          $root.oj.common.SolutionActionState.encode(message.currentUserActions, writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetSolutionResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetSolutionResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetSolutionResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.solution = $root.oj.common.Solution.decode(reader, reader.uint32(), $undefined, _depth + 1, message.solution);
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              message.author = $root.oj.common.User.decode(reader, reader.uint32(), $undefined, _depth + 1, message.author);
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              message.currentUserActions = $root.oj.common.SolutionActionState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.currentUserActions);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetSolutionResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetSolutionResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.solution != null && $Object.hasOwnProperty.call(message, "solution")) {
          let error = $root.oj.common.Solution.verify(message.solution, _depth + 1);
          if (error)
            return "solution." + error;
        }
        if (message.author != null && $Object.hasOwnProperty.call(message, "author")) {
          let error = $root.oj.common.User.verify(message.author, _depth + 1);
          if (error)
            return "author." + error;
        }
        if (message.currentUserActions != null && $Object.hasOwnProperty.call(message, "currentUserActions")) {
          let error = $root.oj.common.SolutionActionState.verify(message.currentUserActions, _depth + 1);
          if (error)
            return "currentUserActions." + error;
        }
        return null;
      };
      GetSolutionResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetSolutionResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetSolutionResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetSolutionResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetSolutionResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.solution != null) {
          if (!$util.isObject(object.solution))
            throw $TypeError(".oj.biz.GetSolutionResponse.solution: object expected");
          message.solution = $root.oj.common.Solution.fromObject(object.solution, _depth + 1);
        }
        if (object.author != null) {
          if (!$util.isObject(object.author))
            throw $TypeError(".oj.biz.GetSolutionResponse.author: object expected");
          message.author = $root.oj.common.User.fromObject(object.author, _depth + 1);
        }
        if (object.currentUserActions != null) {
          if (!$util.isObject(object.currentUserActions))
            throw $TypeError(".oj.biz.GetSolutionResponse.currentUserActions: object expected");
          message.currentUserActions = $root.oj.common.SolutionActionState.fromObject(object.currentUserActions, _depth + 1);
        }
        return message;
      };
      GetSolutionResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.solution = null;
          object.author = null;
          object.currentUserActions = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.solution != null && $Object.hasOwnProperty.call(message, "solution"))
          object.solution = $root.oj.common.Solution.toObject(message.solution, options, _depth + 1);
        if (message.author != null && $Object.hasOwnProperty.call(message, "author"))
          object.author = $root.oj.common.User.toObject(message.author, options, _depth + 1);
        if (message.currentUserActions != null && $Object.hasOwnProperty.call(message, "currentUserActions"))
          object.currentUserActions = $root.oj.common.SolutionActionState.toObject(message.currentUserActions, options, _depth + 1);
        return object;
      };
      GetSolutionResponse.prototype.toJSON = function() {
        return GetSolutionResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetSolutionResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetSolutionResponse";
      };
      return GetSolutionResponse;
    })();
    biz.CreateSolutionRequest = (function() {
      const CreateSolutionRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CreateSolutionRequest.prototype.questionId = "";
      CreateSolutionRequest.prototype.title = "";
      CreateSolutionRequest.prototype.contentMd = "";
      CreateSolutionRequest.create = function(properties) {
        return new CreateSolutionRequest(properties);
      };
      CreateSolutionRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.questionId);
        if (message.title != null && $Object.hasOwnProperty.call(message, "title") && message.title !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.title);
        if (message.contentMd != null && $Object.hasOwnProperty.call(message, "contentMd") && message.contentMd !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.contentMd);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CreateSolutionRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CreateSolutionRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.CreateSolutionRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.title = value;
              else
                delete message.title;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.contentMd = value;
              else
                delete message.contentMd;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CreateSolutionRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CreateSolutionRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        if (message.title != null && $Object.hasOwnProperty.call(message, "title")) {
          if (!$util.isString(message.title))
            return "title: string expected";
        }
        if (message.contentMd != null && $Object.hasOwnProperty.call(message, "contentMd")) {
          if (!$util.isString(message.contentMd))
            return "contentMd: string expected";
        }
        return null;
      };
      CreateSolutionRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.CreateSolutionRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.CreateSolutionRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.CreateSolutionRequest();
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        if (object.title != null) {
          if (typeof object.title !== "string" || object.title.length)
            message.title = $String(object.title);
        }
        if (object.contentMd != null) {
          if (typeof object.contentMd !== "string" || object.contentMd.length)
            message.contentMd = $String(object.contentMd);
        }
        return message;
      };
      CreateSolutionRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.questionId = "";
          object.title = "";
          object.contentMd = "";
        }
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        if (message.title != null && $Object.hasOwnProperty.call(message, "title"))
          object.title = message.title;
        if (message.contentMd != null && $Object.hasOwnProperty.call(message, "contentMd"))
          object.contentMd = message.contentMd;
        return object;
      };
      CreateSolutionRequest.prototype.toJSON = function() {
        return CreateSolutionRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CreateSolutionRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.CreateSolutionRequest";
      };
      return CreateSolutionRequest;
    })();
    biz.UpdateSolutionRequest = (function() {
      const UpdateSolutionRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      UpdateSolutionRequest.prototype.solutionId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      UpdateSolutionRequest.prototype.title = "";
      UpdateSolutionRequest.prototype.contentMd = "";
      UpdateSolutionRequest.create = function(properties) {
        return new UpdateSolutionRequest(properties);
      };
      UpdateSolutionRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId") && (typeof message.solutionId === "object" ? message.solutionId.low || message.solutionId.high : message.solutionId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.solutionId);
        if (message.title != null && $Object.hasOwnProperty.call(message, "title") && message.title !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.title);
        if (message.contentMd != null && $Object.hasOwnProperty.call(message, "contentMd") && message.contentMd !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.contentMd);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      UpdateSolutionRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      UpdateSolutionRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.UpdateSolutionRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.solutionId = value;
              else
                delete message.solutionId;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.title = value;
              else
                delete message.title;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.contentMd = value;
              else
                delete message.contentMd;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      UpdateSolutionRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      UpdateSolutionRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId")) {
          if (!$util.isInteger(message.solutionId) && !(message.solutionId && $util.isInteger(message.solutionId.low) && $util.isInteger(message.solutionId.high)))
            return "solutionId: integer|Long expected";
        }
        if (message.title != null && $Object.hasOwnProperty.call(message, "title")) {
          if (!$util.isString(message.title))
            return "title: string expected";
        }
        if (message.contentMd != null && $Object.hasOwnProperty.call(message, "contentMd")) {
          if (!$util.isString(message.contentMd))
            return "contentMd: string expected";
        }
        return null;
      };
      UpdateSolutionRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.UpdateSolutionRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.UpdateSolutionRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.UpdateSolutionRequest();
        if (object.solutionId != null) {
          if (typeof object.solutionId === "object" ? object.solutionId.low || object.solutionId.high : $Number(object.solutionId) !== 0) {
            if ($util.Long)
              message.solutionId = $util.Long.fromValue(object.solutionId, true);
            else if (typeof object.solutionId === "string")
              message.solutionId = $parseInt(object.solutionId, 10);
            else if (typeof object.solutionId === "number")
              message.solutionId = object.solutionId;
            else if (typeof object.solutionId === "object")
              message.solutionId = new $util.LongBits(object.solutionId.low >>> 0, object.solutionId.high >>> 0).toNumber(true);
          }
        }
        if (object.title != null) {
          if (typeof object.title !== "string" || object.title.length)
            message.title = $String(object.title);
        }
        if (object.contentMd != null) {
          if (typeof object.contentMd !== "string" || object.contentMd.length)
            message.contentMd = $String(object.contentMd);
        }
        return message;
      };
      UpdateSolutionRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.solutionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.solutionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.title = "";
          object.contentMd = "";
        }
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.solutionId = typeof message.solutionId === "number" ? $BigInt(message.solutionId) : $util.Long.fromBits(message.solutionId.low >>> 0, message.solutionId.high >>> 0, true).toBigInt();
          else if (typeof message.solutionId === "number")
            object.solutionId = options.longs === $String ? $String(message.solutionId) : message.solutionId;
          else
            object.solutionId = options.longs === $String ? $util.Long.prototype.toString.call(message.solutionId) : options.longs === $Number ? new $util.LongBits(message.solutionId.low >>> 0, message.solutionId.high >>> 0).toNumber(true) : message.solutionId;
        if (message.title != null && $Object.hasOwnProperty.call(message, "title"))
          object.title = message.title;
        if (message.contentMd != null && $Object.hasOwnProperty.call(message, "contentMd"))
          object.contentMd = message.contentMd;
        return object;
      };
      UpdateSolutionRequest.prototype.toJSON = function() {
        return UpdateSolutionRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      UpdateSolutionRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.UpdateSolutionRequest";
      };
      return UpdateSolutionRequest;
    })();
    biz.SolutionResponse = (function() {
      const SolutionResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      SolutionResponse.prototype.status = null;
      SolutionResponse.prototype.solution = null;
      SolutionResponse.create = function(properties) {
        return new SolutionResponse(properties);
      };
      SolutionResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.solution != null && $Object.hasOwnProperty.call(message, "solution"))
          $root.oj.common.Solution.encode(message.solution, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      SolutionResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      SolutionResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.SolutionResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.solution = $root.oj.common.Solution.decode(reader, reader.uint32(), $undefined, _depth + 1, message.solution);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      SolutionResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      SolutionResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.solution != null && $Object.hasOwnProperty.call(message, "solution")) {
          let error = $root.oj.common.Solution.verify(message.solution, _depth + 1);
          if (error)
            return "solution." + error;
        }
        return null;
      };
      SolutionResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.SolutionResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.SolutionResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.SolutionResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.SolutionResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.solution != null) {
          if (!$util.isObject(object.solution))
            throw $TypeError(".oj.biz.SolutionResponse.solution: object expected");
          message.solution = $root.oj.common.Solution.fromObject(object.solution, _depth + 1);
        }
        return message;
      };
      SolutionResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.solution = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.solution != null && $Object.hasOwnProperty.call(message, "solution"))
          object.solution = $root.oj.common.Solution.toObject(message.solution, options, _depth + 1);
        return object;
      };
      SolutionResponse.prototype.toJSON = function() {
        return SolutionResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      SolutionResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.SolutionResponse";
      };
      return SolutionResponse;
    })();
    biz.SolutionActionResponse = (function() {
      const SolutionActionResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      SolutionActionResponse.prototype.status = null;
      SolutionActionResponse.prototype.actions = null;
      SolutionActionResponse.prototype.likeCount = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      SolutionActionResponse.prototype.favoriteCount = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      SolutionActionResponse.create = function(properties) {
        return new SolutionActionResponse(properties);
      };
      SolutionActionResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.actions != null && $Object.hasOwnProperty.call(message, "actions"))
          $root.oj.common.SolutionActionState.encode(message.actions, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount") && (typeof message.likeCount === "object" ? message.likeCount.low || message.likeCount.high : message.likeCount !== 0))
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint64(message.likeCount);
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount") && (typeof message.favoriteCount === "object" ? message.favoriteCount.low || message.favoriteCount.high : message.favoriteCount !== 0))
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).uint64(message.favoriteCount);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      SolutionActionResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      SolutionActionResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.SolutionActionResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.actions = $root.oj.common.SolutionActionState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.actions);
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.likeCount = value;
              else
                delete message.likeCount;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.favoriteCount = value;
              else
                delete message.favoriteCount;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      SolutionActionResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      SolutionActionResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.actions != null && $Object.hasOwnProperty.call(message, "actions")) {
          let error = $root.oj.common.SolutionActionState.verify(message.actions, _depth + 1);
          if (error)
            return "actions." + error;
        }
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount")) {
          if (!$util.isInteger(message.likeCount) && !(message.likeCount && $util.isInteger(message.likeCount.low) && $util.isInteger(message.likeCount.high)))
            return "likeCount: integer|Long expected";
        }
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount")) {
          if (!$util.isInteger(message.favoriteCount) && !(message.favoriteCount && $util.isInteger(message.favoriteCount.low) && $util.isInteger(message.favoriteCount.high)))
            return "favoriteCount: integer|Long expected";
        }
        return null;
      };
      SolutionActionResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.SolutionActionResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.SolutionActionResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.SolutionActionResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.SolutionActionResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.actions != null) {
          if (!$util.isObject(object.actions))
            throw $TypeError(".oj.biz.SolutionActionResponse.actions: object expected");
          message.actions = $root.oj.common.SolutionActionState.fromObject(object.actions, _depth + 1);
        }
        if (object.likeCount != null) {
          if (typeof object.likeCount === "object" ? object.likeCount.low || object.likeCount.high : $Number(object.likeCount) !== 0) {
            if ($util.Long)
              message.likeCount = $util.Long.fromValue(object.likeCount, true);
            else if (typeof object.likeCount === "string")
              message.likeCount = $parseInt(object.likeCount, 10);
            else if (typeof object.likeCount === "number")
              message.likeCount = object.likeCount;
            else if (typeof object.likeCount === "object")
              message.likeCount = new $util.LongBits(object.likeCount.low >>> 0, object.likeCount.high >>> 0).toNumber(true);
          }
        }
        if (object.favoriteCount != null) {
          if (typeof object.favoriteCount === "object" ? object.favoriteCount.low || object.favoriteCount.high : $Number(object.favoriteCount) !== 0) {
            if ($util.Long)
              message.favoriteCount = $util.Long.fromValue(object.favoriteCount, true);
            else if (typeof object.favoriteCount === "string")
              message.favoriteCount = $parseInt(object.favoriteCount, 10);
            else if (typeof object.favoriteCount === "number")
              message.favoriteCount = object.favoriteCount;
            else if (typeof object.favoriteCount === "object")
              message.favoriteCount = new $util.LongBits(object.favoriteCount.low >>> 0, object.favoriteCount.high >>> 0).toNumber(true);
          }
        }
        return message;
      };
      SolutionActionResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.actions = null;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.likeCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.likeCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.favoriteCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.favoriteCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.actions != null && $Object.hasOwnProperty.call(message, "actions"))
          object.actions = $root.oj.common.SolutionActionState.toObject(message.actions, options, _depth + 1);
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.likeCount = typeof message.likeCount === "number" ? $BigInt(message.likeCount) : $util.Long.fromBits(message.likeCount.low >>> 0, message.likeCount.high >>> 0, true).toBigInt();
          else if (typeof message.likeCount === "number")
            object.likeCount = options.longs === $String ? $String(message.likeCount) : message.likeCount;
          else
            object.likeCount = options.longs === $String ? $util.Long.prototype.toString.call(message.likeCount) : options.longs === $Number ? new $util.LongBits(message.likeCount.low >>> 0, message.likeCount.high >>> 0).toNumber(true) : message.likeCount;
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.favoriteCount = typeof message.favoriteCount === "number" ? $BigInt(message.favoriteCount) : $util.Long.fromBits(message.favoriteCount.low >>> 0, message.favoriteCount.high >>> 0, true).toBigInt();
          else if (typeof message.favoriteCount === "number")
            object.favoriteCount = options.longs === $String ? $String(message.favoriteCount) : message.favoriteCount;
          else
            object.favoriteCount = options.longs === $String ? $util.Long.prototype.toString.call(message.favoriteCount) : options.longs === $Number ? new $util.LongBits(message.favoriteCount.low >>> 0, message.favoriteCount.high >>> 0).toNumber(true) : message.favoriteCount;
        return object;
      };
      SolutionActionResponse.prototype.toJSON = function() {
        return SolutionActionResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      SolutionActionResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.SolutionActionResponse";
      };
      return SolutionActionResponse;
    })();
    biz.SolutionService = (function() {
      const SolutionService = function(rpcImpl, requestDelimited, responseDelimited) {
        import_minimal.default.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperty(SolutionService.prototype = $Object.create(import_minimal.default.rpc.Service.prototype), "constructor", { value: SolutionService, writable: true, enumerable: false, configurable: true });
      SolutionService.create = function(rpcImpl, requestDelimited, responseDelimited) {
        return new this(rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperties(SolutionService.prototype.listSolutions = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SolutionService.prototype.listSolutions, $root.oj.biz.ListSolutionsRequest, $root.oj.biz.ListSolutionsResponse, request, callback);
      }, {
        name: { value: "ListSolutions" },
        path: { value: "/oj.biz.SolutionService/ListSolutions" },
        requestType: { value: "ListSolutionsRequest" },
        responseType: { value: "ListSolutionsResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SolutionService.prototype.getSolution = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SolutionService.prototype.getSolution, $root.oj.biz.SolutionIdRequest, $root.oj.biz.GetSolutionResponse, request, callback);
      }, {
        name: { value: "GetSolution" },
        path: { value: "/oj.biz.SolutionService/GetSolution" },
        requestType: { value: "SolutionIdRequest" },
        responseType: { value: "GetSolutionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SolutionService.prototype.createSolution = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SolutionService.prototype.createSolution, $root.oj.biz.CreateSolutionRequest, $root.oj.biz.SolutionResponse, request, callback);
      }, {
        name: { value: "CreateSolution" },
        path: { value: "/oj.biz.SolutionService/CreateSolution" },
        requestType: { value: "CreateSolutionRequest" },
        responseType: { value: "SolutionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SolutionService.prototype.updateSolution = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SolutionService.prototype.updateSolution, $root.oj.biz.UpdateSolutionRequest, $root.oj.biz.SolutionResponse, request, callback);
      }, {
        name: { value: "UpdateSolution" },
        path: { value: "/oj.biz.SolutionService/UpdateSolution" },
        requestType: { value: "UpdateSolutionRequest" },
        responseType: { value: "SolutionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SolutionService.prototype.deleteSolution = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SolutionService.prototype.deleteSolution, $root.oj.biz.SolutionIdRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "DeleteSolution" },
        path: { value: "/oj.biz.SolutionService/DeleteSolution" },
        requestType: { value: "SolutionIdRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SolutionService.prototype.toggleLike = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SolutionService.prototype.toggleLike, $root.oj.biz.SolutionIdRequest, $root.oj.biz.SolutionActionResponse, request, callback);
      }, {
        name: { value: "ToggleLike" },
        path: { value: "/oj.biz.SolutionService/ToggleLike" },
        requestType: { value: "SolutionIdRequest" },
        responseType: { value: "SolutionActionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SolutionService.prototype.toggleFavorite = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SolutionService.prototype.toggleFavorite, $root.oj.biz.SolutionIdRequest, $root.oj.biz.SolutionActionResponse, request, callback);
      }, {
        name: { value: "ToggleFavorite" },
        path: { value: "/oj.biz.SolutionService/ToggleFavorite" },
        requestType: { value: "SolutionIdRequest" },
        responseType: { value: "SolutionActionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SolutionService.prototype.getActionState = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SolutionService.prototype.getActionState, $root.oj.biz.SolutionIdRequest, $root.oj.biz.SolutionActionResponse, request, callback);
      }, {
        name: { value: "GetActionState" },
        path: { value: "/oj.biz.SolutionService/GetActionState" },
        requestType: { value: "SolutionIdRequest" },
        responseType: { value: "SolutionActionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      return SolutionService;
    })();
    biz.ListCommentsRequest = (function() {
      const ListCommentsRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ListCommentsRequest.prototype.solutionId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      ListCommentsRequest.prototype.page = null;
      ListCommentsRequest.create = function(properties) {
        return new ListCommentsRequest(properties);
      };
      ListCommentsRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId") && (typeof message.solutionId === "object" ? message.solutionId.low || message.solutionId.high : message.solutionId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.solutionId);
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageRequest.encode(message.page, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ListCommentsRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ListCommentsRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ListCommentsRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.solutionId = value;
              else
                delete message.solutionId;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ListCommentsRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ListCommentsRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId")) {
          if (!$util.isInteger(message.solutionId) && !(message.solutionId && $util.isInteger(message.solutionId.low) && $util.isInteger(message.solutionId.high)))
            return "solutionId: integer|Long expected";
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageRequest.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        return null;
      };
      ListCommentsRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ListCommentsRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ListCommentsRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ListCommentsRequest();
        if (object.solutionId != null) {
          if (typeof object.solutionId === "object" ? object.solutionId.low || object.solutionId.high : $Number(object.solutionId) !== 0) {
            if ($util.Long)
              message.solutionId = $util.Long.fromValue(object.solutionId, true);
            else if (typeof object.solutionId === "string")
              message.solutionId = $parseInt(object.solutionId, 10);
            else if (typeof object.solutionId === "number")
              message.solutionId = object.solutionId;
            else if (typeof object.solutionId === "object")
              message.solutionId = new $util.LongBits(object.solutionId.low >>> 0, object.solutionId.high >>> 0).toNumber(true);
          }
        }
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.ListCommentsRequest.page: object expected");
          message.page = $root.oj.common.PageRequest.fromObject(object.page, _depth + 1);
        }
        return message;
      };
      ListCommentsRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.solutionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.solutionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.page = null;
        }
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.solutionId = typeof message.solutionId === "number" ? $BigInt(message.solutionId) : $util.Long.fromBits(message.solutionId.low >>> 0, message.solutionId.high >>> 0, true).toBigInt();
          else if (typeof message.solutionId === "number")
            object.solutionId = options.longs === $String ? $String(message.solutionId) : message.solutionId;
          else
            object.solutionId = options.longs === $String ? $util.Long.prototype.toString.call(message.solutionId) : options.longs === $Number ? new $util.LongBits(message.solutionId.low >>> 0, message.solutionId.high >>> 0).toNumber(true) : message.solutionId;
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageRequest.toObject(message.page, options, _depth + 1);
        return object;
      };
      ListCommentsRequest.prototype.toJSON = function() {
        return ListCommentsRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ListCommentsRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ListCommentsRequest";
      };
      return ListCommentsRequest;
    })();
    biz.ListRepliesRequest = (function() {
      const ListRepliesRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ListRepliesRequest.prototype.parentCommentId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      ListRepliesRequest.prototype.page = null;
      ListRepliesRequest.create = function(properties) {
        return new ListRepliesRequest(properties);
      };
      ListRepliesRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.parentCommentId != null && $Object.hasOwnProperty.call(message, "parentCommentId") && (typeof message.parentCommentId === "object" ? message.parentCommentId.low || message.parentCommentId.high : message.parentCommentId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.parentCommentId);
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageRequest.encode(message.page, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ListRepliesRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ListRepliesRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ListRepliesRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.parentCommentId = value;
              else
                delete message.parentCommentId;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ListRepliesRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ListRepliesRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.parentCommentId != null && $Object.hasOwnProperty.call(message, "parentCommentId")) {
          if (!$util.isInteger(message.parentCommentId) && !(message.parentCommentId && $util.isInteger(message.parentCommentId.low) && $util.isInteger(message.parentCommentId.high)))
            return "parentCommentId: integer|Long expected";
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageRequest.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        return null;
      };
      ListRepliesRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ListRepliesRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ListRepliesRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ListRepliesRequest();
        if (object.parentCommentId != null) {
          if (typeof object.parentCommentId === "object" ? object.parentCommentId.low || object.parentCommentId.high : $Number(object.parentCommentId) !== 0) {
            if ($util.Long)
              message.parentCommentId = $util.Long.fromValue(object.parentCommentId, true);
            else if (typeof object.parentCommentId === "string")
              message.parentCommentId = $parseInt(object.parentCommentId, 10);
            else if (typeof object.parentCommentId === "number")
              message.parentCommentId = object.parentCommentId;
            else if (typeof object.parentCommentId === "object")
              message.parentCommentId = new $util.LongBits(object.parentCommentId.low >>> 0, object.parentCommentId.high >>> 0).toNumber(true);
          }
        }
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.ListRepliesRequest.page: object expected");
          message.page = $root.oj.common.PageRequest.fromObject(object.page, _depth + 1);
        }
        return message;
      };
      ListRepliesRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.parentCommentId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.parentCommentId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.page = null;
        }
        if (message.parentCommentId != null && $Object.hasOwnProperty.call(message, "parentCommentId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.parentCommentId = typeof message.parentCommentId === "number" ? $BigInt(message.parentCommentId) : $util.Long.fromBits(message.parentCommentId.low >>> 0, message.parentCommentId.high >>> 0, true).toBigInt();
          else if (typeof message.parentCommentId === "number")
            object.parentCommentId = options.longs === $String ? $String(message.parentCommentId) : message.parentCommentId;
          else
            object.parentCommentId = options.longs === $String ? $util.Long.prototype.toString.call(message.parentCommentId) : options.longs === $Number ? new $util.LongBits(message.parentCommentId.low >>> 0, message.parentCommentId.high >>> 0).toNumber(true) : message.parentCommentId;
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageRequest.toObject(message.page, options, _depth + 1);
        return object;
      };
      ListRepliesRequest.prototype.toJSON = function() {
        return ListRepliesRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ListRepliesRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ListRepliesRequest";
      };
      return ListRepliesRequest;
    })();
    biz.ListCommentsResponse = (function() {
      const ListCommentsResponse = function(properties) {
        this.comments = [];
        this.currentUserActions = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ListCommentsResponse.prototype.status = null;
      ListCommentsResponse.prototype.page = null;
      ListCommentsResponse.prototype.comments = $util.emptyArray;
      ListCommentsResponse.prototype.currentUserActions = $util.emptyArray;
      ListCommentsResponse.create = function(properties) {
        return new ListCommentsResponse(properties);
      };
      ListCommentsResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageResponse.encode(message.page, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.comments != null && message.comments.length)
          for (let i = 0; i < message.comments.length; ++i)
            $root.oj.common.Comment.encode(message.comments[i], writer.uint32(
              /* id 3, wireType 2 =*/
              26
            ).fork(), _depth + 1).ldelim();
        if (message.currentUserActions != null && message.currentUserActions.length)
          for (let i = 0; i < message.currentUserActions.length; ++i)
            $root.oj.common.CommentActionState.encode(message.currentUserActions[i], writer.uint32(
              /* id 4, wireType 2 =*/
              34
            ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ListCommentsResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ListCommentsResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ListCommentsResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if (!(message.comments && message.comments.length))
                message.comments = [];
              message.comments.push($root.oj.common.Comment.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if (!(message.currentUserActions && message.currentUserActions.length))
                message.currentUserActions = [];
              message.currentUserActions.push($root.oj.common.CommentActionState.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ListCommentsResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ListCommentsResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageResponse.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.comments != null && $Object.hasOwnProperty.call(message, "comments")) {
          if (!$Array.isArray(message.comments))
            return "comments: array expected";
          for (let i = 0; i < message.comments.length; ++i) {
            let error = $root.oj.common.Comment.verify(message.comments[i], _depth + 1);
            if (error)
              return "comments." + error;
          }
        }
        if (message.currentUserActions != null && $Object.hasOwnProperty.call(message, "currentUserActions")) {
          if (!$Array.isArray(message.currentUserActions))
            return "currentUserActions: array expected";
          for (let i = 0; i < message.currentUserActions.length; ++i) {
            let error = $root.oj.common.CommentActionState.verify(message.currentUserActions[i], _depth + 1);
            if (error)
              return "currentUserActions." + error;
          }
        }
        return null;
      };
      ListCommentsResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ListCommentsResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ListCommentsResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ListCommentsResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.ListCommentsResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.ListCommentsResponse.page: object expected");
          message.page = $root.oj.common.PageResponse.fromObject(object.page, _depth + 1);
        }
        if (object.comments) {
          if (!$Array.isArray(object.comments))
            throw $TypeError(".oj.biz.ListCommentsResponse.comments: array expected");
          message.comments = $Array(object.comments.length);
          for (let i = 0; i < object.comments.length; ++i) {
            if (!$util.isObject(object.comments[i]))
              throw $TypeError(".oj.biz.ListCommentsResponse.comments: object expected");
            message.comments[i] = $root.oj.common.Comment.fromObject(object.comments[i], _depth + 1);
          }
        }
        if (object.currentUserActions) {
          if (!$Array.isArray(object.currentUserActions))
            throw $TypeError(".oj.biz.ListCommentsResponse.currentUserActions: array expected");
          message.currentUserActions = $Array(object.currentUserActions.length);
          for (let i = 0; i < object.currentUserActions.length; ++i) {
            if (!$util.isObject(object.currentUserActions[i]))
              throw $TypeError(".oj.biz.ListCommentsResponse.currentUserActions: object expected");
            message.currentUserActions[i] = $root.oj.common.CommentActionState.fromObject(object.currentUserActions[i], _depth + 1);
          }
        }
        return message;
      };
      ListCommentsResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults) {
          object.comments = [];
          object.currentUserActions = [];
        }
        if (options.defaults) {
          object.status = null;
          object.page = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageResponse.toObject(message.page, options, _depth + 1);
        if (message.comments && message.comments.length) {
          object.comments = $Array(message.comments.length);
          for (let j = 0; j < message.comments.length; ++j)
            object.comments[j] = $root.oj.common.Comment.toObject(message.comments[j], options, _depth + 1);
        }
        if (message.currentUserActions && message.currentUserActions.length) {
          object.currentUserActions = $Array(message.currentUserActions.length);
          for (let j = 0; j < message.currentUserActions.length; ++j)
            object.currentUserActions[j] = $root.oj.common.CommentActionState.toObject(message.currentUserActions[j], options, _depth + 1);
        }
        return object;
      };
      ListCommentsResponse.prototype.toJSON = function() {
        return ListCommentsResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ListCommentsResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ListCommentsResponse";
      };
      return ListCommentsResponse;
    })();
    biz.CreateCommentRequest = (function() {
      const CreateCommentRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CreateCommentRequest.prototype.solutionId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      CreateCommentRequest.prototype.parentCommentId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      CreateCommentRequest.prototype.content = "";
      CreateCommentRequest.create = function(properties) {
        return new CreateCommentRequest(properties);
      };
      CreateCommentRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId") && (typeof message.solutionId === "object" ? message.solutionId.low || message.solutionId.high : message.solutionId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.solutionId);
        if (message.parentCommentId != null && $Object.hasOwnProperty.call(message, "parentCommentId") && (typeof message.parentCommentId === "object" ? message.parentCommentId.low || message.parentCommentId.high : message.parentCommentId !== 0))
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint64(message.parentCommentId);
        if (message.content != null && $Object.hasOwnProperty.call(message, "content") && message.content !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.content);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CreateCommentRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CreateCommentRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.CreateCommentRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.solutionId = value;
              else
                delete message.solutionId;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.parentCommentId = value;
              else
                delete message.parentCommentId;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.content = value;
              else
                delete message.content;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CreateCommentRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CreateCommentRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId")) {
          if (!$util.isInteger(message.solutionId) && !(message.solutionId && $util.isInteger(message.solutionId.low) && $util.isInteger(message.solutionId.high)))
            return "solutionId: integer|Long expected";
        }
        if (message.parentCommentId != null && $Object.hasOwnProperty.call(message, "parentCommentId")) {
          if (!$util.isInteger(message.parentCommentId) && !(message.parentCommentId && $util.isInteger(message.parentCommentId.low) && $util.isInteger(message.parentCommentId.high)))
            return "parentCommentId: integer|Long expected";
        }
        if (message.content != null && $Object.hasOwnProperty.call(message, "content")) {
          if (!$util.isString(message.content))
            return "content: string expected";
        }
        return null;
      };
      CreateCommentRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.CreateCommentRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.CreateCommentRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.CreateCommentRequest();
        if (object.solutionId != null) {
          if (typeof object.solutionId === "object" ? object.solutionId.low || object.solutionId.high : $Number(object.solutionId) !== 0) {
            if ($util.Long)
              message.solutionId = $util.Long.fromValue(object.solutionId, true);
            else if (typeof object.solutionId === "string")
              message.solutionId = $parseInt(object.solutionId, 10);
            else if (typeof object.solutionId === "number")
              message.solutionId = object.solutionId;
            else if (typeof object.solutionId === "object")
              message.solutionId = new $util.LongBits(object.solutionId.low >>> 0, object.solutionId.high >>> 0).toNumber(true);
          }
        }
        if (object.parentCommentId != null) {
          if (typeof object.parentCommentId === "object" ? object.parentCommentId.low || object.parentCommentId.high : $Number(object.parentCommentId) !== 0) {
            if ($util.Long)
              message.parentCommentId = $util.Long.fromValue(object.parentCommentId, true);
            else if (typeof object.parentCommentId === "string")
              message.parentCommentId = $parseInt(object.parentCommentId, 10);
            else if (typeof object.parentCommentId === "number")
              message.parentCommentId = object.parentCommentId;
            else if (typeof object.parentCommentId === "object")
              message.parentCommentId = new $util.LongBits(object.parentCommentId.low >>> 0, object.parentCommentId.high >>> 0).toNumber(true);
          }
        }
        if (object.content != null) {
          if (typeof object.content !== "string" || object.content.length)
            message.content = $String(object.content);
        }
        return message;
      };
      CreateCommentRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.solutionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.solutionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.parentCommentId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.parentCommentId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.content = "";
        }
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.solutionId = typeof message.solutionId === "number" ? $BigInt(message.solutionId) : $util.Long.fromBits(message.solutionId.low >>> 0, message.solutionId.high >>> 0, true).toBigInt();
          else if (typeof message.solutionId === "number")
            object.solutionId = options.longs === $String ? $String(message.solutionId) : message.solutionId;
          else
            object.solutionId = options.longs === $String ? $util.Long.prototype.toString.call(message.solutionId) : options.longs === $Number ? new $util.LongBits(message.solutionId.low >>> 0, message.solutionId.high >>> 0).toNumber(true) : message.solutionId;
        if (message.parentCommentId != null && $Object.hasOwnProperty.call(message, "parentCommentId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.parentCommentId = typeof message.parentCommentId === "number" ? $BigInt(message.parentCommentId) : $util.Long.fromBits(message.parentCommentId.low >>> 0, message.parentCommentId.high >>> 0, true).toBigInt();
          else if (typeof message.parentCommentId === "number")
            object.parentCommentId = options.longs === $String ? $String(message.parentCommentId) : message.parentCommentId;
          else
            object.parentCommentId = options.longs === $String ? $util.Long.prototype.toString.call(message.parentCommentId) : options.longs === $Number ? new $util.LongBits(message.parentCommentId.low >>> 0, message.parentCommentId.high >>> 0).toNumber(true) : message.parentCommentId;
        if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
          object.content = message.content;
        return object;
      };
      CreateCommentRequest.prototype.toJSON = function() {
        return CreateCommentRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CreateCommentRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.CreateCommentRequest";
      };
      return CreateCommentRequest;
    })();
    biz.UpdateCommentRequest = (function() {
      const UpdateCommentRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      UpdateCommentRequest.prototype.commentId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      UpdateCommentRequest.prototype.content = "";
      UpdateCommentRequest.create = function(properties) {
        return new UpdateCommentRequest(properties);
      };
      UpdateCommentRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.commentId != null && $Object.hasOwnProperty.call(message, "commentId") && (typeof message.commentId === "object" ? message.commentId.low || message.commentId.high : message.commentId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.commentId);
        if (message.content != null && $Object.hasOwnProperty.call(message, "content") && message.content !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.content);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      UpdateCommentRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      UpdateCommentRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.UpdateCommentRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.commentId = value;
              else
                delete message.commentId;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.content = value;
              else
                delete message.content;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      UpdateCommentRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      UpdateCommentRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.commentId != null && $Object.hasOwnProperty.call(message, "commentId")) {
          if (!$util.isInteger(message.commentId) && !(message.commentId && $util.isInteger(message.commentId.low) && $util.isInteger(message.commentId.high)))
            return "commentId: integer|Long expected";
        }
        if (message.content != null && $Object.hasOwnProperty.call(message, "content")) {
          if (!$util.isString(message.content))
            return "content: string expected";
        }
        return null;
      };
      UpdateCommentRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.UpdateCommentRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.UpdateCommentRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.UpdateCommentRequest();
        if (object.commentId != null) {
          if (typeof object.commentId === "object" ? object.commentId.low || object.commentId.high : $Number(object.commentId) !== 0) {
            if ($util.Long)
              message.commentId = $util.Long.fromValue(object.commentId, true);
            else if (typeof object.commentId === "string")
              message.commentId = $parseInt(object.commentId, 10);
            else if (typeof object.commentId === "number")
              message.commentId = object.commentId;
            else if (typeof object.commentId === "object")
              message.commentId = new $util.LongBits(object.commentId.low >>> 0, object.commentId.high >>> 0).toNumber(true);
          }
        }
        if (object.content != null) {
          if (typeof object.content !== "string" || object.content.length)
            message.content = $String(object.content);
        }
        return message;
      };
      UpdateCommentRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.commentId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.commentId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.content = "";
        }
        if (message.commentId != null && $Object.hasOwnProperty.call(message, "commentId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.commentId = typeof message.commentId === "number" ? $BigInt(message.commentId) : $util.Long.fromBits(message.commentId.low >>> 0, message.commentId.high >>> 0, true).toBigInt();
          else if (typeof message.commentId === "number")
            object.commentId = options.longs === $String ? $String(message.commentId) : message.commentId;
          else
            object.commentId = options.longs === $String ? $util.Long.prototype.toString.call(message.commentId) : options.longs === $Number ? new $util.LongBits(message.commentId.low >>> 0, message.commentId.high >>> 0).toNumber(true) : message.commentId;
        if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
          object.content = message.content;
        return object;
      };
      UpdateCommentRequest.prototype.toJSON = function() {
        return UpdateCommentRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      UpdateCommentRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.UpdateCommentRequest";
      };
      return UpdateCommentRequest;
    })();
    biz.CommentIdRequest = (function() {
      const CommentIdRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CommentIdRequest.prototype.commentId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      CommentIdRequest.create = function(properties) {
        return new CommentIdRequest(properties);
      };
      CommentIdRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.commentId != null && $Object.hasOwnProperty.call(message, "commentId") && (typeof message.commentId === "object" ? message.commentId.low || message.commentId.high : message.commentId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.commentId);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CommentIdRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CommentIdRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.CommentIdRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.commentId = value;
              else
                delete message.commentId;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CommentIdRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CommentIdRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.commentId != null && $Object.hasOwnProperty.call(message, "commentId")) {
          if (!$util.isInteger(message.commentId) && !(message.commentId && $util.isInteger(message.commentId.low) && $util.isInteger(message.commentId.high)))
            return "commentId: integer|Long expected";
        }
        return null;
      };
      CommentIdRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.CommentIdRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.CommentIdRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.CommentIdRequest();
        if (object.commentId != null) {
          if (typeof object.commentId === "object" ? object.commentId.low || object.commentId.high : $Number(object.commentId) !== 0) {
            if ($util.Long)
              message.commentId = $util.Long.fromValue(object.commentId, true);
            else if (typeof object.commentId === "string")
              message.commentId = $parseInt(object.commentId, 10);
            else if (typeof object.commentId === "number")
              message.commentId = object.commentId;
            else if (typeof object.commentId === "object")
              message.commentId = new $util.LongBits(object.commentId.low >>> 0, object.commentId.high >>> 0).toNumber(true);
          }
        }
        return message;
      };
      CommentIdRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.commentId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.commentId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        if (message.commentId != null && $Object.hasOwnProperty.call(message, "commentId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.commentId = typeof message.commentId === "number" ? $BigInt(message.commentId) : $util.Long.fromBits(message.commentId.low >>> 0, message.commentId.high >>> 0, true).toBigInt();
          else if (typeof message.commentId === "number")
            object.commentId = options.longs === $String ? $String(message.commentId) : message.commentId;
          else
            object.commentId = options.longs === $String ? $util.Long.prototype.toString.call(message.commentId) : options.longs === $Number ? new $util.LongBits(message.commentId.low >>> 0, message.commentId.high >>> 0).toNumber(true) : message.commentId;
        return object;
      };
      CommentIdRequest.prototype.toJSON = function() {
        return CommentIdRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CommentIdRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.CommentIdRequest";
      };
      return CommentIdRequest;
    })();
    biz.CommentResponse = (function() {
      const CommentResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CommentResponse.prototype.status = null;
      CommentResponse.prototype.comment = null;
      CommentResponse.create = function(properties) {
        return new CommentResponse(properties);
      };
      CommentResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.comment != null && $Object.hasOwnProperty.call(message, "comment"))
          $root.oj.common.Comment.encode(message.comment, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CommentResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CommentResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.CommentResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.comment = $root.oj.common.Comment.decode(reader, reader.uint32(), $undefined, _depth + 1, message.comment);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CommentResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CommentResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.comment != null && $Object.hasOwnProperty.call(message, "comment")) {
          let error = $root.oj.common.Comment.verify(message.comment, _depth + 1);
          if (error)
            return "comment." + error;
        }
        return null;
      };
      CommentResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.CommentResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.CommentResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.CommentResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.CommentResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.comment != null) {
          if (!$util.isObject(object.comment))
            throw $TypeError(".oj.biz.CommentResponse.comment: object expected");
          message.comment = $root.oj.common.Comment.fromObject(object.comment, _depth + 1);
        }
        return message;
      };
      CommentResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.comment = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.comment != null && $Object.hasOwnProperty.call(message, "comment"))
          object.comment = $root.oj.common.Comment.toObject(message.comment, options, _depth + 1);
        return object;
      };
      CommentResponse.prototype.toJSON = function() {
        return CommentResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CommentResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.CommentResponse";
      };
      return CommentResponse;
    })();
    biz.CommentActionResponse = (function() {
      const CommentActionResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CommentActionResponse.prototype.status = null;
      CommentActionResponse.prototype.actions = null;
      CommentActionResponse.prototype.likeCount = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      CommentActionResponse.prototype.favoriteCount = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      CommentActionResponse.create = function(properties) {
        return new CommentActionResponse(properties);
      };
      CommentActionResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.actions != null && $Object.hasOwnProperty.call(message, "actions"))
          $root.oj.common.CommentActionState.encode(message.actions, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount") && (typeof message.likeCount === "object" ? message.likeCount.low || message.likeCount.high : message.likeCount !== 0))
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint64(message.likeCount);
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount") && (typeof message.favoriteCount === "object" ? message.favoriteCount.low || message.favoriteCount.high : message.favoriteCount !== 0))
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).uint64(message.favoriteCount);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CommentActionResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CommentActionResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.CommentActionResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.actions = $root.oj.common.CommentActionState.decode(reader, reader.uint32(), $undefined, _depth + 1, message.actions);
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.likeCount = value;
              else
                delete message.likeCount;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.favoriteCount = value;
              else
                delete message.favoriteCount;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CommentActionResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CommentActionResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.actions != null && $Object.hasOwnProperty.call(message, "actions")) {
          let error = $root.oj.common.CommentActionState.verify(message.actions, _depth + 1);
          if (error)
            return "actions." + error;
        }
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount")) {
          if (!$util.isInteger(message.likeCount) && !(message.likeCount && $util.isInteger(message.likeCount.low) && $util.isInteger(message.likeCount.high)))
            return "likeCount: integer|Long expected";
        }
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount")) {
          if (!$util.isInteger(message.favoriteCount) && !(message.favoriteCount && $util.isInteger(message.favoriteCount.low) && $util.isInteger(message.favoriteCount.high)))
            return "favoriteCount: integer|Long expected";
        }
        return null;
      };
      CommentActionResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.CommentActionResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.CommentActionResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.CommentActionResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.CommentActionResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.actions != null) {
          if (!$util.isObject(object.actions))
            throw $TypeError(".oj.biz.CommentActionResponse.actions: object expected");
          message.actions = $root.oj.common.CommentActionState.fromObject(object.actions, _depth + 1);
        }
        if (object.likeCount != null) {
          if (typeof object.likeCount === "object" ? object.likeCount.low || object.likeCount.high : $Number(object.likeCount) !== 0) {
            if ($util.Long)
              message.likeCount = $util.Long.fromValue(object.likeCount, true);
            else if (typeof object.likeCount === "string")
              message.likeCount = $parseInt(object.likeCount, 10);
            else if (typeof object.likeCount === "number")
              message.likeCount = object.likeCount;
            else if (typeof object.likeCount === "object")
              message.likeCount = new $util.LongBits(object.likeCount.low >>> 0, object.likeCount.high >>> 0).toNumber(true);
          }
        }
        if (object.favoriteCount != null) {
          if (typeof object.favoriteCount === "object" ? object.favoriteCount.low || object.favoriteCount.high : $Number(object.favoriteCount) !== 0) {
            if ($util.Long)
              message.favoriteCount = $util.Long.fromValue(object.favoriteCount, true);
            else if (typeof object.favoriteCount === "string")
              message.favoriteCount = $parseInt(object.favoriteCount, 10);
            else if (typeof object.favoriteCount === "number")
              message.favoriteCount = object.favoriteCount;
            else if (typeof object.favoriteCount === "object")
              message.favoriteCount = new $util.LongBits(object.favoriteCount.low >>> 0, object.favoriteCount.high >>> 0).toNumber(true);
          }
        }
        return message;
      };
      CommentActionResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.actions = null;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.likeCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.likeCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.favoriteCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.favoriteCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.actions != null && $Object.hasOwnProperty.call(message, "actions"))
          object.actions = $root.oj.common.CommentActionState.toObject(message.actions, options, _depth + 1);
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.likeCount = typeof message.likeCount === "number" ? $BigInt(message.likeCount) : $util.Long.fromBits(message.likeCount.low >>> 0, message.likeCount.high >>> 0, true).toBigInt();
          else if (typeof message.likeCount === "number")
            object.likeCount = options.longs === $String ? $String(message.likeCount) : message.likeCount;
          else
            object.likeCount = options.longs === $String ? $util.Long.prototype.toString.call(message.likeCount) : options.longs === $Number ? new $util.LongBits(message.likeCount.low >>> 0, message.likeCount.high >>> 0).toNumber(true) : message.likeCount;
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.favoriteCount = typeof message.favoriteCount === "number" ? $BigInt(message.favoriteCount) : $util.Long.fromBits(message.favoriteCount.low >>> 0, message.favoriteCount.high >>> 0, true).toBigInt();
          else if (typeof message.favoriteCount === "number")
            object.favoriteCount = options.longs === $String ? $String(message.favoriteCount) : message.favoriteCount;
          else
            object.favoriteCount = options.longs === $String ? $util.Long.prototype.toString.call(message.favoriteCount) : options.longs === $Number ? new $util.LongBits(message.favoriteCount.low >>> 0, message.favoriteCount.high >>> 0).toNumber(true) : message.favoriteCount;
        return object;
      };
      CommentActionResponse.prototype.toJSON = function() {
        return CommentActionResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CommentActionResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.CommentActionResponse";
      };
      return CommentActionResponse;
    })();
    biz.GetCommentActionsRequest = (function() {
      const GetCommentActionsRequest = function(properties) {
        this.commentIds = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetCommentActionsRequest.prototype.commentIds = $util.emptyArray;
      GetCommentActionsRequest.create = function(properties) {
        return new GetCommentActionsRequest(properties);
      };
      GetCommentActionsRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.commentIds != null && message.commentIds.length)
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).uint64s(message.commentIds);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetCommentActionsRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetCommentActionsRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetCommentActionsRequest();
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType === 2) {
                if (!(message.commentIds && message.commentIds.length))
                  message.commentIds = [];
                reader.uint64s(message.commentIds);
                continue;
              }
              if (wireType !== 0)
                break;
              if (!(message.commentIds && message.commentIds.length))
                message.commentIds = [];
              message.commentIds.push(reader.uint64());
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetCommentActionsRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetCommentActionsRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.commentIds != null && $Object.hasOwnProperty.call(message, "commentIds")) {
          if (!$Array.isArray(message.commentIds))
            return "commentIds: array expected";
          for (let i = 0; i < message.commentIds.length; ++i)
            if (!$util.isInteger(message.commentIds[i]) && !(message.commentIds[i] && $util.isInteger(message.commentIds[i].low) && $util.isInteger(message.commentIds[i].high)))
              return "commentIds: integer|Long[] expected";
        }
        return null;
      };
      GetCommentActionsRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetCommentActionsRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetCommentActionsRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetCommentActionsRequest();
        if (object.commentIds) {
          if (!$Array.isArray(object.commentIds))
            throw $TypeError(".oj.biz.GetCommentActionsRequest.commentIds: array expected");
          message.commentIds = $Array(object.commentIds.length);
          for (let i = 0; i < object.commentIds.length; ++i)
            if ($util.Long)
              message.commentIds[i] = $util.Long.fromValue(object.commentIds[i], true);
            else if (typeof object.commentIds[i] === "string")
              message.commentIds[i] = $parseInt(object.commentIds[i], 10);
            else if (typeof object.commentIds[i] === "number")
              message.commentIds[i] = object.commentIds[i];
            else if (typeof object.commentIds[i] === "object")
              message.commentIds[i] = new $util.LongBits(object.commentIds[i].low >>> 0, object.commentIds[i].high >>> 0).toNumber(true);
        }
        return message;
      };
      GetCommentActionsRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.commentIds = [];
        if (message.commentIds && message.commentIds.length) {
          object.commentIds = $Array(message.commentIds.length);
          for (let j = 0; j < message.commentIds.length; ++j)
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
              object.commentIds[j] = typeof message.commentIds[j] === "number" ? $BigInt(message.commentIds[j]) : $util.Long.fromBits(message.commentIds[j].low >>> 0, message.commentIds[j].high >>> 0, true).toBigInt();
            else if (typeof message.commentIds[j] === "number")
              object.commentIds[j] = options.longs === $String ? $String(message.commentIds[j]) : message.commentIds[j];
            else
              object.commentIds[j] = options.longs === $String ? $util.Long.prototype.toString.call(message.commentIds[j]) : options.longs === $Number ? new $util.LongBits(message.commentIds[j].low >>> 0, message.commentIds[j].high >>> 0).toNumber(true) : message.commentIds[j];
        }
        return object;
      };
      GetCommentActionsRequest.prototype.toJSON = function() {
        return GetCommentActionsRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetCommentActionsRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetCommentActionsRequest";
      };
      return GetCommentActionsRequest;
    })();
    biz.GetCommentActionsResponse = (function() {
      const GetCommentActionsResponse = function(properties) {
        this.actions = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetCommentActionsResponse.prototype.status = null;
      GetCommentActionsResponse.prototype.actions = $util.emptyArray;
      GetCommentActionsResponse.create = function(properties) {
        return new GetCommentActionsResponse(properties);
      };
      GetCommentActionsResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.actions != null && message.actions.length)
          for (let i = 0; i < message.actions.length; ++i)
            $root.oj.common.CommentActionState.encode(message.actions[i], writer.uint32(
              /* id 2, wireType 2 =*/
              18
            ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetCommentActionsResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetCommentActionsResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetCommentActionsResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if (!(message.actions && message.actions.length))
                message.actions = [];
              message.actions.push($root.oj.common.CommentActionState.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetCommentActionsResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetCommentActionsResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.actions != null && $Object.hasOwnProperty.call(message, "actions")) {
          if (!$Array.isArray(message.actions))
            return "actions: array expected";
          for (let i = 0; i < message.actions.length; ++i) {
            let error = $root.oj.common.CommentActionState.verify(message.actions[i], _depth + 1);
            if (error)
              return "actions." + error;
          }
        }
        return null;
      };
      GetCommentActionsResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetCommentActionsResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetCommentActionsResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetCommentActionsResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetCommentActionsResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.actions) {
          if (!$Array.isArray(object.actions))
            throw $TypeError(".oj.biz.GetCommentActionsResponse.actions: array expected");
          message.actions = $Array(object.actions.length);
          for (let i = 0; i < object.actions.length; ++i) {
            if (!$util.isObject(object.actions[i]))
              throw $TypeError(".oj.biz.GetCommentActionsResponse.actions: object expected");
            message.actions[i] = $root.oj.common.CommentActionState.fromObject(object.actions[i], _depth + 1);
          }
        }
        return message;
      };
      GetCommentActionsResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.actions = [];
        if (options.defaults)
          object.status = null;
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.actions && message.actions.length) {
          object.actions = $Array(message.actions.length);
          for (let j = 0; j < message.actions.length; ++j)
            object.actions[j] = $root.oj.common.CommentActionState.toObject(message.actions[j], options, _depth + 1);
        }
        return object;
      };
      GetCommentActionsResponse.prototype.toJSON = function() {
        return GetCommentActionsResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetCommentActionsResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetCommentActionsResponse";
      };
      return GetCommentActionsResponse;
    })();
    biz.CommentService = (function() {
      const CommentService = function(rpcImpl, requestDelimited, responseDelimited) {
        import_minimal.default.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperty(CommentService.prototype = $Object.create(import_minimal.default.rpc.Service.prototype), "constructor", { value: CommentService, writable: true, enumerable: false, configurable: true });
      CommentService.create = function(rpcImpl, requestDelimited, responseDelimited) {
        return new this(rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperties(CommentService.prototype.listComments = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, CommentService.prototype.listComments, $root.oj.biz.ListCommentsRequest, $root.oj.biz.ListCommentsResponse, request, callback);
      }, {
        name: { value: "ListComments" },
        path: { value: "/oj.biz.CommentService/ListComments" },
        requestType: { value: "ListCommentsRequest" },
        responseType: { value: "ListCommentsResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(CommentService.prototype.listReplies = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, CommentService.prototype.listReplies, $root.oj.biz.ListRepliesRequest, $root.oj.biz.ListCommentsResponse, request, callback);
      }, {
        name: { value: "ListReplies" },
        path: { value: "/oj.biz.CommentService/ListReplies" },
        requestType: { value: "ListRepliesRequest" },
        responseType: { value: "ListCommentsResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(CommentService.prototype.createComment = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, CommentService.prototype.createComment, $root.oj.biz.CreateCommentRequest, $root.oj.biz.CommentResponse, request, callback);
      }, {
        name: { value: "CreateComment" },
        path: { value: "/oj.biz.CommentService/CreateComment" },
        requestType: { value: "CreateCommentRequest" },
        responseType: { value: "CommentResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(CommentService.prototype.updateComment = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, CommentService.prototype.updateComment, $root.oj.biz.UpdateCommentRequest, $root.oj.biz.CommentResponse, request, callback);
      }, {
        name: { value: "UpdateComment" },
        path: { value: "/oj.biz.CommentService/UpdateComment" },
        requestType: { value: "UpdateCommentRequest" },
        responseType: { value: "CommentResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(CommentService.prototype.deleteComment = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, CommentService.prototype.deleteComment, $root.oj.biz.CommentIdRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "DeleteComment" },
        path: { value: "/oj.biz.CommentService/DeleteComment" },
        requestType: { value: "CommentIdRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(CommentService.prototype.toggleLike = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, CommentService.prototype.toggleLike, $root.oj.biz.CommentIdRequest, $root.oj.biz.CommentActionResponse, request, callback);
      }, {
        name: { value: "ToggleLike" },
        path: { value: "/oj.biz.CommentService/ToggleLike" },
        requestType: { value: "CommentIdRequest" },
        responseType: { value: "CommentActionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(CommentService.prototype.toggleFavorite = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, CommentService.prototype.toggleFavorite, $root.oj.biz.CommentIdRequest, $root.oj.biz.CommentActionResponse, request, callback);
      }, {
        name: { value: "ToggleFavorite" },
        path: { value: "/oj.biz.CommentService/ToggleFavorite" },
        requestType: { value: "CommentIdRequest" },
        responseType: { value: "CommentActionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(CommentService.prototype.getActionStates = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, CommentService.prototype.getActionStates, $root.oj.biz.GetCommentActionsRequest, $root.oj.biz.GetCommentActionsResponse, request, callback);
      }, {
        name: { value: "GetActionStates" },
        path: { value: "/oj.biz.CommentService/GetActionStates" },
        requestType: { value: "GetCommentActionsRequest" },
        responseType: { value: "GetCommentActionsResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      return CommentService;
    })();
    biz.AdminLoginRequest = (function() {
      const AdminLoginRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminLoginRequest.prototype.adminId = 0;
      AdminLoginRequest.prototype.password = "";
      AdminLoginRequest.create = function(properties) {
        return new AdminLoginRequest(properties);
      };
      AdminLoginRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.adminId != null && $Object.hasOwnProperty.call(message, "adminId") && message.adminId !== 0)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint32(message.adminId);
        if (message.password != null && $Object.hasOwnProperty.call(message, "password") && message.password !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.password);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminLoginRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminLoginRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AdminLoginRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.adminId = value;
              else
                delete message.adminId;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.password = value;
              else
                delete message.password;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminLoginRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminLoginRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.adminId != null && $Object.hasOwnProperty.call(message, "adminId")) {
          if (!$util.isInteger(message.adminId))
            return "adminId: integer expected";
        }
        if (message.password != null && $Object.hasOwnProperty.call(message, "password")) {
          if (!$util.isString(message.password))
            return "password: string expected";
        }
        return null;
      };
      AdminLoginRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AdminLoginRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AdminLoginRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AdminLoginRequest();
        if (object.adminId != null) {
          if ($Number(object.adminId) !== 0)
            message.adminId = object.adminId >>> 0;
        }
        if (object.password != null) {
          if (typeof object.password !== "string" || object.password.length)
            message.password = $String(object.password);
        }
        return message;
      };
      AdminLoginRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.adminId = 0;
          object.password = "";
        }
        if (message.adminId != null && $Object.hasOwnProperty.call(message, "adminId"))
          object.adminId = message.adminId;
        if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
          object.password = message.password;
        return object;
      };
      AdminLoginRequest.prototype.toJSON = function() {
        return AdminLoginRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminLoginRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AdminLoginRequest";
      };
      return AdminLoginRequest;
    })();
    biz.AdminLoginResponse = (function() {
      const AdminLoginResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminLoginResponse.prototype.status = null;
      AdminLoginResponse.prototype.admin = null;
      AdminLoginResponse.create = function(properties) {
        return new AdminLoginResponse(properties);
      };
      AdminLoginResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.admin != null && $Object.hasOwnProperty.call(message, "admin"))
          $root.oj.common.AdminAccount.encode(message.admin, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminLoginResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminLoginResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AdminLoginResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.admin = $root.oj.common.AdminAccount.decode(reader, reader.uint32(), $undefined, _depth + 1, message.admin);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminLoginResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminLoginResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.admin != null && $Object.hasOwnProperty.call(message, "admin")) {
          let error = $root.oj.common.AdminAccount.verify(message.admin, _depth + 1);
          if (error)
            return "admin." + error;
        }
        return null;
      };
      AdminLoginResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AdminLoginResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AdminLoginResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AdminLoginResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.AdminLoginResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.admin != null) {
          if (!$util.isObject(object.admin))
            throw $TypeError(".oj.biz.AdminLoginResponse.admin: object expected");
          message.admin = $root.oj.common.AdminAccount.fromObject(object.admin, _depth + 1);
        }
        return message;
      };
      AdminLoginResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.admin = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.admin != null && $Object.hasOwnProperty.call(message, "admin"))
          object.admin = $root.oj.common.AdminAccount.toObject(message.admin, options, _depth + 1);
        return object;
      };
      AdminLoginResponse.prototype.toJSON = function() {
        return AdminLoginResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminLoginResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AdminLoginResponse";
      };
      return AdminLoginResponse;
    })();
    biz.GetVersionResponse = (function() {
      const GetVersionResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetVersionResponse.prototype.status = null;
      GetVersionResponse.prototype.service = "";
      GetVersionResponse.prototype.version = "";
      GetVersionResponse.prototype.buildDate = "";
      GetVersionResponse.prototype.buildTime = "";
      GetVersionResponse.create = function(properties) {
        return new GetVersionResponse(properties);
      };
      GetVersionResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.service != null && $Object.hasOwnProperty.call(message, "service") && message.service !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.service);
        if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.version);
        if (message.buildDate != null && $Object.hasOwnProperty.call(message, "buildDate") && message.buildDate !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.buildDate);
        if (message.buildTime != null && $Object.hasOwnProperty.call(message, "buildTime") && message.buildTime !== "")
          writer.uint32(
            /* id 5, wireType 2 =*/
            42
          ).string(message.buildTime);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetVersionResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetVersionResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetVersionResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.service = value;
              else
                delete message.service;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.version = value;
              else
                delete message.version;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.buildDate = value;
              else
                delete message.buildDate;
              continue;
            }
            case 5: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.buildTime = value;
              else
                delete message.buildTime;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetVersionResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetVersionResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.service != null && $Object.hasOwnProperty.call(message, "service")) {
          if (!$util.isString(message.service))
            return "service: string expected";
        }
        if (message.version != null && $Object.hasOwnProperty.call(message, "version")) {
          if (!$util.isString(message.version))
            return "version: string expected";
        }
        if (message.buildDate != null && $Object.hasOwnProperty.call(message, "buildDate")) {
          if (!$util.isString(message.buildDate))
            return "buildDate: string expected";
        }
        if (message.buildTime != null && $Object.hasOwnProperty.call(message, "buildTime")) {
          if (!$util.isString(message.buildTime))
            return "buildTime: string expected";
        }
        return null;
      };
      GetVersionResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetVersionResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetVersionResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetVersionResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetVersionResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.service != null) {
          if (typeof object.service !== "string" || object.service.length)
            message.service = $String(object.service);
        }
        if (object.version != null) {
          if (typeof object.version !== "string" || object.version.length)
            message.version = $String(object.version);
        }
        if (object.buildDate != null) {
          if (typeof object.buildDate !== "string" || object.buildDate.length)
            message.buildDate = $String(object.buildDate);
        }
        if (object.buildTime != null) {
          if (typeof object.buildTime !== "string" || object.buildTime.length)
            message.buildTime = $String(object.buildTime);
        }
        return message;
      };
      GetVersionResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.service = "";
          object.version = "";
          object.buildDate = "";
          object.buildTime = "";
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.service != null && $Object.hasOwnProperty.call(message, "service"))
          object.service = message.service;
        if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
          object.version = message.version;
        if (message.buildDate != null && $Object.hasOwnProperty.call(message, "buildDate"))
          object.buildDate = message.buildDate;
        if (message.buildTime != null && $Object.hasOwnProperty.call(message, "buildTime"))
          object.buildTime = message.buildTime;
        return object;
      };
      GetVersionResponse.prototype.toJSON = function() {
        return GetVersionResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetVersionResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetVersionResponse";
      };
      return GetVersionResponse;
    })();
    biz.AdminOverviewResponse = (function() {
      const AdminOverviewResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminOverviewResponse.prototype.status = null;
      AdminOverviewResponse.prototype.totalUsers = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      AdminOverviewResponse.prototype.totalQuestions = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      AdminOverviewResponse.prototype.totalSolutions = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      AdminOverviewResponse.prototype.totalSubmissions = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      AdminOverviewResponse.prototype.todaySubmissions = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      AdminOverviewResponse.prototype.judgeServerCount = 0;
      AdminOverviewResponse.create = function(properties) {
        return new AdminOverviewResponse(properties);
      };
      AdminOverviewResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.totalUsers != null && $Object.hasOwnProperty.call(message, "totalUsers") && (typeof message.totalUsers === "object" ? message.totalUsers.low || message.totalUsers.high : message.totalUsers !== 0))
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint64(message.totalUsers);
        if (message.totalQuestions != null && $Object.hasOwnProperty.call(message, "totalQuestions") && (typeof message.totalQuestions === "object" ? message.totalQuestions.low || message.totalQuestions.high : message.totalQuestions !== 0))
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint64(message.totalQuestions);
        if (message.totalSolutions != null && $Object.hasOwnProperty.call(message, "totalSolutions") && (typeof message.totalSolutions === "object" ? message.totalSolutions.low || message.totalSolutions.high : message.totalSolutions !== 0))
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).uint64(message.totalSolutions);
        if (message.totalSubmissions != null && $Object.hasOwnProperty.call(message, "totalSubmissions") && (typeof message.totalSubmissions === "object" ? message.totalSubmissions.low || message.totalSubmissions.high : message.totalSubmissions !== 0))
          writer.uint32(
            /* id 5, wireType 0 =*/
            40
          ).uint64(message.totalSubmissions);
        if (message.todaySubmissions != null && $Object.hasOwnProperty.call(message, "todaySubmissions") && (typeof message.todaySubmissions === "object" ? message.todaySubmissions.low || message.todaySubmissions.high : message.todaySubmissions !== 0))
          writer.uint32(
            /* id 6, wireType 0 =*/
            48
          ).uint64(message.todaySubmissions);
        if (message.judgeServerCount != null && $Object.hasOwnProperty.call(message, "judgeServerCount") && message.judgeServerCount !== 0)
          writer.uint32(
            /* id 7, wireType 0 =*/
            56
          ).uint32(message.judgeServerCount);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminOverviewResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminOverviewResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AdminOverviewResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.totalUsers = value;
              else
                delete message.totalUsers;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.totalQuestions = value;
              else
                delete message.totalQuestions;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.totalSolutions = value;
              else
                delete message.totalSolutions;
              continue;
            }
            case 5: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.totalSubmissions = value;
              else
                delete message.totalSubmissions;
              continue;
            }
            case 6: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.todaySubmissions = value;
              else
                delete message.todaySubmissions;
              continue;
            }
            case 7: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.judgeServerCount = value;
              else
                delete message.judgeServerCount;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminOverviewResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminOverviewResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.totalUsers != null && $Object.hasOwnProperty.call(message, "totalUsers")) {
          if (!$util.isInteger(message.totalUsers) && !(message.totalUsers && $util.isInteger(message.totalUsers.low) && $util.isInteger(message.totalUsers.high)))
            return "totalUsers: integer|Long expected";
        }
        if (message.totalQuestions != null && $Object.hasOwnProperty.call(message, "totalQuestions")) {
          if (!$util.isInteger(message.totalQuestions) && !(message.totalQuestions && $util.isInteger(message.totalQuestions.low) && $util.isInteger(message.totalQuestions.high)))
            return "totalQuestions: integer|Long expected";
        }
        if (message.totalSolutions != null && $Object.hasOwnProperty.call(message, "totalSolutions")) {
          if (!$util.isInteger(message.totalSolutions) && !(message.totalSolutions && $util.isInteger(message.totalSolutions.low) && $util.isInteger(message.totalSolutions.high)))
            return "totalSolutions: integer|Long expected";
        }
        if (message.totalSubmissions != null && $Object.hasOwnProperty.call(message, "totalSubmissions")) {
          if (!$util.isInteger(message.totalSubmissions) && !(message.totalSubmissions && $util.isInteger(message.totalSubmissions.low) && $util.isInteger(message.totalSubmissions.high)))
            return "totalSubmissions: integer|Long expected";
        }
        if (message.todaySubmissions != null && $Object.hasOwnProperty.call(message, "todaySubmissions")) {
          if (!$util.isInteger(message.todaySubmissions) && !(message.todaySubmissions && $util.isInteger(message.todaySubmissions.low) && $util.isInteger(message.todaySubmissions.high)))
            return "todaySubmissions: integer|Long expected";
        }
        if (message.judgeServerCount != null && $Object.hasOwnProperty.call(message, "judgeServerCount")) {
          if (!$util.isInteger(message.judgeServerCount))
            return "judgeServerCount: integer expected";
        }
        return null;
      };
      AdminOverviewResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AdminOverviewResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AdminOverviewResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AdminOverviewResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.AdminOverviewResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.totalUsers != null) {
          if (typeof object.totalUsers === "object" ? object.totalUsers.low || object.totalUsers.high : $Number(object.totalUsers) !== 0) {
            if ($util.Long)
              message.totalUsers = $util.Long.fromValue(object.totalUsers, true);
            else if (typeof object.totalUsers === "string")
              message.totalUsers = $parseInt(object.totalUsers, 10);
            else if (typeof object.totalUsers === "number")
              message.totalUsers = object.totalUsers;
            else if (typeof object.totalUsers === "object")
              message.totalUsers = new $util.LongBits(object.totalUsers.low >>> 0, object.totalUsers.high >>> 0).toNumber(true);
          }
        }
        if (object.totalQuestions != null) {
          if (typeof object.totalQuestions === "object" ? object.totalQuestions.low || object.totalQuestions.high : $Number(object.totalQuestions) !== 0) {
            if ($util.Long)
              message.totalQuestions = $util.Long.fromValue(object.totalQuestions, true);
            else if (typeof object.totalQuestions === "string")
              message.totalQuestions = $parseInt(object.totalQuestions, 10);
            else if (typeof object.totalQuestions === "number")
              message.totalQuestions = object.totalQuestions;
            else if (typeof object.totalQuestions === "object")
              message.totalQuestions = new $util.LongBits(object.totalQuestions.low >>> 0, object.totalQuestions.high >>> 0).toNumber(true);
          }
        }
        if (object.totalSolutions != null) {
          if (typeof object.totalSolutions === "object" ? object.totalSolutions.low || object.totalSolutions.high : $Number(object.totalSolutions) !== 0) {
            if ($util.Long)
              message.totalSolutions = $util.Long.fromValue(object.totalSolutions, true);
            else if (typeof object.totalSolutions === "string")
              message.totalSolutions = $parseInt(object.totalSolutions, 10);
            else if (typeof object.totalSolutions === "number")
              message.totalSolutions = object.totalSolutions;
            else if (typeof object.totalSolutions === "object")
              message.totalSolutions = new $util.LongBits(object.totalSolutions.low >>> 0, object.totalSolutions.high >>> 0).toNumber(true);
          }
        }
        if (object.totalSubmissions != null) {
          if (typeof object.totalSubmissions === "object" ? object.totalSubmissions.low || object.totalSubmissions.high : $Number(object.totalSubmissions) !== 0) {
            if ($util.Long)
              message.totalSubmissions = $util.Long.fromValue(object.totalSubmissions, true);
            else if (typeof object.totalSubmissions === "string")
              message.totalSubmissions = $parseInt(object.totalSubmissions, 10);
            else if (typeof object.totalSubmissions === "number")
              message.totalSubmissions = object.totalSubmissions;
            else if (typeof object.totalSubmissions === "object")
              message.totalSubmissions = new $util.LongBits(object.totalSubmissions.low >>> 0, object.totalSubmissions.high >>> 0).toNumber(true);
          }
        }
        if (object.todaySubmissions != null) {
          if (typeof object.todaySubmissions === "object" ? object.todaySubmissions.low || object.todaySubmissions.high : $Number(object.todaySubmissions) !== 0) {
            if ($util.Long)
              message.todaySubmissions = $util.Long.fromValue(object.todaySubmissions, true);
            else if (typeof object.todaySubmissions === "string")
              message.todaySubmissions = $parseInt(object.todaySubmissions, 10);
            else if (typeof object.todaySubmissions === "number")
              message.todaySubmissions = object.todaySubmissions;
            else if (typeof object.todaySubmissions === "object")
              message.todaySubmissions = new $util.LongBits(object.todaySubmissions.low >>> 0, object.todaySubmissions.high >>> 0).toNumber(true);
          }
        }
        if (object.judgeServerCount != null) {
          if ($Number(object.judgeServerCount) !== 0)
            message.judgeServerCount = object.judgeServerCount >>> 0;
        }
        return message;
      };
      AdminOverviewResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.totalUsers = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.totalUsers = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.totalQuestions = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.totalQuestions = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.totalSolutions = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.totalSolutions = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.totalSubmissions = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.totalSubmissions = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.todaySubmissions = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.todaySubmissions = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.judgeServerCount = 0;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.totalUsers != null && $Object.hasOwnProperty.call(message, "totalUsers"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.totalUsers = typeof message.totalUsers === "number" ? $BigInt(message.totalUsers) : $util.Long.fromBits(message.totalUsers.low >>> 0, message.totalUsers.high >>> 0, true).toBigInt();
          else if (typeof message.totalUsers === "number")
            object.totalUsers = options.longs === $String ? $String(message.totalUsers) : message.totalUsers;
          else
            object.totalUsers = options.longs === $String ? $util.Long.prototype.toString.call(message.totalUsers) : options.longs === $Number ? new $util.LongBits(message.totalUsers.low >>> 0, message.totalUsers.high >>> 0).toNumber(true) : message.totalUsers;
        if (message.totalQuestions != null && $Object.hasOwnProperty.call(message, "totalQuestions"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.totalQuestions = typeof message.totalQuestions === "number" ? $BigInt(message.totalQuestions) : $util.Long.fromBits(message.totalQuestions.low >>> 0, message.totalQuestions.high >>> 0, true).toBigInt();
          else if (typeof message.totalQuestions === "number")
            object.totalQuestions = options.longs === $String ? $String(message.totalQuestions) : message.totalQuestions;
          else
            object.totalQuestions = options.longs === $String ? $util.Long.prototype.toString.call(message.totalQuestions) : options.longs === $Number ? new $util.LongBits(message.totalQuestions.low >>> 0, message.totalQuestions.high >>> 0).toNumber(true) : message.totalQuestions;
        if (message.totalSolutions != null && $Object.hasOwnProperty.call(message, "totalSolutions"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.totalSolutions = typeof message.totalSolutions === "number" ? $BigInt(message.totalSolutions) : $util.Long.fromBits(message.totalSolutions.low >>> 0, message.totalSolutions.high >>> 0, true).toBigInt();
          else if (typeof message.totalSolutions === "number")
            object.totalSolutions = options.longs === $String ? $String(message.totalSolutions) : message.totalSolutions;
          else
            object.totalSolutions = options.longs === $String ? $util.Long.prototype.toString.call(message.totalSolutions) : options.longs === $Number ? new $util.LongBits(message.totalSolutions.low >>> 0, message.totalSolutions.high >>> 0).toNumber(true) : message.totalSolutions;
        if (message.totalSubmissions != null && $Object.hasOwnProperty.call(message, "totalSubmissions"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.totalSubmissions = typeof message.totalSubmissions === "number" ? $BigInt(message.totalSubmissions) : $util.Long.fromBits(message.totalSubmissions.low >>> 0, message.totalSubmissions.high >>> 0, true).toBigInt();
          else if (typeof message.totalSubmissions === "number")
            object.totalSubmissions = options.longs === $String ? $String(message.totalSubmissions) : message.totalSubmissions;
          else
            object.totalSubmissions = options.longs === $String ? $util.Long.prototype.toString.call(message.totalSubmissions) : options.longs === $Number ? new $util.LongBits(message.totalSubmissions.low >>> 0, message.totalSubmissions.high >>> 0).toNumber(true) : message.totalSubmissions;
        if (message.todaySubmissions != null && $Object.hasOwnProperty.call(message, "todaySubmissions"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.todaySubmissions = typeof message.todaySubmissions === "number" ? $BigInt(message.todaySubmissions) : $util.Long.fromBits(message.todaySubmissions.low >>> 0, message.todaySubmissions.high >>> 0, true).toBigInt();
          else if (typeof message.todaySubmissions === "number")
            object.todaySubmissions = options.longs === $String ? $String(message.todaySubmissions) : message.todaySubmissions;
          else
            object.todaySubmissions = options.longs === $String ? $util.Long.prototype.toString.call(message.todaySubmissions) : options.longs === $Number ? new $util.LongBits(message.todaySubmissions.low >>> 0, message.todaySubmissions.high >>> 0).toNumber(true) : message.todaySubmissions;
        if (message.judgeServerCount != null && $Object.hasOwnProperty.call(message, "judgeServerCount"))
          object.judgeServerCount = message.judgeServerCount;
        return object;
      };
      AdminOverviewResponse.prototype.toJSON = function() {
        return AdminOverviewResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminOverviewResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AdminOverviewResponse";
      };
      return AdminOverviewResponse;
    })();
    biz.AdminListUsersRequest = (function() {
      const AdminListUsersRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminListUsersRequest.prototype.page = null;
      AdminListUsersRequest.prototype.search = "";
      AdminListUsersRequest.create = function(properties) {
        return new AdminListUsersRequest(properties);
      };
      AdminListUsersRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageRequest.encode(message.page, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.search != null && $Object.hasOwnProperty.call(message, "search") && message.search !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.search);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminListUsersRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminListUsersRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AdminListUsersRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.search = value;
              else
                delete message.search;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminListUsersRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminListUsersRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageRequest.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.search != null && $Object.hasOwnProperty.call(message, "search")) {
          if (!$util.isString(message.search))
            return "search: string expected";
        }
        return null;
      };
      AdminListUsersRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AdminListUsersRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AdminListUsersRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AdminListUsersRequest();
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.AdminListUsersRequest.page: object expected");
          message.page = $root.oj.common.PageRequest.fromObject(object.page, _depth + 1);
        }
        if (object.search != null) {
          if (typeof object.search !== "string" || object.search.length)
            message.search = $String(object.search);
        }
        return message;
      };
      AdminListUsersRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.page = null;
          object.search = "";
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageRequest.toObject(message.page, options, _depth + 1);
        if (message.search != null && $Object.hasOwnProperty.call(message, "search"))
          object.search = message.search;
        return object;
      };
      AdminListUsersRequest.prototype.toJSON = function() {
        return AdminListUsersRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminListUsersRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AdminListUsersRequest";
      };
      return AdminListUsersRequest;
    })();
    biz.AdminListUsersResponse = (function() {
      const AdminListUsersResponse = function(properties) {
        this.users = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminListUsersResponse.prototype.status = null;
      AdminListUsersResponse.prototype.page = null;
      AdminListUsersResponse.prototype.users = $util.emptyArray;
      AdminListUsersResponse.create = function(properties) {
        return new AdminListUsersResponse(properties);
      };
      AdminListUsersResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageResponse.encode(message.page, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.users != null && message.users.length)
          for (let i = 0; i < message.users.length; ++i)
            $root.oj.common.User.encode(message.users[i], writer.uint32(
              /* id 3, wireType 2 =*/
              26
            ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminListUsersResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminListUsersResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AdminListUsersResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if (!(message.users && message.users.length))
                message.users = [];
              message.users.push($root.oj.common.User.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminListUsersResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminListUsersResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageResponse.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.users != null && $Object.hasOwnProperty.call(message, "users")) {
          if (!$Array.isArray(message.users))
            return "users: array expected";
          for (let i = 0; i < message.users.length; ++i) {
            let error = $root.oj.common.User.verify(message.users[i], _depth + 1);
            if (error)
              return "users." + error;
          }
        }
        return null;
      };
      AdminListUsersResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AdminListUsersResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AdminListUsersResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AdminListUsersResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.AdminListUsersResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.AdminListUsersResponse.page: object expected");
          message.page = $root.oj.common.PageResponse.fromObject(object.page, _depth + 1);
        }
        if (object.users) {
          if (!$Array.isArray(object.users))
            throw $TypeError(".oj.biz.AdminListUsersResponse.users: array expected");
          message.users = $Array(object.users.length);
          for (let i = 0; i < object.users.length; ++i) {
            if (!$util.isObject(object.users[i]))
              throw $TypeError(".oj.biz.AdminListUsersResponse.users: object expected");
            message.users[i] = $root.oj.common.User.fromObject(object.users[i], _depth + 1);
          }
        }
        return message;
      };
      AdminListUsersResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.users = [];
        if (options.defaults) {
          object.status = null;
          object.page = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageResponse.toObject(message.page, options, _depth + 1);
        if (message.users && message.users.length) {
          object.users = $Array(message.users.length);
          for (let j = 0; j < message.users.length; ++j)
            object.users[j] = $root.oj.common.User.toObject(message.users[j], options, _depth + 1);
        }
        return object;
      };
      AdminListUsersResponse.prototype.toJSON = function() {
        return AdminListUsersResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminListUsersResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AdminListUsersResponse";
      };
      return AdminListUsersResponse;
    })();
    biz.AdminCreateUserRequest = (function() {
      const AdminCreateUserRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminCreateUserRequest.prototype.user = null;
      AdminCreateUserRequest.prototype.password = "";
      AdminCreateUserRequest.create = function(properties) {
        return new AdminCreateUserRequest(properties);
      };
      AdminCreateUserRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          $root.oj.common.User.encode(message.user, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.password != null && $Object.hasOwnProperty.call(message, "password") && message.password !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.password);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminCreateUserRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminCreateUserRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AdminCreateUserRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.user = $root.oj.common.User.decode(reader, reader.uint32(), $undefined, _depth + 1, message.user);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.password = value;
              else
                delete message.password;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminCreateUserRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminCreateUserRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.user != null && $Object.hasOwnProperty.call(message, "user")) {
          let error = $root.oj.common.User.verify(message.user, _depth + 1);
          if (error)
            return "user." + error;
        }
        if (message.password != null && $Object.hasOwnProperty.call(message, "password")) {
          if (!$util.isString(message.password))
            return "password: string expected";
        }
        return null;
      };
      AdminCreateUserRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AdminCreateUserRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AdminCreateUserRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AdminCreateUserRequest();
        if (object.user != null) {
          if (!$util.isObject(object.user))
            throw $TypeError(".oj.biz.AdminCreateUserRequest.user: object expected");
          message.user = $root.oj.common.User.fromObject(object.user, _depth + 1);
        }
        if (object.password != null) {
          if (typeof object.password !== "string" || object.password.length)
            message.password = $String(object.password);
        }
        return message;
      };
      AdminCreateUserRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.user = null;
          object.password = "";
        }
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          object.user = $root.oj.common.User.toObject(message.user, options, _depth + 1);
        if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
          object.password = message.password;
        return object;
      };
      AdminCreateUserRequest.prototype.toJSON = function() {
        return AdminCreateUserRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminCreateUserRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AdminCreateUserRequest";
      };
      return AdminCreateUserRequest;
    })();
    biz.AdminUpdateUserRequest = (function() {
      const AdminUpdateUserRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminUpdateUserRequest.prototype.user = null;
      AdminUpdateUserRequest.create = function(properties) {
        return new AdminUpdateUserRequest(properties);
      };
      AdminUpdateUserRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          $root.oj.common.User.encode(message.user, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminUpdateUserRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminUpdateUserRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AdminUpdateUserRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.user = $root.oj.common.User.decode(reader, reader.uint32(), $undefined, _depth + 1, message.user);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminUpdateUserRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminUpdateUserRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.user != null && $Object.hasOwnProperty.call(message, "user")) {
          let error = $root.oj.common.User.verify(message.user, _depth + 1);
          if (error)
            return "user." + error;
        }
        return null;
      };
      AdminUpdateUserRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AdminUpdateUserRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AdminUpdateUserRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AdminUpdateUserRequest();
        if (object.user != null) {
          if (!$util.isObject(object.user))
            throw $TypeError(".oj.biz.AdminUpdateUserRequest.user: object expected");
          message.user = $root.oj.common.User.fromObject(object.user, _depth + 1);
        }
        return message;
      };
      AdminUpdateUserRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.user = null;
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          object.user = $root.oj.common.User.toObject(message.user, options, _depth + 1);
        return object;
      };
      AdminUpdateUserRequest.prototype.toJSON = function() {
        return AdminUpdateUserRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminUpdateUserRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AdminUpdateUserRequest";
      };
      return AdminUpdateUserRequest;
    })();
    biz.AdminUserIdRequest = (function() {
      const AdminUserIdRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminUserIdRequest.prototype.uid = 0;
      AdminUserIdRequest.create = function(properties) {
        return new AdminUserIdRequest(properties);
      };
      AdminUserIdRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid") && message.uid !== 0)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint32(message.uid);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminUserIdRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminUserIdRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AdminUserIdRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.uid = value;
              else
                delete message.uid;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminUserIdRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminUserIdRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
          if (!$util.isInteger(message.uid))
            return "uid: integer expected";
        }
        return null;
      };
      AdminUserIdRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AdminUserIdRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AdminUserIdRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AdminUserIdRequest();
        if (object.uid != null) {
          if ($Number(object.uid) !== 0)
            message.uid = object.uid >>> 0;
        }
        return message;
      };
      AdminUserIdRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.uid = 0;
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
          object.uid = message.uid;
        return object;
      };
      AdminUserIdRequest.prototype.toJSON = function() {
        return AdminUserIdRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminUserIdRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AdminUserIdRequest";
      };
      return AdminUserIdRequest;
    })();
    biz.AdminUserResponse = (function() {
      const AdminUserResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminUserResponse.prototype.status = null;
      AdminUserResponse.prototype.user = null;
      AdminUserResponse.create = function(properties) {
        return new AdminUserResponse(properties);
      };
      AdminUserResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          $root.oj.common.User.encode(message.user, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminUserResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminUserResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AdminUserResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.user = $root.oj.common.User.decode(reader, reader.uint32(), $undefined, _depth + 1, message.user);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminUserResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminUserResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.user != null && $Object.hasOwnProperty.call(message, "user")) {
          let error = $root.oj.common.User.verify(message.user, _depth + 1);
          if (error)
            return "user." + error;
        }
        return null;
      };
      AdminUserResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AdminUserResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AdminUserResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AdminUserResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.AdminUserResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.user != null) {
          if (!$util.isObject(object.user))
            throw $TypeError(".oj.biz.AdminUserResponse.user: object expected");
          message.user = $root.oj.common.User.fromObject(object.user, _depth + 1);
        }
        return message;
      };
      AdminUserResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.user = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.user != null && $Object.hasOwnProperty.call(message, "user"))
          object.user = $root.oj.common.User.toObject(message.user, options, _depth + 1);
        return object;
      };
      AdminUserResponse.prototype.toJSON = function() {
        return AdminUserResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminUserResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AdminUserResponse";
      };
      return AdminUserResponse;
    })();
    biz.ListAdminAccountsRequest = (function() {
      const ListAdminAccountsRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ListAdminAccountsRequest.prototype.page = null;
      ListAdminAccountsRequest.prototype.search = "";
      ListAdminAccountsRequest.create = function(properties) {
        return new ListAdminAccountsRequest(properties);
      };
      ListAdminAccountsRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageRequest.encode(message.page, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.search != null && $Object.hasOwnProperty.call(message, "search") && message.search !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.search);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ListAdminAccountsRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ListAdminAccountsRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ListAdminAccountsRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.search = value;
              else
                delete message.search;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ListAdminAccountsRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ListAdminAccountsRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageRequest.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.search != null && $Object.hasOwnProperty.call(message, "search")) {
          if (!$util.isString(message.search))
            return "search: string expected";
        }
        return null;
      };
      ListAdminAccountsRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ListAdminAccountsRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ListAdminAccountsRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ListAdminAccountsRequest();
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.ListAdminAccountsRequest.page: object expected");
          message.page = $root.oj.common.PageRequest.fromObject(object.page, _depth + 1);
        }
        if (object.search != null) {
          if (typeof object.search !== "string" || object.search.length)
            message.search = $String(object.search);
        }
        return message;
      };
      ListAdminAccountsRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.page = null;
          object.search = "";
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageRequest.toObject(message.page, options, _depth + 1);
        if (message.search != null && $Object.hasOwnProperty.call(message, "search"))
          object.search = message.search;
        return object;
      };
      ListAdminAccountsRequest.prototype.toJSON = function() {
        return ListAdminAccountsRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ListAdminAccountsRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ListAdminAccountsRequest";
      };
      return ListAdminAccountsRequest;
    })();
    biz.ListAdminAccountsResponse = (function() {
      const ListAdminAccountsResponse = function(properties) {
        this.accounts = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ListAdminAccountsResponse.prototype.status = null;
      ListAdminAccountsResponse.prototype.page = null;
      ListAdminAccountsResponse.prototype.accounts = $util.emptyArray;
      ListAdminAccountsResponse.create = function(properties) {
        return new ListAdminAccountsResponse(properties);
      };
      ListAdminAccountsResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageResponse.encode(message.page, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.accounts != null && message.accounts.length)
          for (let i = 0; i < message.accounts.length; ++i)
            $root.oj.common.AdminAccount.encode(message.accounts[i], writer.uint32(
              /* id 3, wireType 2 =*/
              26
            ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ListAdminAccountsResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ListAdminAccountsResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ListAdminAccountsResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if (!(message.accounts && message.accounts.length))
                message.accounts = [];
              message.accounts.push($root.oj.common.AdminAccount.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ListAdminAccountsResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ListAdminAccountsResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageResponse.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.accounts != null && $Object.hasOwnProperty.call(message, "accounts")) {
          if (!$Array.isArray(message.accounts))
            return "accounts: array expected";
          for (let i = 0; i < message.accounts.length; ++i) {
            let error = $root.oj.common.AdminAccount.verify(message.accounts[i], _depth + 1);
            if (error)
              return "accounts." + error;
          }
        }
        return null;
      };
      ListAdminAccountsResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ListAdminAccountsResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ListAdminAccountsResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ListAdminAccountsResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.ListAdminAccountsResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.ListAdminAccountsResponse.page: object expected");
          message.page = $root.oj.common.PageResponse.fromObject(object.page, _depth + 1);
        }
        if (object.accounts) {
          if (!$Array.isArray(object.accounts))
            throw $TypeError(".oj.biz.ListAdminAccountsResponse.accounts: array expected");
          message.accounts = $Array(object.accounts.length);
          for (let i = 0; i < object.accounts.length; ++i) {
            if (!$util.isObject(object.accounts[i]))
              throw $TypeError(".oj.biz.ListAdminAccountsResponse.accounts: object expected");
            message.accounts[i] = $root.oj.common.AdminAccount.fromObject(object.accounts[i], _depth + 1);
          }
        }
        return message;
      };
      ListAdminAccountsResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.accounts = [];
        if (options.defaults) {
          object.status = null;
          object.page = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageResponse.toObject(message.page, options, _depth + 1);
        if (message.accounts && message.accounts.length) {
          object.accounts = $Array(message.accounts.length);
          for (let j = 0; j < message.accounts.length; ++j)
            object.accounts[j] = $root.oj.common.AdminAccount.toObject(message.accounts[j], options, _depth + 1);
        }
        return object;
      };
      ListAdminAccountsResponse.prototype.toJSON = function() {
        return ListAdminAccountsResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ListAdminAccountsResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ListAdminAccountsResponse";
      };
      return ListAdminAccountsResponse;
    })();
    biz.SaveAdminAccountRequest = (function() {
      const SaveAdminAccountRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      SaveAdminAccountRequest.prototype.account = null;
      SaveAdminAccountRequest.prototype.password = "";
      SaveAdminAccountRequest.create = function(properties) {
        return new SaveAdminAccountRequest(properties);
      };
      SaveAdminAccountRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
          $root.oj.common.AdminAccount.encode(message.account, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.password != null && $Object.hasOwnProperty.call(message, "password") && message.password !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.password);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      SaveAdminAccountRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      SaveAdminAccountRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.SaveAdminAccountRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.account = $root.oj.common.AdminAccount.decode(reader, reader.uint32(), $undefined, _depth + 1, message.account);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.password = value;
              else
                delete message.password;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      SaveAdminAccountRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      SaveAdminAccountRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.account != null && $Object.hasOwnProperty.call(message, "account")) {
          let error = $root.oj.common.AdminAccount.verify(message.account, _depth + 1);
          if (error)
            return "account." + error;
        }
        if (message.password != null && $Object.hasOwnProperty.call(message, "password")) {
          if (!$util.isString(message.password))
            return "password: string expected";
        }
        return null;
      };
      SaveAdminAccountRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.SaveAdminAccountRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.SaveAdminAccountRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.SaveAdminAccountRequest();
        if (object.account != null) {
          if (!$util.isObject(object.account))
            throw $TypeError(".oj.biz.SaveAdminAccountRequest.account: object expected");
          message.account = $root.oj.common.AdminAccount.fromObject(object.account, _depth + 1);
        }
        if (object.password != null) {
          if (typeof object.password !== "string" || object.password.length)
            message.password = $String(object.password);
        }
        return message;
      };
      SaveAdminAccountRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.account = null;
          object.password = "";
        }
        if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
          object.account = $root.oj.common.AdminAccount.toObject(message.account, options, _depth + 1);
        if (message.password != null && $Object.hasOwnProperty.call(message, "password"))
          object.password = message.password;
        return object;
      };
      SaveAdminAccountRequest.prototype.toJSON = function() {
        return SaveAdminAccountRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      SaveAdminAccountRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.SaveAdminAccountRequest";
      };
      return SaveAdminAccountRequest;
    })();
    biz.AdminAccountIdRequest = (function() {
      const AdminAccountIdRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminAccountIdRequest.prototype.adminId = 0;
      AdminAccountIdRequest.create = function(properties) {
        return new AdminAccountIdRequest(properties);
      };
      AdminAccountIdRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.adminId != null && $Object.hasOwnProperty.call(message, "adminId") && message.adminId !== 0)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint32(message.adminId);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminAccountIdRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminAccountIdRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AdminAccountIdRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.adminId = value;
              else
                delete message.adminId;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminAccountIdRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminAccountIdRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.adminId != null && $Object.hasOwnProperty.call(message, "adminId")) {
          if (!$util.isInteger(message.adminId))
            return "adminId: integer expected";
        }
        return null;
      };
      AdminAccountIdRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AdminAccountIdRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AdminAccountIdRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AdminAccountIdRequest();
        if (object.adminId != null) {
          if ($Number(object.adminId) !== 0)
            message.adminId = object.adminId >>> 0;
        }
        return message;
      };
      AdminAccountIdRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.adminId = 0;
        if (message.adminId != null && $Object.hasOwnProperty.call(message, "adminId"))
          object.adminId = message.adminId;
        return object;
      };
      AdminAccountIdRequest.prototype.toJSON = function() {
        return AdminAccountIdRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminAccountIdRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AdminAccountIdRequest";
      };
      return AdminAccountIdRequest;
    })();
    biz.AdminAccountResponse = (function() {
      const AdminAccountResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminAccountResponse.prototype.status = null;
      AdminAccountResponse.prototype.account = null;
      AdminAccountResponse.create = function(properties) {
        return new AdminAccountResponse(properties);
      };
      AdminAccountResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
          $root.oj.common.AdminAccount.encode(message.account, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminAccountResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminAccountResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.AdminAccountResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.account = $root.oj.common.AdminAccount.decode(reader, reader.uint32(), $undefined, _depth + 1, message.account);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminAccountResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminAccountResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.account != null && $Object.hasOwnProperty.call(message, "account")) {
          let error = $root.oj.common.AdminAccount.verify(message.account, _depth + 1);
          if (error)
            return "account." + error;
        }
        return null;
      };
      AdminAccountResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.AdminAccountResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.AdminAccountResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.AdminAccountResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.AdminAccountResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.account != null) {
          if (!$util.isObject(object.account))
            throw $TypeError(".oj.biz.AdminAccountResponse.account: object expected");
          message.account = $root.oj.common.AdminAccount.fromObject(object.account, _depth + 1);
        }
        return message;
      };
      AdminAccountResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.account = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.account != null && $Object.hasOwnProperty.call(message, "account"))
          object.account = $root.oj.common.AdminAccount.toObject(message.account, options, _depth + 1);
        return object;
      };
      AdminAccountResponse.prototype.toJSON = function() {
        return AdminAccountResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminAccountResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.AdminAccountResponse";
      };
      return AdminAccountResponse;
    })();
    biz.GetCacheMetricsResponse = (function() {
      const GetCacheMetricsResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetCacheMetricsResponse.prototype.status = null;
      GetCacheMetricsResponse.prototype.metrics = null;
      GetCacheMetricsResponse.create = function(properties) {
        return new GetCacheMetricsResponse(properties);
      };
      GetCacheMetricsResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.metrics != null && $Object.hasOwnProperty.call(message, "metrics"))
          $root.oj.common.CacheMetrics.encode(message.metrics, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetCacheMetricsResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetCacheMetricsResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetCacheMetricsResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.metrics = $root.oj.common.CacheMetrics.decode(reader, reader.uint32(), $undefined, _depth + 1, message.metrics);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetCacheMetricsResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetCacheMetricsResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.metrics != null && $Object.hasOwnProperty.call(message, "metrics")) {
          let error = $root.oj.common.CacheMetrics.verify(message.metrics, _depth + 1);
          if (error)
            return "metrics." + error;
        }
        return null;
      };
      GetCacheMetricsResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetCacheMetricsResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetCacheMetricsResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetCacheMetricsResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetCacheMetricsResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.metrics != null) {
          if (!$util.isObject(object.metrics))
            throw $TypeError(".oj.biz.GetCacheMetricsResponse.metrics: object expected");
          message.metrics = $root.oj.common.CacheMetrics.fromObject(object.metrics, _depth + 1);
        }
        return message;
      };
      GetCacheMetricsResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.metrics = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.metrics != null && $Object.hasOwnProperty.call(message, "metrics"))
          object.metrics = $root.oj.common.CacheMetrics.toObject(message.metrics, options, _depth + 1);
        return object;
      };
      GetCacheMetricsResponse.prototype.toJSON = function() {
        return GetCacheMetricsResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetCacheMetricsResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetCacheMetricsResponse";
      };
      return GetCacheMetricsResponse;
    })();
    biz.GetAuditLogsRequest = (function() {
      const GetAuditLogsRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetAuditLogsRequest.prototype.page = null;
      GetAuditLogsRequest.prototype.action = "";
      GetAuditLogsRequest.prototype.startTime = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      GetAuditLogsRequest.prototype.endTime = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      GetAuditLogsRequest.create = function(properties) {
        return new GetAuditLogsRequest(properties);
      };
      GetAuditLogsRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageRequest.encode(message.page, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.action != null && $Object.hasOwnProperty.call(message, "action") && message.action !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.action);
        if (message.startTime != null && $Object.hasOwnProperty.call(message, "startTime") && (typeof message.startTime === "object" ? message.startTime.low || message.startTime.high : message.startTime !== 0))
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).int64(message.startTime);
        if (message.endTime != null && $Object.hasOwnProperty.call(message, "endTime") && (typeof message.endTime === "object" ? message.endTime.low || message.endTime.high : message.endTime !== 0))
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).int64(message.endTime);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetAuditLogsRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetAuditLogsRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetAuditLogsRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.action = value;
              else
                delete message.action;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.startTime = value;
              else
                delete message.startTime;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.endTime = value;
              else
                delete message.endTime;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetAuditLogsRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetAuditLogsRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageRequest.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.action != null && $Object.hasOwnProperty.call(message, "action")) {
          if (!$util.isString(message.action))
            return "action: string expected";
        }
        if (message.startTime != null && $Object.hasOwnProperty.call(message, "startTime")) {
          if (!$util.isInteger(message.startTime) && !(message.startTime && $util.isInteger(message.startTime.low) && $util.isInteger(message.startTime.high)))
            return "startTime: integer|Long expected";
        }
        if (message.endTime != null && $Object.hasOwnProperty.call(message, "endTime")) {
          if (!$util.isInteger(message.endTime) && !(message.endTime && $util.isInteger(message.endTime.low) && $util.isInteger(message.endTime.high)))
            return "endTime: integer|Long expected";
        }
        return null;
      };
      GetAuditLogsRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetAuditLogsRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetAuditLogsRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetAuditLogsRequest();
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.GetAuditLogsRequest.page: object expected");
          message.page = $root.oj.common.PageRequest.fromObject(object.page, _depth + 1);
        }
        if (object.action != null) {
          if (typeof object.action !== "string" || object.action.length)
            message.action = $String(object.action);
        }
        if (object.startTime != null) {
          if (typeof object.startTime === "object" ? object.startTime.low || object.startTime.high : $Number(object.startTime) !== 0) {
            if ($util.Long)
              message.startTime = $util.Long.fromValue(object.startTime, false);
            else if (typeof object.startTime === "string")
              message.startTime = $parseInt(object.startTime, 10);
            else if (typeof object.startTime === "number")
              message.startTime = object.startTime;
            else if (typeof object.startTime === "object")
              message.startTime = new $util.LongBits(object.startTime.low >>> 0, object.startTime.high >>> 0).toNumber();
          }
        }
        if (object.endTime != null) {
          if (typeof object.endTime === "object" ? object.endTime.low || object.endTime.high : $Number(object.endTime) !== 0) {
            if ($util.Long)
              message.endTime = $util.Long.fromValue(object.endTime, false);
            else if (typeof object.endTime === "string")
              message.endTime = $parseInt(object.endTime, 10);
            else if (typeof object.endTime === "number")
              message.endTime = object.endTime;
            else if (typeof object.endTime === "object")
              message.endTime = new $util.LongBits(object.endTime.low >>> 0, object.endTime.high >>> 0).toNumber();
          }
        }
        return message;
      };
      GetAuditLogsRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.page = null;
          object.action = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.startTime = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.startTime = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.endTime = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.endTime = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageRequest.toObject(message.page, options, _depth + 1);
        if (message.action != null && $Object.hasOwnProperty.call(message, "action"))
          object.action = message.action;
        if (message.startTime != null && $Object.hasOwnProperty.call(message, "startTime"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.startTime = typeof message.startTime === "number" ? $BigInt(message.startTime) : $util.Long.fromBits(message.startTime.low >>> 0, message.startTime.high >>> 0, false).toBigInt();
          else if (typeof message.startTime === "number")
            object.startTime = options.longs === $String ? $String(message.startTime) : message.startTime;
          else
            object.startTime = options.longs === $String ? $util.Long.prototype.toString.call(message.startTime) : options.longs === $Number ? new $util.LongBits(message.startTime.low >>> 0, message.startTime.high >>> 0).toNumber() : message.startTime;
        if (message.endTime != null && $Object.hasOwnProperty.call(message, "endTime"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.endTime = typeof message.endTime === "number" ? $BigInt(message.endTime) : $util.Long.fromBits(message.endTime.low >>> 0, message.endTime.high >>> 0, false).toBigInt();
          else if (typeof message.endTime === "number")
            object.endTime = options.longs === $String ? $String(message.endTime) : message.endTime;
          else
            object.endTime = options.longs === $String ? $util.Long.prototype.toString.call(message.endTime) : options.longs === $Number ? new $util.LongBits(message.endTime.low >>> 0, message.endTime.high >>> 0).toNumber() : message.endTime;
        return object;
      };
      GetAuditLogsRequest.prototype.toJSON = function() {
        return GetAuditLogsRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetAuditLogsRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetAuditLogsRequest";
      };
      return GetAuditLogsRequest;
    })();
    biz.GetAuditLogsResponse = (function() {
      const GetAuditLogsResponse = function(properties) {
        this.logs = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetAuditLogsResponse.prototype.status = null;
      GetAuditLogsResponse.prototype.page = null;
      GetAuditLogsResponse.prototype.logs = $util.emptyArray;
      GetAuditLogsResponse.create = function(properties) {
        return new GetAuditLogsResponse(properties);
      };
      GetAuditLogsResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageResponse.encode(message.page, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.logs != null && message.logs.length)
          for (let i = 0; i < message.logs.length; ++i)
            $root.oj.common.AdminAuditLog.encode(message.logs[i], writer.uint32(
              /* id 3, wireType 2 =*/
              26
            ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetAuditLogsResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetAuditLogsResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetAuditLogsResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if (!(message.logs && message.logs.length))
                message.logs = [];
              message.logs.push($root.oj.common.AdminAuditLog.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetAuditLogsResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetAuditLogsResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageResponse.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.logs != null && $Object.hasOwnProperty.call(message, "logs")) {
          if (!$Array.isArray(message.logs))
            return "logs: array expected";
          for (let i = 0; i < message.logs.length; ++i) {
            let error = $root.oj.common.AdminAuditLog.verify(message.logs[i], _depth + 1);
            if (error)
              return "logs." + error;
          }
        }
        return null;
      };
      GetAuditLogsResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetAuditLogsResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetAuditLogsResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetAuditLogsResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetAuditLogsResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.GetAuditLogsResponse.page: object expected");
          message.page = $root.oj.common.PageResponse.fromObject(object.page, _depth + 1);
        }
        if (object.logs) {
          if (!$Array.isArray(object.logs))
            throw $TypeError(".oj.biz.GetAuditLogsResponse.logs: array expected");
          message.logs = $Array(object.logs.length);
          for (let i = 0; i < object.logs.length; ++i) {
            if (!$util.isObject(object.logs[i]))
              throw $TypeError(".oj.biz.GetAuditLogsResponse.logs: object expected");
            message.logs[i] = $root.oj.common.AdminAuditLog.fromObject(object.logs[i], _depth + 1);
          }
        }
        return message;
      };
      GetAuditLogsResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.logs = [];
        if (options.defaults) {
          object.status = null;
          object.page = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageResponse.toObject(message.page, options, _depth + 1);
        if (message.logs && message.logs.length) {
          object.logs = $Array(message.logs.length);
          for (let j = 0; j < message.logs.length; ++j)
            object.logs[j] = $root.oj.common.AdminAuditLog.toObject(message.logs[j], options, _depth + 1);
        }
        return object;
      };
      GetAuditLogsResponse.prototype.toJSON = function() {
        return GetAuditLogsResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetAuditLogsResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetAuditLogsResponse";
      };
      return GetAuditLogsResponse;
    })();
    biz.AdminService = (function() {
      const AdminService = function(rpcImpl, requestDelimited, responseDelimited) {
        import_minimal.default.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperty(AdminService.prototype = $Object.create(import_minimal.default.rpc.Service.prototype), "constructor", { value: AdminService, writable: true, enumerable: false, configurable: true });
      AdminService.create = function(rpcImpl, requestDelimited, responseDelimited) {
        return new this(rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperties(AdminService.prototype.getVersion = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.getVersion, $root.oj.common.EmptyRequest, $root.oj.biz.GetVersionResponse, request, callback);
      }, {
        name: { value: "GetVersion" },
        path: { value: "/oj.biz.AdminService/GetVersion" },
        requestType: { value: "oj.common.EmptyRequest" },
        responseType: { value: "GetVersionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.login = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.login, $root.oj.biz.AdminLoginRequest, $root.oj.biz.AdminLoginResponse, request, callback);
      }, {
        name: { value: "Login" },
        path: { value: "/oj.biz.AdminService/Login" },
        requestType: { value: "AdminLoginRequest" },
        responseType: { value: "AdminLoginResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.logout = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.logout, $root.oj.common.EmptyRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "Logout" },
        path: { value: "/oj.biz.AdminService/Logout" },
        requestType: { value: "oj.common.EmptyRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.getOverview = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.getOverview, $root.oj.common.EmptyRequest, $root.oj.biz.AdminOverviewResponse, request, callback);
      }, {
        name: { value: "GetOverview" },
        path: { value: "/oj.biz.AdminService/GetOverview" },
        requestType: { value: "oj.common.EmptyRequest" },
        responseType: { value: "AdminOverviewResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.listUsers = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.listUsers, $root.oj.biz.AdminListUsersRequest, $root.oj.biz.AdminListUsersResponse, request, callback);
      }, {
        name: { value: "ListUsers" },
        path: { value: "/oj.biz.AdminService/ListUsers" },
        requestType: { value: "AdminListUsersRequest" },
        responseType: { value: "AdminListUsersResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.getUser = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.getUser, $root.oj.biz.AdminUserIdRequest, $root.oj.biz.AdminUserResponse, request, callback);
      }, {
        name: { value: "GetUser" },
        path: { value: "/oj.biz.AdminService/GetUser" },
        requestType: { value: "AdminUserIdRequest" },
        responseType: { value: "AdminUserResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.createUser = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.createUser, $root.oj.biz.AdminCreateUserRequest, $root.oj.biz.AdminUserResponse, request, callback);
      }, {
        name: { value: "CreateUser" },
        path: { value: "/oj.biz.AdminService/CreateUser" },
        requestType: { value: "AdminCreateUserRequest" },
        responseType: { value: "AdminUserResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.updateUser = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.updateUser, $root.oj.biz.AdminUpdateUserRequest, $root.oj.biz.AdminUserResponse, request, callback);
      }, {
        name: { value: "UpdateUser" },
        path: { value: "/oj.biz.AdminService/UpdateUser" },
        requestType: { value: "AdminUpdateUserRequest" },
        responseType: { value: "AdminUserResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.deleteUser = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.deleteUser, $root.oj.biz.AdminUserIdRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "DeleteUser" },
        path: { value: "/oj.biz.AdminService/DeleteUser" },
        requestType: { value: "AdminUserIdRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.listQuestions = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.listQuestions, $root.oj.biz.GetQuestionListRequest, $root.oj.biz.GetQuestionListResponse, request, callback);
      }, {
        name: { value: "ListQuestions" },
        path: { value: "/oj.biz.AdminService/ListQuestions" },
        requestType: { value: "GetQuestionListRequest" },
        responseType: { value: "GetQuestionListResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.getQuestion = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.getQuestion, $root.oj.biz.QuestionIdRequest, $root.oj.biz.GetQuestionDetailResponse, request, callback);
      }, {
        name: { value: "GetQuestion" },
        path: { value: "/oj.biz.AdminService/GetQuestion" },
        requestType: { value: "QuestionIdRequest" },
        responseType: { value: "GetQuestionDetailResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.createQuestion = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.createQuestion, $root.oj.biz.SaveQuestionRequest, $root.oj.biz.QuestionResponse, request, callback);
      }, {
        name: { value: "CreateQuestion" },
        path: { value: "/oj.biz.AdminService/CreateQuestion" },
        requestType: { value: "SaveQuestionRequest" },
        responseType: { value: "QuestionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.updateQuestion = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.updateQuestion, $root.oj.biz.SaveQuestionRequest, $root.oj.biz.QuestionResponse, request, callback);
      }, {
        name: { value: "UpdateQuestion" },
        path: { value: "/oj.biz.AdminService/UpdateQuestion" },
        requestType: { value: "SaveQuestionRequest" },
        responseType: { value: "QuestionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.deleteQuestion = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.deleteQuestion, $root.oj.biz.QuestionIdRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "DeleteQuestion" },
        path: { value: "/oj.biz.AdminService/DeleteQuestion" },
        requestType: { value: "QuestionIdRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.listTestCases = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.listTestCases, $root.oj.biz.ListTestCasesRequest, $root.oj.biz.ListTestCasesResponse, request, callback);
      }, {
        name: { value: "ListTestCases" },
        path: { value: "/oj.biz.AdminService/ListTestCases" },
        requestType: { value: "ListTestCasesRequest" },
        responseType: { value: "ListTestCasesResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.createTestCase = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.createTestCase, $root.oj.biz.CreateTestCaseRequest, $root.oj.biz.TestCaseResponse, request, callback);
      }, {
        name: { value: "CreateTestCase" },
        path: { value: "/oj.biz.AdminService/CreateTestCase" },
        requestType: { value: "CreateTestCaseRequest" },
        responseType: { value: "TestCaseResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.updateTestCase = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.updateTestCase, $root.oj.biz.UpdateTestCaseRequest, $root.oj.biz.TestCaseResponse, request, callback);
      }, {
        name: { value: "UpdateTestCase" },
        path: { value: "/oj.biz.AdminService/UpdateTestCase" },
        requestType: { value: "UpdateTestCaseRequest" },
        responseType: { value: "TestCaseResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.deleteTestCase = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.deleteTestCase, $root.oj.biz.TestCaseIdRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "DeleteTestCase" },
        path: { value: "/oj.biz.AdminService/DeleteTestCase" },
        requestType: { value: "TestCaseIdRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.invalidateQuestionCache = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.invalidateQuestionCache, $root.oj.biz.InvalidateQuestionCacheRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "InvalidateQuestionCache" },
        path: { value: "/oj.biz.AdminService/InvalidateQuestionCache" },
        requestType: { value: "InvalidateQuestionCacheRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.listAdminAccounts = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.listAdminAccounts, $root.oj.biz.ListAdminAccountsRequest, $root.oj.biz.ListAdminAccountsResponse, request, callback);
      }, {
        name: { value: "ListAdminAccounts" },
        path: { value: "/oj.biz.AdminService/ListAdminAccounts" },
        requestType: { value: "ListAdminAccountsRequest" },
        responseType: { value: "ListAdminAccountsResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.getAdminAccount = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.getAdminAccount, $root.oj.biz.AdminAccountIdRequest, $root.oj.biz.AdminAccountResponse, request, callback);
      }, {
        name: { value: "GetAdminAccount" },
        path: { value: "/oj.biz.AdminService/GetAdminAccount" },
        requestType: { value: "AdminAccountIdRequest" },
        responseType: { value: "AdminAccountResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.createAdminAccount = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.createAdminAccount, $root.oj.biz.SaveAdminAccountRequest, $root.oj.biz.AdminAccountResponse, request, callback);
      }, {
        name: { value: "CreateAdminAccount" },
        path: { value: "/oj.biz.AdminService/CreateAdminAccount" },
        requestType: { value: "SaveAdminAccountRequest" },
        responseType: { value: "AdminAccountResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.updateAdminAccount = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.updateAdminAccount, $root.oj.biz.SaveAdminAccountRequest, $root.oj.biz.AdminAccountResponse, request, callback);
      }, {
        name: { value: "UpdateAdminAccount" },
        path: { value: "/oj.biz.AdminService/UpdateAdminAccount" },
        requestType: { value: "SaveAdminAccountRequest" },
        responseType: { value: "AdminAccountResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.deleteAdminAccount = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.deleteAdminAccount, $root.oj.biz.AdminAccountIdRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "DeleteAdminAccount" },
        path: { value: "/oj.biz.AdminService/DeleteAdminAccount" },
        requestType: { value: "AdminAccountIdRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.getCacheMetrics = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.getCacheMetrics, $root.oj.common.EmptyRequest, $root.oj.biz.GetCacheMetricsResponse, request, callback);
      }, {
        name: { value: "GetCacheMetrics" },
        path: { value: "/oj.biz.AdminService/GetCacheMetrics" },
        requestType: { value: "oj.common.EmptyRequest" },
        responseType: { value: "GetCacheMetricsResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(AdminService.prototype.getAuditLogs = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, AdminService.prototype.getAuditLogs, $root.oj.biz.GetAuditLogsRequest, $root.oj.biz.GetAuditLogsResponse, request, callback);
      }, {
        name: { value: "GetAuditLogs" },
        path: { value: "/oj.biz.AdminService/GetAuditLogs" },
        requestType: { value: "GetAuditLogsRequest" },
        responseType: { value: "GetAuditLogsResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      return AdminService;
    })();
    biz.CreateSubmissionRequest = (function() {
      const CreateSubmissionRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CreateSubmissionRequest.prototype.questionId = "";
      CreateSubmissionRequest.prototype.code = "";
      CreateSubmissionRequest.prototype.language = "";
      CreateSubmissionRequest.create = function(properties) {
        return new CreateSubmissionRequest(properties);
      };
      CreateSubmissionRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.questionId);
        if (message.code != null && $Object.hasOwnProperty.call(message, "code") && message.code !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.code);
        if (message.language != null && $Object.hasOwnProperty.call(message, "language") && message.language !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.language);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CreateSubmissionRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CreateSubmissionRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.CreateSubmissionRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.code = value;
              else
                delete message.code;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.language = value;
              else
                delete message.language;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CreateSubmissionRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CreateSubmissionRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        if (message.code != null && $Object.hasOwnProperty.call(message, "code")) {
          if (!$util.isString(message.code))
            return "code: string expected";
        }
        if (message.language != null && $Object.hasOwnProperty.call(message, "language")) {
          if (!$util.isString(message.language))
            return "language: string expected";
        }
        return null;
      };
      CreateSubmissionRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.CreateSubmissionRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.CreateSubmissionRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.CreateSubmissionRequest();
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        if (object.code != null) {
          if (typeof object.code !== "string" || object.code.length)
            message.code = $String(object.code);
        }
        if (object.language != null) {
          if (typeof object.language !== "string" || object.language.length)
            message.language = $String(object.language);
        }
        return message;
      };
      CreateSubmissionRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.questionId = "";
          object.code = "";
          object.language = "";
        }
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
          object.code = message.code;
        if (message.language != null && $Object.hasOwnProperty.call(message, "language"))
          object.language = message.language;
        return object;
      };
      CreateSubmissionRequest.prototype.toJSON = function() {
        return CreateSubmissionRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CreateSubmissionRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.CreateSubmissionRequest";
      };
      return CreateSubmissionRequest;
    })();
    biz.CreateSubmissionResponse = (function() {
      const CreateSubmissionResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CreateSubmissionResponse.prototype.status = null;
      CreateSubmissionResponse.prototype.submission = null;
      CreateSubmissionResponse.create = function(properties) {
        return new CreateSubmissionResponse(properties);
      };
      CreateSubmissionResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.submission != null && $Object.hasOwnProperty.call(message, "submission"))
          $root.oj.common.Submission.encode(message.submission, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CreateSubmissionResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CreateSubmissionResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.CreateSubmissionResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.submission = $root.oj.common.Submission.decode(reader, reader.uint32(), $undefined, _depth + 1, message.submission);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CreateSubmissionResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CreateSubmissionResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.submission != null && $Object.hasOwnProperty.call(message, "submission")) {
          let error = $root.oj.common.Submission.verify(message.submission, _depth + 1);
          if (error)
            return "submission." + error;
        }
        return null;
      };
      CreateSubmissionResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.CreateSubmissionResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.CreateSubmissionResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.CreateSubmissionResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.CreateSubmissionResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.submission != null) {
          if (!$util.isObject(object.submission))
            throw $TypeError(".oj.biz.CreateSubmissionResponse.submission: object expected");
          message.submission = $root.oj.common.Submission.fromObject(object.submission, _depth + 1);
        }
        return message;
      };
      CreateSubmissionResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.submission = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.submission != null && $Object.hasOwnProperty.call(message, "submission"))
          object.submission = $root.oj.common.Submission.toObject(message.submission, options, _depth + 1);
        return object;
      };
      CreateSubmissionResponse.prototype.toJSON = function() {
        return CreateSubmissionResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CreateSubmissionResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.CreateSubmissionResponse";
      };
      return CreateSubmissionResponse;
    })();
    biz.CreateCustomTestRequest = (function() {
      const CreateCustomTestRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CreateCustomTestRequest.prototype.questionId = "";
      CreateCustomTestRequest.prototype.code = "";
      CreateCustomTestRequest.prototype.language = "";
      CreateCustomTestRequest.prototype.customInput = "";
      CreateCustomTestRequest.create = function(properties) {
        return new CreateCustomTestRequest(properties);
      };
      CreateCustomTestRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.questionId);
        if (message.code != null && $Object.hasOwnProperty.call(message, "code") && message.code !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.code);
        if (message.language != null && $Object.hasOwnProperty.call(message, "language") && message.language !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.language);
        if (message.customInput != null && $Object.hasOwnProperty.call(message, "customInput") && message.customInput !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.customInput);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CreateCustomTestRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CreateCustomTestRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.CreateCustomTestRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.code = value;
              else
                delete message.code;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.language = value;
              else
                delete message.language;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.customInput = value;
              else
                delete message.customInput;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CreateCustomTestRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CreateCustomTestRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        if (message.code != null && $Object.hasOwnProperty.call(message, "code")) {
          if (!$util.isString(message.code))
            return "code: string expected";
        }
        if (message.language != null && $Object.hasOwnProperty.call(message, "language")) {
          if (!$util.isString(message.language))
            return "language: string expected";
        }
        if (message.customInput != null && $Object.hasOwnProperty.call(message, "customInput")) {
          if (!$util.isString(message.customInput))
            return "customInput: string expected";
        }
        return null;
      };
      CreateCustomTestRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.CreateCustomTestRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.CreateCustomTestRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.CreateCustomTestRequest();
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        if (object.code != null) {
          if (typeof object.code !== "string" || object.code.length)
            message.code = $String(object.code);
        }
        if (object.language != null) {
          if (typeof object.language !== "string" || object.language.length)
            message.language = $String(object.language);
        }
        if (object.customInput != null) {
          if (typeof object.customInput !== "string" || object.customInput.length)
            message.customInput = $String(object.customInput);
        }
        return message;
      };
      CreateCustomTestRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.questionId = "";
          object.code = "";
          object.language = "";
          object.customInput = "";
        }
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
          object.code = message.code;
        if (message.language != null && $Object.hasOwnProperty.call(message, "language"))
          object.language = message.language;
        if (message.customInput != null && $Object.hasOwnProperty.call(message, "customInput"))
          object.customInput = message.customInput;
        return object;
      };
      CreateCustomTestRequest.prototype.toJSON = function() {
        return CreateCustomTestRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CreateCustomTestRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.CreateCustomTestRequest";
      };
      return CreateCustomTestRequest;
    })();
    biz.CreateCustomTestResponse = (function() {
      const CreateCustomTestResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CreateCustomTestResponse.prototype.status = null;
      CreateCustomTestResponse.prototype.taskId = "";
      CreateCustomTestResponse.prototype.submissionStatus = 0;
      CreateCustomTestResponse.create = function(properties) {
        return new CreateCustomTestResponse(properties);
      };
      CreateCustomTestResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.taskId != null && $Object.hasOwnProperty.call(message, "taskId") && message.taskId !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.taskId);
        if (message.submissionStatus != null && $Object.hasOwnProperty.call(message, "submissionStatus") && message.submissionStatus !== 0)
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).int32(message.submissionStatus);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CreateCustomTestResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CreateCustomTestResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.CreateCustomTestResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.taskId = value;
              else
                delete message.taskId;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (value = reader.int32())
                message.submissionStatus = value;
              else
                delete message.submissionStatus;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CreateCustomTestResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CreateCustomTestResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.taskId != null && $Object.hasOwnProperty.call(message, "taskId")) {
          if (!$util.isString(message.taskId))
            return "taskId: string expected";
        }
        if (message.submissionStatus != null && $Object.hasOwnProperty.call(message, "submissionStatus")) {
          if (typeof message.submissionStatus !== "number" || (message.submissionStatus | 0) !== message.submissionStatus)
            return "submissionStatus: enum value expected";
        }
        return null;
      };
      CreateCustomTestResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.CreateCustomTestResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.CreateCustomTestResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.CreateCustomTestResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.CreateCustomTestResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.taskId != null) {
          if (typeof object.taskId !== "string" || object.taskId.length)
            message.taskId = $String(object.taskId);
        }
        if (object.submissionStatus !== 0 && (typeof object.submissionStatus !== "string" || $root.oj.common.SubmissionStatus[object.submissionStatus] !== 0))
          switch (object.submissionStatus) {
            case "SUBMISSION_STATUS_UNSPECIFIED":
            case 0:
              message.submissionStatus = 0;
              break;
            case "SUBMISSION_STATUS_PENDING":
            case 1:
              message.submissionStatus = 1;
              break;
            case "SUBMISSION_STATUS_QUEUED":
            case 2:
              message.submissionStatus = 2;
              break;
            case "SUBMISSION_STATUS_JUDGING":
            case 3:
              message.submissionStatus = 3;
              break;
            case "SUBMISSION_STATUS_ACCEPTED":
            case 4:
              message.submissionStatus = 4;
              break;
            case "SUBMISSION_STATUS_WRONG_ANSWER":
            case 5:
              message.submissionStatus = 5;
              break;
            case "SUBMISSION_STATUS_COMPILE_ERROR":
            case 6:
              message.submissionStatus = 6;
              break;
            case "SUBMISSION_STATUS_MEMORY_LIMIT_EXCEEDED":
            case 7:
              message.submissionStatus = 7;
              break;
            case "SUBMISSION_STATUS_TIME_LIMIT_EXCEEDED":
            case 8:
              message.submissionStatus = 8;
              break;
            case "SUBMISSION_STATUS_RUNTIME_ERROR":
            case 9:
              message.submissionStatus = 9;
              break;
            case "SUBMISSION_STATUS_SYSTEM_ERROR":
            case 10:
              message.submissionStatus = 10;
              break;
            case "SUBMISSION_STATUS_CANCELLED":
            case 11:
              message.submissionStatus = 11;
              break;
            default:
              if (typeof object.submissionStatus === "number" && (object.submissionStatus | 0) === object.submissionStatus)
                message.submissionStatus = object.submissionStatus;
          }
        return message;
      };
      CreateCustomTestResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.taskId = "";
          object.submissionStatus = options.enums === $String ? "SUBMISSION_STATUS_UNSPECIFIED" : 0;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.taskId != null && $Object.hasOwnProperty.call(message, "taskId"))
          object.taskId = message.taskId;
        if (message.submissionStatus != null && $Object.hasOwnProperty.call(message, "submissionStatus"))
          object.submissionStatus = options.enums === $String ? $root.oj.common.SubmissionStatus[message.submissionStatus] === $undefined ? message.submissionStatus : $root.oj.common.SubmissionStatus[message.submissionStatus] : message.submissionStatus;
        return object;
      };
      CreateCustomTestResponse.prototype.toJSON = function() {
        return CreateCustomTestResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CreateCustomTestResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.CreateCustomTestResponse";
      };
      return CreateCustomTestResponse;
    })();
    biz.GetSubmissionRequest = (function() {
      const GetSubmissionRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetSubmissionRequest.prototype.submissionId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      GetSubmissionRequest.create = function(properties) {
        return new GetSubmissionRequest(properties);
      };
      GetSubmissionRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId") && (typeof message.submissionId === "object" ? message.submissionId.low || message.submissionId.high : message.submissionId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.submissionId);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetSubmissionRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetSubmissionRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetSubmissionRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.submissionId = value;
              else
                delete message.submissionId;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetSubmissionRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetSubmissionRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId")) {
          if (!$util.isInteger(message.submissionId) && !(message.submissionId && $util.isInteger(message.submissionId.low) && $util.isInteger(message.submissionId.high)))
            return "submissionId: integer|Long expected";
        }
        return null;
      };
      GetSubmissionRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetSubmissionRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetSubmissionRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetSubmissionRequest();
        if (object.submissionId != null) {
          if (typeof object.submissionId === "object" ? object.submissionId.low || object.submissionId.high : $Number(object.submissionId) !== 0) {
            if ($util.Long)
              message.submissionId = $util.Long.fromValue(object.submissionId, true);
            else if (typeof object.submissionId === "string")
              message.submissionId = $parseInt(object.submissionId, 10);
            else if (typeof object.submissionId === "number")
              message.submissionId = object.submissionId;
            else if (typeof object.submissionId === "object")
              message.submissionId = new $util.LongBits(object.submissionId.low >>> 0, object.submissionId.high >>> 0).toNumber(true);
          }
        }
        return message;
      };
      GetSubmissionRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.submissionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.submissionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.submissionId = typeof message.submissionId === "number" ? $BigInt(message.submissionId) : $util.Long.fromBits(message.submissionId.low >>> 0, message.submissionId.high >>> 0, true).toBigInt();
          else if (typeof message.submissionId === "number")
            object.submissionId = options.longs === $String ? $String(message.submissionId) : message.submissionId;
          else
            object.submissionId = options.longs === $String ? $util.Long.prototype.toString.call(message.submissionId) : options.longs === $Number ? new $util.LongBits(message.submissionId.low >>> 0, message.submissionId.high >>> 0).toNumber(true) : message.submissionId;
        return object;
      };
      GetSubmissionRequest.prototype.toJSON = function() {
        return GetSubmissionRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetSubmissionRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetSubmissionRequest";
      };
      return GetSubmissionRequest;
    })();
    biz.GetSubmissionResponse = (function() {
      const GetSubmissionResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetSubmissionResponse.prototype.status = null;
      GetSubmissionResponse.prototype.submission = null;
      GetSubmissionResponse.create = function(properties) {
        return new GetSubmissionResponse(properties);
      };
      GetSubmissionResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.submission != null && $Object.hasOwnProperty.call(message, "submission"))
          $root.oj.common.Submission.encode(message.submission, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetSubmissionResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetSubmissionResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetSubmissionResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.submission = $root.oj.common.Submission.decode(reader, reader.uint32(), $undefined, _depth + 1, message.submission);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetSubmissionResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetSubmissionResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.submission != null && $Object.hasOwnProperty.call(message, "submission")) {
          let error = $root.oj.common.Submission.verify(message.submission, _depth + 1);
          if (error)
            return "submission." + error;
        }
        return null;
      };
      GetSubmissionResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetSubmissionResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetSubmissionResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetSubmissionResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetSubmissionResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.submission != null) {
          if (!$util.isObject(object.submission))
            throw $TypeError(".oj.biz.GetSubmissionResponse.submission: object expected");
          message.submission = $root.oj.common.Submission.fromObject(object.submission, _depth + 1);
        }
        return message;
      };
      GetSubmissionResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.submission = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.submission != null && $Object.hasOwnProperty.call(message, "submission"))
          object.submission = $root.oj.common.Submission.toObject(message.submission, options, _depth + 1);
        return object;
      };
      GetSubmissionResponse.prototype.toJSON = function() {
        return GetSubmissionResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetSubmissionResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetSubmissionResponse";
      };
      return GetSubmissionResponse;
    })();
    biz.GetCustomTestRequest = (function() {
      const GetCustomTestRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetCustomTestRequest.prototype.taskId = "";
      GetCustomTestRequest.create = function(properties) {
        return new GetCustomTestRequest(properties);
      };
      GetCustomTestRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.taskId != null && $Object.hasOwnProperty.call(message, "taskId") && message.taskId !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.taskId);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetCustomTestRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetCustomTestRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetCustomTestRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.taskId = value;
              else
                delete message.taskId;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetCustomTestRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetCustomTestRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.taskId != null && $Object.hasOwnProperty.call(message, "taskId")) {
          if (!$util.isString(message.taskId))
            return "taskId: string expected";
        }
        return null;
      };
      GetCustomTestRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetCustomTestRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetCustomTestRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetCustomTestRequest();
        if (object.taskId != null) {
          if (typeof object.taskId !== "string" || object.taskId.length)
            message.taskId = $String(object.taskId);
        }
        return message;
      };
      GetCustomTestRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.taskId = "";
        if (message.taskId != null && $Object.hasOwnProperty.call(message, "taskId"))
          object.taskId = message.taskId;
        return object;
      };
      GetCustomTestRequest.prototype.toJSON = function() {
        return GetCustomTestRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetCustomTestRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetCustomTestRequest";
      };
      return GetCustomTestRequest;
    })();
    biz.GetCustomTestResponse = (function() {
      const GetCustomTestResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      GetCustomTestResponse.prototype.status = null;
      GetCustomTestResponse.prototype.taskId = "";
      GetCustomTestResponse.prototype.submissionStatus = 0;
      GetCustomTestResponse.prototype.result = null;
      GetCustomTestResponse.create = function(properties) {
        return new GetCustomTestResponse(properties);
      };
      GetCustomTestResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.taskId != null && $Object.hasOwnProperty.call(message, "taskId") && message.taskId !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.taskId);
        if (message.submissionStatus != null && $Object.hasOwnProperty.call(message, "submissionStatus") && message.submissionStatus !== 0)
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).int32(message.submissionStatus);
        if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
          $root.oj.common.JudgeResult.encode(message.result, writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      GetCustomTestResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      GetCustomTestResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.GetCustomTestResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.taskId = value;
              else
                delete message.taskId;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (value = reader.int32())
                message.submissionStatus = value;
              else
                delete message.submissionStatus;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              message.result = $root.oj.common.JudgeResult.decode(reader, reader.uint32(), $undefined, _depth + 1, message.result);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      GetCustomTestResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      GetCustomTestResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.taskId != null && $Object.hasOwnProperty.call(message, "taskId")) {
          if (!$util.isString(message.taskId))
            return "taskId: string expected";
        }
        if (message.submissionStatus != null && $Object.hasOwnProperty.call(message, "submissionStatus")) {
          if (typeof message.submissionStatus !== "number" || (message.submissionStatus | 0) !== message.submissionStatus)
            return "submissionStatus: enum value expected";
        }
        if (message.result != null && $Object.hasOwnProperty.call(message, "result")) {
          let error = $root.oj.common.JudgeResult.verify(message.result, _depth + 1);
          if (error)
            return "result." + error;
        }
        return null;
      };
      GetCustomTestResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.GetCustomTestResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.GetCustomTestResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.GetCustomTestResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.GetCustomTestResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.taskId != null) {
          if (typeof object.taskId !== "string" || object.taskId.length)
            message.taskId = $String(object.taskId);
        }
        if (object.submissionStatus !== 0 && (typeof object.submissionStatus !== "string" || $root.oj.common.SubmissionStatus[object.submissionStatus] !== 0))
          switch (object.submissionStatus) {
            case "SUBMISSION_STATUS_UNSPECIFIED":
            case 0:
              message.submissionStatus = 0;
              break;
            case "SUBMISSION_STATUS_PENDING":
            case 1:
              message.submissionStatus = 1;
              break;
            case "SUBMISSION_STATUS_QUEUED":
            case 2:
              message.submissionStatus = 2;
              break;
            case "SUBMISSION_STATUS_JUDGING":
            case 3:
              message.submissionStatus = 3;
              break;
            case "SUBMISSION_STATUS_ACCEPTED":
            case 4:
              message.submissionStatus = 4;
              break;
            case "SUBMISSION_STATUS_WRONG_ANSWER":
            case 5:
              message.submissionStatus = 5;
              break;
            case "SUBMISSION_STATUS_COMPILE_ERROR":
            case 6:
              message.submissionStatus = 6;
              break;
            case "SUBMISSION_STATUS_MEMORY_LIMIT_EXCEEDED":
            case 7:
              message.submissionStatus = 7;
              break;
            case "SUBMISSION_STATUS_TIME_LIMIT_EXCEEDED":
            case 8:
              message.submissionStatus = 8;
              break;
            case "SUBMISSION_STATUS_RUNTIME_ERROR":
            case 9:
              message.submissionStatus = 9;
              break;
            case "SUBMISSION_STATUS_SYSTEM_ERROR":
            case 10:
              message.submissionStatus = 10;
              break;
            case "SUBMISSION_STATUS_CANCELLED":
            case 11:
              message.submissionStatus = 11;
              break;
            default:
              if (typeof object.submissionStatus === "number" && (object.submissionStatus | 0) === object.submissionStatus)
                message.submissionStatus = object.submissionStatus;
          }
        if (object.result != null) {
          if (!$util.isObject(object.result))
            throw $TypeError(".oj.biz.GetCustomTestResponse.result: object expected");
          message.result = $root.oj.common.JudgeResult.fromObject(object.result, _depth + 1);
        }
        return message;
      };
      GetCustomTestResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.taskId = "";
          object.submissionStatus = options.enums === $String ? "SUBMISSION_STATUS_UNSPECIFIED" : 0;
          object.result = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.taskId != null && $Object.hasOwnProperty.call(message, "taskId"))
          object.taskId = message.taskId;
        if (message.submissionStatus != null && $Object.hasOwnProperty.call(message, "submissionStatus"))
          object.submissionStatus = options.enums === $String ? $root.oj.common.SubmissionStatus[message.submissionStatus] === $undefined ? message.submissionStatus : $root.oj.common.SubmissionStatus[message.submissionStatus] : message.submissionStatus;
        if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
          object.result = $root.oj.common.JudgeResult.toObject(message.result, options, _depth + 1);
        return object;
      };
      GetCustomTestResponse.prototype.toJSON = function() {
        return GetCustomTestResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      GetCustomTestResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.GetCustomTestResponse";
      };
      return GetCustomTestResponse;
    })();
    biz.ListSubmissionsRequest = (function() {
      const ListSubmissionsRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ListSubmissionsRequest.prototype.page = null;
      ListSubmissionsRequest.prototype.questionId = "";
      ListSubmissionsRequest.prototype.submissionStatus = 0;
      ListSubmissionsRequest.create = function(properties) {
        return new ListSubmissionsRequest(properties);
      };
      ListSubmissionsRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageRequest.encode(message.page, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.questionId);
        if (message.submissionStatus != null && $Object.hasOwnProperty.call(message, "submissionStatus") && message.submissionStatus !== 0)
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).int32(message.submissionStatus);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ListSubmissionsRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ListSubmissionsRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ListSubmissionsRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageRequest.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (value = reader.int32())
                message.submissionStatus = value;
              else
                delete message.submissionStatus;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ListSubmissionsRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ListSubmissionsRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageRequest.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        if (message.submissionStatus != null && $Object.hasOwnProperty.call(message, "submissionStatus")) {
          if (typeof message.submissionStatus !== "number" || (message.submissionStatus | 0) !== message.submissionStatus)
            return "submissionStatus: enum value expected";
        }
        return null;
      };
      ListSubmissionsRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ListSubmissionsRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ListSubmissionsRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ListSubmissionsRequest();
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.ListSubmissionsRequest.page: object expected");
          message.page = $root.oj.common.PageRequest.fromObject(object.page, _depth + 1);
        }
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        if (object.submissionStatus !== 0 && (typeof object.submissionStatus !== "string" || $root.oj.common.SubmissionStatus[object.submissionStatus] !== 0))
          switch (object.submissionStatus) {
            case "SUBMISSION_STATUS_UNSPECIFIED":
            case 0:
              message.submissionStatus = 0;
              break;
            case "SUBMISSION_STATUS_PENDING":
            case 1:
              message.submissionStatus = 1;
              break;
            case "SUBMISSION_STATUS_QUEUED":
            case 2:
              message.submissionStatus = 2;
              break;
            case "SUBMISSION_STATUS_JUDGING":
            case 3:
              message.submissionStatus = 3;
              break;
            case "SUBMISSION_STATUS_ACCEPTED":
            case 4:
              message.submissionStatus = 4;
              break;
            case "SUBMISSION_STATUS_WRONG_ANSWER":
            case 5:
              message.submissionStatus = 5;
              break;
            case "SUBMISSION_STATUS_COMPILE_ERROR":
            case 6:
              message.submissionStatus = 6;
              break;
            case "SUBMISSION_STATUS_MEMORY_LIMIT_EXCEEDED":
            case 7:
              message.submissionStatus = 7;
              break;
            case "SUBMISSION_STATUS_TIME_LIMIT_EXCEEDED":
            case 8:
              message.submissionStatus = 8;
              break;
            case "SUBMISSION_STATUS_RUNTIME_ERROR":
            case 9:
              message.submissionStatus = 9;
              break;
            case "SUBMISSION_STATUS_SYSTEM_ERROR":
            case 10:
              message.submissionStatus = 10;
              break;
            case "SUBMISSION_STATUS_CANCELLED":
            case 11:
              message.submissionStatus = 11;
              break;
            default:
              if (typeof object.submissionStatus === "number" && (object.submissionStatus | 0) === object.submissionStatus)
                message.submissionStatus = object.submissionStatus;
          }
        return message;
      };
      ListSubmissionsRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.page = null;
          object.questionId = "";
          object.submissionStatus = options.enums === $String ? "SUBMISSION_STATUS_UNSPECIFIED" : 0;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageRequest.toObject(message.page, options, _depth + 1);
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        if (message.submissionStatus != null && $Object.hasOwnProperty.call(message, "submissionStatus"))
          object.submissionStatus = options.enums === $String ? $root.oj.common.SubmissionStatus[message.submissionStatus] === $undefined ? message.submissionStatus : $root.oj.common.SubmissionStatus[message.submissionStatus] : message.submissionStatus;
        return object;
      };
      ListSubmissionsRequest.prototype.toJSON = function() {
        return ListSubmissionsRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ListSubmissionsRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ListSubmissionsRequest";
      };
      return ListSubmissionsRequest;
    })();
    biz.ListSubmissionsResponse = (function() {
      const ListSubmissionsResponse = function(properties) {
        this.submissions = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ListSubmissionsResponse.prototype.status = null;
      ListSubmissionsResponse.prototype.page = null;
      ListSubmissionsResponse.prototype.submissions = $util.emptyArray;
      ListSubmissionsResponse.create = function(properties) {
        return new ListSubmissionsResponse(properties);
      };
      ListSubmissionsResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          $root.oj.common.PageResponse.encode(message.page, writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).fork(), _depth + 1).ldelim();
        if (message.submissions != null && message.submissions.length)
          for (let i = 0; i < message.submissions.length; ++i)
            $root.oj.common.Submission.encode(message.submissions[i], writer.uint32(
              /* id 3, wireType 2 =*/
              26
            ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ListSubmissionsResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ListSubmissionsResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ListSubmissionsResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              message.page = $root.oj.common.PageResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.page);
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if (!(message.submissions && message.submissions.length))
                message.submissions = [];
              message.submissions.push($root.oj.common.Submission.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ListSubmissionsResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ListSubmissionsResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          let error = $root.oj.common.PageResponse.verify(message.page, _depth + 1);
          if (error)
            return "page." + error;
        }
        if (message.submissions != null && $Object.hasOwnProperty.call(message, "submissions")) {
          if (!$Array.isArray(message.submissions))
            return "submissions: array expected";
          for (let i = 0; i < message.submissions.length; ++i) {
            let error = $root.oj.common.Submission.verify(message.submissions[i], _depth + 1);
            if (error)
              return "submissions." + error;
          }
        }
        return null;
      };
      ListSubmissionsResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ListSubmissionsResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ListSubmissionsResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ListSubmissionsResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.ListSubmissionsResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.page != null) {
          if (!$util.isObject(object.page))
            throw $TypeError(".oj.biz.ListSubmissionsResponse.page: object expected");
          message.page = $root.oj.common.PageResponse.fromObject(object.page, _depth + 1);
        }
        if (object.submissions) {
          if (!$Array.isArray(object.submissions))
            throw $TypeError(".oj.biz.ListSubmissionsResponse.submissions: array expected");
          message.submissions = $Array(object.submissions.length);
          for (let i = 0; i < object.submissions.length; ++i) {
            if (!$util.isObject(object.submissions[i]))
              throw $TypeError(".oj.biz.ListSubmissionsResponse.submissions: object expected");
            message.submissions[i] = $root.oj.common.Submission.fromObject(object.submissions[i], _depth + 1);
          }
        }
        return message;
      };
      ListSubmissionsResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.submissions = [];
        if (options.defaults) {
          object.status = null;
          object.page = null;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = $root.oj.common.PageResponse.toObject(message.page, options, _depth + 1);
        if (message.submissions && message.submissions.length) {
          object.submissions = $Array(message.submissions.length);
          for (let j = 0; j < message.submissions.length; ++j)
            object.submissions[j] = $root.oj.common.Submission.toObject(message.submissions[j], options, _depth + 1);
        }
        return object;
      };
      ListSubmissionsResponse.prototype.toJSON = function() {
        return ListSubmissionsResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ListSubmissionsResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ListSubmissionsResponse";
      };
      return ListSubmissionsResponse;
    })();
    biz.UpdateJudgeResultRequest = (function() {
      const UpdateJudgeResultRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      UpdateJudgeResultRequest.prototype.messageId = "";
      UpdateJudgeResultRequest.prototype.submissionId = null;
      UpdateJudgeResultRequest.prototype.customTaskId = null;
      UpdateJudgeResultRequest.prototype.resultPayload = $util.newBuffer([]);
      UpdateJudgeResultRequest.prototype.serviceAuth = null;
      let $oneOfFields;
      $Object.defineProperty(UpdateJudgeResultRequest.prototype, "taskId", {
        get: $util.oneOfGetter($oneOfFields = ["submissionId", "customTaskId"]),
        set: $util.oneOfSetter($oneOfFields)
      });
      UpdateJudgeResultRequest.create = function(properties) {
        return new UpdateJudgeResultRequest(properties);
      };
      UpdateJudgeResultRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.messageId != null && $Object.hasOwnProperty.call(message, "messageId") && message.messageId !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.messageId);
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId"))
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint64(message.submissionId);
        if (message.customTaskId != null && $Object.hasOwnProperty.call(message, "customTaskId"))
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.customTaskId);
        if (message.resultPayload != null && $Object.hasOwnProperty.call(message, "resultPayload") && message.resultPayload.length)
          writer.uint32(
            /* id 5, wireType 2 =*/
            42
          ).bytes(message.resultPayload);
        if (message.serviceAuth != null && $Object.hasOwnProperty.call(message, "serviceAuth"))
          $root.oj.biz.ServiceAuthentication.encode(message.serviceAuth, writer.uint32(
            /* id 6, wireType 2 =*/
            50
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      UpdateJudgeResultRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      UpdateJudgeResultRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.UpdateJudgeResultRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.messageId = value;
              else
                delete message.messageId;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              message.submissionId = reader.uint64();
              message.taskId = "submissionId";
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              message.customTaskId = reader.stringVerify();
              message.taskId = "customTaskId";
              continue;
            }
            case 5: {
              if (wireType !== 2)
                break;
              if ((value = reader.bytes()).length)
                message.resultPayload = value;
              else
                delete message.resultPayload;
              continue;
            }
            case 6: {
              if (wireType !== 2)
                break;
              message.serviceAuth = $root.oj.biz.ServiceAuthentication.decode(reader, reader.uint32(), $undefined, _depth + 1, message.serviceAuth);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      UpdateJudgeResultRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      UpdateJudgeResultRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        let properties = {};
        if (message.messageId != null && $Object.hasOwnProperty.call(message, "messageId")) {
          if (!$util.isString(message.messageId))
            return "messageId: string expected";
        }
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId")) {
          properties.taskId = 1;
          if (!$util.isInteger(message.submissionId) && !(message.submissionId && $util.isInteger(message.submissionId.low) && $util.isInteger(message.submissionId.high)))
            return "submissionId: integer|Long expected";
        }
        if (message.customTaskId != null && $Object.hasOwnProperty.call(message, "customTaskId")) {
          if (properties.taskId === 1)
            return "taskId: multiple values";
          properties.taskId = 1;
          if (!$util.isString(message.customTaskId))
            return "customTaskId: string expected";
        }
        if (message.resultPayload != null && $Object.hasOwnProperty.call(message, "resultPayload")) {
          if (!(message.resultPayload && typeof message.resultPayload.length === "number" || $util.isString(message.resultPayload)))
            return "resultPayload: buffer expected";
        }
        if (message.serviceAuth != null && $Object.hasOwnProperty.call(message, "serviceAuth")) {
          let error = $root.oj.biz.ServiceAuthentication.verify(message.serviceAuth, _depth + 1);
          if (error)
            return "serviceAuth." + error;
        }
        return null;
      };
      UpdateJudgeResultRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.UpdateJudgeResultRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.UpdateJudgeResultRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.UpdateJudgeResultRequest();
        if (object.messageId != null) {
          if (typeof object.messageId !== "string" || object.messageId.length)
            message.messageId = $String(object.messageId);
        }
        if (object.submissionId != null) {
          if ($util.Long)
            message.submissionId = $util.Long.fromValue(object.submissionId, true);
          else if (typeof object.submissionId === "string")
            message.submissionId = $parseInt(object.submissionId, 10);
          else if (typeof object.submissionId === "number")
            message.submissionId = object.submissionId;
          else if (typeof object.submissionId === "object")
            message.submissionId = new $util.LongBits(object.submissionId.low >>> 0, object.submissionId.high >>> 0).toNumber(true);
        }
        if (object.customTaskId != null)
          message.customTaskId = $String(object.customTaskId);
        if (object.resultPayload != null) {
          if (object.resultPayload.length) {
            if (typeof object.resultPayload === "string")
              $util.base64.decode(object.resultPayload, message.resultPayload = $util.newBuffer($util.base64.length(object.resultPayload)), 0);
            else if (object.resultPayload.length >= 0)
              message.resultPayload = object.resultPayload;
          }
        }
        if (object.serviceAuth != null) {
          if (!$util.isObject(object.serviceAuth))
            throw $TypeError(".oj.biz.UpdateJudgeResultRequest.serviceAuth: object expected");
          message.serviceAuth = $root.oj.biz.ServiceAuthentication.fromObject(object.serviceAuth, _depth + 1);
        }
        return message;
      };
      UpdateJudgeResultRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.messageId = "";
          if (options.bytes === $String)
            object.resultPayload = "";
          else {
            object.resultPayload = [];
            if (options.bytes !== $Array)
              object.resultPayload = $util.newBuffer(object.resultPayload);
          }
          object.serviceAuth = null;
        }
        if (message.messageId != null && $Object.hasOwnProperty.call(message, "messageId"))
          object.messageId = message.messageId;
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId")) {
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.submissionId = typeof message.submissionId === "number" ? $BigInt(message.submissionId) : $util.Long.fromBits(message.submissionId.low >>> 0, message.submissionId.high >>> 0, true).toBigInt();
          else if (typeof message.submissionId === "number")
            object.submissionId = options.longs === $String ? $String(message.submissionId) : message.submissionId;
          else
            object.submissionId = options.longs === $String ? $util.Long.prototype.toString.call(message.submissionId) : options.longs === $Number ? new $util.LongBits(message.submissionId.low >>> 0, message.submissionId.high >>> 0).toNumber(true) : message.submissionId;
          if (options.oneofs)
            object.taskId = "submissionId";
        }
        if (message.customTaskId != null && $Object.hasOwnProperty.call(message, "customTaskId")) {
          object.customTaskId = message.customTaskId;
          if (options.oneofs)
            object.taskId = "customTaskId";
        }
        if (message.resultPayload != null && $Object.hasOwnProperty.call(message, "resultPayload"))
          object.resultPayload = options.bytes === $String ? $util.base64.encode(message.resultPayload, 0, message.resultPayload.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.resultPayload) : message.resultPayload;
        if (message.serviceAuth != null && $Object.hasOwnProperty.call(message, "serviceAuth"))
          object.serviceAuth = $root.oj.biz.ServiceAuthentication.toObject(message.serviceAuth, options, _depth + 1);
        return object;
      };
      UpdateJudgeResultRequest.prototype.toJSON = function() {
        return UpdateJudgeResultRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      UpdateJudgeResultRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.UpdateJudgeResultRequest";
      };
      return UpdateJudgeResultRequest;
    })();
    biz.ServiceAuthentication = (function() {
      const ServiceAuthentication = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      ServiceAuthentication.prototype.keyId = "";
      ServiceAuthentication.prototype.timestamp = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      ServiceAuthentication.prototype.nonce = "";
      ServiceAuthentication.prototype.hmacSha256 = $util.newBuffer([]);
      ServiceAuthentication.create = function(properties) {
        return new ServiceAuthentication(properties);
      };
      ServiceAuthentication.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.keyId != null && $Object.hasOwnProperty.call(message, "keyId") && message.keyId !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.keyId);
        if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp") && (typeof message.timestamp === "object" ? message.timestamp.low || message.timestamp.high : message.timestamp !== 0))
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).int64(message.timestamp);
        if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce") && message.nonce !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.nonce);
        if (message.hmacSha256 != null && $Object.hasOwnProperty.call(message, "hmacSha256") && message.hmacSha256.length)
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).bytes(message.hmacSha256);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      ServiceAuthentication.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      ServiceAuthentication.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.ServiceAuthentication(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.keyId = value;
              else
                delete message.keyId;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.timestamp = value;
              else
                delete message.timestamp;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.nonce = value;
              else
                delete message.nonce;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.bytes()).length)
                message.hmacSha256 = value;
              else
                delete message.hmacSha256;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      ServiceAuthentication.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      ServiceAuthentication.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.keyId != null && $Object.hasOwnProperty.call(message, "keyId")) {
          if (!$util.isString(message.keyId))
            return "keyId: string expected";
        }
        if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp")) {
          if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
            return "timestamp: integer|Long expected";
        }
        if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce")) {
          if (!$util.isString(message.nonce))
            return "nonce: string expected";
        }
        if (message.hmacSha256 != null && $Object.hasOwnProperty.call(message, "hmacSha256")) {
          if (!(message.hmacSha256 && typeof message.hmacSha256.length === "number" || $util.isString(message.hmacSha256)))
            return "hmacSha256: buffer expected";
        }
        return null;
      };
      ServiceAuthentication.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.ServiceAuthentication)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.ServiceAuthentication: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.ServiceAuthentication();
        if (object.keyId != null) {
          if (typeof object.keyId !== "string" || object.keyId.length)
            message.keyId = $String(object.keyId);
        }
        if (object.timestamp != null) {
          if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : $Number(object.timestamp) !== 0) {
            if ($util.Long)
              message.timestamp = $util.Long.fromValue(object.timestamp, false);
            else if (typeof object.timestamp === "string")
              message.timestamp = $parseInt(object.timestamp, 10);
            else if (typeof object.timestamp === "number")
              message.timestamp = object.timestamp;
            else if (typeof object.timestamp === "object")
              message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
          }
        }
        if (object.nonce != null) {
          if (typeof object.nonce !== "string" || object.nonce.length)
            message.nonce = $String(object.nonce);
        }
        if (object.hmacSha256 != null) {
          if (object.hmacSha256.length) {
            if (typeof object.hmacSha256 === "string")
              $util.base64.decode(object.hmacSha256, message.hmacSha256 = $util.newBuffer($util.base64.length(object.hmacSha256)), 0);
            else if (object.hmacSha256.length >= 0)
              message.hmacSha256 = object.hmacSha256;
          }
        }
        return message;
      };
      ServiceAuthentication.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.keyId = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.nonce = "";
          if (options.bytes === $String)
            object.hmacSha256 = "";
          else {
            object.hmacSha256 = [];
            if (options.bytes !== $Array)
              object.hmacSha256 = $util.newBuffer(object.hmacSha256);
          }
        }
        if (message.keyId != null && $Object.hasOwnProperty.call(message, "keyId"))
          object.keyId = message.keyId;
        if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.timestamp = typeof message.timestamp === "number" ? $BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
          else if (typeof message.timestamp === "number")
            object.timestamp = options.longs === $String ? $String(message.timestamp) : message.timestamp;
          else
            object.timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === $Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
        if (message.nonce != null && $Object.hasOwnProperty.call(message, "nonce"))
          object.nonce = message.nonce;
        if (message.hmacSha256 != null && $Object.hasOwnProperty.call(message, "hmacSha256"))
          object.hmacSha256 = options.bytes === $String ? $util.base64.encode(message.hmacSha256, 0, message.hmacSha256.length) : options.bytes === $Array ? $Array.prototype.slice.call(message.hmacSha256) : message.hmacSha256;
        return object;
      };
      ServiceAuthentication.prototype.toJSON = function() {
        return ServiceAuthentication.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      ServiceAuthentication.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.ServiceAuthentication";
      };
      return ServiceAuthentication;
    })();
    biz.JudgeResultPersistenceOutcome = (function() {
      const valuesById = $Object.create(null), values = $Object.create(valuesById);
      values[valuesById[0] = "JUDGE_RESULT_PERSISTENCE_OUTCOME_UNSPECIFIED"] = 0;
      values[valuesById[1] = "JUDGE_RESULT_PERSISTENCE_OUTCOME_PERSISTED"] = 1;
      values[valuesById[2] = "JUDGE_RESULT_PERSISTENCE_OUTCOME_IDEMPOTENT"] = 2;
      values[valuesById[3] = "JUDGE_RESULT_PERSISTENCE_OUTCOME_RETRYABLE_FAILURE"] = 3;
      values[valuesById[4] = "JUDGE_RESULT_PERSISTENCE_OUTCOME_REJECTED"] = 4;
      return values;
    })();
    biz.UpdateJudgeResultResponse = (function() {
      const UpdateJudgeResultResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      UpdateJudgeResultResponse.prototype.status = null;
      UpdateJudgeResultResponse.prototype.outcome = 0;
      UpdateJudgeResultResponse.prototype.retryAfterMs = 0;
      UpdateJudgeResultResponse.create = function(properties) {
        return new UpdateJudgeResultResponse(properties);
      };
      UpdateJudgeResultResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.outcome != null && $Object.hasOwnProperty.call(message, "outcome") && message.outcome !== 0)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).int32(message.outcome);
        if (message.retryAfterMs != null && $Object.hasOwnProperty.call(message, "retryAfterMs") && message.retryAfterMs !== 0)
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint32(message.retryAfterMs);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      UpdateJudgeResultResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      UpdateJudgeResultResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.UpdateJudgeResultResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.int32())
                message.outcome = value;
              else
                delete message.outcome;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.retryAfterMs = value;
              else
                delete message.retryAfterMs;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      UpdateJudgeResultResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      UpdateJudgeResultResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        if (message.outcome != null && $Object.hasOwnProperty.call(message, "outcome")) {
          if (typeof message.outcome !== "number" || (message.outcome | 0) !== message.outcome)
            return "outcome: enum value expected";
        }
        if (message.retryAfterMs != null && $Object.hasOwnProperty.call(message, "retryAfterMs")) {
          if (!$util.isInteger(message.retryAfterMs))
            return "retryAfterMs: integer expected";
        }
        return null;
      };
      UpdateJudgeResultResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.UpdateJudgeResultResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.UpdateJudgeResultResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.UpdateJudgeResultResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.biz.UpdateJudgeResultResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        if (object.outcome !== 0 && (typeof object.outcome !== "string" || $root.oj.biz.JudgeResultPersistenceOutcome[object.outcome] !== 0))
          switch (object.outcome) {
            case "JUDGE_RESULT_PERSISTENCE_OUTCOME_UNSPECIFIED":
            case 0:
              message.outcome = 0;
              break;
            case "JUDGE_RESULT_PERSISTENCE_OUTCOME_PERSISTED":
            case 1:
              message.outcome = 1;
              break;
            case "JUDGE_RESULT_PERSISTENCE_OUTCOME_IDEMPOTENT":
            case 2:
              message.outcome = 2;
              break;
            case "JUDGE_RESULT_PERSISTENCE_OUTCOME_RETRYABLE_FAILURE":
            case 3:
              message.outcome = 3;
              break;
            case "JUDGE_RESULT_PERSISTENCE_OUTCOME_REJECTED":
            case 4:
              message.outcome = 4;
              break;
            default:
              if (typeof object.outcome === "number" && (object.outcome | 0) === object.outcome)
                message.outcome = object.outcome;
          }
        if (object.retryAfterMs != null) {
          if ($Number(object.retryAfterMs) !== 0)
            message.retryAfterMs = object.retryAfterMs >>> 0;
        }
        return message;
      };
      UpdateJudgeResultResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = null;
          object.outcome = options.enums === $String ? "JUDGE_RESULT_PERSISTENCE_OUTCOME_UNSPECIFIED" : 0;
          object.retryAfterMs = 0;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        if (message.outcome != null && $Object.hasOwnProperty.call(message, "outcome"))
          object.outcome = options.enums === $String ? $root.oj.biz.JudgeResultPersistenceOutcome[message.outcome] === $undefined ? message.outcome : $root.oj.biz.JudgeResultPersistenceOutcome[message.outcome] : message.outcome;
        if (message.retryAfterMs != null && $Object.hasOwnProperty.call(message, "retryAfterMs"))
          object.retryAfterMs = message.retryAfterMs;
        return object;
      };
      UpdateJudgeResultResponse.prototype.toJSON = function() {
        return UpdateJudgeResultResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      UpdateJudgeResultResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.UpdateJudgeResultResponse";
      };
      return UpdateJudgeResultResponse;
    })();
    biz.SubmissionService = (function() {
      const SubmissionService = function(rpcImpl, requestDelimited, responseDelimited) {
        import_minimal.default.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperty(SubmissionService.prototype = $Object.create(import_minimal.default.rpc.Service.prototype), "constructor", { value: SubmissionService, writable: true, enumerable: false, configurable: true });
      SubmissionService.create = function(rpcImpl, requestDelimited, responseDelimited) {
        return new this(rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperties(SubmissionService.prototype.createSubmission = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SubmissionService.prototype.createSubmission, $root.oj.biz.CreateSubmissionRequest, $root.oj.biz.CreateSubmissionResponse, request, callback);
      }, {
        name: { value: "CreateSubmission" },
        path: { value: "/oj.biz.SubmissionService/CreateSubmission" },
        requestType: { value: "CreateSubmissionRequest" },
        responseType: { value: "CreateSubmissionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SubmissionService.prototype.createCustomTest = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SubmissionService.prototype.createCustomTest, $root.oj.biz.CreateCustomTestRequest, $root.oj.biz.CreateCustomTestResponse, request, callback);
      }, {
        name: { value: "CreateCustomTest" },
        path: { value: "/oj.biz.SubmissionService/CreateCustomTest" },
        requestType: { value: "CreateCustomTestRequest" },
        responseType: { value: "CreateCustomTestResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SubmissionService.prototype.getSubmission = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SubmissionService.prototype.getSubmission, $root.oj.biz.GetSubmissionRequest, $root.oj.biz.GetSubmissionResponse, request, callback);
      }, {
        name: { value: "GetSubmission" },
        path: { value: "/oj.biz.SubmissionService/GetSubmission" },
        requestType: { value: "GetSubmissionRequest" },
        responseType: { value: "GetSubmissionResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SubmissionService.prototype.getCustomTest = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SubmissionService.prototype.getCustomTest, $root.oj.biz.GetCustomTestRequest, $root.oj.biz.GetCustomTestResponse, request, callback);
      }, {
        name: { value: "GetCustomTest" },
        path: { value: "/oj.biz.SubmissionService/GetCustomTest" },
        requestType: { value: "GetCustomTestRequest" },
        responseType: { value: "GetCustomTestResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SubmissionService.prototype.listSubmissions = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SubmissionService.prototype.listSubmissions, $root.oj.biz.ListSubmissionsRequest, $root.oj.biz.ListSubmissionsResponse, request, callback);
      }, {
        name: { value: "ListSubmissions" },
        path: { value: "/oj.biz.SubmissionService/ListSubmissions" },
        requestType: { value: "ListSubmissionsRequest" },
        responseType: { value: "ListSubmissionsResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SubmissionService.prototype.updateJudgeResult = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SubmissionService.prototype.updateJudgeResult, $root.oj.biz.UpdateJudgeResultRequest, $root.oj.biz.UpdateJudgeResultResponse, request, callback);
      }, {
        name: { value: "UpdateJudgeResult" },
        path: { value: "/oj.biz.SubmissionService/UpdateJudgeResult" },
        requestType: { value: "UpdateJudgeResultRequest" },
        responseType: { value: "UpdateJudgeResultResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      $Object.defineProperties(SubmissionService.prototype.legacyUpdateJudgeResult = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, SubmissionService.prototype.legacyUpdateJudgeResult, $root.oj_judge.JudgeFinishedRequest, $root.oj_judge.NullRsp, request, callback);
      }, {
        name: { value: "LegacyUpdateJudgeResult" },
        path: { value: "/oj.biz.SubmissionService/LegacyUpdateJudgeResult" },
        requestType: { value: "oj_judge.JudgeFinishedRequest" },
        responseType: { value: "oj_judge.NullRsp" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      return SubmissionService;
    })();
    biz.InvalidateStaticCacheRequest = (function() {
      const InvalidateStaticCacheRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      InvalidateStaticCacheRequest.prototype.path = "";
      InvalidateStaticCacheRequest.create = function(properties) {
        return new InvalidateStaticCacheRequest(properties);
      };
      InvalidateStaticCacheRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.path != null && $Object.hasOwnProperty.call(message, "path") && message.path !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.path);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      InvalidateStaticCacheRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      InvalidateStaticCacheRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.biz.InvalidateStaticCacheRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.path = value;
              else
                delete message.path;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      InvalidateStaticCacheRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      InvalidateStaticCacheRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.path != null && $Object.hasOwnProperty.call(message, "path")) {
          if (!$util.isString(message.path))
            return "path: string expected";
        }
        return null;
      };
      InvalidateStaticCacheRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.biz.InvalidateStaticCacheRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.biz.InvalidateStaticCacheRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.biz.InvalidateStaticCacheRequest();
        if (object.path != null) {
          if (typeof object.path !== "string" || object.path.length)
            message.path = $String(object.path);
        }
        return message;
      };
      InvalidateStaticCacheRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.path = "";
        if (message.path != null && $Object.hasOwnProperty.call(message, "path"))
          object.path = message.path;
        return object;
      };
      InvalidateStaticCacheRequest.prototype.toJSON = function() {
        return InvalidateStaticCacheRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      InvalidateStaticCacheRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.biz.InvalidateStaticCacheRequest";
      };
      return InvalidateStaticCacheRequest;
    })();
    biz.StaticFileService = (function() {
      const StaticFileService = function(rpcImpl, requestDelimited, responseDelimited) {
        import_minimal.default.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperty(StaticFileService.prototype = $Object.create(import_minimal.default.rpc.Service.prototype), "constructor", { value: StaticFileService, writable: true, enumerable: false, configurable: true });
      StaticFileService.create = function(rpcImpl, requestDelimited, responseDelimited) {
        return new this(rpcImpl, requestDelimited, responseDelimited);
      };
      $Object.defineProperties(StaticFileService.prototype.invalidateCache = function(request, callback) {
        return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, StaticFileService.prototype.invalidateCache, $root.oj.biz.InvalidateStaticCacheRequest, $root.oj.common.EmptyResponse, request, callback);
      }, {
        name: { value: "InvalidateCache" },
        path: { value: "/oj.biz.StaticFileService/InvalidateCache" },
        requestType: { value: "InvalidateStaticCacheRequest" },
        responseType: { value: "oj.common.EmptyResponse" },
        requestStream: { value: $undefined },
        responseStream: { value: $undefined }
      });
      return StaticFileService;
    })();
    return biz;
  })();
  oj2.common = (function() {
    const common = {};
    common.ErrorCode = (function() {
      const valuesById = $Object.create(null), values = $Object.create(valuesById);
      values[valuesById[0] = "ERROR_CODE_UNSPECIFIED"] = 0;
      values[valuesById[1] = "ERROR_CODE_OK"] = 1;
      values[valuesById[2] = "ERROR_CODE_INVALID_ARGUMENT"] = 2;
      values[valuesById[3] = "ERROR_CODE_UNAUTHENTICATED"] = 3;
      values[valuesById[4] = "ERROR_CODE_PERMISSION_DENIED"] = 4;
      values[valuesById[5] = "ERROR_CODE_NOT_FOUND"] = 5;
      values[valuesById[6] = "ERROR_CODE_CONFLICT"] = 6;
      values[valuesById[7] = "ERROR_CODE_RATE_LIMITED"] = 7;
      values[valuesById[8] = "ERROR_CODE_UNAVAILABLE"] = 8;
      values[valuesById[9] = "ERROR_CODE_INTERNAL"] = 9;
      return values;
    })();
    common.StatusResponse = (function() {
      const StatusResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      StatusResponse.prototype.code = 0;
      StatusResponse.prototype.message = "";
      StatusResponse.prototype.retryable = false;
      StatusResponse.create = function(properties) {
        return new StatusResponse(properties);
      };
      StatusResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.code != null && $Object.hasOwnProperty.call(message, "code") && message.code !== 0)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).int32(message.code);
        if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.message);
        if (message.retryable != null && $Object.hasOwnProperty.call(message, "retryable") && message.retryable !== false)
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).bool(message.retryable);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      StatusResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      StatusResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.StatusResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.int32())
                message.code = value;
              else
                delete message.code;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.message = value;
              else
                delete message.message;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.retryable = value;
              else
                delete message.retryable;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      StatusResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      StatusResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.code != null && $Object.hasOwnProperty.call(message, "code")) {
          if (typeof message.code !== "number" || (message.code | 0) !== message.code)
            return "code: enum value expected";
        }
        if (message.message != null && $Object.hasOwnProperty.call(message, "message")) {
          if (!$util.isString(message.message))
            return "message: string expected";
        }
        if (message.retryable != null && $Object.hasOwnProperty.call(message, "retryable")) {
          if (typeof message.retryable !== "boolean")
            return "retryable: boolean expected";
        }
        return null;
      };
      StatusResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.StatusResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.StatusResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.StatusResponse();
        if (object.code !== 0 && (typeof object.code !== "string" || $root.oj.common.ErrorCode[object.code] !== 0))
          switch (object.code) {
            case "ERROR_CODE_UNSPECIFIED":
            case 0:
              message.code = 0;
              break;
            case "ERROR_CODE_OK":
            case 1:
              message.code = 1;
              break;
            case "ERROR_CODE_INVALID_ARGUMENT":
            case 2:
              message.code = 2;
              break;
            case "ERROR_CODE_UNAUTHENTICATED":
            case 3:
              message.code = 3;
              break;
            case "ERROR_CODE_PERMISSION_DENIED":
            case 4:
              message.code = 4;
              break;
            case "ERROR_CODE_NOT_FOUND":
            case 5:
              message.code = 5;
              break;
            case "ERROR_CODE_CONFLICT":
            case 6:
              message.code = 6;
              break;
            case "ERROR_CODE_RATE_LIMITED":
            case 7:
              message.code = 7;
              break;
            case "ERROR_CODE_UNAVAILABLE":
            case 8:
              message.code = 8;
              break;
            case "ERROR_CODE_INTERNAL":
            case 9:
              message.code = 9;
              break;
            default:
              if (typeof object.code === "number" && (object.code | 0) === object.code)
                message.code = object.code;
          }
        if (object.message != null) {
          if (typeof object.message !== "string" || object.message.length)
            message.message = $String(object.message);
        }
        if (object.retryable != null) {
          if (object.retryable)
            message.retryable = $Boolean(object.retryable);
        }
        return message;
      };
      StatusResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.code = options.enums === $String ? "ERROR_CODE_UNSPECIFIED" : 0;
          object.message = "";
          object.retryable = false;
        }
        if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
          object.code = options.enums === $String ? $root.oj.common.ErrorCode[message.code] === $undefined ? message.code : $root.oj.common.ErrorCode[message.code] : message.code;
        if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
          object.message = message.message;
        if (message.retryable != null && $Object.hasOwnProperty.call(message, "retryable"))
          object.retryable = message.retryable;
        return object;
      };
      StatusResponse.prototype.toJSON = function() {
        return StatusResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      StatusResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.StatusResponse";
      };
      return StatusResponse;
    })();
    common.PageRequest = (function() {
      const PageRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      PageRequest.prototype.page = 0;
      PageRequest.prototype.pageSize = 0;
      PageRequest.create = function(properties) {
        return new PageRequest(properties);
      };
      PageRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.page != null && $Object.hasOwnProperty.call(message, "page") && message.page !== 0)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint32(message.page);
        if (message.pageSize != null && $Object.hasOwnProperty.call(message, "pageSize") && message.pageSize !== 0)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint32(message.pageSize);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      PageRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      PageRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.PageRequest(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.page = value;
              else
                delete message.page;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.pageSize = value;
              else
                delete message.pageSize;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      PageRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      PageRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          if (!$util.isInteger(message.page))
            return "page: integer expected";
        }
        if (message.pageSize != null && $Object.hasOwnProperty.call(message, "pageSize")) {
          if (!$util.isInteger(message.pageSize))
            return "pageSize: integer expected";
        }
        return null;
      };
      PageRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.PageRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.PageRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.PageRequest();
        if (object.page != null) {
          if ($Number(object.page) !== 0)
            message.page = object.page >>> 0;
        }
        if (object.pageSize != null) {
          if ($Number(object.pageSize) !== 0)
            message.pageSize = object.pageSize >>> 0;
        }
        return message;
      };
      PageRequest.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.page = 0;
          object.pageSize = 0;
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = message.page;
        if (message.pageSize != null && $Object.hasOwnProperty.call(message, "pageSize"))
          object.pageSize = message.pageSize;
        return object;
      };
      PageRequest.prototype.toJSON = function() {
        return PageRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      PageRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.PageRequest";
      };
      return PageRequest;
    })();
    common.PageResponse = (function() {
      const PageResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      PageResponse.prototype.totalItems = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      PageResponse.prototype.page = 0;
      PageResponse.prototype.pageSize = 0;
      PageResponse.prototype.totalPages = 0;
      PageResponse.create = function(properties) {
        return new PageResponse(properties);
      };
      PageResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.totalItems != null && $Object.hasOwnProperty.call(message, "totalItems") && (typeof message.totalItems === "object" ? message.totalItems.low || message.totalItems.high : message.totalItems !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.totalItems);
        if (message.page != null && $Object.hasOwnProperty.call(message, "page") && message.page !== 0)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint32(message.page);
        if (message.pageSize != null && $Object.hasOwnProperty.call(message, "pageSize") && message.pageSize !== 0)
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint32(message.pageSize);
        if (message.totalPages != null && $Object.hasOwnProperty.call(message, "totalPages") && message.totalPages !== 0)
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).uint32(message.totalPages);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      PageResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      PageResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.PageResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.totalItems = value;
              else
                delete message.totalItems;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.page = value;
              else
                delete message.page;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.pageSize = value;
              else
                delete message.pageSize;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.totalPages = value;
              else
                delete message.totalPages;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      PageResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      PageResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.totalItems != null && $Object.hasOwnProperty.call(message, "totalItems")) {
          if (!$util.isInteger(message.totalItems) && !(message.totalItems && $util.isInteger(message.totalItems.low) && $util.isInteger(message.totalItems.high)))
            return "totalItems: integer|Long expected";
        }
        if (message.page != null && $Object.hasOwnProperty.call(message, "page")) {
          if (!$util.isInteger(message.page))
            return "page: integer expected";
        }
        if (message.pageSize != null && $Object.hasOwnProperty.call(message, "pageSize")) {
          if (!$util.isInteger(message.pageSize))
            return "pageSize: integer expected";
        }
        if (message.totalPages != null && $Object.hasOwnProperty.call(message, "totalPages")) {
          if (!$util.isInteger(message.totalPages))
            return "totalPages: integer expected";
        }
        return null;
      };
      PageResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.PageResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.PageResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.PageResponse();
        if (object.totalItems != null) {
          if (typeof object.totalItems === "object" ? object.totalItems.low || object.totalItems.high : $Number(object.totalItems) !== 0) {
            if ($util.Long)
              message.totalItems = $util.Long.fromValue(object.totalItems, true);
            else if (typeof object.totalItems === "string")
              message.totalItems = $parseInt(object.totalItems, 10);
            else if (typeof object.totalItems === "number")
              message.totalItems = object.totalItems;
            else if (typeof object.totalItems === "object")
              message.totalItems = new $util.LongBits(object.totalItems.low >>> 0, object.totalItems.high >>> 0).toNumber(true);
          }
        }
        if (object.page != null) {
          if ($Number(object.page) !== 0)
            message.page = object.page >>> 0;
        }
        if (object.pageSize != null) {
          if ($Number(object.pageSize) !== 0)
            message.pageSize = object.pageSize >>> 0;
        }
        if (object.totalPages != null) {
          if ($Number(object.totalPages) !== 0)
            message.totalPages = object.totalPages >>> 0;
        }
        return message;
      };
      PageResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.totalItems = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.totalItems = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.page = 0;
          object.pageSize = 0;
          object.totalPages = 0;
        }
        if (message.totalItems != null && $Object.hasOwnProperty.call(message, "totalItems"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.totalItems = typeof message.totalItems === "number" ? $BigInt(message.totalItems) : $util.Long.fromBits(message.totalItems.low >>> 0, message.totalItems.high >>> 0, true).toBigInt();
          else if (typeof message.totalItems === "number")
            object.totalItems = options.longs === $String ? $String(message.totalItems) : message.totalItems;
          else
            object.totalItems = options.longs === $String ? $util.Long.prototype.toString.call(message.totalItems) : options.longs === $Number ? new $util.LongBits(message.totalItems.low >>> 0, message.totalItems.high >>> 0).toNumber(true) : message.totalItems;
        if (message.page != null && $Object.hasOwnProperty.call(message, "page"))
          object.page = message.page;
        if (message.pageSize != null && $Object.hasOwnProperty.call(message, "pageSize"))
          object.pageSize = message.pageSize;
        if (message.totalPages != null && $Object.hasOwnProperty.call(message, "totalPages"))
          object.totalPages = message.totalPages;
        return object;
      };
      PageResponse.prototype.toJSON = function() {
        return PageResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      PageResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.PageResponse";
      };
      return PageResponse;
    })();
    common.SubmissionStatus = (function() {
      const valuesById = $Object.create(null), values = $Object.create(valuesById);
      values[valuesById[0] = "SUBMISSION_STATUS_UNSPECIFIED"] = 0;
      values[valuesById[1] = "SUBMISSION_STATUS_PENDING"] = 1;
      values[valuesById[2] = "SUBMISSION_STATUS_QUEUED"] = 2;
      values[valuesById[3] = "SUBMISSION_STATUS_JUDGING"] = 3;
      values[valuesById[4] = "SUBMISSION_STATUS_ACCEPTED"] = 4;
      values[valuesById[5] = "SUBMISSION_STATUS_WRONG_ANSWER"] = 5;
      values[valuesById[6] = "SUBMISSION_STATUS_COMPILE_ERROR"] = 6;
      values[valuesById[7] = "SUBMISSION_STATUS_MEMORY_LIMIT_EXCEEDED"] = 7;
      values[valuesById[8] = "SUBMISSION_STATUS_TIME_LIMIT_EXCEEDED"] = 8;
      values[valuesById[9] = "SUBMISSION_STATUS_RUNTIME_ERROR"] = 9;
      values[valuesById[10] = "SUBMISSION_STATUS_SYSTEM_ERROR"] = 10;
      values[valuesById[11] = "SUBMISSION_STATUS_CANCELLED"] = 11;
      return values;
    })();
    common.TestCaseResult = (function() {
      const TestCaseResult = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      TestCaseResult.prototype.index = 0;
      TestCaseResult.prototype.status = 0;
      TestCaseResult.prototype.timeUsedMs = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      TestCaseResult.prototype.memoryUsedBytes = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      TestCaseResult.prototype.input = "";
      TestCaseResult.prototype.expectedOutput = "";
      TestCaseResult.prototype.actualOutput = "";
      TestCaseResult.prototype.message = "";
      TestCaseResult.create = function(properties) {
        return new TestCaseResult(properties);
      };
      TestCaseResult.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.index != null && $Object.hasOwnProperty.call(message, "index") && message.index !== 0)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint32(message.index);
        if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== 0)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).int32(message.status);
        if (message.timeUsedMs != null && $Object.hasOwnProperty.call(message, "timeUsedMs") && (typeof message.timeUsedMs === "object" ? message.timeUsedMs.low || message.timeUsedMs.high : message.timeUsedMs !== 0))
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint64(message.timeUsedMs);
        if (message.memoryUsedBytes != null && $Object.hasOwnProperty.call(message, "memoryUsedBytes") && (typeof message.memoryUsedBytes === "object" ? message.memoryUsedBytes.low || message.memoryUsedBytes.high : message.memoryUsedBytes !== 0))
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).uint64(message.memoryUsedBytes);
        if (message.input != null && $Object.hasOwnProperty.call(message, "input") && message.input !== "")
          writer.uint32(
            /* id 5, wireType 2 =*/
            42
          ).string(message.input);
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput") && message.expectedOutput !== "")
          writer.uint32(
            /* id 6, wireType 2 =*/
            50
          ).string(message.expectedOutput);
        if (message.actualOutput != null && $Object.hasOwnProperty.call(message, "actualOutput") && message.actualOutput !== "")
          writer.uint32(
            /* id 7, wireType 2 =*/
            58
          ).string(message.actualOutput);
        if (message.message != null && $Object.hasOwnProperty.call(message, "message") && message.message !== "")
          writer.uint32(
            /* id 8, wireType 2 =*/
            66
          ).string(message.message);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      TestCaseResult.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      TestCaseResult.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.TestCaseResult(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.index = value;
              else
                delete message.index;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.int32())
                message.status = value;
              else
                delete message.status;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.timeUsedMs = value;
              else
                delete message.timeUsedMs;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.memoryUsedBytes = value;
              else
                delete message.memoryUsedBytes;
              continue;
            }
            case 5: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.input = value;
              else
                delete message.input;
              continue;
            }
            case 6: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.expectedOutput = value;
              else
                delete message.expectedOutput;
              continue;
            }
            case 7: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.actualOutput = value;
              else
                delete message.actualOutput;
              continue;
            }
            case 8: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.message = value;
              else
                delete message.message;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      TestCaseResult.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      TestCaseResult.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.index != null && $Object.hasOwnProperty.call(message, "index")) {
          if (!$util.isInteger(message.index))
            return "index: integer expected";
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          if (typeof message.status !== "number" || (message.status | 0) !== message.status)
            return "status: enum value expected";
        }
        if (message.timeUsedMs != null && $Object.hasOwnProperty.call(message, "timeUsedMs")) {
          if (!$util.isInteger(message.timeUsedMs) && !(message.timeUsedMs && $util.isInteger(message.timeUsedMs.low) && $util.isInteger(message.timeUsedMs.high)))
            return "timeUsedMs: integer|Long expected";
        }
        if (message.memoryUsedBytes != null && $Object.hasOwnProperty.call(message, "memoryUsedBytes")) {
          if (!$util.isInteger(message.memoryUsedBytes) && !(message.memoryUsedBytes && $util.isInteger(message.memoryUsedBytes.low) && $util.isInteger(message.memoryUsedBytes.high)))
            return "memoryUsedBytes: integer|Long expected";
        }
        if (message.input != null && $Object.hasOwnProperty.call(message, "input")) {
          if (!$util.isString(message.input))
            return "input: string expected";
        }
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput")) {
          if (!$util.isString(message.expectedOutput))
            return "expectedOutput: string expected";
        }
        if (message.actualOutput != null && $Object.hasOwnProperty.call(message, "actualOutput")) {
          if (!$util.isString(message.actualOutput))
            return "actualOutput: string expected";
        }
        if (message.message != null && $Object.hasOwnProperty.call(message, "message")) {
          if (!$util.isString(message.message))
            return "message: string expected";
        }
        return null;
      };
      TestCaseResult.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.TestCaseResult)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.TestCaseResult: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.TestCaseResult();
        if (object.index != null) {
          if ($Number(object.index) !== 0)
            message.index = object.index >>> 0;
        }
        if (object.status !== 0 && (typeof object.status !== "string" || $root.oj.common.SubmissionStatus[object.status] !== 0))
          switch (object.status) {
            case "SUBMISSION_STATUS_UNSPECIFIED":
            case 0:
              message.status = 0;
              break;
            case "SUBMISSION_STATUS_PENDING":
            case 1:
              message.status = 1;
              break;
            case "SUBMISSION_STATUS_QUEUED":
            case 2:
              message.status = 2;
              break;
            case "SUBMISSION_STATUS_JUDGING":
            case 3:
              message.status = 3;
              break;
            case "SUBMISSION_STATUS_ACCEPTED":
            case 4:
              message.status = 4;
              break;
            case "SUBMISSION_STATUS_WRONG_ANSWER":
            case 5:
              message.status = 5;
              break;
            case "SUBMISSION_STATUS_COMPILE_ERROR":
            case 6:
              message.status = 6;
              break;
            case "SUBMISSION_STATUS_MEMORY_LIMIT_EXCEEDED":
            case 7:
              message.status = 7;
              break;
            case "SUBMISSION_STATUS_TIME_LIMIT_EXCEEDED":
            case 8:
              message.status = 8;
              break;
            case "SUBMISSION_STATUS_RUNTIME_ERROR":
            case 9:
              message.status = 9;
              break;
            case "SUBMISSION_STATUS_SYSTEM_ERROR":
            case 10:
              message.status = 10;
              break;
            case "SUBMISSION_STATUS_CANCELLED":
            case 11:
              message.status = 11;
              break;
            default:
              if (typeof object.status === "number" && (object.status | 0) === object.status)
                message.status = object.status;
          }
        if (object.timeUsedMs != null) {
          if (typeof object.timeUsedMs === "object" ? object.timeUsedMs.low || object.timeUsedMs.high : $Number(object.timeUsedMs) !== 0) {
            if ($util.Long)
              message.timeUsedMs = $util.Long.fromValue(object.timeUsedMs, true);
            else if (typeof object.timeUsedMs === "string")
              message.timeUsedMs = $parseInt(object.timeUsedMs, 10);
            else if (typeof object.timeUsedMs === "number")
              message.timeUsedMs = object.timeUsedMs;
            else if (typeof object.timeUsedMs === "object")
              message.timeUsedMs = new $util.LongBits(object.timeUsedMs.low >>> 0, object.timeUsedMs.high >>> 0).toNumber(true);
          }
        }
        if (object.memoryUsedBytes != null) {
          if (typeof object.memoryUsedBytes === "object" ? object.memoryUsedBytes.low || object.memoryUsedBytes.high : $Number(object.memoryUsedBytes) !== 0) {
            if ($util.Long)
              message.memoryUsedBytes = $util.Long.fromValue(object.memoryUsedBytes, true);
            else if (typeof object.memoryUsedBytes === "string")
              message.memoryUsedBytes = $parseInt(object.memoryUsedBytes, 10);
            else if (typeof object.memoryUsedBytes === "number")
              message.memoryUsedBytes = object.memoryUsedBytes;
            else if (typeof object.memoryUsedBytes === "object")
              message.memoryUsedBytes = new $util.LongBits(object.memoryUsedBytes.low >>> 0, object.memoryUsedBytes.high >>> 0).toNumber(true);
          }
        }
        if (object.input != null) {
          if (typeof object.input !== "string" || object.input.length)
            message.input = $String(object.input);
        }
        if (object.expectedOutput != null) {
          if (typeof object.expectedOutput !== "string" || object.expectedOutput.length)
            message.expectedOutput = $String(object.expectedOutput);
        }
        if (object.actualOutput != null) {
          if (typeof object.actualOutput !== "string" || object.actualOutput.length)
            message.actualOutput = $String(object.actualOutput);
        }
        if (object.message != null) {
          if (typeof object.message !== "string" || object.message.length)
            message.message = $String(object.message);
        }
        return message;
      };
      TestCaseResult.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.index = 0;
          object.status = options.enums === $String ? "SUBMISSION_STATUS_UNSPECIFIED" : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.timeUsedMs = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.timeUsedMs = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.memoryUsedBytes = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.memoryUsedBytes = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.input = "";
          object.expectedOutput = "";
          object.actualOutput = "";
          object.message = "";
        }
        if (message.index != null && $Object.hasOwnProperty.call(message, "index"))
          object.index = message.index;
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = options.enums === $String ? $root.oj.common.SubmissionStatus[message.status] === $undefined ? message.status : $root.oj.common.SubmissionStatus[message.status] : message.status;
        if (message.timeUsedMs != null && $Object.hasOwnProperty.call(message, "timeUsedMs"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.timeUsedMs = typeof message.timeUsedMs === "number" ? $BigInt(message.timeUsedMs) : $util.Long.fromBits(message.timeUsedMs.low >>> 0, message.timeUsedMs.high >>> 0, true).toBigInt();
          else if (typeof message.timeUsedMs === "number")
            object.timeUsedMs = options.longs === $String ? $String(message.timeUsedMs) : message.timeUsedMs;
          else
            object.timeUsedMs = options.longs === $String ? $util.Long.prototype.toString.call(message.timeUsedMs) : options.longs === $Number ? new $util.LongBits(message.timeUsedMs.low >>> 0, message.timeUsedMs.high >>> 0).toNumber(true) : message.timeUsedMs;
        if (message.memoryUsedBytes != null && $Object.hasOwnProperty.call(message, "memoryUsedBytes"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.memoryUsedBytes = typeof message.memoryUsedBytes === "number" ? $BigInt(message.memoryUsedBytes) : $util.Long.fromBits(message.memoryUsedBytes.low >>> 0, message.memoryUsedBytes.high >>> 0, true).toBigInt();
          else if (typeof message.memoryUsedBytes === "number")
            object.memoryUsedBytes = options.longs === $String ? $String(message.memoryUsedBytes) : message.memoryUsedBytes;
          else
            object.memoryUsedBytes = options.longs === $String ? $util.Long.prototype.toString.call(message.memoryUsedBytes) : options.longs === $Number ? new $util.LongBits(message.memoryUsedBytes.low >>> 0, message.memoryUsedBytes.high >>> 0).toNumber(true) : message.memoryUsedBytes;
        if (message.input != null && $Object.hasOwnProperty.call(message, "input"))
          object.input = message.input;
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput"))
          object.expectedOutput = message.expectedOutput;
        if (message.actualOutput != null && $Object.hasOwnProperty.call(message, "actualOutput"))
          object.actualOutput = message.actualOutput;
        if (message.message != null && $Object.hasOwnProperty.call(message, "message"))
          object.message = message.message;
        return object;
      };
      TestCaseResult.prototype.toJSON = function() {
        return TestCaseResult.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      TestCaseResult.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.TestCaseResult";
      };
      return TestCaseResult;
    })();
    common.JudgeResult = (function() {
      const JudgeResult = function(properties) {
        this.testCaseResults = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      JudgeResult.prototype.status = 0;
      JudgeResult.prototype.timeUsedMs = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      JudgeResult.prototype.memoryUsedBytes = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      JudgeResult.prototype.compileError = "";
      JudgeResult.prototype.testCaseResults = $util.emptyArray;
      JudgeResult.prototype.completedAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      JudgeResult.create = function(properties) {
        return new JudgeResult(properties);
      };
      JudgeResult.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== 0)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).int32(message.status);
        if (message.timeUsedMs != null && $Object.hasOwnProperty.call(message, "timeUsedMs") && (typeof message.timeUsedMs === "object" ? message.timeUsedMs.low || message.timeUsedMs.high : message.timeUsedMs !== 0))
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint64(message.timeUsedMs);
        if (message.memoryUsedBytes != null && $Object.hasOwnProperty.call(message, "memoryUsedBytes") && (typeof message.memoryUsedBytes === "object" ? message.memoryUsedBytes.low || message.memoryUsedBytes.high : message.memoryUsedBytes !== 0))
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint64(message.memoryUsedBytes);
        if (message.compileError != null && $Object.hasOwnProperty.call(message, "compileError") && message.compileError !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.compileError);
        if (message.testCaseResults != null && message.testCaseResults.length)
          for (let i = 0; i < message.testCaseResults.length; ++i)
            $root.oj.common.TestCaseResult.encode(message.testCaseResults[i], writer.uint32(
              /* id 5, wireType 2 =*/
              42
            ).fork(), _depth + 1).ldelim();
        if (message.completedAt != null && $Object.hasOwnProperty.call(message, "completedAt") && (typeof message.completedAt === "object" ? message.completedAt.low || message.completedAt.high : message.completedAt !== 0))
          writer.uint32(
            /* id 6, wireType 0 =*/
            48
          ).int64(message.completedAt);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      JudgeResult.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      JudgeResult.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.JudgeResult(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.int32())
                message.status = value;
              else
                delete message.status;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.timeUsedMs = value;
              else
                delete message.timeUsedMs;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.memoryUsedBytes = value;
              else
                delete message.memoryUsedBytes;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.compileError = value;
              else
                delete message.compileError;
              continue;
            }
            case 5: {
              if (wireType !== 2)
                break;
              if (!(message.testCaseResults && message.testCaseResults.length))
                message.testCaseResults = [];
              message.testCaseResults.push($root.oj.common.TestCaseResult.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
            case 6: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.completedAt = value;
              else
                delete message.completedAt;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      JudgeResult.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      JudgeResult.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          if (typeof message.status !== "number" || (message.status | 0) !== message.status)
            return "status: enum value expected";
        }
        if (message.timeUsedMs != null && $Object.hasOwnProperty.call(message, "timeUsedMs")) {
          if (!$util.isInteger(message.timeUsedMs) && !(message.timeUsedMs && $util.isInteger(message.timeUsedMs.low) && $util.isInteger(message.timeUsedMs.high)))
            return "timeUsedMs: integer|Long expected";
        }
        if (message.memoryUsedBytes != null && $Object.hasOwnProperty.call(message, "memoryUsedBytes")) {
          if (!$util.isInteger(message.memoryUsedBytes) && !(message.memoryUsedBytes && $util.isInteger(message.memoryUsedBytes.low) && $util.isInteger(message.memoryUsedBytes.high)))
            return "memoryUsedBytes: integer|Long expected";
        }
        if (message.compileError != null && $Object.hasOwnProperty.call(message, "compileError")) {
          if (!$util.isString(message.compileError))
            return "compileError: string expected";
        }
        if (message.testCaseResults != null && $Object.hasOwnProperty.call(message, "testCaseResults")) {
          if (!$Array.isArray(message.testCaseResults))
            return "testCaseResults: array expected";
          for (let i = 0; i < message.testCaseResults.length; ++i) {
            let error = $root.oj.common.TestCaseResult.verify(message.testCaseResults[i], _depth + 1);
            if (error)
              return "testCaseResults." + error;
          }
        }
        if (message.completedAt != null && $Object.hasOwnProperty.call(message, "completedAt")) {
          if (!$util.isInteger(message.completedAt) && !(message.completedAt && $util.isInteger(message.completedAt.low) && $util.isInteger(message.completedAt.high)))
            return "completedAt: integer|Long expected";
        }
        return null;
      };
      JudgeResult.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.JudgeResult)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.JudgeResult: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.JudgeResult();
        if (object.status !== 0 && (typeof object.status !== "string" || $root.oj.common.SubmissionStatus[object.status] !== 0))
          switch (object.status) {
            case "SUBMISSION_STATUS_UNSPECIFIED":
            case 0:
              message.status = 0;
              break;
            case "SUBMISSION_STATUS_PENDING":
            case 1:
              message.status = 1;
              break;
            case "SUBMISSION_STATUS_QUEUED":
            case 2:
              message.status = 2;
              break;
            case "SUBMISSION_STATUS_JUDGING":
            case 3:
              message.status = 3;
              break;
            case "SUBMISSION_STATUS_ACCEPTED":
            case 4:
              message.status = 4;
              break;
            case "SUBMISSION_STATUS_WRONG_ANSWER":
            case 5:
              message.status = 5;
              break;
            case "SUBMISSION_STATUS_COMPILE_ERROR":
            case 6:
              message.status = 6;
              break;
            case "SUBMISSION_STATUS_MEMORY_LIMIT_EXCEEDED":
            case 7:
              message.status = 7;
              break;
            case "SUBMISSION_STATUS_TIME_LIMIT_EXCEEDED":
            case 8:
              message.status = 8;
              break;
            case "SUBMISSION_STATUS_RUNTIME_ERROR":
            case 9:
              message.status = 9;
              break;
            case "SUBMISSION_STATUS_SYSTEM_ERROR":
            case 10:
              message.status = 10;
              break;
            case "SUBMISSION_STATUS_CANCELLED":
            case 11:
              message.status = 11;
              break;
            default:
              if (typeof object.status === "number" && (object.status | 0) === object.status)
                message.status = object.status;
          }
        if (object.timeUsedMs != null) {
          if (typeof object.timeUsedMs === "object" ? object.timeUsedMs.low || object.timeUsedMs.high : $Number(object.timeUsedMs) !== 0) {
            if ($util.Long)
              message.timeUsedMs = $util.Long.fromValue(object.timeUsedMs, true);
            else if (typeof object.timeUsedMs === "string")
              message.timeUsedMs = $parseInt(object.timeUsedMs, 10);
            else if (typeof object.timeUsedMs === "number")
              message.timeUsedMs = object.timeUsedMs;
            else if (typeof object.timeUsedMs === "object")
              message.timeUsedMs = new $util.LongBits(object.timeUsedMs.low >>> 0, object.timeUsedMs.high >>> 0).toNumber(true);
          }
        }
        if (object.memoryUsedBytes != null) {
          if (typeof object.memoryUsedBytes === "object" ? object.memoryUsedBytes.low || object.memoryUsedBytes.high : $Number(object.memoryUsedBytes) !== 0) {
            if ($util.Long)
              message.memoryUsedBytes = $util.Long.fromValue(object.memoryUsedBytes, true);
            else if (typeof object.memoryUsedBytes === "string")
              message.memoryUsedBytes = $parseInt(object.memoryUsedBytes, 10);
            else if (typeof object.memoryUsedBytes === "number")
              message.memoryUsedBytes = object.memoryUsedBytes;
            else if (typeof object.memoryUsedBytes === "object")
              message.memoryUsedBytes = new $util.LongBits(object.memoryUsedBytes.low >>> 0, object.memoryUsedBytes.high >>> 0).toNumber(true);
          }
        }
        if (object.compileError != null) {
          if (typeof object.compileError !== "string" || object.compileError.length)
            message.compileError = $String(object.compileError);
        }
        if (object.testCaseResults) {
          if (!$Array.isArray(object.testCaseResults))
            throw $TypeError(".oj.common.JudgeResult.testCaseResults: array expected");
          message.testCaseResults = $Array(object.testCaseResults.length);
          for (let i = 0; i < object.testCaseResults.length; ++i) {
            if (!$util.isObject(object.testCaseResults[i]))
              throw $TypeError(".oj.common.JudgeResult.testCaseResults: object expected");
            message.testCaseResults[i] = $root.oj.common.TestCaseResult.fromObject(object.testCaseResults[i], _depth + 1);
          }
        }
        if (object.completedAt != null) {
          if (typeof object.completedAt === "object" ? object.completedAt.low || object.completedAt.high : $Number(object.completedAt) !== 0) {
            if ($util.Long)
              message.completedAt = $util.Long.fromValue(object.completedAt, false);
            else if (typeof object.completedAt === "string")
              message.completedAt = $parseInt(object.completedAt, 10);
            else if (typeof object.completedAt === "number")
              message.completedAt = object.completedAt;
            else if (typeof object.completedAt === "object")
              message.completedAt = new $util.LongBits(object.completedAt.low >>> 0, object.completedAt.high >>> 0).toNumber();
          }
        }
        return message;
      };
      JudgeResult.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.testCaseResults = [];
        if (options.defaults) {
          object.status = options.enums === $String ? "SUBMISSION_STATUS_UNSPECIFIED" : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.timeUsedMs = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.timeUsedMs = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.memoryUsedBytes = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.memoryUsedBytes = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.compileError = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.completedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.completedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = options.enums === $String ? $root.oj.common.SubmissionStatus[message.status] === $undefined ? message.status : $root.oj.common.SubmissionStatus[message.status] : message.status;
        if (message.timeUsedMs != null && $Object.hasOwnProperty.call(message, "timeUsedMs"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.timeUsedMs = typeof message.timeUsedMs === "number" ? $BigInt(message.timeUsedMs) : $util.Long.fromBits(message.timeUsedMs.low >>> 0, message.timeUsedMs.high >>> 0, true).toBigInt();
          else if (typeof message.timeUsedMs === "number")
            object.timeUsedMs = options.longs === $String ? $String(message.timeUsedMs) : message.timeUsedMs;
          else
            object.timeUsedMs = options.longs === $String ? $util.Long.prototype.toString.call(message.timeUsedMs) : options.longs === $Number ? new $util.LongBits(message.timeUsedMs.low >>> 0, message.timeUsedMs.high >>> 0).toNumber(true) : message.timeUsedMs;
        if (message.memoryUsedBytes != null && $Object.hasOwnProperty.call(message, "memoryUsedBytes"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.memoryUsedBytes = typeof message.memoryUsedBytes === "number" ? $BigInt(message.memoryUsedBytes) : $util.Long.fromBits(message.memoryUsedBytes.low >>> 0, message.memoryUsedBytes.high >>> 0, true).toBigInt();
          else if (typeof message.memoryUsedBytes === "number")
            object.memoryUsedBytes = options.longs === $String ? $String(message.memoryUsedBytes) : message.memoryUsedBytes;
          else
            object.memoryUsedBytes = options.longs === $String ? $util.Long.prototype.toString.call(message.memoryUsedBytes) : options.longs === $Number ? new $util.LongBits(message.memoryUsedBytes.low >>> 0, message.memoryUsedBytes.high >>> 0).toNumber(true) : message.memoryUsedBytes;
        if (message.compileError != null && $Object.hasOwnProperty.call(message, "compileError"))
          object.compileError = message.compileError;
        if (message.testCaseResults && message.testCaseResults.length) {
          object.testCaseResults = $Array(message.testCaseResults.length);
          for (let j = 0; j < message.testCaseResults.length; ++j)
            object.testCaseResults[j] = $root.oj.common.TestCaseResult.toObject(message.testCaseResults[j], options, _depth + 1);
        }
        if (message.completedAt != null && $Object.hasOwnProperty.call(message, "completedAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.completedAt = typeof message.completedAt === "number" ? $BigInt(message.completedAt) : $util.Long.fromBits(message.completedAt.low >>> 0, message.completedAt.high >>> 0, false).toBigInt();
          else if (typeof message.completedAt === "number")
            object.completedAt = options.longs === $String ? $String(message.completedAt) : message.completedAt;
          else
            object.completedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.completedAt) : options.longs === $Number ? new $util.LongBits(message.completedAt.low >>> 0, message.completedAt.high >>> 0).toNumber() : message.completedAt;
        return object;
      };
      JudgeResult.prototype.toJSON = function() {
        return JudgeResult.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      JudgeResult.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.JudgeResult";
      };
      return JudgeResult;
    })();
    common.User = (function() {
      const User = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      User.prototype.uid = 0;
      User.prototype.name = "";
      User.prototype.email = "";
      User.prototype.createTime = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      User.prototype.lastLogin = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      User.prototype.role = 0;
      User.prototype.avatar = null;
      User.prototype.disabled = false;
      User.create = function(properties) {
        return new User(properties);
      };
      User.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid") && message.uid !== 0)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint32(message.uid);
        if (message.name != null && $Object.hasOwnProperty.call(message, "name") && message.name !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.name);
        if (message.email != null && $Object.hasOwnProperty.call(message, "email") && message.email !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.email);
        if (message.createTime != null && $Object.hasOwnProperty.call(message, "createTime") && (typeof message.createTime === "object" ? message.createTime.low || message.createTime.high : message.createTime !== 0))
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).int64(message.createTime);
        if (message.lastLogin != null && $Object.hasOwnProperty.call(message, "lastLogin") && (typeof message.lastLogin === "object" ? message.lastLogin.low || message.lastLogin.high : message.lastLogin !== 0))
          writer.uint32(
            /* id 5, wireType 0 =*/
            40
          ).int64(message.lastLogin);
        if (message.role != null && $Object.hasOwnProperty.call(message, "role") && message.role !== 0)
          writer.uint32(
            /* id 6, wireType 0 =*/
            48
          ).int32(message.role);
        if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar"))
          $root.oj.common.AvatarMetadata.encode(message.avatar, writer.uint32(
            /* id 7, wireType 2 =*/
            58
          ).fork(), _depth + 1).ldelim();
        if (message.disabled != null && $Object.hasOwnProperty.call(message, "disabled") && message.disabled !== false)
          writer.uint32(
            /* id 8, wireType 0 =*/
            64
          ).bool(message.disabled);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      User.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      User.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.User(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.uid = value;
              else
                delete message.uid;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.name = value;
              else
                delete message.name;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.email = value;
              else
                delete message.email;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.createTime = value;
              else
                delete message.createTime;
              continue;
            }
            case 5: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.lastLogin = value;
              else
                delete message.lastLogin;
              continue;
            }
            case 6: {
              if (wireType !== 0)
                break;
              if (value = reader.int32())
                message.role = value;
              else
                delete message.role;
              continue;
            }
            case 7: {
              if (wireType !== 2)
                break;
              message.avatar = $root.oj.common.AvatarMetadata.decode(reader, reader.uint32(), $undefined, _depth + 1, message.avatar);
              continue;
            }
            case 8: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.disabled = value;
              else
                delete message.disabled;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      User.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      User.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
          if (!$util.isInteger(message.uid))
            return "uid: integer expected";
        }
        if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
          if (!$util.isString(message.name))
            return "name: string expected";
        }
        if (message.email != null && $Object.hasOwnProperty.call(message, "email")) {
          if (!$util.isString(message.email))
            return "email: string expected";
        }
        if (message.createTime != null && $Object.hasOwnProperty.call(message, "createTime")) {
          if (!$util.isInteger(message.createTime) && !(message.createTime && $util.isInteger(message.createTime.low) && $util.isInteger(message.createTime.high)))
            return "createTime: integer|Long expected";
        }
        if (message.lastLogin != null && $Object.hasOwnProperty.call(message, "lastLogin")) {
          if (!$util.isInteger(message.lastLogin) && !(message.lastLogin && $util.isInteger(message.lastLogin.low) && $util.isInteger(message.lastLogin.high)))
            return "lastLogin: integer|Long expected";
        }
        if (message.role != null && $Object.hasOwnProperty.call(message, "role")) {
          if (!$util.isInteger(message.role))
            return "role: integer expected";
        }
        if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar")) {
          let error = $root.oj.common.AvatarMetadata.verify(message.avatar, _depth + 1);
          if (error)
            return "avatar." + error;
        }
        if (message.disabled != null && $Object.hasOwnProperty.call(message, "disabled")) {
          if (typeof message.disabled !== "boolean")
            return "disabled: boolean expected";
        }
        return null;
      };
      User.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.User)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.User: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.User();
        if (object.uid != null) {
          if ($Number(object.uid) !== 0)
            message.uid = object.uid >>> 0;
        }
        if (object.name != null) {
          if (typeof object.name !== "string" || object.name.length)
            message.name = $String(object.name);
        }
        if (object.email != null) {
          if (typeof object.email !== "string" || object.email.length)
            message.email = $String(object.email);
        }
        if (object.createTime != null) {
          if (typeof object.createTime === "object" ? object.createTime.low || object.createTime.high : $Number(object.createTime) !== 0) {
            if ($util.Long)
              message.createTime = $util.Long.fromValue(object.createTime, false);
            else if (typeof object.createTime === "string")
              message.createTime = $parseInt(object.createTime, 10);
            else if (typeof object.createTime === "number")
              message.createTime = object.createTime;
            else if (typeof object.createTime === "object")
              message.createTime = new $util.LongBits(object.createTime.low >>> 0, object.createTime.high >>> 0).toNumber();
          }
        }
        if (object.lastLogin != null) {
          if (typeof object.lastLogin === "object" ? object.lastLogin.low || object.lastLogin.high : $Number(object.lastLogin) !== 0) {
            if ($util.Long)
              message.lastLogin = $util.Long.fromValue(object.lastLogin, false);
            else if (typeof object.lastLogin === "string")
              message.lastLogin = $parseInt(object.lastLogin, 10);
            else if (typeof object.lastLogin === "number")
              message.lastLogin = object.lastLogin;
            else if (typeof object.lastLogin === "object")
              message.lastLogin = new $util.LongBits(object.lastLogin.low >>> 0, object.lastLogin.high >>> 0).toNumber();
          }
        }
        if (object.role != null) {
          if ($Number(object.role) !== 0)
            message.role = object.role | 0;
        }
        if (object.avatar != null) {
          if (!$util.isObject(object.avatar))
            throw $TypeError(".oj.common.User.avatar: object expected");
          message.avatar = $root.oj.common.AvatarMetadata.fromObject(object.avatar, _depth + 1);
        }
        if (object.disabled != null) {
          if (object.disabled)
            message.disabled = $Boolean(object.disabled);
        }
        return message;
      };
      User.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.uid = 0;
          object.name = "";
          object.email = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.createTime = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.createTime = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.lastLogin = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.lastLogin = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.role = 0;
          object.avatar = null;
          object.disabled = false;
        }
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
          object.uid = message.uid;
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
          object.name = message.name;
        if (message.email != null && $Object.hasOwnProperty.call(message, "email"))
          object.email = message.email;
        if (message.createTime != null && $Object.hasOwnProperty.call(message, "createTime"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.createTime = typeof message.createTime === "number" ? $BigInt(message.createTime) : $util.Long.fromBits(message.createTime.low >>> 0, message.createTime.high >>> 0, false).toBigInt();
          else if (typeof message.createTime === "number")
            object.createTime = options.longs === $String ? $String(message.createTime) : message.createTime;
          else
            object.createTime = options.longs === $String ? $util.Long.prototype.toString.call(message.createTime) : options.longs === $Number ? new $util.LongBits(message.createTime.low >>> 0, message.createTime.high >>> 0).toNumber() : message.createTime;
        if (message.lastLogin != null && $Object.hasOwnProperty.call(message, "lastLogin"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.lastLogin = typeof message.lastLogin === "number" ? $BigInt(message.lastLogin) : $util.Long.fromBits(message.lastLogin.low >>> 0, message.lastLogin.high >>> 0, false).toBigInt();
          else if (typeof message.lastLogin === "number")
            object.lastLogin = options.longs === $String ? $String(message.lastLogin) : message.lastLogin;
          else
            object.lastLogin = options.longs === $String ? $util.Long.prototype.toString.call(message.lastLogin) : options.longs === $Number ? new $util.LongBits(message.lastLogin.low >>> 0, message.lastLogin.high >>> 0).toNumber() : message.lastLogin;
        if (message.role != null && $Object.hasOwnProperty.call(message, "role"))
          object.role = message.role;
        if (message.avatar != null && $Object.hasOwnProperty.call(message, "avatar"))
          object.avatar = $root.oj.common.AvatarMetadata.toObject(message.avatar, options, _depth + 1);
        if (message.disabled != null && $Object.hasOwnProperty.call(message, "disabled"))
          object.disabled = message.disabled;
        return object;
      };
      User.prototype.toJSON = function() {
        return User.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      User.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.User";
      };
      return User;
    })();
    common.AvatarMetadata = (function() {
      const AvatarMetadata = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AvatarMetadata.prototype.url = "";
      AvatarMetadata.prototype.contentType = "";
      AvatarMetadata.prototype.sizeBytes = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      AvatarMetadata.prototype.updatedAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      AvatarMetadata.create = function(properties) {
        return new AvatarMetadata(properties);
      };
      AvatarMetadata.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.url != null && $Object.hasOwnProperty.call(message, "url") && message.url !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.url);
        if (message.contentType != null && $Object.hasOwnProperty.call(message, "contentType") && message.contentType !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.contentType);
        if (message.sizeBytes != null && $Object.hasOwnProperty.call(message, "sizeBytes") && (typeof message.sizeBytes === "object" ? message.sizeBytes.low || message.sizeBytes.high : message.sizeBytes !== 0))
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint64(message.sizeBytes);
        if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt") && (typeof message.updatedAt === "object" ? message.updatedAt.low || message.updatedAt.high : message.updatedAt !== 0))
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).int64(message.updatedAt);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AvatarMetadata.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AvatarMetadata.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.AvatarMetadata(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.url = value;
              else
                delete message.url;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.contentType = value;
              else
                delete message.contentType;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.sizeBytes = value;
              else
                delete message.sizeBytes;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.updatedAt = value;
              else
                delete message.updatedAt;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AvatarMetadata.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AvatarMetadata.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.url != null && $Object.hasOwnProperty.call(message, "url")) {
          if (!$util.isString(message.url))
            return "url: string expected";
        }
        if (message.contentType != null && $Object.hasOwnProperty.call(message, "contentType")) {
          if (!$util.isString(message.contentType))
            return "contentType: string expected";
        }
        if (message.sizeBytes != null && $Object.hasOwnProperty.call(message, "sizeBytes")) {
          if (!$util.isInteger(message.sizeBytes) && !(message.sizeBytes && $util.isInteger(message.sizeBytes.low) && $util.isInteger(message.sizeBytes.high)))
            return "sizeBytes: integer|Long expected";
        }
        if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt")) {
          if (!$util.isInteger(message.updatedAt) && !(message.updatedAt && $util.isInteger(message.updatedAt.low) && $util.isInteger(message.updatedAt.high)))
            return "updatedAt: integer|Long expected";
        }
        return null;
      };
      AvatarMetadata.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.AvatarMetadata)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.AvatarMetadata: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.AvatarMetadata();
        if (object.url != null) {
          if (typeof object.url !== "string" || object.url.length)
            message.url = $String(object.url);
        }
        if (object.contentType != null) {
          if (typeof object.contentType !== "string" || object.contentType.length)
            message.contentType = $String(object.contentType);
        }
        if (object.sizeBytes != null) {
          if (typeof object.sizeBytes === "object" ? object.sizeBytes.low || object.sizeBytes.high : $Number(object.sizeBytes) !== 0) {
            if ($util.Long)
              message.sizeBytes = $util.Long.fromValue(object.sizeBytes, true);
            else if (typeof object.sizeBytes === "string")
              message.sizeBytes = $parseInt(object.sizeBytes, 10);
            else if (typeof object.sizeBytes === "number")
              message.sizeBytes = object.sizeBytes;
            else if (typeof object.sizeBytes === "object")
              message.sizeBytes = new $util.LongBits(object.sizeBytes.low >>> 0, object.sizeBytes.high >>> 0).toNumber(true);
          }
        }
        if (object.updatedAt != null) {
          if (typeof object.updatedAt === "object" ? object.updatedAt.low || object.updatedAt.high : $Number(object.updatedAt) !== 0) {
            if ($util.Long)
              message.updatedAt = $util.Long.fromValue(object.updatedAt, false);
            else if (typeof object.updatedAt === "string")
              message.updatedAt = $parseInt(object.updatedAt, 10);
            else if (typeof object.updatedAt === "number")
              message.updatedAt = object.updatedAt;
            else if (typeof object.updatedAt === "object")
              message.updatedAt = new $util.LongBits(object.updatedAt.low >>> 0, object.updatedAt.high >>> 0).toNumber();
          }
        }
        return message;
      };
      AvatarMetadata.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.url = "";
          object.contentType = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.sizeBytes = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.sizeBytes = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.updatedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.updatedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        }
        if (message.url != null && $Object.hasOwnProperty.call(message, "url"))
          object.url = message.url;
        if (message.contentType != null && $Object.hasOwnProperty.call(message, "contentType"))
          object.contentType = message.contentType;
        if (message.sizeBytes != null && $Object.hasOwnProperty.call(message, "sizeBytes"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.sizeBytes = typeof message.sizeBytes === "number" ? $BigInt(message.sizeBytes) : $util.Long.fromBits(message.sizeBytes.low >>> 0, message.sizeBytes.high >>> 0, true).toBigInt();
          else if (typeof message.sizeBytes === "number")
            object.sizeBytes = options.longs === $String ? $String(message.sizeBytes) : message.sizeBytes;
          else
            object.sizeBytes = options.longs === $String ? $util.Long.prototype.toString.call(message.sizeBytes) : options.longs === $Number ? new $util.LongBits(message.sizeBytes.low >>> 0, message.sizeBytes.high >>> 0).toNumber(true) : message.sizeBytes;
        if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.updatedAt = typeof message.updatedAt === "number" ? $BigInt(message.updatedAt) : $util.Long.fromBits(message.updatedAt.low >>> 0, message.updatedAt.high >>> 0, false).toBigInt();
          else if (typeof message.updatedAt === "number")
            object.updatedAt = options.longs === $String ? $String(message.updatedAt) : message.updatedAt;
          else
            object.updatedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.updatedAt) : options.longs === $Number ? new $util.LongBits(message.updatedAt.low >>> 0, message.updatedAt.high >>> 0).toNumber() : message.updatedAt;
        return object;
      };
      AvatarMetadata.prototype.toJSON = function() {
        return AvatarMetadata.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AvatarMetadata.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.AvatarMetadata";
      };
      return AvatarMetadata;
    })();
    common.UserStatistics = (function() {
      const UserStatistics = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      UserStatistics.prototype.totalSubmissions = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      UserStatistics.prototype.acceptedSubmissions = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      UserStatistics.prototype.acceptedQuestions = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      UserStatistics.prototype.totalSolutions = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      UserStatistics.prototype.totalLikesReceived = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      UserStatistics.create = function(properties) {
        return new UserStatistics(properties);
      };
      UserStatistics.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.totalSubmissions != null && $Object.hasOwnProperty.call(message, "totalSubmissions") && (typeof message.totalSubmissions === "object" ? message.totalSubmissions.low || message.totalSubmissions.high : message.totalSubmissions !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.totalSubmissions);
        if (message.acceptedSubmissions != null && $Object.hasOwnProperty.call(message, "acceptedSubmissions") && (typeof message.acceptedSubmissions === "object" ? message.acceptedSubmissions.low || message.acceptedSubmissions.high : message.acceptedSubmissions !== 0))
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint64(message.acceptedSubmissions);
        if (message.acceptedQuestions != null && $Object.hasOwnProperty.call(message, "acceptedQuestions") && (typeof message.acceptedQuestions === "object" ? message.acceptedQuestions.low || message.acceptedQuestions.high : message.acceptedQuestions !== 0))
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint64(message.acceptedQuestions);
        if (message.totalSolutions != null && $Object.hasOwnProperty.call(message, "totalSolutions") && (typeof message.totalSolutions === "object" ? message.totalSolutions.low || message.totalSolutions.high : message.totalSolutions !== 0))
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).uint64(message.totalSolutions);
        if (message.totalLikesReceived != null && $Object.hasOwnProperty.call(message, "totalLikesReceived") && (typeof message.totalLikesReceived === "object" ? message.totalLikesReceived.low || message.totalLikesReceived.high : message.totalLikesReceived !== 0))
          writer.uint32(
            /* id 5, wireType 0 =*/
            40
          ).uint64(message.totalLikesReceived);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      UserStatistics.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      UserStatistics.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.UserStatistics(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.totalSubmissions = value;
              else
                delete message.totalSubmissions;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.acceptedSubmissions = value;
              else
                delete message.acceptedSubmissions;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.acceptedQuestions = value;
              else
                delete message.acceptedQuestions;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.totalSolutions = value;
              else
                delete message.totalSolutions;
              continue;
            }
            case 5: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.totalLikesReceived = value;
              else
                delete message.totalLikesReceived;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      UserStatistics.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      UserStatistics.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.totalSubmissions != null && $Object.hasOwnProperty.call(message, "totalSubmissions")) {
          if (!$util.isInteger(message.totalSubmissions) && !(message.totalSubmissions && $util.isInteger(message.totalSubmissions.low) && $util.isInteger(message.totalSubmissions.high)))
            return "totalSubmissions: integer|Long expected";
        }
        if (message.acceptedSubmissions != null && $Object.hasOwnProperty.call(message, "acceptedSubmissions")) {
          if (!$util.isInteger(message.acceptedSubmissions) && !(message.acceptedSubmissions && $util.isInteger(message.acceptedSubmissions.low) && $util.isInteger(message.acceptedSubmissions.high)))
            return "acceptedSubmissions: integer|Long expected";
        }
        if (message.acceptedQuestions != null && $Object.hasOwnProperty.call(message, "acceptedQuestions")) {
          if (!$util.isInteger(message.acceptedQuestions) && !(message.acceptedQuestions && $util.isInteger(message.acceptedQuestions.low) && $util.isInteger(message.acceptedQuestions.high)))
            return "acceptedQuestions: integer|Long expected";
        }
        if (message.totalSolutions != null && $Object.hasOwnProperty.call(message, "totalSolutions")) {
          if (!$util.isInteger(message.totalSolutions) && !(message.totalSolutions && $util.isInteger(message.totalSolutions.low) && $util.isInteger(message.totalSolutions.high)))
            return "totalSolutions: integer|Long expected";
        }
        if (message.totalLikesReceived != null && $Object.hasOwnProperty.call(message, "totalLikesReceived")) {
          if (!$util.isInteger(message.totalLikesReceived) && !(message.totalLikesReceived && $util.isInteger(message.totalLikesReceived.low) && $util.isInteger(message.totalLikesReceived.high)))
            return "totalLikesReceived: integer|Long expected";
        }
        return null;
      };
      UserStatistics.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.UserStatistics)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.UserStatistics: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.UserStatistics();
        if (object.totalSubmissions != null) {
          if (typeof object.totalSubmissions === "object" ? object.totalSubmissions.low || object.totalSubmissions.high : $Number(object.totalSubmissions) !== 0) {
            if ($util.Long)
              message.totalSubmissions = $util.Long.fromValue(object.totalSubmissions, true);
            else if (typeof object.totalSubmissions === "string")
              message.totalSubmissions = $parseInt(object.totalSubmissions, 10);
            else if (typeof object.totalSubmissions === "number")
              message.totalSubmissions = object.totalSubmissions;
            else if (typeof object.totalSubmissions === "object")
              message.totalSubmissions = new $util.LongBits(object.totalSubmissions.low >>> 0, object.totalSubmissions.high >>> 0).toNumber(true);
          }
        }
        if (object.acceptedSubmissions != null) {
          if (typeof object.acceptedSubmissions === "object" ? object.acceptedSubmissions.low || object.acceptedSubmissions.high : $Number(object.acceptedSubmissions) !== 0) {
            if ($util.Long)
              message.acceptedSubmissions = $util.Long.fromValue(object.acceptedSubmissions, true);
            else if (typeof object.acceptedSubmissions === "string")
              message.acceptedSubmissions = $parseInt(object.acceptedSubmissions, 10);
            else if (typeof object.acceptedSubmissions === "number")
              message.acceptedSubmissions = object.acceptedSubmissions;
            else if (typeof object.acceptedSubmissions === "object")
              message.acceptedSubmissions = new $util.LongBits(object.acceptedSubmissions.low >>> 0, object.acceptedSubmissions.high >>> 0).toNumber(true);
          }
        }
        if (object.acceptedQuestions != null) {
          if (typeof object.acceptedQuestions === "object" ? object.acceptedQuestions.low || object.acceptedQuestions.high : $Number(object.acceptedQuestions) !== 0) {
            if ($util.Long)
              message.acceptedQuestions = $util.Long.fromValue(object.acceptedQuestions, true);
            else if (typeof object.acceptedQuestions === "string")
              message.acceptedQuestions = $parseInt(object.acceptedQuestions, 10);
            else if (typeof object.acceptedQuestions === "number")
              message.acceptedQuestions = object.acceptedQuestions;
            else if (typeof object.acceptedQuestions === "object")
              message.acceptedQuestions = new $util.LongBits(object.acceptedQuestions.low >>> 0, object.acceptedQuestions.high >>> 0).toNumber(true);
          }
        }
        if (object.totalSolutions != null) {
          if (typeof object.totalSolutions === "object" ? object.totalSolutions.low || object.totalSolutions.high : $Number(object.totalSolutions) !== 0) {
            if ($util.Long)
              message.totalSolutions = $util.Long.fromValue(object.totalSolutions, true);
            else if (typeof object.totalSolutions === "string")
              message.totalSolutions = $parseInt(object.totalSolutions, 10);
            else if (typeof object.totalSolutions === "number")
              message.totalSolutions = object.totalSolutions;
            else if (typeof object.totalSolutions === "object")
              message.totalSolutions = new $util.LongBits(object.totalSolutions.low >>> 0, object.totalSolutions.high >>> 0).toNumber(true);
          }
        }
        if (object.totalLikesReceived != null) {
          if (typeof object.totalLikesReceived === "object" ? object.totalLikesReceived.low || object.totalLikesReceived.high : $Number(object.totalLikesReceived) !== 0) {
            if ($util.Long)
              message.totalLikesReceived = $util.Long.fromValue(object.totalLikesReceived, true);
            else if (typeof object.totalLikesReceived === "string")
              message.totalLikesReceived = $parseInt(object.totalLikesReceived, 10);
            else if (typeof object.totalLikesReceived === "number")
              message.totalLikesReceived = object.totalLikesReceived;
            else if (typeof object.totalLikesReceived === "object")
              message.totalLikesReceived = new $util.LongBits(object.totalLikesReceived.low >>> 0, object.totalLikesReceived.high >>> 0).toNumber(true);
          }
        }
        return message;
      };
      UserStatistics.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.totalSubmissions = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.totalSubmissions = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.acceptedSubmissions = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.acceptedSubmissions = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.acceptedQuestions = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.acceptedQuestions = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.totalSolutions = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.totalSolutions = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.totalLikesReceived = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.totalLikesReceived = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        }
        if (message.totalSubmissions != null && $Object.hasOwnProperty.call(message, "totalSubmissions"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.totalSubmissions = typeof message.totalSubmissions === "number" ? $BigInt(message.totalSubmissions) : $util.Long.fromBits(message.totalSubmissions.low >>> 0, message.totalSubmissions.high >>> 0, true).toBigInt();
          else if (typeof message.totalSubmissions === "number")
            object.totalSubmissions = options.longs === $String ? $String(message.totalSubmissions) : message.totalSubmissions;
          else
            object.totalSubmissions = options.longs === $String ? $util.Long.prototype.toString.call(message.totalSubmissions) : options.longs === $Number ? new $util.LongBits(message.totalSubmissions.low >>> 0, message.totalSubmissions.high >>> 0).toNumber(true) : message.totalSubmissions;
        if (message.acceptedSubmissions != null && $Object.hasOwnProperty.call(message, "acceptedSubmissions"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.acceptedSubmissions = typeof message.acceptedSubmissions === "number" ? $BigInt(message.acceptedSubmissions) : $util.Long.fromBits(message.acceptedSubmissions.low >>> 0, message.acceptedSubmissions.high >>> 0, true).toBigInt();
          else if (typeof message.acceptedSubmissions === "number")
            object.acceptedSubmissions = options.longs === $String ? $String(message.acceptedSubmissions) : message.acceptedSubmissions;
          else
            object.acceptedSubmissions = options.longs === $String ? $util.Long.prototype.toString.call(message.acceptedSubmissions) : options.longs === $Number ? new $util.LongBits(message.acceptedSubmissions.low >>> 0, message.acceptedSubmissions.high >>> 0).toNumber(true) : message.acceptedSubmissions;
        if (message.acceptedQuestions != null && $Object.hasOwnProperty.call(message, "acceptedQuestions"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.acceptedQuestions = typeof message.acceptedQuestions === "number" ? $BigInt(message.acceptedQuestions) : $util.Long.fromBits(message.acceptedQuestions.low >>> 0, message.acceptedQuestions.high >>> 0, true).toBigInt();
          else if (typeof message.acceptedQuestions === "number")
            object.acceptedQuestions = options.longs === $String ? $String(message.acceptedQuestions) : message.acceptedQuestions;
          else
            object.acceptedQuestions = options.longs === $String ? $util.Long.prototype.toString.call(message.acceptedQuestions) : options.longs === $Number ? new $util.LongBits(message.acceptedQuestions.low >>> 0, message.acceptedQuestions.high >>> 0).toNumber(true) : message.acceptedQuestions;
        if (message.totalSolutions != null && $Object.hasOwnProperty.call(message, "totalSolutions"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.totalSolutions = typeof message.totalSolutions === "number" ? $BigInt(message.totalSolutions) : $util.Long.fromBits(message.totalSolutions.low >>> 0, message.totalSolutions.high >>> 0, true).toBigInt();
          else if (typeof message.totalSolutions === "number")
            object.totalSolutions = options.longs === $String ? $String(message.totalSolutions) : message.totalSolutions;
          else
            object.totalSolutions = options.longs === $String ? $util.Long.prototype.toString.call(message.totalSolutions) : options.longs === $Number ? new $util.LongBits(message.totalSolutions.low >>> 0, message.totalSolutions.high >>> 0).toNumber(true) : message.totalSolutions;
        if (message.totalLikesReceived != null && $Object.hasOwnProperty.call(message, "totalLikesReceived"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.totalLikesReceived = typeof message.totalLikesReceived === "number" ? $BigInt(message.totalLikesReceived) : $util.Long.fromBits(message.totalLikesReceived.low >>> 0, message.totalLikesReceived.high >>> 0, true).toBigInt();
          else if (typeof message.totalLikesReceived === "number")
            object.totalLikesReceived = options.longs === $String ? $String(message.totalLikesReceived) : message.totalLikesReceived;
          else
            object.totalLikesReceived = options.longs === $String ? $util.Long.prototype.toString.call(message.totalLikesReceived) : options.longs === $Number ? new $util.LongBits(message.totalLikesReceived.low >>> 0, message.totalLikesReceived.high >>> 0).toNumber(true) : message.totalLikesReceived;
        return object;
      };
      UserStatistics.prototype.toJSON = function() {
        return UserStatistics.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      UserStatistics.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.UserStatistics";
      };
      return UserStatistics;
    })();
    common.Question = (function() {
      const Question = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      Question.prototype.id = "";
      Question.prototype.title = "";
      Question.prototype.description = "";
      Question.prototype.difficulty = "";
      Question.prototype.timeLimitMs = 0;
      Question.prototype.memoryLimitMb = 0;
      Question.prototype.createTime = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      Question.prototype.updateTime = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      Question.prototype.visible = false;
      Question.create = function(properties) {
        return new Question(properties);
      };
      Question.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.id);
        if (message.title != null && $Object.hasOwnProperty.call(message, "title") && message.title !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.title);
        if (message.description != null && $Object.hasOwnProperty.call(message, "description") && message.description !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.description);
        if (message.difficulty != null && $Object.hasOwnProperty.call(message, "difficulty") && message.difficulty !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.difficulty);
        if (message.timeLimitMs != null && $Object.hasOwnProperty.call(message, "timeLimitMs") && message.timeLimitMs !== 0)
          writer.uint32(
            /* id 5, wireType 0 =*/
            40
          ).uint32(message.timeLimitMs);
        if (message.memoryLimitMb != null && $Object.hasOwnProperty.call(message, "memoryLimitMb") && message.memoryLimitMb !== 0)
          writer.uint32(
            /* id 6, wireType 0 =*/
            48
          ).uint32(message.memoryLimitMb);
        if (message.createTime != null && $Object.hasOwnProperty.call(message, "createTime") && (typeof message.createTime === "object" ? message.createTime.low || message.createTime.high : message.createTime !== 0))
          writer.uint32(
            /* id 7, wireType 0 =*/
            56
          ).int64(message.createTime);
        if (message.updateTime != null && $Object.hasOwnProperty.call(message, "updateTime") && (typeof message.updateTime === "object" ? message.updateTime.low || message.updateTime.high : message.updateTime !== 0))
          writer.uint32(
            /* id 8, wireType 0 =*/
            64
          ).int64(message.updateTime);
        if (message.visible != null && $Object.hasOwnProperty.call(message, "visible") && message.visible !== false)
          writer.uint32(
            /* id 9, wireType 0 =*/
            72
          ).bool(message.visible);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      Question.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      Question.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.Question(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.id = value;
              else
                delete message.id;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.title = value;
              else
                delete message.title;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.description = value;
              else
                delete message.description;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.difficulty = value;
              else
                delete message.difficulty;
              continue;
            }
            case 5: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.timeLimitMs = value;
              else
                delete message.timeLimitMs;
              continue;
            }
            case 6: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.memoryLimitMb = value;
              else
                delete message.memoryLimitMb;
              continue;
            }
            case 7: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.createTime = value;
              else
                delete message.createTime;
              continue;
            }
            case 8: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.updateTime = value;
              else
                delete message.updateTime;
              continue;
            }
            case 9: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.visible = value;
              else
                delete message.visible;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      Question.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      Question.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
          if (!$util.isString(message.id))
            return "id: string expected";
        }
        if (message.title != null && $Object.hasOwnProperty.call(message, "title")) {
          if (!$util.isString(message.title))
            return "title: string expected";
        }
        if (message.description != null && $Object.hasOwnProperty.call(message, "description")) {
          if (!$util.isString(message.description))
            return "description: string expected";
        }
        if (message.difficulty != null && $Object.hasOwnProperty.call(message, "difficulty")) {
          if (!$util.isString(message.difficulty))
            return "difficulty: string expected";
        }
        if (message.timeLimitMs != null && $Object.hasOwnProperty.call(message, "timeLimitMs")) {
          if (!$util.isInteger(message.timeLimitMs))
            return "timeLimitMs: integer expected";
        }
        if (message.memoryLimitMb != null && $Object.hasOwnProperty.call(message, "memoryLimitMb")) {
          if (!$util.isInteger(message.memoryLimitMb))
            return "memoryLimitMb: integer expected";
        }
        if (message.createTime != null && $Object.hasOwnProperty.call(message, "createTime")) {
          if (!$util.isInteger(message.createTime) && !(message.createTime && $util.isInteger(message.createTime.low) && $util.isInteger(message.createTime.high)))
            return "createTime: integer|Long expected";
        }
        if (message.updateTime != null && $Object.hasOwnProperty.call(message, "updateTime")) {
          if (!$util.isInteger(message.updateTime) && !(message.updateTime && $util.isInteger(message.updateTime.low) && $util.isInteger(message.updateTime.high)))
            return "updateTime: integer|Long expected";
        }
        if (message.visible != null && $Object.hasOwnProperty.call(message, "visible")) {
          if (typeof message.visible !== "boolean")
            return "visible: boolean expected";
        }
        return null;
      };
      Question.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.Question)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.Question: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.Question();
        if (object.id != null) {
          if (typeof object.id !== "string" || object.id.length)
            message.id = $String(object.id);
        }
        if (object.title != null) {
          if (typeof object.title !== "string" || object.title.length)
            message.title = $String(object.title);
        }
        if (object.description != null) {
          if (typeof object.description !== "string" || object.description.length)
            message.description = $String(object.description);
        }
        if (object.difficulty != null) {
          if (typeof object.difficulty !== "string" || object.difficulty.length)
            message.difficulty = $String(object.difficulty);
        }
        if (object.timeLimitMs != null) {
          if ($Number(object.timeLimitMs) !== 0)
            message.timeLimitMs = object.timeLimitMs >>> 0;
        }
        if (object.memoryLimitMb != null) {
          if ($Number(object.memoryLimitMb) !== 0)
            message.memoryLimitMb = object.memoryLimitMb >>> 0;
        }
        if (object.createTime != null) {
          if (typeof object.createTime === "object" ? object.createTime.low || object.createTime.high : $Number(object.createTime) !== 0) {
            if ($util.Long)
              message.createTime = $util.Long.fromValue(object.createTime, false);
            else if (typeof object.createTime === "string")
              message.createTime = $parseInt(object.createTime, 10);
            else if (typeof object.createTime === "number")
              message.createTime = object.createTime;
            else if (typeof object.createTime === "object")
              message.createTime = new $util.LongBits(object.createTime.low >>> 0, object.createTime.high >>> 0).toNumber();
          }
        }
        if (object.updateTime != null) {
          if (typeof object.updateTime === "object" ? object.updateTime.low || object.updateTime.high : $Number(object.updateTime) !== 0) {
            if ($util.Long)
              message.updateTime = $util.Long.fromValue(object.updateTime, false);
            else if (typeof object.updateTime === "string")
              message.updateTime = $parseInt(object.updateTime, 10);
            else if (typeof object.updateTime === "number")
              message.updateTime = object.updateTime;
            else if (typeof object.updateTime === "object")
              message.updateTime = new $util.LongBits(object.updateTime.low >>> 0, object.updateTime.high >>> 0).toNumber();
          }
        }
        if (object.visible != null) {
          if (object.visible)
            message.visible = $Boolean(object.visible);
        }
        return message;
      };
      Question.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.id = "";
          object.title = "";
          object.description = "";
          object.difficulty = "";
          object.timeLimitMs = 0;
          object.memoryLimitMb = 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.createTime = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.createTime = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.updateTime = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.updateTime = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.visible = false;
        }
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
          object.id = message.id;
        if (message.title != null && $Object.hasOwnProperty.call(message, "title"))
          object.title = message.title;
        if (message.description != null && $Object.hasOwnProperty.call(message, "description"))
          object.description = message.description;
        if (message.difficulty != null && $Object.hasOwnProperty.call(message, "difficulty"))
          object.difficulty = message.difficulty;
        if (message.timeLimitMs != null && $Object.hasOwnProperty.call(message, "timeLimitMs"))
          object.timeLimitMs = message.timeLimitMs;
        if (message.memoryLimitMb != null && $Object.hasOwnProperty.call(message, "memoryLimitMb"))
          object.memoryLimitMb = message.memoryLimitMb;
        if (message.createTime != null && $Object.hasOwnProperty.call(message, "createTime"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.createTime = typeof message.createTime === "number" ? $BigInt(message.createTime) : $util.Long.fromBits(message.createTime.low >>> 0, message.createTime.high >>> 0, false).toBigInt();
          else if (typeof message.createTime === "number")
            object.createTime = options.longs === $String ? $String(message.createTime) : message.createTime;
          else
            object.createTime = options.longs === $String ? $util.Long.prototype.toString.call(message.createTime) : options.longs === $Number ? new $util.LongBits(message.createTime.low >>> 0, message.createTime.high >>> 0).toNumber() : message.createTime;
        if (message.updateTime != null && $Object.hasOwnProperty.call(message, "updateTime"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.updateTime = typeof message.updateTime === "number" ? $BigInt(message.updateTime) : $util.Long.fromBits(message.updateTime.low >>> 0, message.updateTime.high >>> 0, false).toBigInt();
          else if (typeof message.updateTime === "number")
            object.updateTime = options.longs === $String ? $String(message.updateTime) : message.updateTime;
          else
            object.updateTime = options.longs === $String ? $util.Long.prototype.toString.call(message.updateTime) : options.longs === $Number ? new $util.LongBits(message.updateTime.low >>> 0, message.updateTime.high >>> 0).toNumber() : message.updateTime;
        if (message.visible != null && $Object.hasOwnProperty.call(message, "visible"))
          object.visible = message.visible;
        return object;
      };
      Question.prototype.toJSON = function() {
        return Question.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      Question.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.Question";
      };
      return Question;
    })();
    common.TestCase = (function() {
      const TestCase = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      TestCase.prototype.testCaseId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      TestCase.prototype.questionId = "";
      TestCase.prototype.ordinal = 0;
      TestCase.prototype.input = "";
      TestCase.prototype.expectedOutput = "";
      TestCase.prototype.isSample = false;
      TestCase.create = function(properties) {
        return new TestCase(properties);
      };
      TestCase.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.testCaseId != null && $Object.hasOwnProperty.call(message, "testCaseId") && (typeof message.testCaseId === "object" ? message.testCaseId.low || message.testCaseId.high : message.testCaseId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.testCaseId);
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.questionId);
        if (message.ordinal != null && $Object.hasOwnProperty.call(message, "ordinal") && message.ordinal !== 0)
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint32(message.ordinal);
        if (message.input != null && $Object.hasOwnProperty.call(message, "input") && message.input !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.input);
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput") && message.expectedOutput !== "")
          writer.uint32(
            /* id 5, wireType 2 =*/
            42
          ).string(message.expectedOutput);
        if (message.isSample != null && $Object.hasOwnProperty.call(message, "isSample") && message.isSample !== false)
          writer.uint32(
            /* id 6, wireType 0 =*/
            48
          ).bool(message.isSample);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      TestCase.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      TestCase.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.TestCase(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.testCaseId = value;
              else
                delete message.testCaseId;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.ordinal = value;
              else
                delete message.ordinal;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.input = value;
              else
                delete message.input;
              continue;
            }
            case 5: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.expectedOutput = value;
              else
                delete message.expectedOutput;
              continue;
            }
            case 6: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.isSample = value;
              else
                delete message.isSample;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      TestCase.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      TestCase.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.testCaseId != null && $Object.hasOwnProperty.call(message, "testCaseId")) {
          if (!$util.isInteger(message.testCaseId) && !(message.testCaseId && $util.isInteger(message.testCaseId.low) && $util.isInteger(message.testCaseId.high)))
            return "testCaseId: integer|Long expected";
        }
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        if (message.ordinal != null && $Object.hasOwnProperty.call(message, "ordinal")) {
          if (!$util.isInteger(message.ordinal))
            return "ordinal: integer expected";
        }
        if (message.input != null && $Object.hasOwnProperty.call(message, "input")) {
          if (!$util.isString(message.input))
            return "input: string expected";
        }
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput")) {
          if (!$util.isString(message.expectedOutput))
            return "expectedOutput: string expected";
        }
        if (message.isSample != null && $Object.hasOwnProperty.call(message, "isSample")) {
          if (typeof message.isSample !== "boolean")
            return "isSample: boolean expected";
        }
        return null;
      };
      TestCase.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.TestCase)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.TestCase: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.TestCase();
        if (object.testCaseId != null) {
          if (typeof object.testCaseId === "object" ? object.testCaseId.low || object.testCaseId.high : $Number(object.testCaseId) !== 0) {
            if ($util.Long)
              message.testCaseId = $util.Long.fromValue(object.testCaseId, true);
            else if (typeof object.testCaseId === "string")
              message.testCaseId = $parseInt(object.testCaseId, 10);
            else if (typeof object.testCaseId === "number")
              message.testCaseId = object.testCaseId;
            else if (typeof object.testCaseId === "object")
              message.testCaseId = new $util.LongBits(object.testCaseId.low >>> 0, object.testCaseId.high >>> 0).toNumber(true);
          }
        }
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        if (object.ordinal != null) {
          if ($Number(object.ordinal) !== 0)
            message.ordinal = object.ordinal >>> 0;
        }
        if (object.input != null) {
          if (typeof object.input !== "string" || object.input.length)
            message.input = $String(object.input);
        }
        if (object.expectedOutput != null) {
          if (typeof object.expectedOutput !== "string" || object.expectedOutput.length)
            message.expectedOutput = $String(object.expectedOutput);
        }
        if (object.isSample != null) {
          if (object.isSample)
            message.isSample = $Boolean(object.isSample);
        }
        return message;
      };
      TestCase.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.testCaseId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.testCaseId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.questionId = "";
          object.ordinal = 0;
          object.input = "";
          object.expectedOutput = "";
          object.isSample = false;
        }
        if (message.testCaseId != null && $Object.hasOwnProperty.call(message, "testCaseId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.testCaseId = typeof message.testCaseId === "number" ? $BigInt(message.testCaseId) : $util.Long.fromBits(message.testCaseId.low >>> 0, message.testCaseId.high >>> 0, true).toBigInt();
          else if (typeof message.testCaseId === "number")
            object.testCaseId = options.longs === $String ? $String(message.testCaseId) : message.testCaseId;
          else
            object.testCaseId = options.longs === $String ? $util.Long.prototype.toString.call(message.testCaseId) : options.longs === $Number ? new $util.LongBits(message.testCaseId.low >>> 0, message.testCaseId.high >>> 0).toNumber(true) : message.testCaseId;
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        if (message.ordinal != null && $Object.hasOwnProperty.call(message, "ordinal"))
          object.ordinal = message.ordinal;
        if (message.input != null && $Object.hasOwnProperty.call(message, "input"))
          object.input = message.input;
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput"))
          object.expectedOutput = message.expectedOutput;
        if (message.isSample != null && $Object.hasOwnProperty.call(message, "isSample"))
          object.isSample = message.isSample;
        return object;
      };
      TestCase.prototype.toJSON = function() {
        return TestCase.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      TestCase.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.TestCase";
      };
      return TestCase;
    })();
    common.Solution = (function() {
      const Solution = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      Solution.prototype.id = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      Solution.prototype.questionId = "";
      Solution.prototype.userId = 0;
      Solution.prototype.title = "";
      Solution.prototype.contentMd = "";
      Solution.prototype.likeCount = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      Solution.prototype.favoriteCount = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      Solution.prototype.commentCount = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      Solution.prototype.moderationStatus = "";
      Solution.prototype.createdAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      Solution.prototype.updatedAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      Solution.create = function(properties) {
        return new Solution(properties);
      };
      Solution.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id") && (typeof message.id === "object" ? message.id.low || message.id.high : message.id !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.id);
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.questionId);
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId") && message.userId !== 0)
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint32(message.userId);
        if (message.title != null && $Object.hasOwnProperty.call(message, "title") && message.title !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.title);
        if (message.contentMd != null && $Object.hasOwnProperty.call(message, "contentMd") && message.contentMd !== "")
          writer.uint32(
            /* id 5, wireType 2 =*/
            42
          ).string(message.contentMd);
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount") && (typeof message.likeCount === "object" ? message.likeCount.low || message.likeCount.high : message.likeCount !== 0))
          writer.uint32(
            /* id 6, wireType 0 =*/
            48
          ).uint64(message.likeCount);
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount") && (typeof message.favoriteCount === "object" ? message.favoriteCount.low || message.favoriteCount.high : message.favoriteCount !== 0))
          writer.uint32(
            /* id 7, wireType 0 =*/
            56
          ).uint64(message.favoriteCount);
        if (message.commentCount != null && $Object.hasOwnProperty.call(message, "commentCount") && (typeof message.commentCount === "object" ? message.commentCount.low || message.commentCount.high : message.commentCount !== 0))
          writer.uint32(
            /* id 8, wireType 0 =*/
            64
          ).uint64(message.commentCount);
        if (message.moderationStatus != null && $Object.hasOwnProperty.call(message, "moderationStatus") && message.moderationStatus !== "")
          writer.uint32(
            /* id 9, wireType 2 =*/
            74
          ).string(message.moderationStatus);
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt") && (typeof message.createdAt === "object" ? message.createdAt.low || message.createdAt.high : message.createdAt !== 0))
          writer.uint32(
            /* id 10, wireType 0 =*/
            80
          ).int64(message.createdAt);
        if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt") && (typeof message.updatedAt === "object" ? message.updatedAt.low || message.updatedAt.high : message.updatedAt !== 0))
          writer.uint32(
            /* id 11, wireType 0 =*/
            88
          ).int64(message.updatedAt);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      Solution.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      Solution.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.Solution(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.id = value;
              else
                delete message.id;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.userId = value;
              else
                delete message.userId;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.title = value;
              else
                delete message.title;
              continue;
            }
            case 5: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.contentMd = value;
              else
                delete message.contentMd;
              continue;
            }
            case 6: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.likeCount = value;
              else
                delete message.likeCount;
              continue;
            }
            case 7: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.favoriteCount = value;
              else
                delete message.favoriteCount;
              continue;
            }
            case 8: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.commentCount = value;
              else
                delete message.commentCount;
              continue;
            }
            case 9: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.moderationStatus = value;
              else
                delete message.moderationStatus;
              continue;
            }
            case 10: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.createdAt = value;
              else
                delete message.createdAt;
              continue;
            }
            case 11: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.updatedAt = value;
              else
                delete message.updatedAt;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      Solution.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      Solution.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
          if (!$util.isInteger(message.id) && !(message.id && $util.isInteger(message.id.low) && $util.isInteger(message.id.high)))
            return "id: integer|Long expected";
        }
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId")) {
          if (!$util.isInteger(message.userId))
            return "userId: integer expected";
        }
        if (message.title != null && $Object.hasOwnProperty.call(message, "title")) {
          if (!$util.isString(message.title))
            return "title: string expected";
        }
        if (message.contentMd != null && $Object.hasOwnProperty.call(message, "contentMd")) {
          if (!$util.isString(message.contentMd))
            return "contentMd: string expected";
        }
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount")) {
          if (!$util.isInteger(message.likeCount) && !(message.likeCount && $util.isInteger(message.likeCount.low) && $util.isInteger(message.likeCount.high)))
            return "likeCount: integer|Long expected";
        }
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount")) {
          if (!$util.isInteger(message.favoriteCount) && !(message.favoriteCount && $util.isInteger(message.favoriteCount.low) && $util.isInteger(message.favoriteCount.high)))
            return "favoriteCount: integer|Long expected";
        }
        if (message.commentCount != null && $Object.hasOwnProperty.call(message, "commentCount")) {
          if (!$util.isInteger(message.commentCount) && !(message.commentCount && $util.isInteger(message.commentCount.low) && $util.isInteger(message.commentCount.high)))
            return "commentCount: integer|Long expected";
        }
        if (message.moderationStatus != null && $Object.hasOwnProperty.call(message, "moderationStatus")) {
          if (!$util.isString(message.moderationStatus))
            return "moderationStatus: string expected";
        }
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt")) {
          if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
            return "createdAt: integer|Long expected";
        }
        if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt")) {
          if (!$util.isInteger(message.updatedAt) && !(message.updatedAt && $util.isInteger(message.updatedAt.low) && $util.isInteger(message.updatedAt.high)))
            return "updatedAt: integer|Long expected";
        }
        return null;
      };
      Solution.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.Solution)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.Solution: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.Solution();
        if (object.id != null) {
          if (typeof object.id === "object" ? object.id.low || object.id.high : $Number(object.id) !== 0) {
            if ($util.Long)
              message.id = $util.Long.fromValue(object.id, true);
            else if (typeof object.id === "string")
              message.id = $parseInt(object.id, 10);
            else if (typeof object.id === "number")
              message.id = object.id;
            else if (typeof object.id === "object")
              message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber(true);
          }
        }
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        if (object.userId != null) {
          if ($Number(object.userId) !== 0)
            message.userId = object.userId >>> 0;
        }
        if (object.title != null) {
          if (typeof object.title !== "string" || object.title.length)
            message.title = $String(object.title);
        }
        if (object.contentMd != null) {
          if (typeof object.contentMd !== "string" || object.contentMd.length)
            message.contentMd = $String(object.contentMd);
        }
        if (object.likeCount != null) {
          if (typeof object.likeCount === "object" ? object.likeCount.low || object.likeCount.high : $Number(object.likeCount) !== 0) {
            if ($util.Long)
              message.likeCount = $util.Long.fromValue(object.likeCount, true);
            else if (typeof object.likeCount === "string")
              message.likeCount = $parseInt(object.likeCount, 10);
            else if (typeof object.likeCount === "number")
              message.likeCount = object.likeCount;
            else if (typeof object.likeCount === "object")
              message.likeCount = new $util.LongBits(object.likeCount.low >>> 0, object.likeCount.high >>> 0).toNumber(true);
          }
        }
        if (object.favoriteCount != null) {
          if (typeof object.favoriteCount === "object" ? object.favoriteCount.low || object.favoriteCount.high : $Number(object.favoriteCount) !== 0) {
            if ($util.Long)
              message.favoriteCount = $util.Long.fromValue(object.favoriteCount, true);
            else if (typeof object.favoriteCount === "string")
              message.favoriteCount = $parseInt(object.favoriteCount, 10);
            else if (typeof object.favoriteCount === "number")
              message.favoriteCount = object.favoriteCount;
            else if (typeof object.favoriteCount === "object")
              message.favoriteCount = new $util.LongBits(object.favoriteCount.low >>> 0, object.favoriteCount.high >>> 0).toNumber(true);
          }
        }
        if (object.commentCount != null) {
          if (typeof object.commentCount === "object" ? object.commentCount.low || object.commentCount.high : $Number(object.commentCount) !== 0) {
            if ($util.Long)
              message.commentCount = $util.Long.fromValue(object.commentCount, true);
            else if (typeof object.commentCount === "string")
              message.commentCount = $parseInt(object.commentCount, 10);
            else if (typeof object.commentCount === "number")
              message.commentCount = object.commentCount;
            else if (typeof object.commentCount === "object")
              message.commentCount = new $util.LongBits(object.commentCount.low >>> 0, object.commentCount.high >>> 0).toNumber(true);
          }
        }
        if (object.moderationStatus != null) {
          if (typeof object.moderationStatus !== "string" || object.moderationStatus.length)
            message.moderationStatus = $String(object.moderationStatus);
        }
        if (object.createdAt != null) {
          if (typeof object.createdAt === "object" ? object.createdAt.low || object.createdAt.high : $Number(object.createdAt) !== 0) {
            if ($util.Long)
              message.createdAt = $util.Long.fromValue(object.createdAt, false);
            else if (typeof object.createdAt === "string")
              message.createdAt = $parseInt(object.createdAt, 10);
            else if (typeof object.createdAt === "number")
              message.createdAt = object.createdAt;
            else if (typeof object.createdAt === "object")
              message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
          }
        }
        if (object.updatedAt != null) {
          if (typeof object.updatedAt === "object" ? object.updatedAt.low || object.updatedAt.high : $Number(object.updatedAt) !== 0) {
            if ($util.Long)
              message.updatedAt = $util.Long.fromValue(object.updatedAt, false);
            else if (typeof object.updatedAt === "string")
              message.updatedAt = $parseInt(object.updatedAt, 10);
            else if (typeof object.updatedAt === "number")
              message.updatedAt = object.updatedAt;
            else if (typeof object.updatedAt === "object")
              message.updatedAt = new $util.LongBits(object.updatedAt.low >>> 0, object.updatedAt.high >>> 0).toNumber();
          }
        }
        return message;
      };
      Solution.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.questionId = "";
          object.userId = 0;
          object.title = "";
          object.contentMd = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.likeCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.likeCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.favoriteCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.favoriteCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.commentCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.commentCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.moderationStatus = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.createdAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.createdAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.updatedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.updatedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        }
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.id = typeof message.id === "number" ? $BigInt(message.id) : $util.Long.fromBits(message.id.low >>> 0, message.id.high >>> 0, true).toBigInt();
          else if (typeof message.id === "number")
            object.id = options.longs === $String ? $String(message.id) : message.id;
          else
            object.id = options.longs === $String ? $util.Long.prototype.toString.call(message.id) : options.longs === $Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber(true) : message.id;
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
          object.userId = message.userId;
        if (message.title != null && $Object.hasOwnProperty.call(message, "title"))
          object.title = message.title;
        if (message.contentMd != null && $Object.hasOwnProperty.call(message, "contentMd"))
          object.contentMd = message.contentMd;
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.likeCount = typeof message.likeCount === "number" ? $BigInt(message.likeCount) : $util.Long.fromBits(message.likeCount.low >>> 0, message.likeCount.high >>> 0, true).toBigInt();
          else if (typeof message.likeCount === "number")
            object.likeCount = options.longs === $String ? $String(message.likeCount) : message.likeCount;
          else
            object.likeCount = options.longs === $String ? $util.Long.prototype.toString.call(message.likeCount) : options.longs === $Number ? new $util.LongBits(message.likeCount.low >>> 0, message.likeCount.high >>> 0).toNumber(true) : message.likeCount;
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.favoriteCount = typeof message.favoriteCount === "number" ? $BigInt(message.favoriteCount) : $util.Long.fromBits(message.favoriteCount.low >>> 0, message.favoriteCount.high >>> 0, true).toBigInt();
          else if (typeof message.favoriteCount === "number")
            object.favoriteCount = options.longs === $String ? $String(message.favoriteCount) : message.favoriteCount;
          else
            object.favoriteCount = options.longs === $String ? $util.Long.prototype.toString.call(message.favoriteCount) : options.longs === $Number ? new $util.LongBits(message.favoriteCount.low >>> 0, message.favoriteCount.high >>> 0).toNumber(true) : message.favoriteCount;
        if (message.commentCount != null && $Object.hasOwnProperty.call(message, "commentCount"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.commentCount = typeof message.commentCount === "number" ? $BigInt(message.commentCount) : $util.Long.fromBits(message.commentCount.low >>> 0, message.commentCount.high >>> 0, true).toBigInt();
          else if (typeof message.commentCount === "number")
            object.commentCount = options.longs === $String ? $String(message.commentCount) : message.commentCount;
          else
            object.commentCount = options.longs === $String ? $util.Long.prototype.toString.call(message.commentCount) : options.longs === $Number ? new $util.LongBits(message.commentCount.low >>> 0, message.commentCount.high >>> 0).toNumber(true) : message.commentCount;
        if (message.moderationStatus != null && $Object.hasOwnProperty.call(message, "moderationStatus"))
          object.moderationStatus = message.moderationStatus;
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.createdAt = typeof message.createdAt === "number" ? $BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
          else if (typeof message.createdAt === "number")
            object.createdAt = options.longs === $String ? $String(message.createdAt) : message.createdAt;
          else
            object.createdAt = options.longs === $String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === $Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
        if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.updatedAt = typeof message.updatedAt === "number" ? $BigInt(message.updatedAt) : $util.Long.fromBits(message.updatedAt.low >>> 0, message.updatedAt.high >>> 0, false).toBigInt();
          else if (typeof message.updatedAt === "number")
            object.updatedAt = options.longs === $String ? $String(message.updatedAt) : message.updatedAt;
          else
            object.updatedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.updatedAt) : options.longs === $Number ? new $util.LongBits(message.updatedAt.low >>> 0, message.updatedAt.high >>> 0).toNumber() : message.updatedAt;
        return object;
      };
      Solution.prototype.toJSON = function() {
        return Solution.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      Solution.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.Solution";
      };
      return Solution;
    })();
    common.SolutionActionState = (function() {
      const SolutionActionState = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      SolutionActionState.prototype.liked = false;
      SolutionActionState.prototype.favorited = false;
      SolutionActionState.create = function(properties) {
        return new SolutionActionState(properties);
      };
      SolutionActionState.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.liked != null && $Object.hasOwnProperty.call(message, "liked") && message.liked !== false)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).bool(message.liked);
        if (message.favorited != null && $Object.hasOwnProperty.call(message, "favorited") && message.favorited !== false)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).bool(message.favorited);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      SolutionActionState.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      SolutionActionState.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.SolutionActionState(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.liked = value;
              else
                delete message.liked;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.favorited = value;
              else
                delete message.favorited;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      SolutionActionState.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      SolutionActionState.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.liked != null && $Object.hasOwnProperty.call(message, "liked")) {
          if (typeof message.liked !== "boolean")
            return "liked: boolean expected";
        }
        if (message.favorited != null && $Object.hasOwnProperty.call(message, "favorited")) {
          if (typeof message.favorited !== "boolean")
            return "favorited: boolean expected";
        }
        return null;
      };
      SolutionActionState.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.SolutionActionState)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.SolutionActionState: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.SolutionActionState();
        if (object.liked != null) {
          if (object.liked)
            message.liked = $Boolean(object.liked);
        }
        if (object.favorited != null) {
          if (object.favorited)
            message.favorited = $Boolean(object.favorited);
        }
        return message;
      };
      SolutionActionState.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.liked = false;
          object.favorited = false;
        }
        if (message.liked != null && $Object.hasOwnProperty.call(message, "liked"))
          object.liked = message.liked;
        if (message.favorited != null && $Object.hasOwnProperty.call(message, "favorited"))
          object.favorited = message.favorited;
        return object;
      };
      SolutionActionState.prototype.toJSON = function() {
        return SolutionActionState.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      SolutionActionState.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.SolutionActionState";
      };
      return SolutionActionState;
    })();
    common.Comment = (function() {
      const Comment = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      Comment.prototype.id = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      Comment.prototype.solutionId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      Comment.prototype.userId = 0;
      Comment.prototype.parentId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      Comment.prototype.replyToUserId = 0;
      Comment.prototype.content = "";
      Comment.prototype.likeCount = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      Comment.prototype.favoriteCount = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      Comment.prototype.isEdited = false;
      Comment.prototype.createdAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      Comment.prototype.updatedAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      Comment.create = function(properties) {
        return new Comment(properties);
      };
      Comment.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id") && (typeof message.id === "object" ? message.id.low || message.id.high : message.id !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.id);
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId") && (typeof message.solutionId === "object" ? message.solutionId.low || message.solutionId.high : message.solutionId !== 0))
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint64(message.solutionId);
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId") && message.userId !== 0)
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint32(message.userId);
        if (message.parentId != null && $Object.hasOwnProperty.call(message, "parentId") && (typeof message.parentId === "object" ? message.parentId.low || message.parentId.high : message.parentId !== 0))
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).uint64(message.parentId);
        if (message.replyToUserId != null && $Object.hasOwnProperty.call(message, "replyToUserId") && message.replyToUserId !== 0)
          writer.uint32(
            /* id 5, wireType 0 =*/
            40
          ).uint32(message.replyToUserId);
        if (message.content != null && $Object.hasOwnProperty.call(message, "content") && message.content !== "")
          writer.uint32(
            /* id 6, wireType 2 =*/
            50
          ).string(message.content);
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount") && (typeof message.likeCount === "object" ? message.likeCount.low || message.likeCount.high : message.likeCount !== 0))
          writer.uint32(
            /* id 7, wireType 0 =*/
            56
          ).uint64(message.likeCount);
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount") && (typeof message.favoriteCount === "object" ? message.favoriteCount.low || message.favoriteCount.high : message.favoriteCount !== 0))
          writer.uint32(
            /* id 8, wireType 0 =*/
            64
          ).uint64(message.favoriteCount);
        if (message.isEdited != null && $Object.hasOwnProperty.call(message, "isEdited") && message.isEdited !== false)
          writer.uint32(
            /* id 9, wireType 0 =*/
            72
          ).bool(message.isEdited);
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt") && (typeof message.createdAt === "object" ? message.createdAt.low || message.createdAt.high : message.createdAt !== 0))
          writer.uint32(
            /* id 10, wireType 0 =*/
            80
          ).int64(message.createdAt);
        if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt") && (typeof message.updatedAt === "object" ? message.updatedAt.low || message.updatedAt.high : message.updatedAt !== 0))
          writer.uint32(
            /* id 11, wireType 0 =*/
            88
          ).int64(message.updatedAt);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      Comment.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      Comment.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.Comment(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.id = value;
              else
                delete message.id;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.solutionId = value;
              else
                delete message.solutionId;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.userId = value;
              else
                delete message.userId;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.parentId = value;
              else
                delete message.parentId;
              continue;
            }
            case 5: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.replyToUserId = value;
              else
                delete message.replyToUserId;
              continue;
            }
            case 6: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.content = value;
              else
                delete message.content;
              continue;
            }
            case 7: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.likeCount = value;
              else
                delete message.likeCount;
              continue;
            }
            case 8: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.favoriteCount = value;
              else
                delete message.favoriteCount;
              continue;
            }
            case 9: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.isEdited = value;
              else
                delete message.isEdited;
              continue;
            }
            case 10: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.createdAt = value;
              else
                delete message.createdAt;
              continue;
            }
            case 11: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.updatedAt = value;
              else
                delete message.updatedAt;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      Comment.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      Comment.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
          if (!$util.isInteger(message.id) && !(message.id && $util.isInteger(message.id.low) && $util.isInteger(message.id.high)))
            return "id: integer|Long expected";
        }
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId")) {
          if (!$util.isInteger(message.solutionId) && !(message.solutionId && $util.isInteger(message.solutionId.low) && $util.isInteger(message.solutionId.high)))
            return "solutionId: integer|Long expected";
        }
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId")) {
          if (!$util.isInteger(message.userId))
            return "userId: integer expected";
        }
        if (message.parentId != null && $Object.hasOwnProperty.call(message, "parentId")) {
          if (!$util.isInteger(message.parentId) && !(message.parentId && $util.isInteger(message.parentId.low) && $util.isInteger(message.parentId.high)))
            return "parentId: integer|Long expected";
        }
        if (message.replyToUserId != null && $Object.hasOwnProperty.call(message, "replyToUserId")) {
          if (!$util.isInteger(message.replyToUserId))
            return "replyToUserId: integer expected";
        }
        if (message.content != null && $Object.hasOwnProperty.call(message, "content")) {
          if (!$util.isString(message.content))
            return "content: string expected";
        }
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount")) {
          if (!$util.isInteger(message.likeCount) && !(message.likeCount && $util.isInteger(message.likeCount.low) && $util.isInteger(message.likeCount.high)))
            return "likeCount: integer|Long expected";
        }
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount")) {
          if (!$util.isInteger(message.favoriteCount) && !(message.favoriteCount && $util.isInteger(message.favoriteCount.low) && $util.isInteger(message.favoriteCount.high)))
            return "favoriteCount: integer|Long expected";
        }
        if (message.isEdited != null && $Object.hasOwnProperty.call(message, "isEdited")) {
          if (typeof message.isEdited !== "boolean")
            return "isEdited: boolean expected";
        }
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt")) {
          if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
            return "createdAt: integer|Long expected";
        }
        if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt")) {
          if (!$util.isInteger(message.updatedAt) && !(message.updatedAt && $util.isInteger(message.updatedAt.low) && $util.isInteger(message.updatedAt.high)))
            return "updatedAt: integer|Long expected";
        }
        return null;
      };
      Comment.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.Comment)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.Comment: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.Comment();
        if (object.id != null) {
          if (typeof object.id === "object" ? object.id.low || object.id.high : $Number(object.id) !== 0) {
            if ($util.Long)
              message.id = $util.Long.fromValue(object.id, true);
            else if (typeof object.id === "string")
              message.id = $parseInt(object.id, 10);
            else if (typeof object.id === "number")
              message.id = object.id;
            else if (typeof object.id === "object")
              message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber(true);
          }
        }
        if (object.solutionId != null) {
          if (typeof object.solutionId === "object" ? object.solutionId.low || object.solutionId.high : $Number(object.solutionId) !== 0) {
            if ($util.Long)
              message.solutionId = $util.Long.fromValue(object.solutionId, true);
            else if (typeof object.solutionId === "string")
              message.solutionId = $parseInt(object.solutionId, 10);
            else if (typeof object.solutionId === "number")
              message.solutionId = object.solutionId;
            else if (typeof object.solutionId === "object")
              message.solutionId = new $util.LongBits(object.solutionId.low >>> 0, object.solutionId.high >>> 0).toNumber(true);
          }
        }
        if (object.userId != null) {
          if ($Number(object.userId) !== 0)
            message.userId = object.userId >>> 0;
        }
        if (object.parentId != null) {
          if (typeof object.parentId === "object" ? object.parentId.low || object.parentId.high : $Number(object.parentId) !== 0) {
            if ($util.Long)
              message.parentId = $util.Long.fromValue(object.parentId, true);
            else if (typeof object.parentId === "string")
              message.parentId = $parseInt(object.parentId, 10);
            else if (typeof object.parentId === "number")
              message.parentId = object.parentId;
            else if (typeof object.parentId === "object")
              message.parentId = new $util.LongBits(object.parentId.low >>> 0, object.parentId.high >>> 0).toNumber(true);
          }
        }
        if (object.replyToUserId != null) {
          if ($Number(object.replyToUserId) !== 0)
            message.replyToUserId = object.replyToUserId >>> 0;
        }
        if (object.content != null) {
          if (typeof object.content !== "string" || object.content.length)
            message.content = $String(object.content);
        }
        if (object.likeCount != null) {
          if (typeof object.likeCount === "object" ? object.likeCount.low || object.likeCount.high : $Number(object.likeCount) !== 0) {
            if ($util.Long)
              message.likeCount = $util.Long.fromValue(object.likeCount, true);
            else if (typeof object.likeCount === "string")
              message.likeCount = $parseInt(object.likeCount, 10);
            else if (typeof object.likeCount === "number")
              message.likeCount = object.likeCount;
            else if (typeof object.likeCount === "object")
              message.likeCount = new $util.LongBits(object.likeCount.low >>> 0, object.likeCount.high >>> 0).toNumber(true);
          }
        }
        if (object.favoriteCount != null) {
          if (typeof object.favoriteCount === "object" ? object.favoriteCount.low || object.favoriteCount.high : $Number(object.favoriteCount) !== 0) {
            if ($util.Long)
              message.favoriteCount = $util.Long.fromValue(object.favoriteCount, true);
            else if (typeof object.favoriteCount === "string")
              message.favoriteCount = $parseInt(object.favoriteCount, 10);
            else if (typeof object.favoriteCount === "number")
              message.favoriteCount = object.favoriteCount;
            else if (typeof object.favoriteCount === "object")
              message.favoriteCount = new $util.LongBits(object.favoriteCount.low >>> 0, object.favoriteCount.high >>> 0).toNumber(true);
          }
        }
        if (object.isEdited != null) {
          if (object.isEdited)
            message.isEdited = $Boolean(object.isEdited);
        }
        if (object.createdAt != null) {
          if (typeof object.createdAt === "object" ? object.createdAt.low || object.createdAt.high : $Number(object.createdAt) !== 0) {
            if ($util.Long)
              message.createdAt = $util.Long.fromValue(object.createdAt, false);
            else if (typeof object.createdAt === "string")
              message.createdAt = $parseInt(object.createdAt, 10);
            else if (typeof object.createdAt === "number")
              message.createdAt = object.createdAt;
            else if (typeof object.createdAt === "object")
              message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
          }
        }
        if (object.updatedAt != null) {
          if (typeof object.updatedAt === "object" ? object.updatedAt.low || object.updatedAt.high : $Number(object.updatedAt) !== 0) {
            if ($util.Long)
              message.updatedAt = $util.Long.fromValue(object.updatedAt, false);
            else if (typeof object.updatedAt === "string")
              message.updatedAt = $parseInt(object.updatedAt, 10);
            else if (typeof object.updatedAt === "number")
              message.updatedAt = object.updatedAt;
            else if (typeof object.updatedAt === "object")
              message.updatedAt = new $util.LongBits(object.updatedAt.low >>> 0, object.updatedAt.high >>> 0).toNumber();
          }
        }
        return message;
      };
      Comment.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.id = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.id = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.solutionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.solutionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.userId = 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.parentId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.parentId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.replyToUserId = 0;
          object.content = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.likeCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.likeCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.favoriteCount = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.favoriteCount = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.isEdited = false;
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.createdAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.createdAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.updatedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.updatedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        }
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.id = typeof message.id === "number" ? $BigInt(message.id) : $util.Long.fromBits(message.id.low >>> 0, message.id.high >>> 0, true).toBigInt();
          else if (typeof message.id === "number")
            object.id = options.longs === $String ? $String(message.id) : message.id;
          else
            object.id = options.longs === $String ? $util.Long.prototype.toString.call(message.id) : options.longs === $Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber(true) : message.id;
        if (message.solutionId != null && $Object.hasOwnProperty.call(message, "solutionId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.solutionId = typeof message.solutionId === "number" ? $BigInt(message.solutionId) : $util.Long.fromBits(message.solutionId.low >>> 0, message.solutionId.high >>> 0, true).toBigInt();
          else if (typeof message.solutionId === "number")
            object.solutionId = options.longs === $String ? $String(message.solutionId) : message.solutionId;
          else
            object.solutionId = options.longs === $String ? $util.Long.prototype.toString.call(message.solutionId) : options.longs === $Number ? new $util.LongBits(message.solutionId.low >>> 0, message.solutionId.high >>> 0).toNumber(true) : message.solutionId;
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
          object.userId = message.userId;
        if (message.parentId != null && $Object.hasOwnProperty.call(message, "parentId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.parentId = typeof message.parentId === "number" ? $BigInt(message.parentId) : $util.Long.fromBits(message.parentId.low >>> 0, message.parentId.high >>> 0, true).toBigInt();
          else if (typeof message.parentId === "number")
            object.parentId = options.longs === $String ? $String(message.parentId) : message.parentId;
          else
            object.parentId = options.longs === $String ? $util.Long.prototype.toString.call(message.parentId) : options.longs === $Number ? new $util.LongBits(message.parentId.low >>> 0, message.parentId.high >>> 0).toNumber(true) : message.parentId;
        if (message.replyToUserId != null && $Object.hasOwnProperty.call(message, "replyToUserId"))
          object.replyToUserId = message.replyToUserId;
        if (message.content != null && $Object.hasOwnProperty.call(message, "content"))
          object.content = message.content;
        if (message.likeCount != null && $Object.hasOwnProperty.call(message, "likeCount"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.likeCount = typeof message.likeCount === "number" ? $BigInt(message.likeCount) : $util.Long.fromBits(message.likeCount.low >>> 0, message.likeCount.high >>> 0, true).toBigInt();
          else if (typeof message.likeCount === "number")
            object.likeCount = options.longs === $String ? $String(message.likeCount) : message.likeCount;
          else
            object.likeCount = options.longs === $String ? $util.Long.prototype.toString.call(message.likeCount) : options.longs === $Number ? new $util.LongBits(message.likeCount.low >>> 0, message.likeCount.high >>> 0).toNumber(true) : message.likeCount;
        if (message.favoriteCount != null && $Object.hasOwnProperty.call(message, "favoriteCount"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.favoriteCount = typeof message.favoriteCount === "number" ? $BigInt(message.favoriteCount) : $util.Long.fromBits(message.favoriteCount.low >>> 0, message.favoriteCount.high >>> 0, true).toBigInt();
          else if (typeof message.favoriteCount === "number")
            object.favoriteCount = options.longs === $String ? $String(message.favoriteCount) : message.favoriteCount;
          else
            object.favoriteCount = options.longs === $String ? $util.Long.prototype.toString.call(message.favoriteCount) : options.longs === $Number ? new $util.LongBits(message.favoriteCount.low >>> 0, message.favoriteCount.high >>> 0).toNumber(true) : message.favoriteCount;
        if (message.isEdited != null && $Object.hasOwnProperty.call(message, "isEdited"))
          object.isEdited = message.isEdited;
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.createdAt = typeof message.createdAt === "number" ? $BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
          else if (typeof message.createdAt === "number")
            object.createdAt = options.longs === $String ? $String(message.createdAt) : message.createdAt;
          else
            object.createdAt = options.longs === $String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === $Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
        if (message.updatedAt != null && $Object.hasOwnProperty.call(message, "updatedAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.updatedAt = typeof message.updatedAt === "number" ? $BigInt(message.updatedAt) : $util.Long.fromBits(message.updatedAt.low >>> 0, message.updatedAt.high >>> 0, false).toBigInt();
          else if (typeof message.updatedAt === "number")
            object.updatedAt = options.longs === $String ? $String(message.updatedAt) : message.updatedAt;
          else
            object.updatedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.updatedAt) : options.longs === $Number ? new $util.LongBits(message.updatedAt.low >>> 0, message.updatedAt.high >>> 0).toNumber() : message.updatedAt;
        return object;
      };
      Comment.prototype.toJSON = function() {
        return Comment.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      Comment.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.Comment";
      };
      return Comment;
    })();
    common.CommentActionState = (function() {
      const CommentActionState = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CommentActionState.prototype.commentId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      CommentActionState.prototype.liked = false;
      CommentActionState.prototype.favorited = false;
      CommentActionState.create = function(properties) {
        return new CommentActionState(properties);
      };
      CommentActionState.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.commentId != null && $Object.hasOwnProperty.call(message, "commentId") && (typeof message.commentId === "object" ? message.commentId.low || message.commentId.high : message.commentId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.commentId);
        if (message.liked != null && $Object.hasOwnProperty.call(message, "liked") && message.liked !== false)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).bool(message.liked);
        if (message.favorited != null && $Object.hasOwnProperty.call(message, "favorited") && message.favorited !== false)
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).bool(message.favorited);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CommentActionState.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CommentActionState.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.CommentActionState(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.commentId = value;
              else
                delete message.commentId;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.liked = value;
              else
                delete message.liked;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.favorited = value;
              else
                delete message.favorited;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CommentActionState.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CommentActionState.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.commentId != null && $Object.hasOwnProperty.call(message, "commentId")) {
          if (!$util.isInteger(message.commentId) && !(message.commentId && $util.isInteger(message.commentId.low) && $util.isInteger(message.commentId.high)))
            return "commentId: integer|Long expected";
        }
        if (message.liked != null && $Object.hasOwnProperty.call(message, "liked")) {
          if (typeof message.liked !== "boolean")
            return "liked: boolean expected";
        }
        if (message.favorited != null && $Object.hasOwnProperty.call(message, "favorited")) {
          if (typeof message.favorited !== "boolean")
            return "favorited: boolean expected";
        }
        return null;
      };
      CommentActionState.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.CommentActionState)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.CommentActionState: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.CommentActionState();
        if (object.commentId != null) {
          if (typeof object.commentId === "object" ? object.commentId.low || object.commentId.high : $Number(object.commentId) !== 0) {
            if ($util.Long)
              message.commentId = $util.Long.fromValue(object.commentId, true);
            else if (typeof object.commentId === "string")
              message.commentId = $parseInt(object.commentId, 10);
            else if (typeof object.commentId === "number")
              message.commentId = object.commentId;
            else if (typeof object.commentId === "object")
              message.commentId = new $util.LongBits(object.commentId.low >>> 0, object.commentId.high >>> 0).toNumber(true);
          }
        }
        if (object.liked != null) {
          if (object.liked)
            message.liked = $Boolean(object.liked);
        }
        if (object.favorited != null) {
          if (object.favorited)
            message.favorited = $Boolean(object.favorited);
        }
        return message;
      };
      CommentActionState.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.commentId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.commentId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.liked = false;
          object.favorited = false;
        }
        if (message.commentId != null && $Object.hasOwnProperty.call(message, "commentId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.commentId = typeof message.commentId === "number" ? $BigInt(message.commentId) : $util.Long.fromBits(message.commentId.low >>> 0, message.commentId.high >>> 0, true).toBigInt();
          else if (typeof message.commentId === "number")
            object.commentId = options.longs === $String ? $String(message.commentId) : message.commentId;
          else
            object.commentId = options.longs === $String ? $util.Long.prototype.toString.call(message.commentId) : options.longs === $Number ? new $util.LongBits(message.commentId.low >>> 0, message.commentId.high >>> 0).toNumber(true) : message.commentId;
        if (message.liked != null && $Object.hasOwnProperty.call(message, "liked"))
          object.liked = message.liked;
        if (message.favorited != null && $Object.hasOwnProperty.call(message, "favorited"))
          object.favorited = message.favorited;
        return object;
      };
      CommentActionState.prototype.toJSON = function() {
        return CommentActionState.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CommentActionState.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.CommentActionState";
      };
      return CommentActionState;
    })();
    common.Submission = (function() {
      const Submission = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      Submission.prototype.submissionId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      Submission.prototype.userId = 0;
      Submission.prototype.questionId = "";
      Submission.prototype.code = "";
      Submission.prototype.language = "";
      Submission.prototype.status = 0;
      Submission.prototype.result = null;
      Submission.prototype.createdAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      Submission.prototype.completedAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      Submission.prototype.resultVersion = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      Submission.create = function(properties) {
        return new Submission(properties);
      };
      Submission.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId") && (typeof message.submissionId === "object" ? message.submissionId.low || message.submissionId.high : message.submissionId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.submissionId);
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId") && message.userId !== 0)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint32(message.userId);
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.questionId);
        if (message.code != null && $Object.hasOwnProperty.call(message, "code") && message.code !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.code);
        if (message.language != null && $Object.hasOwnProperty.call(message, "language") && message.language !== "")
          writer.uint32(
            /* id 5, wireType 2 =*/
            42
          ).string(message.language);
        if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== 0)
          writer.uint32(
            /* id 6, wireType 0 =*/
            48
          ).int32(message.status);
        if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
          $root.oj.common.JudgeResult.encode(message.result, writer.uint32(
            /* id 7, wireType 2 =*/
            58
          ).fork(), _depth + 1).ldelim();
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt") && (typeof message.createdAt === "object" ? message.createdAt.low || message.createdAt.high : message.createdAt !== 0))
          writer.uint32(
            /* id 8, wireType 0 =*/
            64
          ).int64(message.createdAt);
        if (message.completedAt != null && $Object.hasOwnProperty.call(message, "completedAt") && (typeof message.completedAt === "object" ? message.completedAt.low || message.completedAt.high : message.completedAt !== 0))
          writer.uint32(
            /* id 9, wireType 0 =*/
            72
          ).int64(message.completedAt);
        if (message.resultVersion != null && $Object.hasOwnProperty.call(message, "resultVersion") && (typeof message.resultVersion === "object" ? message.resultVersion.low || message.resultVersion.high : message.resultVersion !== 0))
          writer.uint32(
            /* id 10, wireType 0 =*/
            80
          ).uint64(message.resultVersion);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      Submission.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      Submission.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.Submission(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.submissionId = value;
              else
                delete message.submissionId;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.userId = value;
              else
                delete message.userId;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.code = value;
              else
                delete message.code;
              continue;
            }
            case 5: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.language = value;
              else
                delete message.language;
              continue;
            }
            case 6: {
              if (wireType !== 0)
                break;
              if (value = reader.int32())
                message.status = value;
              else
                delete message.status;
              continue;
            }
            case 7: {
              if (wireType !== 2)
                break;
              message.result = $root.oj.common.JudgeResult.decode(reader, reader.uint32(), $undefined, _depth + 1, message.result);
              continue;
            }
            case 8: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.createdAt = value;
              else
                delete message.createdAt;
              continue;
            }
            case 9: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.completedAt = value;
              else
                delete message.completedAt;
              continue;
            }
            case 10: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.resultVersion = value;
              else
                delete message.resultVersion;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      Submission.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      Submission.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId")) {
          if (!$util.isInteger(message.submissionId) && !(message.submissionId && $util.isInteger(message.submissionId.low) && $util.isInteger(message.submissionId.high)))
            return "submissionId: integer|Long expected";
        }
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId")) {
          if (!$util.isInteger(message.userId))
            return "userId: integer expected";
        }
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        if (message.code != null && $Object.hasOwnProperty.call(message, "code")) {
          if (!$util.isString(message.code))
            return "code: string expected";
        }
        if (message.language != null && $Object.hasOwnProperty.call(message, "language")) {
          if (!$util.isString(message.language))
            return "language: string expected";
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          if (typeof message.status !== "number" || (message.status | 0) !== message.status)
            return "status: enum value expected";
        }
        if (message.result != null && $Object.hasOwnProperty.call(message, "result")) {
          let error = $root.oj.common.JudgeResult.verify(message.result, _depth + 1);
          if (error)
            return "result." + error;
        }
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt")) {
          if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
            return "createdAt: integer|Long expected";
        }
        if (message.completedAt != null && $Object.hasOwnProperty.call(message, "completedAt")) {
          if (!$util.isInteger(message.completedAt) && !(message.completedAt && $util.isInteger(message.completedAt.low) && $util.isInteger(message.completedAt.high)))
            return "completedAt: integer|Long expected";
        }
        if (message.resultVersion != null && $Object.hasOwnProperty.call(message, "resultVersion")) {
          if (!$util.isInteger(message.resultVersion) && !(message.resultVersion && $util.isInteger(message.resultVersion.low) && $util.isInteger(message.resultVersion.high)))
            return "resultVersion: integer|Long expected";
        }
        return null;
      };
      Submission.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.Submission)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.Submission: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.Submission();
        if (object.submissionId != null) {
          if (typeof object.submissionId === "object" ? object.submissionId.low || object.submissionId.high : $Number(object.submissionId) !== 0) {
            if ($util.Long)
              message.submissionId = $util.Long.fromValue(object.submissionId, true);
            else if (typeof object.submissionId === "string")
              message.submissionId = $parseInt(object.submissionId, 10);
            else if (typeof object.submissionId === "number")
              message.submissionId = object.submissionId;
            else if (typeof object.submissionId === "object")
              message.submissionId = new $util.LongBits(object.submissionId.low >>> 0, object.submissionId.high >>> 0).toNumber(true);
          }
        }
        if (object.userId != null) {
          if ($Number(object.userId) !== 0)
            message.userId = object.userId >>> 0;
        }
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        if (object.code != null) {
          if (typeof object.code !== "string" || object.code.length)
            message.code = $String(object.code);
        }
        if (object.language != null) {
          if (typeof object.language !== "string" || object.language.length)
            message.language = $String(object.language);
        }
        if (object.status !== 0 && (typeof object.status !== "string" || $root.oj.common.SubmissionStatus[object.status] !== 0))
          switch (object.status) {
            case "SUBMISSION_STATUS_UNSPECIFIED":
            case 0:
              message.status = 0;
              break;
            case "SUBMISSION_STATUS_PENDING":
            case 1:
              message.status = 1;
              break;
            case "SUBMISSION_STATUS_QUEUED":
            case 2:
              message.status = 2;
              break;
            case "SUBMISSION_STATUS_JUDGING":
            case 3:
              message.status = 3;
              break;
            case "SUBMISSION_STATUS_ACCEPTED":
            case 4:
              message.status = 4;
              break;
            case "SUBMISSION_STATUS_WRONG_ANSWER":
            case 5:
              message.status = 5;
              break;
            case "SUBMISSION_STATUS_COMPILE_ERROR":
            case 6:
              message.status = 6;
              break;
            case "SUBMISSION_STATUS_MEMORY_LIMIT_EXCEEDED":
            case 7:
              message.status = 7;
              break;
            case "SUBMISSION_STATUS_TIME_LIMIT_EXCEEDED":
            case 8:
              message.status = 8;
              break;
            case "SUBMISSION_STATUS_RUNTIME_ERROR":
            case 9:
              message.status = 9;
              break;
            case "SUBMISSION_STATUS_SYSTEM_ERROR":
            case 10:
              message.status = 10;
              break;
            case "SUBMISSION_STATUS_CANCELLED":
            case 11:
              message.status = 11;
              break;
            default:
              if (typeof object.status === "number" && (object.status | 0) === object.status)
                message.status = object.status;
          }
        if (object.result != null) {
          if (!$util.isObject(object.result))
            throw $TypeError(".oj.common.Submission.result: object expected");
          message.result = $root.oj.common.JudgeResult.fromObject(object.result, _depth + 1);
        }
        if (object.createdAt != null) {
          if (typeof object.createdAt === "object" ? object.createdAt.low || object.createdAt.high : $Number(object.createdAt) !== 0) {
            if ($util.Long)
              message.createdAt = $util.Long.fromValue(object.createdAt, false);
            else if (typeof object.createdAt === "string")
              message.createdAt = $parseInt(object.createdAt, 10);
            else if (typeof object.createdAt === "number")
              message.createdAt = object.createdAt;
            else if (typeof object.createdAt === "object")
              message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
          }
        }
        if (object.completedAt != null) {
          if (typeof object.completedAt === "object" ? object.completedAt.low || object.completedAt.high : $Number(object.completedAt) !== 0) {
            if ($util.Long)
              message.completedAt = $util.Long.fromValue(object.completedAt, false);
            else if (typeof object.completedAt === "string")
              message.completedAt = $parseInt(object.completedAt, 10);
            else if (typeof object.completedAt === "number")
              message.completedAt = object.completedAt;
            else if (typeof object.completedAt === "object")
              message.completedAt = new $util.LongBits(object.completedAt.low >>> 0, object.completedAt.high >>> 0).toNumber();
          }
        }
        if (object.resultVersion != null) {
          if (typeof object.resultVersion === "object" ? object.resultVersion.low || object.resultVersion.high : $Number(object.resultVersion) !== 0) {
            if ($util.Long)
              message.resultVersion = $util.Long.fromValue(object.resultVersion, true);
            else if (typeof object.resultVersion === "string")
              message.resultVersion = $parseInt(object.resultVersion, 10);
            else if (typeof object.resultVersion === "number")
              message.resultVersion = object.resultVersion;
            else if (typeof object.resultVersion === "object")
              message.resultVersion = new $util.LongBits(object.resultVersion.low >>> 0, object.resultVersion.high >>> 0).toNumber(true);
          }
        }
        return message;
      };
      Submission.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.submissionId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.submissionId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.userId = 0;
          object.questionId = "";
          object.code = "";
          object.language = "";
          object.status = options.enums === $String ? "SUBMISSION_STATUS_UNSPECIFIED" : 0;
          object.result = null;
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.createdAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.createdAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.completedAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.completedAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.resultVersion = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.resultVersion = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        }
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.submissionId = typeof message.submissionId === "number" ? $BigInt(message.submissionId) : $util.Long.fromBits(message.submissionId.low >>> 0, message.submissionId.high >>> 0, true).toBigInt();
          else if (typeof message.submissionId === "number")
            object.submissionId = options.longs === $String ? $String(message.submissionId) : message.submissionId;
          else
            object.submissionId = options.longs === $String ? $util.Long.prototype.toString.call(message.submissionId) : options.longs === $Number ? new $util.LongBits(message.submissionId.low >>> 0, message.submissionId.high >>> 0).toNumber(true) : message.submissionId;
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
          object.userId = message.userId;
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
          object.code = message.code;
        if (message.language != null && $Object.hasOwnProperty.call(message, "language"))
          object.language = message.language;
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = options.enums === $String ? $root.oj.common.SubmissionStatus[message.status] === $undefined ? message.status : $root.oj.common.SubmissionStatus[message.status] : message.status;
        if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
          object.result = $root.oj.common.JudgeResult.toObject(message.result, options, _depth + 1);
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.createdAt = typeof message.createdAt === "number" ? $BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
          else if (typeof message.createdAt === "number")
            object.createdAt = options.longs === $String ? $String(message.createdAt) : message.createdAt;
          else
            object.createdAt = options.longs === $String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === $Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
        if (message.completedAt != null && $Object.hasOwnProperty.call(message, "completedAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.completedAt = typeof message.completedAt === "number" ? $BigInt(message.completedAt) : $util.Long.fromBits(message.completedAt.low >>> 0, message.completedAt.high >>> 0, false).toBigInt();
          else if (typeof message.completedAt === "number")
            object.completedAt = options.longs === $String ? $String(message.completedAt) : message.completedAt;
          else
            object.completedAt = options.longs === $String ? $util.Long.prototype.toString.call(message.completedAt) : options.longs === $Number ? new $util.LongBits(message.completedAt.low >>> 0, message.completedAt.high >>> 0).toNumber() : message.completedAt;
        if (message.resultVersion != null && $Object.hasOwnProperty.call(message, "resultVersion"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.resultVersion = typeof message.resultVersion === "number" ? $BigInt(message.resultVersion) : $util.Long.fromBits(message.resultVersion.low >>> 0, message.resultVersion.high >>> 0, true).toBigInt();
          else if (typeof message.resultVersion === "number")
            object.resultVersion = options.longs === $String ? $String(message.resultVersion) : message.resultVersion;
          else
            object.resultVersion = options.longs === $String ? $util.Long.prototype.toString.call(message.resultVersion) : options.longs === $Number ? new $util.LongBits(message.resultVersion.low >>> 0, message.resultVersion.high >>> 0).toNumber(true) : message.resultVersion;
        return object;
      };
      Submission.prototype.toJSON = function() {
        return Submission.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      Submission.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.Submission";
      };
      return Submission;
    })();
    common.AdminAccount = (function() {
      const AdminAccount = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminAccount.prototype.adminId = 0;
      AdminAccount.prototype.uid = 0;
      AdminAccount.prototype.role = "";
      AdminAccount.prototype.createdAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      AdminAccount.prototype.disabled = false;
      AdminAccount.create = function(properties) {
        return new AdminAccount(properties);
      };
      AdminAccount.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.adminId != null && $Object.hasOwnProperty.call(message, "adminId") && message.adminId !== 0)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint32(message.adminId);
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid") && message.uid !== 0)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint32(message.uid);
        if (message.role != null && $Object.hasOwnProperty.call(message, "role") && message.role !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.role);
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt") && (typeof message.createdAt === "object" ? message.createdAt.low || message.createdAt.high : message.createdAt !== 0))
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).int64(message.createdAt);
        if (message.disabled != null && $Object.hasOwnProperty.call(message, "disabled") && message.disabled !== false)
          writer.uint32(
            /* id 5, wireType 0 =*/
            40
          ).bool(message.disabled);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminAccount.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminAccount.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.AdminAccount(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.adminId = value;
              else
                delete message.adminId;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.uid = value;
              else
                delete message.uid;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.role = value;
              else
                delete message.role;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.createdAt = value;
              else
                delete message.createdAt;
              continue;
            }
            case 5: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.disabled = value;
              else
                delete message.disabled;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminAccount.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminAccount.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.adminId != null && $Object.hasOwnProperty.call(message, "adminId")) {
          if (!$util.isInteger(message.adminId))
            return "adminId: integer expected";
        }
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
          if (!$util.isInteger(message.uid))
            return "uid: integer expected";
        }
        if (message.role != null && $Object.hasOwnProperty.call(message, "role")) {
          if (!$util.isString(message.role))
            return "role: string expected";
        }
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt")) {
          if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
            return "createdAt: integer|Long expected";
        }
        if (message.disabled != null && $Object.hasOwnProperty.call(message, "disabled")) {
          if (typeof message.disabled !== "boolean")
            return "disabled: boolean expected";
        }
        return null;
      };
      AdminAccount.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.AdminAccount)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.AdminAccount: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.AdminAccount();
        if (object.adminId != null) {
          if ($Number(object.adminId) !== 0)
            message.adminId = object.adminId >>> 0;
        }
        if (object.uid != null) {
          if ($Number(object.uid) !== 0)
            message.uid = object.uid >>> 0;
        }
        if (object.role != null) {
          if (typeof object.role !== "string" || object.role.length)
            message.role = $String(object.role);
        }
        if (object.createdAt != null) {
          if (typeof object.createdAt === "object" ? object.createdAt.low || object.createdAt.high : $Number(object.createdAt) !== 0) {
            if ($util.Long)
              message.createdAt = $util.Long.fromValue(object.createdAt, false);
            else if (typeof object.createdAt === "string")
              message.createdAt = $parseInt(object.createdAt, 10);
            else if (typeof object.createdAt === "number")
              message.createdAt = object.createdAt;
            else if (typeof object.createdAt === "object")
              message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
          }
        }
        if (object.disabled != null) {
          if (object.disabled)
            message.disabled = $Boolean(object.disabled);
        }
        return message;
      };
      AdminAccount.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.adminId = 0;
          object.uid = 0;
          object.role = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.createdAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.createdAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.disabled = false;
        }
        if (message.adminId != null && $Object.hasOwnProperty.call(message, "adminId"))
          object.adminId = message.adminId;
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
          object.uid = message.uid;
        if (message.role != null && $Object.hasOwnProperty.call(message, "role"))
          object.role = message.role;
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.createdAt = typeof message.createdAt === "number" ? $BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
          else if (typeof message.createdAt === "number")
            object.createdAt = options.longs === $String ? $String(message.createdAt) : message.createdAt;
          else
            object.createdAt = options.longs === $String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === $Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
        if (message.disabled != null && $Object.hasOwnProperty.call(message, "disabled"))
          object.disabled = message.disabled;
        return object;
      };
      AdminAccount.prototype.toJSON = function() {
        return AdminAccount.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminAccount.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.AdminAccount";
      };
      return AdminAccount;
    })();
    common.AdminAuditLog = (function() {
      const AdminAuditLog = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AdminAuditLog.prototype.logId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      AdminAuditLog.prototype.requestId = "";
      AdminAuditLog.prototype.operatorAdminId = 0;
      AdminAuditLog.prototype.operatorRole = "";
      AdminAuditLog.prototype.action = "";
      AdminAuditLog.prototype.resourceType = "";
      AdminAuditLog.prototype.result = "";
      AdminAuditLog.prototype.createdAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      AdminAuditLog.prototype.payloadText = "";
      AdminAuditLog.create = function(properties) {
        return new AdminAuditLog(properties);
      };
      AdminAuditLog.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.logId != null && $Object.hasOwnProperty.call(message, "logId") && (typeof message.logId === "object" ? message.logId.low || message.logId.high : message.logId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.logId);
        if (message.requestId != null && $Object.hasOwnProperty.call(message, "requestId") && message.requestId !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.requestId);
        if (message.operatorAdminId != null && $Object.hasOwnProperty.call(message, "operatorAdminId") && message.operatorAdminId !== 0)
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint32(message.operatorAdminId);
        if (message.operatorRole != null && $Object.hasOwnProperty.call(message, "operatorRole") && message.operatorRole !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.operatorRole);
        if (message.action != null && $Object.hasOwnProperty.call(message, "action") && message.action !== "")
          writer.uint32(
            /* id 5, wireType 2 =*/
            42
          ).string(message.action);
        if (message.resourceType != null && $Object.hasOwnProperty.call(message, "resourceType") && message.resourceType !== "")
          writer.uint32(
            /* id 6, wireType 2 =*/
            50
          ).string(message.resourceType);
        if (message.result != null && $Object.hasOwnProperty.call(message, "result") && message.result !== "")
          writer.uint32(
            /* id 7, wireType 2 =*/
            58
          ).string(message.result);
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt") && (typeof message.createdAt === "object" ? message.createdAt.low || message.createdAt.high : message.createdAt !== 0))
          writer.uint32(
            /* id 8, wireType 0 =*/
            64
          ).int64(message.createdAt);
        if (message.payloadText != null && $Object.hasOwnProperty.call(message, "payloadText") && message.payloadText !== "")
          writer.uint32(
            /* id 9, wireType 2 =*/
            74
          ).string(message.payloadText);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AdminAuditLog.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AdminAuditLog.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.AdminAuditLog(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.logId = value;
              else
                delete message.logId;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.requestId = value;
              else
                delete message.requestId;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.operatorAdminId = value;
              else
                delete message.operatorAdminId;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.operatorRole = value;
              else
                delete message.operatorRole;
              continue;
            }
            case 5: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.action = value;
              else
                delete message.action;
              continue;
            }
            case 6: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.resourceType = value;
              else
                delete message.resourceType;
              continue;
            }
            case 7: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.result = value;
              else
                delete message.result;
              continue;
            }
            case 8: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.createdAt = value;
              else
                delete message.createdAt;
              continue;
            }
            case 9: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.payloadText = value;
              else
                delete message.payloadText;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AdminAuditLog.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AdminAuditLog.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.logId != null && $Object.hasOwnProperty.call(message, "logId")) {
          if (!$util.isInteger(message.logId) && !(message.logId && $util.isInteger(message.logId.low) && $util.isInteger(message.logId.high)))
            return "logId: integer|Long expected";
        }
        if (message.requestId != null && $Object.hasOwnProperty.call(message, "requestId")) {
          if (!$util.isString(message.requestId))
            return "requestId: string expected";
        }
        if (message.operatorAdminId != null && $Object.hasOwnProperty.call(message, "operatorAdminId")) {
          if (!$util.isInteger(message.operatorAdminId))
            return "operatorAdminId: integer expected";
        }
        if (message.operatorRole != null && $Object.hasOwnProperty.call(message, "operatorRole")) {
          if (!$util.isString(message.operatorRole))
            return "operatorRole: string expected";
        }
        if (message.action != null && $Object.hasOwnProperty.call(message, "action")) {
          if (!$util.isString(message.action))
            return "action: string expected";
        }
        if (message.resourceType != null && $Object.hasOwnProperty.call(message, "resourceType")) {
          if (!$util.isString(message.resourceType))
            return "resourceType: string expected";
        }
        if (message.result != null && $Object.hasOwnProperty.call(message, "result")) {
          if (!$util.isString(message.result))
            return "result: string expected";
        }
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt")) {
          if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
            return "createdAt: integer|Long expected";
        }
        if (message.payloadText != null && $Object.hasOwnProperty.call(message, "payloadText")) {
          if (!$util.isString(message.payloadText))
            return "payloadText: string expected";
        }
        return null;
      };
      AdminAuditLog.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.AdminAuditLog)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.AdminAuditLog: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.AdminAuditLog();
        if (object.logId != null) {
          if (typeof object.logId === "object" ? object.logId.low || object.logId.high : $Number(object.logId) !== 0) {
            if ($util.Long)
              message.logId = $util.Long.fromValue(object.logId, true);
            else if (typeof object.logId === "string")
              message.logId = $parseInt(object.logId, 10);
            else if (typeof object.logId === "number")
              message.logId = object.logId;
            else if (typeof object.logId === "object")
              message.logId = new $util.LongBits(object.logId.low >>> 0, object.logId.high >>> 0).toNumber(true);
          }
        }
        if (object.requestId != null) {
          if (typeof object.requestId !== "string" || object.requestId.length)
            message.requestId = $String(object.requestId);
        }
        if (object.operatorAdminId != null) {
          if ($Number(object.operatorAdminId) !== 0)
            message.operatorAdminId = object.operatorAdminId >>> 0;
        }
        if (object.operatorRole != null) {
          if (typeof object.operatorRole !== "string" || object.operatorRole.length)
            message.operatorRole = $String(object.operatorRole);
        }
        if (object.action != null) {
          if (typeof object.action !== "string" || object.action.length)
            message.action = $String(object.action);
        }
        if (object.resourceType != null) {
          if (typeof object.resourceType !== "string" || object.resourceType.length)
            message.resourceType = $String(object.resourceType);
        }
        if (object.result != null) {
          if (typeof object.result !== "string" || object.result.length)
            message.result = $String(object.result);
        }
        if (object.createdAt != null) {
          if (typeof object.createdAt === "object" ? object.createdAt.low || object.createdAt.high : $Number(object.createdAt) !== 0) {
            if ($util.Long)
              message.createdAt = $util.Long.fromValue(object.createdAt, false);
            else if (typeof object.createdAt === "string")
              message.createdAt = $parseInt(object.createdAt, 10);
            else if (typeof object.createdAt === "number")
              message.createdAt = object.createdAt;
            else if (typeof object.createdAt === "object")
              message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
          }
        }
        if (object.payloadText != null) {
          if (typeof object.payloadText !== "string" || object.payloadText.length)
            message.payloadText = $String(object.payloadText);
        }
        return message;
      };
      AdminAuditLog.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.logId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.logId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.requestId = "";
          object.operatorAdminId = 0;
          object.operatorRole = "";
          object.action = "";
          object.resourceType = "";
          object.result = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.createdAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.createdAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.payloadText = "";
        }
        if (message.logId != null && $Object.hasOwnProperty.call(message, "logId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.logId = typeof message.logId === "number" ? $BigInt(message.logId) : $util.Long.fromBits(message.logId.low >>> 0, message.logId.high >>> 0, true).toBigInt();
          else if (typeof message.logId === "number")
            object.logId = options.longs === $String ? $String(message.logId) : message.logId;
          else
            object.logId = options.longs === $String ? $util.Long.prototype.toString.call(message.logId) : options.longs === $Number ? new $util.LongBits(message.logId.low >>> 0, message.logId.high >>> 0).toNumber(true) : message.logId;
        if (message.requestId != null && $Object.hasOwnProperty.call(message, "requestId"))
          object.requestId = message.requestId;
        if (message.operatorAdminId != null && $Object.hasOwnProperty.call(message, "operatorAdminId"))
          object.operatorAdminId = message.operatorAdminId;
        if (message.operatorRole != null && $Object.hasOwnProperty.call(message, "operatorRole"))
          object.operatorRole = message.operatorRole;
        if (message.action != null && $Object.hasOwnProperty.call(message, "action"))
          object.action = message.action;
        if (message.resourceType != null && $Object.hasOwnProperty.call(message, "resourceType"))
          object.resourceType = message.resourceType;
        if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
          object.result = message.result;
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.createdAt = typeof message.createdAt === "number" ? $BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
          else if (typeof message.createdAt === "number")
            object.createdAt = options.longs === $String ? $String(message.createdAt) : message.createdAt;
          else
            object.createdAt = options.longs === $String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === $Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
        if (message.payloadText != null && $Object.hasOwnProperty.call(message, "payloadText"))
          object.payloadText = message.payloadText;
        return object;
      };
      AdminAuditLog.prototype.toJSON = function() {
        return AdminAuditLog.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AdminAuditLog.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.AdminAuditLog";
      };
      return AdminAuditLog;
    })();
    common.CacheMetrics = (function() {
      const CacheMetrics = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      CacheMetrics.prototype.hits = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      CacheMetrics.prototype.misses = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      CacheMetrics.prototype.errors = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      CacheMetrics.prototype.invalidations = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      CacheMetrics.prototype.sampledAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      CacheMetrics.create = function(properties) {
        return new CacheMetrics(properties);
      };
      CacheMetrics.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.hits != null && $Object.hasOwnProperty.call(message, "hits") && (typeof message.hits === "object" ? message.hits.low || message.hits.high : message.hits !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.hits);
        if (message.misses != null && $Object.hasOwnProperty.call(message, "misses") && (typeof message.misses === "object" ? message.misses.low || message.misses.high : message.misses !== 0))
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint64(message.misses);
        if (message.errors != null && $Object.hasOwnProperty.call(message, "errors") && (typeof message.errors === "object" ? message.errors.low || message.errors.high : message.errors !== 0))
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint64(message.errors);
        if (message.invalidations != null && $Object.hasOwnProperty.call(message, "invalidations") && (typeof message.invalidations === "object" ? message.invalidations.low || message.invalidations.high : message.invalidations !== 0))
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).uint64(message.invalidations);
        if (message.sampledAt != null && $Object.hasOwnProperty.call(message, "sampledAt") && (typeof message.sampledAt === "object" ? message.sampledAt.low || message.sampledAt.high : message.sampledAt !== 0))
          writer.uint32(
            /* id 5, wireType 0 =*/
            40
          ).int64(message.sampledAt);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      CacheMetrics.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      CacheMetrics.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.CacheMetrics(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.hits = value;
              else
                delete message.hits;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.misses = value;
              else
                delete message.misses;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.errors = value;
              else
                delete message.errors;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.invalidations = value;
              else
                delete message.invalidations;
              continue;
            }
            case 5: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.sampledAt = value;
              else
                delete message.sampledAt;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      CacheMetrics.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      CacheMetrics.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.hits != null && $Object.hasOwnProperty.call(message, "hits")) {
          if (!$util.isInteger(message.hits) && !(message.hits && $util.isInteger(message.hits.low) && $util.isInteger(message.hits.high)))
            return "hits: integer|Long expected";
        }
        if (message.misses != null && $Object.hasOwnProperty.call(message, "misses")) {
          if (!$util.isInteger(message.misses) && !(message.misses && $util.isInteger(message.misses.low) && $util.isInteger(message.misses.high)))
            return "misses: integer|Long expected";
        }
        if (message.errors != null && $Object.hasOwnProperty.call(message, "errors")) {
          if (!$util.isInteger(message.errors) && !(message.errors && $util.isInteger(message.errors.low) && $util.isInteger(message.errors.high)))
            return "errors: integer|Long expected";
        }
        if (message.invalidations != null && $Object.hasOwnProperty.call(message, "invalidations")) {
          if (!$util.isInteger(message.invalidations) && !(message.invalidations && $util.isInteger(message.invalidations.low) && $util.isInteger(message.invalidations.high)))
            return "invalidations: integer|Long expected";
        }
        if (message.sampledAt != null && $Object.hasOwnProperty.call(message, "sampledAt")) {
          if (!$util.isInteger(message.sampledAt) && !(message.sampledAt && $util.isInteger(message.sampledAt.low) && $util.isInteger(message.sampledAt.high)))
            return "sampledAt: integer|Long expected";
        }
        return null;
      };
      CacheMetrics.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.CacheMetrics)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.CacheMetrics: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.CacheMetrics();
        if (object.hits != null) {
          if (typeof object.hits === "object" ? object.hits.low || object.hits.high : $Number(object.hits) !== 0) {
            if ($util.Long)
              message.hits = $util.Long.fromValue(object.hits, true);
            else if (typeof object.hits === "string")
              message.hits = $parseInt(object.hits, 10);
            else if (typeof object.hits === "number")
              message.hits = object.hits;
            else if (typeof object.hits === "object")
              message.hits = new $util.LongBits(object.hits.low >>> 0, object.hits.high >>> 0).toNumber(true);
          }
        }
        if (object.misses != null) {
          if (typeof object.misses === "object" ? object.misses.low || object.misses.high : $Number(object.misses) !== 0) {
            if ($util.Long)
              message.misses = $util.Long.fromValue(object.misses, true);
            else if (typeof object.misses === "string")
              message.misses = $parseInt(object.misses, 10);
            else if (typeof object.misses === "number")
              message.misses = object.misses;
            else if (typeof object.misses === "object")
              message.misses = new $util.LongBits(object.misses.low >>> 0, object.misses.high >>> 0).toNumber(true);
          }
        }
        if (object.errors != null) {
          if (typeof object.errors === "object" ? object.errors.low || object.errors.high : $Number(object.errors) !== 0) {
            if ($util.Long)
              message.errors = $util.Long.fromValue(object.errors, true);
            else if (typeof object.errors === "string")
              message.errors = $parseInt(object.errors, 10);
            else if (typeof object.errors === "number")
              message.errors = object.errors;
            else if (typeof object.errors === "object")
              message.errors = new $util.LongBits(object.errors.low >>> 0, object.errors.high >>> 0).toNumber(true);
          }
        }
        if (object.invalidations != null) {
          if (typeof object.invalidations === "object" ? object.invalidations.low || object.invalidations.high : $Number(object.invalidations) !== 0) {
            if ($util.Long)
              message.invalidations = $util.Long.fromValue(object.invalidations, true);
            else if (typeof object.invalidations === "string")
              message.invalidations = $parseInt(object.invalidations, 10);
            else if (typeof object.invalidations === "number")
              message.invalidations = object.invalidations;
            else if (typeof object.invalidations === "object")
              message.invalidations = new $util.LongBits(object.invalidations.low >>> 0, object.invalidations.high >>> 0).toNumber(true);
          }
        }
        if (object.sampledAt != null) {
          if (typeof object.sampledAt === "object" ? object.sampledAt.low || object.sampledAt.high : $Number(object.sampledAt) !== 0) {
            if ($util.Long)
              message.sampledAt = $util.Long.fromValue(object.sampledAt, false);
            else if (typeof object.sampledAt === "string")
              message.sampledAt = $parseInt(object.sampledAt, 10);
            else if (typeof object.sampledAt === "number")
              message.sampledAt = object.sampledAt;
            else if (typeof object.sampledAt === "object")
              message.sampledAt = new $util.LongBits(object.sampledAt.low >>> 0, object.sampledAt.high >>> 0).toNumber();
          }
        }
        return message;
      };
      CacheMetrics.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.hits = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.hits = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.misses = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.misses = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.errors = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.errors = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.invalidations = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.invalidations = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.sampledAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.sampledAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        }
        if (message.hits != null && $Object.hasOwnProperty.call(message, "hits"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.hits = typeof message.hits === "number" ? $BigInt(message.hits) : $util.Long.fromBits(message.hits.low >>> 0, message.hits.high >>> 0, true).toBigInt();
          else if (typeof message.hits === "number")
            object.hits = options.longs === $String ? $String(message.hits) : message.hits;
          else
            object.hits = options.longs === $String ? $util.Long.prototype.toString.call(message.hits) : options.longs === $Number ? new $util.LongBits(message.hits.low >>> 0, message.hits.high >>> 0).toNumber(true) : message.hits;
        if (message.misses != null && $Object.hasOwnProperty.call(message, "misses"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.misses = typeof message.misses === "number" ? $BigInt(message.misses) : $util.Long.fromBits(message.misses.low >>> 0, message.misses.high >>> 0, true).toBigInt();
          else if (typeof message.misses === "number")
            object.misses = options.longs === $String ? $String(message.misses) : message.misses;
          else
            object.misses = options.longs === $String ? $util.Long.prototype.toString.call(message.misses) : options.longs === $Number ? new $util.LongBits(message.misses.low >>> 0, message.misses.high >>> 0).toNumber(true) : message.misses;
        if (message.errors != null && $Object.hasOwnProperty.call(message, "errors"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.errors = typeof message.errors === "number" ? $BigInt(message.errors) : $util.Long.fromBits(message.errors.low >>> 0, message.errors.high >>> 0, true).toBigInt();
          else if (typeof message.errors === "number")
            object.errors = options.longs === $String ? $String(message.errors) : message.errors;
          else
            object.errors = options.longs === $String ? $util.Long.prototype.toString.call(message.errors) : options.longs === $Number ? new $util.LongBits(message.errors.low >>> 0, message.errors.high >>> 0).toNumber(true) : message.errors;
        if (message.invalidations != null && $Object.hasOwnProperty.call(message, "invalidations"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.invalidations = typeof message.invalidations === "number" ? $BigInt(message.invalidations) : $util.Long.fromBits(message.invalidations.low >>> 0, message.invalidations.high >>> 0, true).toBigInt();
          else if (typeof message.invalidations === "number")
            object.invalidations = options.longs === $String ? $String(message.invalidations) : message.invalidations;
          else
            object.invalidations = options.longs === $String ? $util.Long.prototype.toString.call(message.invalidations) : options.longs === $Number ? new $util.LongBits(message.invalidations.low >>> 0, message.invalidations.high >>> 0).toNumber(true) : message.invalidations;
        if (message.sampledAt != null && $Object.hasOwnProperty.call(message, "sampledAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.sampledAt = typeof message.sampledAt === "number" ? $BigInt(message.sampledAt) : $util.Long.fromBits(message.sampledAt.low >>> 0, message.sampledAt.high >>> 0, false).toBigInt();
          else if (typeof message.sampledAt === "number")
            object.sampledAt = options.longs === $String ? $String(message.sampledAt) : message.sampledAt;
          else
            object.sampledAt = options.longs === $String ? $util.Long.prototype.toString.call(message.sampledAt) : options.longs === $Number ? new $util.LongBits(message.sampledAt.low >>> 0, message.sampledAt.high >>> 0).toNumber() : message.sampledAt;
        return object;
      };
      CacheMetrics.prototype.toJSON = function() {
        return CacheMetrics.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      CacheMetrics.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.CacheMetrics";
      };
      return CacheMetrics;
    })();
    common.EmptyRequest = (function() {
      const EmptyRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      EmptyRequest.create = function(properties) {
        return new EmptyRequest(properties);
      };
      EmptyRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      EmptyRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      EmptyRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.EmptyRequest();
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          reader.skipType(tag & 7, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      EmptyRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      EmptyRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        return null;
      };
      EmptyRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.EmptyRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.EmptyRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        return new $root.oj.common.EmptyRequest();
      };
      EmptyRequest.toObject = function() {
        return {};
      };
      EmptyRequest.prototype.toJSON = function() {
        return EmptyRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      EmptyRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.EmptyRequest";
      };
      return EmptyRequest;
    })();
    common.EmptyResponse = (function() {
      const EmptyResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      EmptyResponse.prototype.status = null;
      EmptyResponse.create = function(properties) {
        return new EmptyResponse(properties);
      };
      EmptyResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          $root.oj.common.StatusResponse.encode(message.status, writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      EmptyResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      EmptyResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.common.EmptyResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              message.status = $root.oj.common.StatusResponse.decode(reader, reader.uint32(), $undefined, _depth + 1, message.status);
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      EmptyResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      EmptyResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          let error = $root.oj.common.StatusResponse.verify(message.status, _depth + 1);
          if (error)
            return "status." + error;
        }
        return null;
      };
      EmptyResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.common.EmptyResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.common.EmptyResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.common.EmptyResponse();
        if (object.status != null) {
          if (!$util.isObject(object.status))
            throw $TypeError(".oj.common.EmptyResponse.status: object expected");
          message.status = $root.oj.common.StatusResponse.fromObject(object.status, _depth + 1);
        }
        return message;
      };
      EmptyResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults)
          object.status = null;
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = $root.oj.common.StatusResponse.toObject(message.status, options, _depth + 1);
        return object;
      };
      EmptyResponse.prototype.toJSON = function() {
        return EmptyResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      EmptyResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.common.EmptyResponse";
      };
      return EmptyResponse;
    })();
    return common;
  })();
  oj2.gateway = (function() {
    const gateway = {};
    gateway.AuthContext = (function() {
      const AuthContext = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      AuthContext.prototype.userId = 0;
      AuthContext.prototype.email = "";
      AuthContext.prototype.role = 0;
      AuthContext.prototype.jti = "";
      AuthContext.prototype.exp = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      AuthContext.create = function(properties) {
        return new AuthContext(properties);
      };
      AuthContext.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId") && message.userId !== 0)
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint32(message.userId);
        if (message.email != null && $Object.hasOwnProperty.call(message, "email") && message.email !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.email);
        if (message.role != null && $Object.hasOwnProperty.call(message, "role") && message.role !== 0)
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).int32(message.role);
        if (message.jti != null && $Object.hasOwnProperty.call(message, "jti") && message.jti !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.jti);
        if (message.exp != null && $Object.hasOwnProperty.call(message, "exp") && (typeof message.exp === "object" ? message.exp.low || message.exp.high : message.exp !== 0))
          writer.uint32(
            /* id 5, wireType 0 =*/
            40
          ).int64(message.exp);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      AuthContext.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      AuthContext.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.gateway.AuthContext(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.userId = value;
              else
                delete message.userId;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.email = value;
              else
                delete message.email;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              if (value = reader.int32())
                message.role = value;
              else
                delete message.role;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.jti = value;
              else
                delete message.jti;
              continue;
            }
            case 5: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.exp = value;
              else
                delete message.exp;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      AuthContext.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      AuthContext.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId")) {
          if (!$util.isInteger(message.userId))
            return "userId: integer expected";
        }
        if (message.email != null && $Object.hasOwnProperty.call(message, "email")) {
          if (!$util.isString(message.email))
            return "email: string expected";
        }
        if (message.role != null && $Object.hasOwnProperty.call(message, "role")) {
          if (!$util.isInteger(message.role))
            return "role: integer expected";
        }
        if (message.jti != null && $Object.hasOwnProperty.call(message, "jti")) {
          if (!$util.isString(message.jti))
            return "jti: string expected";
        }
        if (message.exp != null && $Object.hasOwnProperty.call(message, "exp")) {
          if (!$util.isInteger(message.exp) && !(message.exp && $util.isInteger(message.exp.low) && $util.isInteger(message.exp.high)))
            return "exp: integer|Long expected";
        }
        return null;
      };
      AuthContext.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.gateway.AuthContext)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.gateway.AuthContext: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.gateway.AuthContext();
        if (object.userId != null) {
          if ($Number(object.userId) !== 0)
            message.userId = object.userId >>> 0;
        }
        if (object.email != null) {
          if (typeof object.email !== "string" || object.email.length)
            message.email = $String(object.email);
        }
        if (object.role != null) {
          if ($Number(object.role) !== 0)
            message.role = object.role | 0;
        }
        if (object.jti != null) {
          if (typeof object.jti !== "string" || object.jti.length)
            message.jti = $String(object.jti);
        }
        if (object.exp != null) {
          if (typeof object.exp === "object" ? object.exp.low || object.exp.high : $Number(object.exp) !== 0) {
            if ($util.Long)
              message.exp = $util.Long.fromValue(object.exp, false);
            else if (typeof object.exp === "string")
              message.exp = $parseInt(object.exp, 10);
            else if (typeof object.exp === "number")
              message.exp = object.exp;
            else if (typeof object.exp === "object")
              message.exp = new $util.LongBits(object.exp.low >>> 0, object.exp.high >>> 0).toNumber();
          }
        }
        return message;
      };
      AuthContext.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.userId = 0;
          object.email = "";
          object.role = 0;
          object.jti = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.exp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.exp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        }
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
          object.userId = message.userId;
        if (message.email != null && $Object.hasOwnProperty.call(message, "email"))
          object.email = message.email;
        if (message.role != null && $Object.hasOwnProperty.call(message, "role"))
          object.role = message.role;
        if (message.jti != null && $Object.hasOwnProperty.call(message, "jti"))
          object.jti = message.jti;
        if (message.exp != null && $Object.hasOwnProperty.call(message, "exp"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.exp = typeof message.exp === "number" ? $BigInt(message.exp) : $util.Long.fromBits(message.exp.low >>> 0, message.exp.high >>> 0, false).toBigInt();
          else if (typeof message.exp === "number")
            object.exp = options.longs === $String ? $String(message.exp) : message.exp;
          else
            object.exp = options.longs === $String ? $util.Long.prototype.toString.call(message.exp) : options.longs === $Number ? new $util.LongBits(message.exp.low >>> 0, message.exp.high >>> 0).toNumber() : message.exp;
        return object;
      };
      AuthContext.prototype.toJSON = function() {
        return AuthContext.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      AuthContext.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.gateway.AuthContext";
      };
      return AuthContext;
    })();
    gateway.RouteEntry = (function() {
      const RouteEntry = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      RouteEntry.prototype.pathPrefix = "";
      RouteEntry.prototype.backendService = "";
      RouteEntry.prototype.backendMethod = "";
      RouteEntry.prototype.requireAuth = false;
      RouteEntry.prototype.requireAdmin = false;
      RouteEntry.create = function(properties) {
        return new RouteEntry(properties);
      };
      RouteEntry.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.pathPrefix != null && $Object.hasOwnProperty.call(message, "pathPrefix") && message.pathPrefix !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.pathPrefix);
        if (message.backendService != null && $Object.hasOwnProperty.call(message, "backendService") && message.backendService !== "")
          writer.uint32(
            /* id 2, wireType 2 =*/
            18
          ).string(message.backendService);
        if (message.backendMethod != null && $Object.hasOwnProperty.call(message, "backendMethod") && message.backendMethod !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.backendMethod);
        if (message.requireAuth != null && $Object.hasOwnProperty.call(message, "requireAuth") && message.requireAuth !== false)
          writer.uint32(
            /* id 4, wireType 0 =*/
            32
          ).bool(message.requireAuth);
        if (message.requireAdmin != null && $Object.hasOwnProperty.call(message, "requireAdmin") && message.requireAdmin !== false)
          writer.uint32(
            /* id 5, wireType 0 =*/
            40
          ).bool(message.requireAdmin);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      RouteEntry.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      RouteEntry.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.gateway.RouteEntry(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.pathPrefix = value;
              else
                delete message.pathPrefix;
              continue;
            }
            case 2: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.backendService = value;
              else
                delete message.backendService;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.backendMethod = value;
              else
                delete message.backendMethod;
              continue;
            }
            case 4: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.requireAuth = value;
              else
                delete message.requireAuth;
              continue;
            }
            case 5: {
              if (wireType !== 0)
                break;
              if (value = reader.bool())
                message.requireAdmin = value;
              else
                delete message.requireAdmin;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      RouteEntry.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      RouteEntry.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.pathPrefix != null && $Object.hasOwnProperty.call(message, "pathPrefix")) {
          if (!$util.isString(message.pathPrefix))
            return "pathPrefix: string expected";
        }
        if (message.backendService != null && $Object.hasOwnProperty.call(message, "backendService")) {
          if (!$util.isString(message.backendService))
            return "backendService: string expected";
        }
        if (message.backendMethod != null && $Object.hasOwnProperty.call(message, "backendMethod")) {
          if (!$util.isString(message.backendMethod))
            return "backendMethod: string expected";
        }
        if (message.requireAuth != null && $Object.hasOwnProperty.call(message, "requireAuth")) {
          if (typeof message.requireAuth !== "boolean")
            return "requireAuth: boolean expected";
        }
        if (message.requireAdmin != null && $Object.hasOwnProperty.call(message, "requireAdmin")) {
          if (typeof message.requireAdmin !== "boolean")
            return "requireAdmin: boolean expected";
        }
        return null;
      };
      RouteEntry.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.gateway.RouteEntry)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.gateway.RouteEntry: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.gateway.RouteEntry();
        if (object.pathPrefix != null) {
          if (typeof object.pathPrefix !== "string" || object.pathPrefix.length)
            message.pathPrefix = $String(object.pathPrefix);
        }
        if (object.backendService != null) {
          if (typeof object.backendService !== "string" || object.backendService.length)
            message.backendService = $String(object.backendService);
        }
        if (object.backendMethod != null) {
          if (typeof object.backendMethod !== "string" || object.backendMethod.length)
            message.backendMethod = $String(object.backendMethod);
        }
        if (object.requireAuth != null) {
          if (object.requireAuth)
            message.requireAuth = $Boolean(object.requireAuth);
        }
        if (object.requireAdmin != null) {
          if (object.requireAdmin)
            message.requireAdmin = $Boolean(object.requireAdmin);
        }
        return message;
      };
      RouteEntry.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.pathPrefix = "";
          object.backendService = "";
          object.backendMethod = "";
          object.requireAuth = false;
          object.requireAdmin = false;
        }
        if (message.pathPrefix != null && $Object.hasOwnProperty.call(message, "pathPrefix"))
          object.pathPrefix = message.pathPrefix;
        if (message.backendService != null && $Object.hasOwnProperty.call(message, "backendService"))
          object.backendService = message.backendService;
        if (message.backendMethod != null && $Object.hasOwnProperty.call(message, "backendMethod"))
          object.backendMethod = message.backendMethod;
        if (message.requireAuth != null && $Object.hasOwnProperty.call(message, "requireAuth"))
          object.requireAuth = message.requireAuth;
        if (message.requireAdmin != null && $Object.hasOwnProperty.call(message, "requireAdmin"))
          object.requireAdmin = message.requireAdmin;
        return object;
      };
      RouteEntry.prototype.toJSON = function() {
        return RouteEntry.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      RouteEntry.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.gateway.RouteEntry";
      };
      return RouteEntry;
    })();
    gateway.RouteTable = (function() {
      const RouteTable = function(properties) {
        this.routes = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      RouteTable.prototype.routes = $util.emptyArray;
      RouteTable.create = function(properties) {
        return new RouteTable(properties);
      };
      RouteTable.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.routes != null && message.routes.length)
          for (let i = 0; i < message.routes.length; ++i)
            $root.oj.gateway.RouteEntry.encode(message.routes[i], writer.uint32(
              /* id 1, wireType 2 =*/
              10
            ).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      RouteTable.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      RouteTable.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.gateway.RouteTable();
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if (!(message.routes && message.routes.length))
                message.routes = [];
              message.routes.push($root.oj.gateway.RouteEntry.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      RouteTable.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      RouteTable.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.routes != null && $Object.hasOwnProperty.call(message, "routes")) {
          if (!$Array.isArray(message.routes))
            return "routes: array expected";
          for (let i = 0; i < message.routes.length; ++i) {
            let error = $root.oj.gateway.RouteEntry.verify(message.routes[i], _depth + 1);
            if (error)
              return "routes." + error;
          }
        }
        return null;
      };
      RouteTable.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.gateway.RouteTable)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.gateway.RouteTable: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.gateway.RouteTable();
        if (object.routes) {
          if (!$Array.isArray(object.routes))
            throw $TypeError(".oj.gateway.RouteTable.routes: array expected");
          message.routes = $Array(object.routes.length);
          for (let i = 0; i < object.routes.length; ++i) {
            if (!$util.isObject(object.routes[i]))
              throw $TypeError(".oj.gateway.RouteTable.routes: object expected");
            message.routes[i] = $root.oj.gateway.RouteEntry.fromObject(object.routes[i], _depth + 1);
          }
        }
        return message;
      };
      RouteTable.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.routes = [];
        if (message.routes && message.routes.length) {
          object.routes = $Array(message.routes.length);
          for (let j = 0; j < message.routes.length; ++j)
            object.routes[j] = $root.oj.gateway.RouteEntry.toObject(message.routes[j], options, _depth + 1);
        }
        return object;
      };
      RouteTable.prototype.toJSON = function() {
        return RouteTable.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      RouteTable.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.gateway.RouteTable";
      };
      return RouteTable;
    })();
    gateway.PingRequest = (function() {
      const PingRequest = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      PingRequest.create = function(properties) {
        return new PingRequest(properties);
      };
      PingRequest.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      PingRequest.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      PingRequest.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.gateway.PingRequest();
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          reader.skipType(tag & 7, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      PingRequest.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      PingRequest.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        return null;
      };
      PingRequest.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.gateway.PingRequest)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.gateway.PingRequest: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        return new $root.oj.gateway.PingRequest();
      };
      PingRequest.toObject = function() {
        return {};
      };
      PingRequest.prototype.toJSON = function() {
        return PingRequest.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      PingRequest.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.gateway.PingRequest";
      };
      return PingRequest;
    })();
    gateway.PingResponse = (function() {
      const PingResponse = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      PingResponse.prototype.status = "";
      PingResponse.prototype.timestamp = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      PingResponse.prototype.version = "";
      PingResponse.create = function(properties) {
        return new PingResponse(properties);
      };
      PingResponse.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.status);
        if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp") && (typeof message.timestamp === "object" ? message.timestamp.low || message.timestamp.high : message.timestamp !== 0))
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).int64(message.timestamp);
        if (message.version != null && $Object.hasOwnProperty.call(message, "version") && message.version !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.version);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      PingResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      PingResponse.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.gateway.PingResponse(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.status = value;
              else
                delete message.status;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.timestamp = value;
              else
                delete message.timestamp;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.version = value;
              else
                delete message.version;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      PingResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      PingResponse.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
          if (!$util.isString(message.status))
            return "status: string expected";
        }
        if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp")) {
          if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
            return "timestamp: integer|Long expected";
        }
        if (message.version != null && $Object.hasOwnProperty.call(message, "version")) {
          if (!$util.isString(message.version))
            return "version: string expected";
        }
        return null;
      };
      PingResponse.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.gateway.PingResponse)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.gateway.PingResponse: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.gateway.PingResponse();
        if (object.status != null) {
          if (typeof object.status !== "string" || object.status.length)
            message.status = $String(object.status);
        }
        if (object.timestamp != null) {
          if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : $Number(object.timestamp) !== 0) {
            if ($util.Long)
              message.timestamp = $util.Long.fromValue(object.timestamp, false);
            else if (typeof object.timestamp === "string")
              message.timestamp = $parseInt(object.timestamp, 10);
            else if (typeof object.timestamp === "number")
              message.timestamp = object.timestamp;
            else if (typeof object.timestamp === "object")
              message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber();
          }
        }
        if (object.version != null) {
          if (typeof object.version !== "string" || object.version.length)
            message.version = $String(object.version);
        }
        return message;
      };
      PingResponse.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          object.status = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.version = "";
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
          object.status = message.status;
        if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.timestamp = typeof message.timestamp === "number" ? $BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, false).toBigInt();
          else if (typeof message.timestamp === "number")
            object.timestamp = options.longs === $String ? $String(message.timestamp) : message.timestamp;
          else
            object.timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === $Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber() : message.timestamp;
        if (message.version != null && $Object.hasOwnProperty.call(message, "version"))
          object.version = message.version;
        return object;
      };
      PingResponse.prototype.toJSON = function() {
        return PingResponse.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      PingResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.gateway.PingResponse";
      };
      return PingResponse;
    })();
    return gateway;
  })();
  oj2.mq = (function() {
    const mq = {};
    mq.TestCaseInput = (function() {
      const TestCaseInput = function(properties) {
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      TestCaseInput.prototype.testCaseId = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
      TestCaseInput.prototype.index = 0;
      TestCaseInput.prototype.input = "";
      TestCaseInput.prototype.expectedOutput = "";
      TestCaseInput.create = function(properties) {
        return new TestCaseInput(properties);
      };
      TestCaseInput.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.testCaseId != null && $Object.hasOwnProperty.call(message, "testCaseId") && (typeof message.testCaseId === "object" ? message.testCaseId.low || message.testCaseId.high : message.testCaseId !== 0))
          writer.uint32(
            /* id 1, wireType 0 =*/
            8
          ).uint64(message.testCaseId);
        if (message.index != null && $Object.hasOwnProperty.call(message, "index") && message.index !== 0)
          writer.uint32(
            /* id 2, wireType 0 =*/
            16
          ).uint32(message.index);
        if (message.input != null && $Object.hasOwnProperty.call(message, "input") && message.input !== "")
          writer.uint32(
            /* id 3, wireType 2 =*/
            26
          ).string(message.input);
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput") && message.expectedOutput !== "")
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.expectedOutput);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      TestCaseInput.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      TestCaseInput.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.mq.TestCaseInput(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
                message.testCaseId = value;
              else
                delete message.testCaseId;
              continue;
            }
            case 2: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.index = value;
              else
                delete message.index;
              continue;
            }
            case 3: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.input = value;
              else
                delete message.input;
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.expectedOutput = value;
              else
                delete message.expectedOutput;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      TestCaseInput.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      TestCaseInput.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        if (message.testCaseId != null && $Object.hasOwnProperty.call(message, "testCaseId")) {
          if (!$util.isInteger(message.testCaseId) && !(message.testCaseId && $util.isInteger(message.testCaseId.low) && $util.isInteger(message.testCaseId.high)))
            return "testCaseId: integer|Long expected";
        }
        if (message.index != null && $Object.hasOwnProperty.call(message, "index")) {
          if (!$util.isInteger(message.index))
            return "index: integer expected";
        }
        if (message.input != null && $Object.hasOwnProperty.call(message, "input")) {
          if (!$util.isString(message.input))
            return "input: string expected";
        }
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput")) {
          if (!$util.isString(message.expectedOutput))
            return "expectedOutput: string expected";
        }
        return null;
      };
      TestCaseInput.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.mq.TestCaseInput)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.mq.TestCaseInput: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.mq.TestCaseInput();
        if (object.testCaseId != null) {
          if (typeof object.testCaseId === "object" ? object.testCaseId.low || object.testCaseId.high : $Number(object.testCaseId) !== 0) {
            if ($util.Long)
              message.testCaseId = $util.Long.fromValue(object.testCaseId, true);
            else if (typeof object.testCaseId === "string")
              message.testCaseId = $parseInt(object.testCaseId, 10);
            else if (typeof object.testCaseId === "number")
              message.testCaseId = object.testCaseId;
            else if (typeof object.testCaseId === "object")
              message.testCaseId = new $util.LongBits(object.testCaseId.low >>> 0, object.testCaseId.high >>> 0).toNumber(true);
          }
        }
        if (object.index != null) {
          if ($Number(object.index) !== 0)
            message.index = object.index >>> 0;
        }
        if (object.input != null) {
          if (typeof object.input !== "string" || object.input.length)
            message.input = $String(object.input);
        }
        if (object.expectedOutput != null) {
          if (typeof object.expectedOutput !== "string" || object.expectedOutput.length)
            message.expectedOutput = $String(object.expectedOutput);
        }
        return message;
      };
      TestCaseInput.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.defaults) {
          if ($util.Long) {
            let long = new $util.Long(0, 0, true);
            object.testCaseId = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.testCaseId = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.index = 0;
          object.input = "";
          object.expectedOutput = "";
        }
        if (message.testCaseId != null && $Object.hasOwnProperty.call(message, "testCaseId"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.testCaseId = typeof message.testCaseId === "number" ? $BigInt(message.testCaseId) : $util.Long.fromBits(message.testCaseId.low >>> 0, message.testCaseId.high >>> 0, true).toBigInt();
          else if (typeof message.testCaseId === "number")
            object.testCaseId = options.longs === $String ? $String(message.testCaseId) : message.testCaseId;
          else
            object.testCaseId = options.longs === $String ? $util.Long.prototype.toString.call(message.testCaseId) : options.longs === $Number ? new $util.LongBits(message.testCaseId.low >>> 0, message.testCaseId.high >>> 0).toNumber(true) : message.testCaseId;
        if (message.index != null && $Object.hasOwnProperty.call(message, "index"))
          object.index = message.index;
        if (message.input != null && $Object.hasOwnProperty.call(message, "input"))
          object.input = message.input;
        if (message.expectedOutput != null && $Object.hasOwnProperty.call(message, "expectedOutput"))
          object.expectedOutput = message.expectedOutput;
        return object;
      };
      TestCaseInput.prototype.toJSON = function() {
        return TestCaseInput.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      TestCaseInput.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.mq.TestCaseInput";
      };
      return TestCaseInput;
    })();
    mq.JudgeTaskMessage = (function() {
      const JudgeTaskMessage = function(properties) {
        this.testCases = [];
        if (properties) {
          for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
            if (properties[keys[i]] != null && keys[i] !== "__proto__")
              this[keys[i]] = properties[keys[i]];
        }
      };
      JudgeTaskMessage.prototype.messageId = "";
      JudgeTaskMessage.prototype.submissionId = null;
      JudgeTaskMessage.prototype.customTaskId = null;
      JudgeTaskMessage.prototype.userId = 0;
      JudgeTaskMessage.prototype.questionId = "";
      JudgeTaskMessage.prototype.code = "";
      JudgeTaskMessage.prototype.language = "";
      JudgeTaskMessage.prototype.timeLimitMs = 0;
      JudgeTaskMessage.prototype.memoryLimitMb = 0;
      JudgeTaskMessage.prototype.customInput = "";
      JudgeTaskMessage.prototype.testCases = $util.emptyArray;
      JudgeTaskMessage.prototype.createdAt = $util.Long ? $util.Long.fromBits(0, 0, false) : 0;
      JudgeTaskMessage.prototype.deliveryAttempt = 0;
      let $oneOfFields;
      $Object.defineProperty(JudgeTaskMessage.prototype, "taskId", {
        get: $util.oneOfGetter($oneOfFields = ["submissionId", "customTaskId"]),
        set: $util.oneOfSetter($oneOfFields)
      });
      JudgeTaskMessage.create = function(properties) {
        return new JudgeTaskMessage(properties);
      };
      JudgeTaskMessage.encode = function(message, writer, _depth) {
        if (!writer)
          writer = $Writer.create();
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        if (message.messageId != null && $Object.hasOwnProperty.call(message, "messageId") && message.messageId !== "")
          writer.uint32(
            /* id 1, wireType 2 =*/
            10
          ).string(message.messageId);
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId"))
          writer.uint32(
            /* id 3, wireType 0 =*/
            24
          ).uint64(message.submissionId);
        if (message.customTaskId != null && $Object.hasOwnProperty.call(message, "customTaskId"))
          writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).string(message.customTaskId);
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId") && message.userId !== 0)
          writer.uint32(
            /* id 5, wireType 0 =*/
            40
          ).uint32(message.userId);
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
          writer.uint32(
            /* id 6, wireType 2 =*/
            50
          ).string(message.questionId);
        if (message.code != null && $Object.hasOwnProperty.call(message, "code") && message.code !== "")
          writer.uint32(
            /* id 7, wireType 2 =*/
            58
          ).string(message.code);
        if (message.language != null && $Object.hasOwnProperty.call(message, "language") && message.language !== "")
          writer.uint32(
            /* id 8, wireType 2 =*/
            66
          ).string(message.language);
        if (message.timeLimitMs != null && $Object.hasOwnProperty.call(message, "timeLimitMs") && message.timeLimitMs !== 0)
          writer.uint32(
            /* id 9, wireType 0 =*/
            72
          ).uint32(message.timeLimitMs);
        if (message.memoryLimitMb != null && $Object.hasOwnProperty.call(message, "memoryLimitMb") && message.memoryLimitMb !== 0)
          writer.uint32(
            /* id 10, wireType 0 =*/
            80
          ).uint32(message.memoryLimitMb);
        if (message.customInput != null && $Object.hasOwnProperty.call(message, "customInput") && message.customInput !== "")
          writer.uint32(
            /* id 11, wireType 2 =*/
            90
          ).string(message.customInput);
        if (message.testCases != null && message.testCases.length)
          for (let i = 0; i < message.testCases.length; ++i)
            $root.oj.mq.TestCaseInput.encode(message.testCases[i], writer.uint32(
              /* id 12, wireType 2 =*/
              98
            ).fork(), _depth + 1).ldelim();
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt") && (typeof message.createdAt === "object" ? message.createdAt.low || message.createdAt.high : message.createdAt !== 0))
          writer.uint32(
            /* id 13, wireType 0 =*/
            104
          ).int64(message.createdAt);
        if (message.deliveryAttempt != null && $Object.hasOwnProperty.call(message, "deliveryAttempt") && message.deliveryAttempt !== 0)
          writer.uint32(
            /* id 14, wireType 0 =*/
            112
          ).uint32(message.deliveryAttempt);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
          for (let i = 0; i < message.$unknowns.length; ++i)
            writer.raw(message.$unknowns[i]);
        return writer;
      };
      JudgeTaskMessage.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
      };
      JudgeTaskMessage.decode = function(reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
          reader = $Reader.create(reader);
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $Reader.recursionLimit)
          throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj.mq.JudgeTaskMessage(), value;
        while (reader.pos < end) {
          let start = reader.pos;
          let tag = reader.tag();
          if (tag === _end) {
            _end = $undefined;
            break;
          }
          let wireType = tag & 7;
          switch (tag >>>= 3) {
            case 1: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.messageId = value;
              else
                delete message.messageId;
              continue;
            }
            case 3: {
              if (wireType !== 0)
                break;
              message.submissionId = reader.uint64();
              message.taskId = "submissionId";
              continue;
            }
            case 4: {
              if (wireType !== 2)
                break;
              message.customTaskId = reader.stringVerify();
              message.taskId = "customTaskId";
              continue;
            }
            case 5: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.userId = value;
              else
                delete message.userId;
              continue;
            }
            case 6: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.questionId = value;
              else
                delete message.questionId;
              continue;
            }
            case 7: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.code = value;
              else
                delete message.code;
              continue;
            }
            case 8: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.language = value;
              else
                delete message.language;
              continue;
            }
            case 9: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.timeLimitMs = value;
              else
                delete message.timeLimitMs;
              continue;
            }
            case 10: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.memoryLimitMb = value;
              else
                delete message.memoryLimitMb;
              continue;
            }
            case 11: {
              if (wireType !== 2)
                break;
              if ((value = reader.stringVerify()).length)
                message.customInput = value;
              else
                delete message.customInput;
              continue;
            }
            case 12: {
              if (wireType !== 2)
                break;
              if (!(message.testCases && message.testCases.length))
                message.testCases = [];
              message.testCases.push($root.oj.mq.TestCaseInput.decode(reader, reader.uint32(), $undefined, _depth + 1));
              continue;
            }
            case 13: {
              if (wireType !== 0)
                break;
              if (typeof (value = reader.int64()) === "object" ? value.low || value.high : value !== 0)
                message.createdAt = value;
              else
                delete message.createdAt;
              continue;
            }
            case 14: {
              if (wireType !== 0)
                break;
              if (value = reader.uint32())
                message.deliveryAttempt = value;
              else
                delete message.deliveryAttempt;
              continue;
            }
          }
          reader.skipType(wireType, _depth, tag);
          if (!reader.discardUnknown) {
            $util.makeProp(message, "$unknowns", false);
            (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
          }
        }
        if (_end !== $undefined)
          throw $Error("missing end group");
        return message;
      };
      JudgeTaskMessage.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
          reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
      };
      JudgeTaskMessage.verify = function(message, _depth) {
        if (typeof message !== "object" || message === null)
          return "object expected";
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          return "max depth exceeded";
        let properties = {};
        if (message.messageId != null && $Object.hasOwnProperty.call(message, "messageId")) {
          if (!$util.isString(message.messageId))
            return "messageId: string expected";
        }
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId")) {
          properties.taskId = 1;
          if (!$util.isInteger(message.submissionId) && !(message.submissionId && $util.isInteger(message.submissionId.low) && $util.isInteger(message.submissionId.high)))
            return "submissionId: integer|Long expected";
        }
        if (message.customTaskId != null && $Object.hasOwnProperty.call(message, "customTaskId")) {
          if (properties.taskId === 1)
            return "taskId: multiple values";
          properties.taskId = 1;
          if (!$util.isString(message.customTaskId))
            return "customTaskId: string expected";
        }
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId")) {
          if (!$util.isInteger(message.userId))
            return "userId: integer expected";
        }
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
          if (!$util.isString(message.questionId))
            return "questionId: string expected";
        }
        if (message.code != null && $Object.hasOwnProperty.call(message, "code")) {
          if (!$util.isString(message.code))
            return "code: string expected";
        }
        if (message.language != null && $Object.hasOwnProperty.call(message, "language")) {
          if (!$util.isString(message.language))
            return "language: string expected";
        }
        if (message.timeLimitMs != null && $Object.hasOwnProperty.call(message, "timeLimitMs")) {
          if (!$util.isInteger(message.timeLimitMs))
            return "timeLimitMs: integer expected";
        }
        if (message.memoryLimitMb != null && $Object.hasOwnProperty.call(message, "memoryLimitMb")) {
          if (!$util.isInteger(message.memoryLimitMb))
            return "memoryLimitMb: integer expected";
        }
        if (message.customInput != null && $Object.hasOwnProperty.call(message, "customInput")) {
          if (!$util.isString(message.customInput))
            return "customInput: string expected";
        }
        if (message.testCases != null && $Object.hasOwnProperty.call(message, "testCases")) {
          if (!$Array.isArray(message.testCases))
            return "testCases: array expected";
          for (let i = 0; i < message.testCases.length; ++i) {
            let error = $root.oj.mq.TestCaseInput.verify(message.testCases[i], _depth + 1);
            if (error)
              return "testCases." + error;
          }
        }
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt")) {
          if (!$util.isInteger(message.createdAt) && !(message.createdAt && $util.isInteger(message.createdAt.low) && $util.isInteger(message.createdAt.high)))
            return "createdAt: integer|Long expected";
        }
        if (message.deliveryAttempt != null && $Object.hasOwnProperty.call(message, "deliveryAttempt")) {
          if (!$util.isInteger(message.deliveryAttempt))
            return "deliveryAttempt: integer expected";
        }
        return null;
      };
      JudgeTaskMessage.fromObject = function(object, _depth) {
        if (object instanceof $root.oj.mq.JudgeTaskMessage)
          return object;
        if (!$util.isObject(object))
          throw $TypeError(".oj.mq.JudgeTaskMessage: object expected");
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let message = new $root.oj.mq.JudgeTaskMessage();
        if (object.messageId != null) {
          if (typeof object.messageId !== "string" || object.messageId.length)
            message.messageId = $String(object.messageId);
        }
        if (object.submissionId != null) {
          if ($util.Long)
            message.submissionId = $util.Long.fromValue(object.submissionId, true);
          else if (typeof object.submissionId === "string")
            message.submissionId = $parseInt(object.submissionId, 10);
          else if (typeof object.submissionId === "number")
            message.submissionId = object.submissionId;
          else if (typeof object.submissionId === "object")
            message.submissionId = new $util.LongBits(object.submissionId.low >>> 0, object.submissionId.high >>> 0).toNumber(true);
        }
        if (object.customTaskId != null)
          message.customTaskId = $String(object.customTaskId);
        if (object.userId != null) {
          if ($Number(object.userId) !== 0)
            message.userId = object.userId >>> 0;
        }
        if (object.questionId != null) {
          if (typeof object.questionId !== "string" || object.questionId.length)
            message.questionId = $String(object.questionId);
        }
        if (object.code != null) {
          if (typeof object.code !== "string" || object.code.length)
            message.code = $String(object.code);
        }
        if (object.language != null) {
          if (typeof object.language !== "string" || object.language.length)
            message.language = $String(object.language);
        }
        if (object.timeLimitMs != null) {
          if ($Number(object.timeLimitMs) !== 0)
            message.timeLimitMs = object.timeLimitMs >>> 0;
        }
        if (object.memoryLimitMb != null) {
          if ($Number(object.memoryLimitMb) !== 0)
            message.memoryLimitMb = object.memoryLimitMb >>> 0;
        }
        if (object.customInput != null) {
          if (typeof object.customInput !== "string" || object.customInput.length)
            message.customInput = $String(object.customInput);
        }
        if (object.testCases) {
          if (!$Array.isArray(object.testCases))
            throw $TypeError(".oj.mq.JudgeTaskMessage.testCases: array expected");
          message.testCases = $Array(object.testCases.length);
          for (let i = 0; i < object.testCases.length; ++i) {
            if (!$util.isObject(object.testCases[i]))
              throw $TypeError(".oj.mq.JudgeTaskMessage.testCases: object expected");
            message.testCases[i] = $root.oj.mq.TestCaseInput.fromObject(object.testCases[i], _depth + 1);
          }
        }
        if (object.createdAt != null) {
          if (typeof object.createdAt === "object" ? object.createdAt.low || object.createdAt.high : $Number(object.createdAt) !== 0) {
            if ($util.Long)
              message.createdAt = $util.Long.fromValue(object.createdAt, false);
            else if (typeof object.createdAt === "string")
              message.createdAt = $parseInt(object.createdAt, 10);
            else if (typeof object.createdAt === "number")
              message.createdAt = object.createdAt;
            else if (typeof object.createdAt === "object")
              message.createdAt = new $util.LongBits(object.createdAt.low >>> 0, object.createdAt.high >>> 0).toNumber();
          }
        }
        if (object.deliveryAttempt != null) {
          if ($Number(object.deliveryAttempt) !== 0)
            message.deliveryAttempt = object.deliveryAttempt >>> 0;
        }
        return message;
      };
      JudgeTaskMessage.toObject = function(message, options, _depth) {
        if (!options)
          options = {};
        if (_depth === $undefined)
          _depth = 0;
        if (_depth > $util.recursionLimit)
          throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
          object.testCases = [];
        if (options.defaults) {
          object.messageId = "";
          object.userId = 0;
          object.questionId = "";
          object.code = "";
          object.language = "";
          object.timeLimitMs = 0;
          object.memoryLimitMb = 0;
          object.customInput = "";
          if ($util.Long) {
            let long = new $util.Long(0, 0, false);
            object.createdAt = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
          } else
            object.createdAt = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
          object.deliveryAttempt = 0;
        }
        if (message.messageId != null && $Object.hasOwnProperty.call(message, "messageId"))
          object.messageId = message.messageId;
        if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId")) {
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.submissionId = typeof message.submissionId === "number" ? $BigInt(message.submissionId) : $util.Long.fromBits(message.submissionId.low >>> 0, message.submissionId.high >>> 0, true).toBigInt();
          else if (typeof message.submissionId === "number")
            object.submissionId = options.longs === $String ? $String(message.submissionId) : message.submissionId;
          else
            object.submissionId = options.longs === $String ? $util.Long.prototype.toString.call(message.submissionId) : options.longs === $Number ? new $util.LongBits(message.submissionId.low >>> 0, message.submissionId.high >>> 0).toNumber(true) : message.submissionId;
          if (options.oneofs)
            object.taskId = "submissionId";
        }
        if (message.customTaskId != null && $Object.hasOwnProperty.call(message, "customTaskId")) {
          object.customTaskId = message.customTaskId;
          if (options.oneofs)
            object.taskId = "customTaskId";
        }
        if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
          object.userId = message.userId;
        if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
          object.questionId = message.questionId;
        if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
          object.code = message.code;
        if (message.language != null && $Object.hasOwnProperty.call(message, "language"))
          object.language = message.language;
        if (message.timeLimitMs != null && $Object.hasOwnProperty.call(message, "timeLimitMs"))
          object.timeLimitMs = message.timeLimitMs;
        if (message.memoryLimitMb != null && $Object.hasOwnProperty.call(message, "memoryLimitMb"))
          object.memoryLimitMb = message.memoryLimitMb;
        if (message.customInput != null && $Object.hasOwnProperty.call(message, "customInput"))
          object.customInput = message.customInput;
        if (message.testCases && message.testCases.length) {
          object.testCases = $Array(message.testCases.length);
          for (let j = 0; j < message.testCases.length; ++j)
            object.testCases[j] = $root.oj.mq.TestCaseInput.toObject(message.testCases[j], options, _depth + 1);
        }
        if (message.createdAt != null && $Object.hasOwnProperty.call(message, "createdAt"))
          if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
            object.createdAt = typeof message.createdAt === "number" ? $BigInt(message.createdAt) : $util.Long.fromBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0, false).toBigInt();
          else if (typeof message.createdAt === "number")
            object.createdAt = options.longs === $String ? $String(message.createdAt) : message.createdAt;
          else
            object.createdAt = options.longs === $String ? $util.Long.prototype.toString.call(message.createdAt) : options.longs === $Number ? new $util.LongBits(message.createdAt.low >>> 0, message.createdAt.high >>> 0).toNumber() : message.createdAt;
        if (message.deliveryAttempt != null && $Object.hasOwnProperty.call(message, "deliveryAttempt"))
          object.deliveryAttempt = message.deliveryAttempt;
        return object;
      };
      JudgeTaskMessage.prototype.toJSON = function() {
        return JudgeTaskMessage.toObject(this, import_minimal.default.util.toJSONOptions);
      };
      JudgeTaskMessage.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
          prefix = "type.googleapis.com";
        return prefix + "/oj.mq.JudgeTaskMessage";
      };
      return JudgeTaskMessage;
    })();
    return mq;
  })();
  return oj2;
})();
var oj_judge = $root.oj_judge = (() => {
  const oj_judge2 = {};
  oj_judge2.SubmitRequest = (function() {
    const SubmitRequest = function(properties) {
      if (properties) {
        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
      }
    };
    SubmitRequest.prototype.submissionId = 0;
    SubmitRequest.prototype.questionId = "";
    SubmitRequest.prototype.userId = 0;
    SubmitRequest.prototype.code = "";
    SubmitRequest.prototype.timestamp = $util.Long ? $util.Long.fromBits(0, 0, true) : 0;
    SubmitRequest.prototype.language = "";
    SubmitRequest.prototype.timeLimit = 0;
    SubmitRequest.prototype.memoryLimit = 0;
    SubmitRequest.prototype.isCustomTest = false;
    SubmitRequest.prototype.customInput = "";
    SubmitRequest.create = function(properties) {
      return new SubmitRequest(properties);
    };
    SubmitRequest.encode = function(message, writer, _depth) {
      if (!writer)
        writer = $Writer.create();
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId") && message.submissionId !== 0)
        writer.uint32(
          /* id 1, wireType 0 =*/
          8
        ).uint32(message.submissionId);
      if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
        writer.uint32(
          /* id 2, wireType 2 =*/
          18
        ).string(message.questionId);
      if (message.userId != null && $Object.hasOwnProperty.call(message, "userId") && message.userId !== 0)
        writer.uint32(
          /* id 3, wireType 0 =*/
          24
        ).uint32(message.userId);
      if (message.code != null && $Object.hasOwnProperty.call(message, "code") && message.code !== "")
        writer.uint32(
          /* id 4, wireType 2 =*/
          34
        ).string(message.code);
      if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp") && (typeof message.timestamp === "object" ? message.timestamp.low || message.timestamp.high : message.timestamp !== 0))
        writer.uint32(
          /* id 5, wireType 0 =*/
          40
        ).uint64(message.timestamp);
      if (message.language != null && $Object.hasOwnProperty.call(message, "language") && message.language !== "")
        writer.uint32(
          /* id 6, wireType 2 =*/
          50
        ).string(message.language);
      if (message.timeLimit != null && $Object.hasOwnProperty.call(message, "timeLimit") && message.timeLimit !== 0)
        writer.uint32(
          /* id 7, wireType 0 =*/
          56
        ).int32(message.timeLimit);
      if (message.memoryLimit != null && $Object.hasOwnProperty.call(message, "memoryLimit") && message.memoryLimit !== 0)
        writer.uint32(
          /* id 8, wireType 0 =*/
          64
        ).int32(message.memoryLimit);
      if (message.isCustomTest != null && $Object.hasOwnProperty.call(message, "isCustomTest") && message.isCustomTest !== false)
        writer.uint32(
          /* id 9, wireType 0 =*/
          72
        ).bool(message.isCustomTest);
      if (message.customInput != null && $Object.hasOwnProperty.call(message, "customInput") && message.customInput !== "")
        writer.uint32(
          /* id 10, wireType 2 =*/
          82
        ).string(message.customInput);
      if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
        for (let i = 0; i < message.$unknowns.length; ++i)
          writer.raw(message.$unknowns[i]);
      return writer;
    };
    SubmitRequest.encodeDelimited = function(message, writer) {
      return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };
    SubmitRequest.decode = function(reader, length, _end, _depth, _target) {
      if (!(reader instanceof $Reader))
        reader = $Reader.create(reader);
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $Reader.recursionLimit)
        throw $Error("max depth exceeded");
      let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj_judge.SubmitRequest(), value;
      while (reader.pos < end) {
        let start = reader.pos;
        let tag = reader.tag();
        if (tag === _end) {
          _end = $undefined;
          break;
        }
        let wireType = tag & 7;
        switch (tag >>>= 3) {
          case 1: {
            if (wireType !== 0)
              break;
            if (value = reader.uint32())
              message.submissionId = value;
            else
              delete message.submissionId;
            continue;
          }
          case 2: {
            if (wireType !== 2)
              break;
            if ((value = reader.stringVerify()).length)
              message.questionId = value;
            else
              delete message.questionId;
            continue;
          }
          case 3: {
            if (wireType !== 0)
              break;
            if (value = reader.uint32())
              message.userId = value;
            else
              delete message.userId;
            continue;
          }
          case 4: {
            if (wireType !== 2)
              break;
            if ((value = reader.stringVerify()).length)
              message.code = value;
            else
              delete message.code;
            continue;
          }
          case 5: {
            if (wireType !== 0)
              break;
            if (typeof (value = reader.uint64()) === "object" ? value.low || value.high : value !== 0)
              message.timestamp = value;
            else
              delete message.timestamp;
            continue;
          }
          case 6: {
            if (wireType !== 2)
              break;
            if ((value = reader.stringVerify()).length)
              message.language = value;
            else
              delete message.language;
            continue;
          }
          case 7: {
            if (wireType !== 0)
              break;
            if (value = reader.int32())
              message.timeLimit = value;
            else
              delete message.timeLimit;
            continue;
          }
          case 8: {
            if (wireType !== 0)
              break;
            if (value = reader.int32())
              message.memoryLimit = value;
            else
              delete message.memoryLimit;
            continue;
          }
          case 9: {
            if (wireType !== 0)
              break;
            if (value = reader.bool())
              message.isCustomTest = value;
            else
              delete message.isCustomTest;
            continue;
          }
          case 10: {
            if (wireType !== 2)
              break;
            if ((value = reader.stringVerify()).length)
              message.customInput = value;
            else
              delete message.customInput;
            continue;
          }
        }
        reader.skipType(wireType, _depth, tag);
        if (!reader.discardUnknown) {
          $util.makeProp(message, "$unknowns", false);
          (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
        }
      }
      if (_end !== $undefined)
        throw $Error("missing end group");
      return message;
    };
    SubmitRequest.decodeDelimited = function(reader) {
      if (!(reader instanceof $Reader))
        reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };
    SubmitRequest.verify = function(message, _depth) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        return "max depth exceeded";
      if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId")) {
        if (!$util.isInteger(message.submissionId))
          return "submissionId: integer expected";
      }
      if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
        if (!$util.isString(message.questionId))
          return "questionId: string expected";
      }
      if (message.userId != null && $Object.hasOwnProperty.call(message, "userId")) {
        if (!$util.isInteger(message.userId))
          return "userId: integer expected";
      }
      if (message.code != null && $Object.hasOwnProperty.call(message, "code")) {
        if (!$util.isString(message.code))
          return "code: string expected";
      }
      if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp")) {
        if (!$util.isInteger(message.timestamp) && !(message.timestamp && $util.isInteger(message.timestamp.low) && $util.isInteger(message.timestamp.high)))
          return "timestamp: integer|Long expected";
      }
      if (message.language != null && $Object.hasOwnProperty.call(message, "language")) {
        if (!$util.isString(message.language))
          return "language: string expected";
      }
      if (message.timeLimit != null && $Object.hasOwnProperty.call(message, "timeLimit")) {
        if (!$util.isInteger(message.timeLimit))
          return "timeLimit: integer expected";
      }
      if (message.memoryLimit != null && $Object.hasOwnProperty.call(message, "memoryLimit")) {
        if (!$util.isInteger(message.memoryLimit))
          return "memoryLimit: integer expected";
      }
      if (message.isCustomTest != null && $Object.hasOwnProperty.call(message, "isCustomTest")) {
        if (typeof message.isCustomTest !== "boolean")
          return "isCustomTest: boolean expected";
      }
      if (message.customInput != null && $Object.hasOwnProperty.call(message, "customInput")) {
        if (!$util.isString(message.customInput))
          return "customInput: string expected";
      }
      return null;
    };
    SubmitRequest.fromObject = function(object, _depth) {
      if (object instanceof $root.oj_judge.SubmitRequest)
        return object;
      if (!$util.isObject(object))
        throw $TypeError(".oj_judge.SubmitRequest: object expected");
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      let message = new $root.oj_judge.SubmitRequest();
      if (object.submissionId != null) {
        if ($Number(object.submissionId) !== 0)
          message.submissionId = object.submissionId >>> 0;
      }
      if (object.questionId != null) {
        if (typeof object.questionId !== "string" || object.questionId.length)
          message.questionId = $String(object.questionId);
      }
      if (object.userId != null) {
        if ($Number(object.userId) !== 0)
          message.userId = object.userId >>> 0;
      }
      if (object.code != null) {
        if (typeof object.code !== "string" || object.code.length)
          message.code = $String(object.code);
      }
      if (object.timestamp != null) {
        if (typeof object.timestamp === "object" ? object.timestamp.low || object.timestamp.high : $Number(object.timestamp) !== 0) {
          if ($util.Long)
            message.timestamp = $util.Long.fromValue(object.timestamp, true);
          else if (typeof object.timestamp === "string")
            message.timestamp = $parseInt(object.timestamp, 10);
          else if (typeof object.timestamp === "number")
            message.timestamp = object.timestamp;
          else if (typeof object.timestamp === "object")
            message.timestamp = new $util.LongBits(object.timestamp.low >>> 0, object.timestamp.high >>> 0).toNumber(true);
        }
      }
      if (object.language != null) {
        if (typeof object.language !== "string" || object.language.length)
          message.language = $String(object.language);
      }
      if (object.timeLimit != null) {
        if ($Number(object.timeLimit) !== 0)
          message.timeLimit = object.timeLimit | 0;
      }
      if (object.memoryLimit != null) {
        if ($Number(object.memoryLimit) !== 0)
          message.memoryLimit = object.memoryLimit | 0;
      }
      if (object.isCustomTest != null) {
        if (object.isCustomTest)
          message.isCustomTest = $Boolean(object.isCustomTest);
      }
      if (object.customInput != null) {
        if (typeof object.customInput !== "string" || object.customInput.length)
          message.customInput = $String(object.customInput);
      }
      return message;
    };
    SubmitRequest.toObject = function(message, options, _depth) {
      if (!options)
        options = {};
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      let object = {};
      if (options.defaults) {
        object.submissionId = 0;
        object.questionId = "";
        object.userId = 0;
        object.code = "";
        if ($util.Long) {
          let long = new $util.Long(0, 0, true);
          object.timestamp = options.longs === $String ? long.toString() : options.longs === $Number ? long.toNumber() : typeof $BigInt !== "undefined" && options.longs === $BigInt ? long.toBigInt() : long;
        } else
          object.timestamp = options.longs === $String ? "0" : typeof $BigInt !== "undefined" && options.longs === $BigInt ? $BigInt("0") : 0;
        object.language = "";
        object.timeLimit = 0;
        object.memoryLimit = 0;
        object.isCustomTest = false;
        object.customInput = "";
      }
      if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId"))
        object.submissionId = message.submissionId;
      if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
        object.questionId = message.questionId;
      if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
        object.userId = message.userId;
      if (message.code != null && $Object.hasOwnProperty.call(message, "code"))
        object.code = message.code;
      if (message.timestamp != null && $Object.hasOwnProperty.call(message, "timestamp"))
        if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
          object.timestamp = typeof message.timestamp === "number" ? $BigInt(message.timestamp) : $util.Long.fromBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0, true).toBigInt();
        else if (typeof message.timestamp === "number")
          object.timestamp = options.longs === $String ? $String(message.timestamp) : message.timestamp;
        else
          object.timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.timestamp) : options.longs === $Number ? new $util.LongBits(message.timestamp.low >>> 0, message.timestamp.high >>> 0).toNumber(true) : message.timestamp;
      if (message.language != null && $Object.hasOwnProperty.call(message, "language"))
        object.language = message.language;
      if (message.timeLimit != null && $Object.hasOwnProperty.call(message, "timeLimit"))
        object.timeLimit = message.timeLimit;
      if (message.memoryLimit != null && $Object.hasOwnProperty.call(message, "memoryLimit"))
        object.memoryLimit = message.memoryLimit;
      if (message.isCustomTest != null && $Object.hasOwnProperty.call(message, "isCustomTest"))
        object.isCustomTest = message.isCustomTest;
      if (message.customInput != null && $Object.hasOwnProperty.call(message, "customInput"))
        object.customInput = message.customInput;
      return object;
    };
    SubmitRequest.prototype.toJSON = function() {
      return SubmitRequest.toObject(this, import_minimal.default.util.toJSONOptions);
    };
    SubmitRequest.getTypeUrl = function(prefix) {
      if (prefix === $undefined)
        prefix = "type.googleapis.com";
      return prefix + "/oj_judge.SubmitRequest";
    };
    return SubmitRequest;
  })();
  oj_judge2.SubmitResult = (function() {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "AC"] = 0;
    values[valuesById[1] = "WA"] = 1;
    values[valuesById[2] = "COMPILE_ERROR"] = 2;
    values[valuesById[3] = "MEMORY_LIMIT"] = 3;
    values[valuesById[4] = "TIME_LIMIT"] = 4;
    values[valuesById[5] = "SEGV_ERROR"] = 5;
    values[valuesById[6] = "FPE_ERROR"] = 6;
    values[valuesById[7] = "RUNTIME_ERROR"] = 7;
    values[valuesById[8] = "UNKNOWN"] = 8;
    return values;
  })();
  oj_judge2.SubmitStatus = (function() {
    const SubmitStatus = function(properties) {
      if (properties) {
        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
      }
    };
    SubmitStatus.prototype.timeCost = 0;
    SubmitStatus.prototype.memoryCost = 0;
    SubmitStatus.prototype.result = 0;
    SubmitStatus.create = function(properties) {
      return new SubmitStatus(properties);
    };
    SubmitStatus.encode = function(message, writer, _depth) {
      if (!writer)
        writer = $Writer.create();
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      if (message.timeCost != null && $Object.hasOwnProperty.call(message, "timeCost") && message.timeCost !== 0)
        writer.uint32(
          /* id 1, wireType 0 =*/
          8
        ).int32(message.timeCost);
      if (message.memoryCost != null && $Object.hasOwnProperty.call(message, "memoryCost") && message.memoryCost !== 0)
        writer.uint32(
          /* id 2, wireType 0 =*/
          16
        ).int32(message.memoryCost);
      if (message.result != null && $Object.hasOwnProperty.call(message, "result") && message.result !== 0)
        writer.uint32(
          /* id 3, wireType 0 =*/
          24
        ).int32(message.result);
      if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
        for (let i = 0; i < message.$unknowns.length; ++i)
          writer.raw(message.$unknowns[i]);
      return writer;
    };
    SubmitStatus.encodeDelimited = function(message, writer) {
      return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };
    SubmitStatus.decode = function(reader, length, _end, _depth, _target) {
      if (!(reader instanceof $Reader))
        reader = $Reader.create(reader);
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $Reader.recursionLimit)
        throw $Error("max depth exceeded");
      let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj_judge.SubmitStatus(), value;
      while (reader.pos < end) {
        let start = reader.pos;
        let tag = reader.tag();
        if (tag === _end) {
          _end = $undefined;
          break;
        }
        let wireType = tag & 7;
        switch (tag >>>= 3) {
          case 1: {
            if (wireType !== 0)
              break;
            if (value = reader.int32())
              message.timeCost = value;
            else
              delete message.timeCost;
            continue;
          }
          case 2: {
            if (wireType !== 0)
              break;
            if (value = reader.int32())
              message.memoryCost = value;
            else
              delete message.memoryCost;
            continue;
          }
          case 3: {
            if (wireType !== 0)
              break;
            if (value = reader.int32())
              message.result = value;
            else
              delete message.result;
            continue;
          }
        }
        reader.skipType(wireType, _depth, tag);
        if (!reader.discardUnknown) {
          $util.makeProp(message, "$unknowns", false);
          (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
        }
      }
      if (_end !== $undefined)
        throw $Error("missing end group");
      return message;
    };
    SubmitStatus.decodeDelimited = function(reader) {
      if (!(reader instanceof $Reader))
        reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };
    SubmitStatus.verify = function(message, _depth) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        return "max depth exceeded";
      if (message.timeCost != null && $Object.hasOwnProperty.call(message, "timeCost")) {
        if (!$util.isInteger(message.timeCost))
          return "timeCost: integer expected";
      }
      if (message.memoryCost != null && $Object.hasOwnProperty.call(message, "memoryCost")) {
        if (!$util.isInteger(message.memoryCost))
          return "memoryCost: integer expected";
      }
      if (message.result != null && $Object.hasOwnProperty.call(message, "result")) {
        if (typeof message.result !== "number" || (message.result | 0) !== message.result)
          return "result: enum value expected";
      }
      return null;
    };
    SubmitStatus.fromObject = function(object, _depth) {
      if (object instanceof $root.oj_judge.SubmitStatus)
        return object;
      if (!$util.isObject(object))
        throw $TypeError(".oj_judge.SubmitStatus: object expected");
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      let message = new $root.oj_judge.SubmitStatus();
      if (object.timeCost != null) {
        if ($Number(object.timeCost) !== 0)
          message.timeCost = object.timeCost | 0;
      }
      if (object.memoryCost != null) {
        if ($Number(object.memoryCost) !== 0)
          message.memoryCost = object.memoryCost | 0;
      }
      if (object.result !== 0 && (typeof object.result !== "string" || $root.oj_judge.SubmitResult[object.result] !== 0))
        switch (object.result) {
          case "AC":
          case 0:
            message.result = 0;
            break;
          case "WA":
          case 1:
            message.result = 1;
            break;
          case "COMPILE_ERROR":
          case 2:
            message.result = 2;
            break;
          case "MEMORY_LIMIT":
          case 3:
            message.result = 3;
            break;
          case "TIME_LIMIT":
          case 4:
            message.result = 4;
            break;
          case "SEGV_ERROR":
          case 5:
            message.result = 5;
            break;
          case "FPE_ERROR":
          case 6:
            message.result = 6;
            break;
          case "RUNTIME_ERROR":
          case 7:
            message.result = 7;
            break;
          case "UNKNOWN":
          case 8:
            message.result = 8;
            break;
          default:
            if (typeof object.result === "number" && (object.result | 0) === object.result)
              message.result = object.result;
        }
      return message;
    };
    SubmitStatus.toObject = function(message, options, _depth) {
      if (!options)
        options = {};
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      let object = {};
      if (options.defaults) {
        object.timeCost = 0;
        object.memoryCost = 0;
        object.result = options.enums === $String ? "AC" : 0;
      }
      if (message.timeCost != null && $Object.hasOwnProperty.call(message, "timeCost"))
        object.timeCost = message.timeCost;
      if (message.memoryCost != null && $Object.hasOwnProperty.call(message, "memoryCost"))
        object.memoryCost = message.memoryCost;
      if (message.result != null && $Object.hasOwnProperty.call(message, "result"))
        object.result = options.enums === $String ? $root.oj_judge.SubmitResult[message.result] === $undefined ? message.result : $root.oj_judge.SubmitResult[message.result] : message.result;
      return object;
    };
    SubmitStatus.prototype.toJSON = function() {
      return SubmitStatus.toObject(this, import_minimal.default.util.toJSONOptions);
    };
    SubmitStatus.getTypeUrl = function(prefix) {
      if (prefix === $undefined)
        prefix = "type.googleapis.com";
      return prefix + "/oj_judge.SubmitStatus";
    };
    return SubmitStatus;
  })();
  oj_judge2.JudgeFinishedRequest = (function() {
    const JudgeFinishedRequest = function(properties) {
      this.statusList = [];
      if (properties) {
        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
      }
    };
    JudgeFinishedRequest.prototype.submissionId = 0;
    JudgeFinishedRequest.prototype.questionId = "";
    JudgeFinishedRequest.prototype.userId = 0;
    JudgeFinishedRequest.prototype.statusList = $util.emptyArray;
    JudgeFinishedRequest.create = function(properties) {
      return new JudgeFinishedRequest(properties);
    };
    JudgeFinishedRequest.encode = function(message, writer, _depth) {
      if (!writer)
        writer = $Writer.create();
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId") && message.submissionId !== 0)
        writer.uint32(
          /* id 1, wireType 0 =*/
          8
        ).uint32(message.submissionId);
      if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId") && message.questionId !== "")
        writer.uint32(
          /* id 2, wireType 2 =*/
          18
        ).string(message.questionId);
      if (message.userId != null && $Object.hasOwnProperty.call(message, "userId") && message.userId !== 0)
        writer.uint32(
          /* id 3, wireType 0 =*/
          24
        ).uint32(message.userId);
      if (message.statusList != null && message.statusList.length)
        for (let i = 0; i < message.statusList.length; ++i)
          $root.oj_judge.SubmitStatus.encode(message.statusList[i], writer.uint32(
            /* id 4, wireType 2 =*/
            34
          ).fork(), _depth + 1).ldelim();
      if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
        for (let i = 0; i < message.$unknowns.length; ++i)
          writer.raw(message.$unknowns[i]);
      return writer;
    };
    JudgeFinishedRequest.encodeDelimited = function(message, writer) {
      return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };
    JudgeFinishedRequest.decode = function(reader, length, _end, _depth, _target) {
      if (!(reader instanceof $Reader))
        reader = $Reader.create(reader);
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $Reader.recursionLimit)
        throw $Error("max depth exceeded");
      let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj_judge.JudgeFinishedRequest(), value;
      while (reader.pos < end) {
        let start = reader.pos;
        let tag = reader.tag();
        if (tag === _end) {
          _end = $undefined;
          break;
        }
        let wireType = tag & 7;
        switch (tag >>>= 3) {
          case 1: {
            if (wireType !== 0)
              break;
            if (value = reader.uint32())
              message.submissionId = value;
            else
              delete message.submissionId;
            continue;
          }
          case 2: {
            if (wireType !== 2)
              break;
            if ((value = reader.stringVerify()).length)
              message.questionId = value;
            else
              delete message.questionId;
            continue;
          }
          case 3: {
            if (wireType !== 0)
              break;
            if (value = reader.uint32())
              message.userId = value;
            else
              delete message.userId;
            continue;
          }
          case 4: {
            if (wireType !== 2)
              break;
            if (!(message.statusList && message.statusList.length))
              message.statusList = [];
            message.statusList.push($root.oj_judge.SubmitStatus.decode(reader, reader.uint32(), $undefined, _depth + 1));
            continue;
          }
        }
        reader.skipType(wireType, _depth, tag);
        if (!reader.discardUnknown) {
          $util.makeProp(message, "$unknowns", false);
          (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
        }
      }
      if (_end !== $undefined)
        throw $Error("missing end group");
      return message;
    };
    JudgeFinishedRequest.decodeDelimited = function(reader) {
      if (!(reader instanceof $Reader))
        reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };
    JudgeFinishedRequest.verify = function(message, _depth) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        return "max depth exceeded";
      if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId")) {
        if (!$util.isInteger(message.submissionId))
          return "submissionId: integer expected";
      }
      if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId")) {
        if (!$util.isString(message.questionId))
          return "questionId: string expected";
      }
      if (message.userId != null && $Object.hasOwnProperty.call(message, "userId")) {
        if (!$util.isInteger(message.userId))
          return "userId: integer expected";
      }
      if (message.statusList != null && $Object.hasOwnProperty.call(message, "statusList")) {
        if (!$Array.isArray(message.statusList))
          return "statusList: array expected";
        for (let i = 0; i < message.statusList.length; ++i) {
          let error = $root.oj_judge.SubmitStatus.verify(message.statusList[i], _depth + 1);
          if (error)
            return "statusList." + error;
        }
      }
      return null;
    };
    JudgeFinishedRequest.fromObject = function(object, _depth) {
      if (object instanceof $root.oj_judge.JudgeFinishedRequest)
        return object;
      if (!$util.isObject(object))
        throw $TypeError(".oj_judge.JudgeFinishedRequest: object expected");
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      let message = new $root.oj_judge.JudgeFinishedRequest();
      if (object.submissionId != null) {
        if ($Number(object.submissionId) !== 0)
          message.submissionId = object.submissionId >>> 0;
      }
      if (object.questionId != null) {
        if (typeof object.questionId !== "string" || object.questionId.length)
          message.questionId = $String(object.questionId);
      }
      if (object.userId != null) {
        if ($Number(object.userId) !== 0)
          message.userId = object.userId >>> 0;
      }
      if (object.statusList) {
        if (!$Array.isArray(object.statusList))
          throw $TypeError(".oj_judge.JudgeFinishedRequest.statusList: array expected");
        message.statusList = $Array(object.statusList.length);
        for (let i = 0; i < object.statusList.length; ++i) {
          if (!$util.isObject(object.statusList[i]))
            throw $TypeError(".oj_judge.JudgeFinishedRequest.statusList: object expected");
          message.statusList[i] = $root.oj_judge.SubmitStatus.fromObject(object.statusList[i], _depth + 1);
        }
      }
      return message;
    };
    JudgeFinishedRequest.toObject = function(message, options, _depth) {
      if (!options)
        options = {};
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      let object = {};
      if (options.arrays || options.defaults)
        object.statusList = [];
      if (options.defaults) {
        object.submissionId = 0;
        object.questionId = "";
        object.userId = 0;
      }
      if (message.submissionId != null && $Object.hasOwnProperty.call(message, "submissionId"))
        object.submissionId = message.submissionId;
      if (message.questionId != null && $Object.hasOwnProperty.call(message, "questionId"))
        object.questionId = message.questionId;
      if (message.userId != null && $Object.hasOwnProperty.call(message, "userId"))
        object.userId = message.userId;
      if (message.statusList && message.statusList.length) {
        object.statusList = $Array(message.statusList.length);
        for (let j = 0; j < message.statusList.length; ++j)
          object.statusList[j] = $root.oj_judge.SubmitStatus.toObject(message.statusList[j], options, _depth + 1);
      }
      return object;
    };
    JudgeFinishedRequest.prototype.toJSON = function() {
      return JudgeFinishedRequest.toObject(this, import_minimal.default.util.toJSONOptions);
    };
    JudgeFinishedRequest.getTypeUrl = function(prefix) {
      if (prefix === $undefined)
        prefix = "type.googleapis.com";
      return prefix + "/oj_judge.JudgeFinishedRequest";
    };
    return JudgeFinishedRequest;
  })();
  oj_judge2.DockerWorkDoneRequest = (function() {
    const DockerWorkDoneRequest = function(properties) {
      if (properties) {
        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
      }
    };
    DockerWorkDoneRequest.prototype.id = "";
    DockerWorkDoneRequest.prototype.status = "";
    DockerWorkDoneRequest.prototype.exitCode = 0;
    DockerWorkDoneRequest.create = function(properties) {
      return new DockerWorkDoneRequest(properties);
    };
    DockerWorkDoneRequest.encode = function(message, writer, _depth) {
      if (!writer)
        writer = $Writer.create();
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      if (message.id != null && $Object.hasOwnProperty.call(message, "id") && message.id !== "")
        writer.uint32(
          /* id 1, wireType 2 =*/
          10
        ).string(message.id);
      if (message.status != null && $Object.hasOwnProperty.call(message, "status") && message.status !== "")
        writer.uint32(
          /* id 2, wireType 2 =*/
          18
        ).string(message.status);
      if (message.exitCode != null && $Object.hasOwnProperty.call(message, "exitCode") && message.exitCode !== 0)
        writer.uint32(
          /* id 3, wireType 0 =*/
          24
        ).int32(message.exitCode);
      if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
        for (let i = 0; i < message.$unknowns.length; ++i)
          writer.raw(message.$unknowns[i]);
      return writer;
    };
    DockerWorkDoneRequest.encodeDelimited = function(message, writer) {
      return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };
    DockerWorkDoneRequest.decode = function(reader, length, _end, _depth, _target) {
      if (!(reader instanceof $Reader))
        reader = $Reader.create(reader);
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $Reader.recursionLimit)
        throw $Error("max depth exceeded");
      let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj_judge.DockerWorkDoneRequest(), value;
      while (reader.pos < end) {
        let start = reader.pos;
        let tag = reader.tag();
        if (tag === _end) {
          _end = $undefined;
          break;
        }
        let wireType = tag & 7;
        switch (tag >>>= 3) {
          case 1: {
            if (wireType !== 2)
              break;
            if ((value = reader.stringVerify()).length)
              message.id = value;
            else
              delete message.id;
            continue;
          }
          case 2: {
            if (wireType !== 2)
              break;
            if ((value = reader.stringVerify()).length)
              message.status = value;
            else
              delete message.status;
            continue;
          }
          case 3: {
            if (wireType !== 0)
              break;
            if (value = reader.int32())
              message.exitCode = value;
            else
              delete message.exitCode;
            continue;
          }
        }
        reader.skipType(wireType, _depth, tag);
        if (!reader.discardUnknown) {
          $util.makeProp(message, "$unknowns", false);
          (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
        }
      }
      if (_end !== $undefined)
        throw $Error("missing end group");
      return message;
    };
    DockerWorkDoneRequest.decodeDelimited = function(reader) {
      if (!(reader instanceof $Reader))
        reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };
    DockerWorkDoneRequest.verify = function(message, _depth) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        return "max depth exceeded";
      if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
        if (!$util.isString(message.id))
          return "id: string expected";
      }
      if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
        if (!$util.isString(message.status))
          return "status: string expected";
      }
      if (message.exitCode != null && $Object.hasOwnProperty.call(message, "exitCode")) {
        if (!$util.isInteger(message.exitCode))
          return "exitCode: integer expected";
      }
      return null;
    };
    DockerWorkDoneRequest.fromObject = function(object, _depth) {
      if (object instanceof $root.oj_judge.DockerWorkDoneRequest)
        return object;
      if (!$util.isObject(object))
        throw $TypeError(".oj_judge.DockerWorkDoneRequest: object expected");
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      let message = new $root.oj_judge.DockerWorkDoneRequest();
      if (object.id != null) {
        if (typeof object.id !== "string" || object.id.length)
          message.id = $String(object.id);
      }
      if (object.status != null) {
        if (typeof object.status !== "string" || object.status.length)
          message.status = $String(object.status);
      }
      if (object.exitCode != null) {
        if ($Number(object.exitCode) !== 0)
          message.exitCode = object.exitCode | 0;
      }
      return message;
    };
    DockerWorkDoneRequest.toObject = function(message, options, _depth) {
      if (!options)
        options = {};
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      let object = {};
      if (options.defaults) {
        object.id = "";
        object.status = "";
        object.exitCode = 0;
      }
      if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
        object.id = message.id;
      if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
        object.status = message.status;
      if (message.exitCode != null && $Object.hasOwnProperty.call(message, "exitCode"))
        object.exitCode = message.exitCode;
      return object;
    };
    DockerWorkDoneRequest.prototype.toJSON = function() {
      return DockerWorkDoneRequest.toObject(this, import_minimal.default.util.toJSONOptions);
    };
    DockerWorkDoneRequest.getTypeUrl = function(prefix) {
      if (prefix === $undefined)
        prefix = "type.googleapis.com";
      return prefix + "/oj_judge.DockerWorkDoneRequest";
    };
    return DockerWorkDoneRequest;
  })();
  oj_judge2.NullRsp = (function() {
    const NullRsp = function(properties) {
      if (properties) {
        for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
          if (properties[keys[i]] != null && keys[i] !== "__proto__")
            this[keys[i]] = properties[keys[i]];
      }
    };
    NullRsp.create = function(properties) {
      return new NullRsp(properties);
    };
    NullRsp.encode = function(message, writer, _depth) {
      if (!writer)
        writer = $Writer.create();
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
        for (let i = 0; i < message.$unknowns.length; ++i)
          writer.raw(message.$unknowns[i]);
      return writer;
    };
    NullRsp.encodeDelimited = function(message, writer) {
      return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };
    NullRsp.decode = function(reader, length, _end, _depth, _target) {
      if (!(reader instanceof $Reader))
        reader = $Reader.create(reader);
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $Reader.recursionLimit)
        throw $Error("max depth exceeded");
      let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.oj_judge.NullRsp();
      while (reader.pos < end) {
        let start = reader.pos;
        let tag = reader.tag();
        if (tag === _end) {
          _end = $undefined;
          break;
        }
        reader.skipType(tag & 7, _depth, tag);
        if (!reader.discardUnknown) {
          $util.makeProp(message, "$unknowns", false);
          (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
        }
      }
      if (_end !== $undefined)
        throw $Error("missing end group");
      return message;
    };
    NullRsp.decodeDelimited = function(reader) {
      if (!(reader instanceof $Reader))
        reader = new $Reader(reader);
      return this.decode(reader, reader.uint32());
    };
    NullRsp.verify = function(message, _depth) {
      if (typeof message !== "object" || message === null)
        return "object expected";
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        return "max depth exceeded";
      return null;
    };
    NullRsp.fromObject = function(object, _depth) {
      if (object instanceof $root.oj_judge.NullRsp)
        return object;
      if (!$util.isObject(object))
        throw $TypeError(".oj_judge.NullRsp: object expected");
      if (_depth === $undefined)
        _depth = 0;
      if (_depth > $util.recursionLimit)
        throw $Error("max depth exceeded");
      return new $root.oj_judge.NullRsp();
    };
    NullRsp.toObject = function() {
      return {};
    };
    NullRsp.prototype.toJSON = function() {
      return NullRsp.toObject(this, import_minimal.default.util.toJSONOptions);
    };
    NullRsp.getTypeUrl = function(prefix) {
      if (prefix === $undefined)
        prefix = "type.googleapis.com";
      return prefix + "/oj_judge.NullRsp";
    };
    return NullRsp;
  })();
  oj_judge2.JudgeService = (function() {
    const JudgeService = function(rpcImpl, requestDelimited, responseDelimited) {
      import_minimal.default.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
    };
    $Object.defineProperty(JudgeService.prototype = $Object.create(import_minimal.default.rpc.Service.prototype), "constructor", { value: JudgeService, writable: true, enumerable: false, configurable: true });
    JudgeService.create = function(rpcImpl, requestDelimited, responseDelimited) {
      return new this(rpcImpl, requestDelimited, responseDelimited);
    };
    $Object.defineProperties(JudgeService.prototype.dockerWorkDone = function(request, callback) {
      return import_minimal.default.rpc.Service.prototype.rpcCall.call(this, JudgeService.prototype.dockerWorkDone, $root.oj_judge.DockerWorkDoneRequest, $root.oj_judge.NullRsp, request, callback);
    }, {
      name: { value: "DockerWorkDone" },
      path: { value: "/oj_judge.JudgeService/DockerWorkDone" },
      requestType: { value: "DockerWorkDoneRequest" },
      responseType: { value: "NullRsp" },
      requestStream: { value: $undefined },
      responseStream: { value: $undefined }
    });
    return JudgeService;
  })();
  return oj_judge2;
})();
export {
  $root as default,
  oj,
  oj_judge
};
